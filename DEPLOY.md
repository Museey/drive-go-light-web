# คู่มือขึ้นใช้งานจริง — DriveGoLight!

เอกสารนี้เขียนไว้ให้คนที่ต้องกู้ระบบตอนตีสามอ่านแล้วทำตามได้ทันที
ไม่ใช่เอกสารแนะนำสถาปัตยกรรม

---

## 1. สิ่งที่ต้องมีบนเครื่องเซิร์ฟเวอร์

| อย่าง | รุ่น | หมายเหตุ |
|---|---|---|
| Node.js | 22 LTS | รุ่นต่ำกว่า 20 ใช้ไม่ได้ |
| PostgreSQL | 16 | ต้องเป็นเครื่องที่สำรองข้อมูลได้จริง |
| ตัวรับ HTTPS | nginx / Caddy | คุกกี้ session ตั้ง `secure` เมื่อ `NODE_ENV=production` ถ้าไม่มี HTTPS จะเข้าสู่ระบบไม่ได้ |

## 2. ตัวแปรสภาพแวดล้อม

เก็บไว้ที่ `apps/web/.env.local` หรือส่งผ่าน systemd

```
DATABASE_URL=postgresql://dgl_app:<รหัสผ่าน>@127.0.0.1:5432/dgl
NODE_ENV=production
```

`DATABASE_URL` **ต้องเป็น role `dgl_app` เท่านั้น** ห้ามใช้ `postgres`
แอปตรวจตอนเปิดการเชื่อมต่อว่า role มีสิทธิ์ข้าม Row Level Security หรือไม่ ถ้ามีจะปฏิเสธการทำงานทันที
เพราะ role ที่ข้าม RLS ได้แปลว่าทุกอู่มองเห็นข้อมูลของกันและกันโดยไม่มีอาการอะไรให้เห็น

## 3. ติดตั้งครั้งแรก

```bash
createdb dgl
psql "$ADMIN_URL" -v ON_ERROR_STOP=1 -f db/001_init.sql
psql "$ADMIN_URL" -v ON_ERROR_STOP=1 -f db/002_auth.sql
# แก้รหัสผ่านในไฟล์ก่อน แล้วค่อยรัน
psql "$ADMIN_URL" -v ON_ERROR_STOP=1 -f db/app-role.sql

npm ci
npm run build -w @drivegolight/core -w @drivegolight/importer
npm run build -w @drivegolight/web
npm run start -w @drivegolight/web     # ฟังที่พอร์ต 3100
```

`db/app-role.sql` รันซ้ำได้ และ**ต้องรันซ้ำทุกครั้งหลังกู้ฐานข้อมูล** เพราะสิทธิ์บนตารางหายไปพร้อมฐานข้อมูลเก่า
ส่วนตัว role อยู่ระดับคลัสเตอร์จึงยังอยู่

## 4. เปิดอู่ใหม่หนึ่งราย

```bash
DATABASE_URL=... node packages/importer/dist/cli.js <ไฟล์สำรองของอู่>.json \
  --owner-email=<อีเมลเจ้าของอู่> \
  --app-url=https://<โดเมนจริง>
```

คำสั่งนี้พิมพ์ลิงก์ตั้งรหัสผ่านออกมา ใช้ได้ครั้งเดียว หมดอายุ 7 วัน
ส่งให้เจ้าของอู่ทางช่องทางที่ยืนยันตัวตนได้ (โทรบอก หรือไลน์ที่คุยกันอยู่แล้ว) ไม่ใช่โพสต์ในกลุ่ม

อู่ที่เพิ่งเปิดจะได้ทดลองใช้ฟรี 15 วันนับจากวันเปิด หลังจากนั้นบันทึกรายการใหม่ไม่ได้จนกว่าจะบันทึกการต่ออายุ
ที่เมนู 08 — แต่ยังเปิดดู พิมพ์ และดาวน์โหลดไฟล์สำรองได้เสมอ

## 5. สำรองข้อมูลอัตโนมัติ

```bash
0 3 * * * DATABASE_URL=postgresql://postgres@127.0.0.1/dgl BACKUP_DIR=/srv/dgl-backups \
  /srv/drivegolight/tools/backup-db.sh >> /var/log/dgl-backup.log 2>&1
```

`tools/backup-db.sh` ทำสามอย่างที่สคริปต์สำรองทั่วไปมักลืม

1. อ่านสารบัญไฟล์กลับทันทีหลังดัมป์ ไฟล์ที่เปิดไม่ออกไม่นับว่าสำเร็จ
2. เขียน `.sha256` คู่ไว้ ใช้เทียบตอนคัดลอกไปเก็บที่อื่น
3. ล้มถ้าไฟล์เล็กกว่า 10 KB — อาการของการต่อผิดฐานข้อมูล

เก็บย้อนหลัง 90 วัน (`KEEP_DAYS`) ให้ตรงกับที่เขียนไว้ในหน้านโยบายข้อมูลส่วนบุคคล
**ต้องคัดลอกออกไปเก็บนอกเครื่องด้วย** ไฟล์สำรองที่อยู่บนเครื่องเดียวกับฐานข้อมูลไม่ช่วยอะไรตอนดิสก์พัง

## 6. ซ้อมกู้คืน

ซ้อมทุกไตรมาส และซ้อมก่อนย้ายลูกค้าเข้าใหม่ทุกครั้ง ห้ามซ้อมด้วยการอ่านคู่มือเฉย ๆ

```bash
createdb dgl_drill
tools/restore-db.sh /srv/dgl-backups/dgl-<วันที่>.dump \
  postgresql://postgres@127.0.0.1/dgl_drill
```

สคริปต์จะเทียบลายนิ้วมือไฟล์ ล้าง schema ปลายทาง กู้ข้อมูล แล้วตรวจสองอย่าง
คือจำนวนแถวกับยอดขายรวม และว่า **ทุกตารางที่มี `tenant_id` ยังเปิด row level security แบบ force อยู่**
ข้อหลังสำคัญที่สุด เพราะฐานข้อมูลที่กู้มาแล้ว RLS ไม่ติดคือรอยรั่วข้ามอู่ที่ไม่มีอาการให้เห็น

ตรวจต่ออีกสองข้อด้วยมือ

```bash
# 1. role ของแอปใช้งานได้จริงหลังกู้
psql "$ADMIN_URL/dgl_drill" -f db/app-role.sql
psql "postgresql://dgl_app:<รหัส>@127.0.0.1/dgl_drill" \
  -c "select set_config('app.tenant_id','<รหัสอู่>',false)" \
  -c "select count(*), sum(grand_total) from documents"

# 2. ตั้งรหัสอู่ปลอมแล้วต้องได้ 0 แถว
psql "postgresql://dgl_app:<รหัส>@127.0.0.1/dgl_drill" \
  -c "select set_config('app.tenant_id','00000000-0000-0000-0000-000000000000',false)" \
  -c "select count(*) from documents"
```

ผลการซ้อมรอบล่าสุด — 31 สิงหาคม 2569 กับข้อมูลชุดทดสอบ 734 เอกสาร

| ตรวจ | ต้นทาง | ที่กู้มา |
|---|---|---|
| อู่ / ผู้ใช้ / สินค้า | 1 / 1 / 200 | 1 / 1 / 200 |
| ลูกค้า / รถ | 30 / 33 | 30 / 33 |
| เอกสาร / รายการ / การชำระ | 734 / 2275 / 424 | 734 / 2275 / 424 |
| ยอดรวมเอกสาร | 7,282,074.60 | 7,282,074.60 |
| ยอดชำระรวม | 3,808,520.23 | 3,808,520.23 |
| สต๊อกคงเหลือรวม | 3,177.000 | 3,177.000 |
| ชุดเลขที่เอกสาร | 6 | 6 |

หลังกู้ `dgl_app` อ่านข้อมูลของอู่ตัวเองได้ครบ 734 เอกสาร ตั้งรหัสอู่ปลอมได้ 0 แถว
และ `select ... from auth.sessions` ถูกปฏิเสธ ซึ่งถูกต้อง — แอปต้องแตะสคีมา `auth` ผ่านฟังก์ชันเท่านั้น

ลบฐานข้อมูลซ้อมทิ้งเมื่อเสร็จ `dropdb dgl_drill`

## 7. ย้ายลูกค้าเดิมจากโปรแกรม HTML

โปรแกรมเดิมเก็บข้อมูลไว้ในเครื่องลูกค้า ไม่มีใครถือสำเนากลาง การย้ายจึงต้องให้อู่ส่งไฟล์สำรองมาเอง
และต้องย้ายทีละราย ไม่ย้ายพร้อมกันทั้งหมด

**ลำดับการย้าย**

1. **รอบที่ 1 — อู่ของเราเอง** ใช้จริงหนึ่งเดือนเต็มก่อนชวนใครมา ปิดรอบภาษีบนระบบใหม่ให้ได้หนึ่งรอบ
2. **รอบที่ 2 — อู่ที่ยินดีเป็นคนแรก 2–3 ราย** เลือกรายที่เอกสารไม่เยอะและติดต่อได้ง่าย
   นัดย้ายวันจันทร์ต้นเดือน จะได้มีทั้งสัปดาห์ให้แก้ถ้าติดปัญหา
3. **รอบที่ 3 — ที่เหลือ** เมื่อรอบที่ 2 ผ่านหนึ่งเดือนโดยไม่ต้องย้อนกลับ

**ขั้นตอนของอู่หนึ่งราย**

1. อู่กดสำรองข้อมูลจากโปรแกรมเดิม ส่งไฟล์ `.json` มาให้
2. **เก็บไฟล์นั้นไว้ไม่แตะต้อง** เป็นจุดย้อนกลับ ตั้งชื่อ `<ชื่ออู่>-ก่อนย้าย-<วันที่>.json`
3. นำเข้าด้วยคำสั่งในข้อ 4 บนฐานข้อมูลจริง
4. อ่านรายการเตือนที่ตัวนำเข้าพิมพ์ออกมาให้ครบ โดยเฉพาะเลขที่เอกสารซ้ำหรือว่าง ซึ่งตัวนำเข้าเติมให้เอง
5. เทียบกับหน้าจอโปรแกรมเดิมสามตัวเลขต่อหน้าอู่ — ยอดขายรวมทั้งปี ยอดลูกหนี้คงค้าง และจำนวนเอกสาร
6. ให้อู่ลองออกใบกำกับภาษีใหม่หนึ่งใบแล้วพิมพ์ ดูว่าเลขที่ต่อจากของเดิมและหน้าตากระดาษใช้ได้
7. **บอกให้ชัดว่าตั้งแต่นี้ให้บันทึกงานในระบบใหม่เท่านั้น** ห้ามบันทึกสองที่พร้อมกัน
   ข้อมูลสองชุดที่ไม่ตรงกันแก้ยากกว่าการย้ายใหม่ทั้งหมด
8. บอกให้เก็บไฟล์ HTML เดิมไว้ก่อน อย่าเพิ่งลบ ใช้เปิดดูย้อนหลังได้

**ถ้าอู่ไม่พอใจและจะย้อนกลับ**

ภายใน 30 วันแรกทำได้ไม่ยาก เพราะโปรแกรมเดิมยังอยู่ในเครื่องเขา

1. อู่ดาวน์โหลดไฟล์สำรองจากระบบใหม่ที่เมนู 07 — เป็นรูปแบบเดียวกับโปรแกรมเดิมทุกประการ
   (มีชุดทดสอบตรวจว่าส่งออกแล้วนำกลับเข้าได้ยอดตรงทุกบาท)
2. เปิดโปรแกรม HTML เดิม กดนำเข้าไฟล์นั้น งานที่ทำในระบบใหม่ระหว่างนั้นตามกลับไปด้วย
3. ถ้าไฟล์มีปัญหา ใช้ไฟล์ `<ชื่ออู่>-ก่อนย้าย-<วันที่>.json` ที่เก็บไว้ในข้อ 2 แทน
   เสียแค่งานช่วงที่อยู่บนระบบใหม่ ไม่เสียของเก่า
4. ลบข้อมูลของอู่ออกจากระบบใหม่ที่เมนู 08 หรือให้เราลบให้

ทางกลับต้องใช้งานได้จริงเสมอ ไม่ใช่ข้อความปลอบใจ — ถ้าวันไหนไฟล์สำรองนำกลับเข้าโปรแกรมเดิมไม่ได้
แปลว่าเราผิดสัญญาที่ให้ไว้ว่าข้อมูลเป็นของอู่ ให้ถือเป็นข้อบกพร่องระดับต้องแก้ก่อนงานอื่น

## 8. รายการตรวจก่อนเปิดให้คนนอกใช้

- [ ] HTTPS ใช้ได้และเปลี่ยนเส้นทางจาก HTTP อัตโนมัติ
- [ ] `DATABASE_URL` ใช้ role `dgl_app` และเปลี่ยนรหัสผ่านจากค่าในไฟล์ตัวอย่างแล้ว
- [ ] Postgres ไม่เปิดพอร์ตออกอินเทอร์เน็ต ฟังเฉพาะ 127.0.0.1
- [ ] cron สำรองข้อมูลรันจริง และมีไฟล์เกิดขึ้นจริงในสองวันติดกัน
- [ ] คัดลอกไฟล์สำรองออกนอกเครื่องแล้ว
- [ ] ซ้อมกู้คืนผ่านหนึ่งรอบ ตามข้อ 6
- [ ] `npm test` ผ่านทั้งหมดบนโค้ดชุดที่กำลังจะขึ้น
- [ ] หน้านโยบายข้อมูลส่วนบุคคลเปิดได้โดยไม่ต้องเข้าสู่ระบบ
- [ ] ข้อมูลติดต่อในหน้า `/privacy` และหน้าต่ออายุตรงกับความจริง

## 9. ยังไม่ได้ทำ

- **รับชำระเงินออนไลน์** ต้องเปิดบัญชีร้านค้ากับผู้ให้บริการรับชำระเงินก่อน ซึ่งเป็นเรื่องนอกโค้ด
  ตอนนี้อู่โอนเงินมาแล้วเราบันทึกการต่ออายุให้ที่เมนู 08
- **แจ้งเตือนก่อนหมดอายุทางอีเมลหรือไลน์** ตอนนี้เตือนบนหน้าจอเมื่อเหลือไม่ถึง 7 วันเท่านั้น
- **ตั้งค่าหน้าเว็บให้ทำงานหลายเครื่องพร้อมกัน** ตอนนี้ออกแบบไว้สำหรับเครื่องเดียว
