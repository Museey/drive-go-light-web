# คู่มือขึ้นใช้งานจริง — DriveGoLight!

เอกสารนี้เขียนไว้ให้คนที่ต้องกู้ระบบตอนตีสามอ่านแล้วทำตามได้ทันที
ไม่ใช่เอกสารแนะนำสถาปัตยกรรม

---

## 0. ขึ้น Render — ทางที่ใช้อยู่จริง

ทางนี้ไม่ต้องดูแลเซิร์ฟเวอร์เอง **ข้อมูลอยู่ที่สิงคโปร์** ซึ่งบอกไว้ในหน้า `/privacy` แล้ว
ถ้าวันหนึ่งต้องการให้ข้อมูลอยู่ในไทย ให้ข้ามไปทำข้อ 0ข ซึ่งเป็นทาง VPS

### 1. สร้าง Postgres

Dashboard → **New +** → **Postgres**

| ช่อง | ค่า |
|---|---|
| Region | **Singapore** — เปลี่ยนทีหลังไม่ได้ |
| PostgreSQL Version | 16 |
| Instance Type | แพ็กเกจที่เสียเงิน (ฟรีไม่มีการสำรองข้อมูลและหมดอายุ) |
| Storage | เริ่มที่ 1 GB — **เพิ่มได้ ลดไม่ได้** |

วัดจากข้อมูลจริง อู่หนึ่งรายใช้ราว 5 KB ต่อเอกสาร — อู่ 30 รายที่ออกเอกสาร
เดือนละ 150 ใบ ใช้ราว 280 MB ต่อปี

### 2. ตรวจว่าฐานข้อมูลใช้ได้

```bash
DATABASE_URL='<External URL>?sslmode=no-verify' node tools/probe-db.mjs
```

ต้องได้ทั้งสองบรรทัด — บทบาทแอปผ่าน และบทบาทผู้ดูแลผ่าน
ถ้าไม่ผ่านข้อใดข้อหนึ่ง ตัวตรวจจะบอกเองว่าต้องทำอะไรต่อ

`?sslmode=no-verify` ต้องมี เพราะ Render บังคับ SSL ส่วน `pg` ตีความ `require`
เป็น `verify-full` ซึ่งล้มกับใบรับรองที่ผู้ให้บริการออกเอง

### 3. รันไมเกรชัน ตั้งเขตเวลา และสร้าง role ของแอป

**ไม่ต้องมี `psql` บนเครื่อง** — เครื่องมือทั้งหมดเป็น Node ใช้ไลบรารีที่โปรเจกต์มีอยู่แล้ว

```bash
ADMIN='<External URL>?sslmode=no-verify'

# สร้างสคีมา
ADMIN_URL="$ADMIN" node tools/migrate.mjs --fresh

# ตั้งเขตเวลา + สร้าง role ของแอป + ให้สิทธิ์ ในคำสั่งเดียว
ADMIN_URL="$ADMIN" node tools/setup-db.mjs
```

คำสั่งที่สองพิมพ์ `DATABASE_URL` ที่ประกอบเสร็จแล้วออกมาให้ **เก็บไว้ให้ดี**
เพราะไม่แสดงรหัสผ่านซ้ำอีก ถ้าทำหายให้รันใหม่ จะได้รหัสใหม่มาแทน

อยากกำหนดรหัสเองก็ได้ — `node tools/setup-db.mjs --password='รหัสที่ต้องการ'`

> คนที่มี `psql` อยู่แล้ว (เช่นตอนดูแลเซิร์ฟเวอร์เอง) ใช้ `db/app-role-managed.sql`
> แทนได้ ผลเหมือนกัน

**ทำไมต้องมี role แยก** — role ที่ Render ให้มาเป็นเจ้าของตาราง และเจ้าของตาราง
สั่ง `alter table ... no force row level security` ได้ คือปิดเกราะของตัวเอง
ในคำสั่งเดียว ถ้าวันหนึ่งมีช่องโหว่ที่ยิง SQL อะไรก็ได้ ความต่างนี้คือ
"เห็นเฉพาะอู่ตัวเอง" กับ "เห็นทุกอู่"

### 4. สร้างเว็บและงานตามเวลา

Dashboard → **New +** → **Blueprint** → เลือก repo นี้ → Render อ่าน `render.yaml` เอง

กรอกค่าลับสองตัวที่ Blueprint ถาม

| ตัวแปร | ใส่อะไร |
|---|---|
| `DATABASE_URL` | URL ที่เปลี่ยน user กับรหัสเป็น `dgl_app` และรหัสที่สุ่มไว้ · ต่อท้าย `?sslmode=no-verify` |
| `ADMIN_URL` | External URL เดิมของ Render · ต่อท้าย `?sslmode=no-verify` |

รูปแบบของ `DATABASE_URL`

```
postgresql://dgl_app:<APP_PASS>@dpg-xxxxx-a.singapore-postgres.render.com/dgl?sslmode=no-verify
```

### 5. ตรวจว่าขึ้นแล้วใช้ได้

```bash
curl -s https://<ชื่อบริการ>.onrender.com/healthz
```

ต้องได้ `{"ok":true,"failed":[]}` เท่านั้น

### 6. เปิดอู่แรก

รันตัวนำเข้า**จากเครื่องเรา** ชี้ไปฐานข้อมูลจริง ไม่ต้องรันบน Render

```bash
DATABASE_URL="$ADMIN" node packages/importer/dist/cli.js <ไฟล์สำรอง>.json \
  --owner-email=<อีเมลเจ้าของอู่> \
  --app-url=https://<ชื่อบริการ>.onrender.com
```

ใช้ `ADMIN_URL` เพราะตัวนำเข้าต้องเขียนข้ามอู่ตอนสร้างอู่ใหม่

---

## 0ข. ทางเลือก — เซิร์ฟเวอร์ที่เราดูแลเอง (VPS)

ยังไม่ได้ใช้ทางนี้ — เก็บไว้เผื่อวันหนึ่งต้องย้ายข้อมูลมาไทย
ย้ายคือ dump แล้ว restore เพราะทั้งระบบเป็น Postgres กับ Node ธรรมดา

ทำครั้งเดียวตอนตั้งเครื่องใหม่ ใช้เวลาราวหนึ่งชั่วโมง

### 0.1 เช่าเครื่อง

| อย่าง | ที่ต้องเลือก |
|---|---|
| สเปก | 2 vCPU · 4 GB RAM · SSD 60 GB |
| ระบบปฏิบัติการ | **Ubuntu 24.04 LTS** (Postgres 16 อยู่ในคลังปริยาย) |
| ที่ตั้ง | **ศูนย์ข้อมูลในไทย** — ผู้ใช้อยู่ไทยทั้งหมด และข้อมูลอยู่ในประเทศตามที่หน้า `/privacy` บอกไว้ |
| การเข้าเครื่อง | ใส่ **SSH public key** ตอนสั่งซื้อ อย่าเลือกให้ส่งรหัสผ่านทางอีเมล |

ยังไม่มีคู่กุญแจ SSH ให้สร้างบนเครื่องตัวเองก่อน แล้วเอาค่าที่พิมพ์ออกมาไปวางตอนสั่งซื้อ

```bash
ssh-keygen -t ed25519 -C "dgl-deploy"
cat ~/.ssh/id_ed25519.pub
```

### 0.2 ตั้งชื่อโดเมนให้ชี้มาที่เครื่อง

ที่ผู้ให้บริการโดเมน เพิ่มเรคคอร์ด `A` ชี้ไปที่ IP ของเครื่อง
**ทำก่อนตั้ง Caddy** เพราะ Caddy ขอใบรับรอง HTTPS ทันทีที่เริ่มทำงาน
ถ้าโดเมนยังไม่ชี้มาจะขอไม่ผ่านแล้วต้องรอโควตาใหม่

```bash
dig +short app.example.com      # ต้องได้ IP ของเครื่อง
```

### 0.3 เข้าเครื่องครั้งแรกและปิดประตูหลัง

```bash
ssh root@<IP>

apt update && apt upgrade -y
timedatectl set-timezone Asia/Bangkok        # สำคัญ — ทั้งระบบคิดวันที่ตามเวลาไทย

# ผู้ใช้ที่แอปจะรันด้วย ไม่มีสิทธิ์ล็อกอินและไม่มี shell
adduser --system --group --home /srv/drivegolight dgl

# ผู้ใช้ที่เราใช้ดูแลเครื่อง
adduser deploy
usermod -aG sudo deploy
mkdir -p /home/deploy/.ssh
cp ~/.ssh/authorized_keys /home/deploy/.ssh/
chown -R deploy:deploy /home/deploy/.ssh
chmod 700 /home/deploy/.ssh && chmod 600 /home/deploy/.ssh/authorized_keys
```

ทดสอบว่าเข้าด้วย `deploy` ได้ **จากอีกหน้าต่างหนึ่ง โดยยังไม่ปิดหน้าต่าง root**
ถ้าปิดก่อนแล้วเข้าไม่ได้จะเข้าเครื่องไม่ได้อีกเลย

```bash
ssh deploy@<IP> 'sudo whoami'     # ต้องได้ root
```

ได้แล้วค่อยปิดการล็อกอินด้วยรหัสผ่านและปิด root

```bash
sudo sed -i 's/^#*PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
sudo sed -i 's/^#*PasswordAuthentication.*/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo systemctl restart ssh
```

### 0.4 ไฟร์วอลล์

```bash
sudo ufw allow OpenSSH
sudo ufw allow 80,443/tcp
sudo ufw --force enable
sudo ufw status
```

**ห้ามเปิดพอร์ต 5432 ออกอินเทอร์เน็ต** — Postgres ฟังเฉพาะ 127.0.0.1 เท่านั้น

### 0.5 ติดตั้งของที่ต้องใช้

```bash
# Node 22 จาก NodeSource — ต้องได้ npm ที่ /usr/bin/npm ตามที่ไฟล์ systemd อ้าง
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node -v && which npm                      # ต้องเป็น v22.x และ /usr/bin/npm

# Postgres 16
sudo apt install -y postgresql postgresql-contrib
psql --version

# Caddy
sudo apt install -y debian-keyring debian-archive-keyring apt-transport-https curl
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' \
  | sudo gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' \
  | sudo tee /etc/apt/sources.list.d/caddy-stable.list
sudo apt update && sudo apt install -y caddy
```

### 0.6 เอาโค้ดขึ้นเครื่อง

repo เป็นของส่วนตัว จึงต้องมีสิทธิ์อ่าน — วิธีที่ง่ายที่สุดคือสร้าง **deploy key**
(กุญแจที่อ่านได้อย่างเดียว ผูกกับ repo เดียว ถอนได้โดยไม่กระทบบัญชี)

```bash
sudo -u dgl ssh-keygen -t ed25519 -f /srv/drivegolight/.ssh/id_ed25519 -N ''
sudo cat /srv/drivegolight/.ssh/id_ed25519.pub
```

เอาค่าที่ได้ไปใส่ที่ GitHub → repo → Settings → Deploy keys → Add deploy key
(ไม่ต้องติ๊ก Allow write access)

```bash
sudo -u dgl git clone git@github.com:Museey/drive-go-light-web.git /srv/drivegolight
```

### 0.7 สร้างฐานข้อมูลและไฟล์ค่าลับ

```bash
sudo -u postgres createdb dgl

# ตั้งรหัสผ่านของ role แอป — สุ่มมา อย่าตั้งเอง
DGL_PASS=$(openssl rand -base64 24)
echo "$DGL_PASS"                          # จดไว้ ใช้ในขั้นตอนถัดไป

sudo mkdir -p /etc/drivegolight
sudo tee /etc/drivegolight/env > /dev/null <<EOF
DATABASE_URL=postgresql://dgl_app:$DGL_PASS@127.0.0.1:5432/dgl
EOF
sudo chown root:dgl /etc/drivegolight/env
sudo chmod 640 /etc/drivegolight/env
```

ไฟล์นี้อ่านได้เฉพาะ root กับกลุ่ม `dgl` — **ห้ามเก็บรหัสผ่านไว้ในไฟล์ systemd**
เพราะไฟล์นั้นอ่านได้ทั้งเครื่องและมักถูกคัดลอกไปแปะเวลาถามปัญหา

จากนั้นไปทำข้อ 3 ต่อ

---

## 1. สิ่งที่ต้องมีบนเครื่องเซิร์ฟเวอร์

| อย่าง | รุ่น | หมายเหตุ |
|---|---|---|
| Node.js | 22 LTS | รุ่นต่ำกว่า 20 ใช้ไม่ได้ |
| PostgreSQL | 16 | ต้องเป็นเครื่องที่สำรองข้อมูลได้จริง |
| ตัวรับ HTTPS | nginx / Caddy | คุกกี้ session ตั้ง `secure` เมื่อ `NODE_ENV=production` ถ้าไม่มี HTTPS จะเข้าสู่ระบบไม่ได้ |

### เขตเวลา

แอปคิดวันที่ตามเวลาไทยเสมอ ไม่ว่าเครื่องจะตั้งเขตเวลาเป็นอะไร
ส่วนฐานข้อมูลตั้งให้ role ของแอปแล้วใน `db/app-role.sql`
แอปตรวจตอนเปิดการเชื่อมต่อว่าสองฝั่งเห็นวันที่ตรงกัน ถ้าไม่ตรงจะปฏิเสธการทำงานพร้อมบอกวิธีแก้

ที่ต้องตรวจเพราะเซิร์ฟเวอร์และ Docker ตั้งต้นเป็น UTC ซึ่งช้ากว่าไทยเจ็ดชั่วโมง
ช่วงเที่ยงคืนถึงเจ็ดโมงเช้า เอกสารจะลงวันที่ของเมื่อวานโดยไม่มีอาการให้เห็น

## 2. ตัวแปรสภาพแวดล้อม

เก็บไว้ที่ `apps/web/.env.local` หรือส่งผ่าน systemd

```
DATABASE_URL=postgresql://dgl_app:<รหัสผ่าน>@127.0.0.1:5432/dgl
NODE_ENV=production
```

`DATABASE_URL` **ต้องเป็น role `dgl_app` เท่านั้น** ห้ามใช้ `postgres`
แอปตรวจตอนเปิดการเชื่อมต่อว่า role มีสิทธิ์ข้าม Row Level Security หรือไม่ ถ้ามีจะปฏิเสธการทำงานทันที
เพราะ role ที่ข้าม RLS ได้แปลว่าทุกอู่มองเห็นข้อมูลของกันและกันโดยไม่มีอาการอะไรให้เห็น

## 3. ติดตั้งครั้งแรก

ทำต่อจากข้อ 0 — ฐานข้อมูล `dgl` และไฟล์ `/etc/drivegolight/env` มีแล้ว

**สร้างสคีมาและ role ของแอป**

```bash
cd /srv/drivegolight

# ไมเกรชันรันด้วยสิทธิ์ผู้ดูแลฐานข้อมูล ไม่ใช่ role ของแอป
sudo -u postgres env ADMIN_URL=dgl ./tools/migrate.sh --fresh
sudo -u postgres env ADMIN_URL=dgl ./tools/migrate.sh --status   # ต้องขึ้นครบทุกไฟล์

# ใส่รหัสผ่านที่สุ่มไว้ในข้อ 0.7 ลงไปแทนค่าตัวอย่าง แล้วรัน
DGL_PASS=$(sudo grep -oP '(?<=dgl_app:)[^@]+' /etc/drivegolight/env)
sed "s/เปลี่ยนรหัสนี้ก่อนใช้จริง/$DGL_PASS/" db/app-role.sql \
  | sudo -u postgres psql -d dgl -v ON_ERROR_STOP=1
```

**สร้างไฟล์ที่จะรัน**

```bash
# -H สำคัญ — ให้ npm เขียนแคชลงบ้านของ dgl ไม่ใช่บ้านของคนที่สั่ง sudo
sudo -u dgl -H npm ci
sudo -u dgl -H npm run build -w @drivegolight/core -w @drivegolight/importer
sudo -u dgl -H npm run build -w @drivegolight/web
```

**เปิดบริการ**

```bash
sudo cp deploy/drivegolight.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now drivegolight
systemctl status drivegolight            # ต้องเป็น active (running)

sudo sed -i 's/app\.example\.com/<โดเมนจริง>/' deploy/Caddyfile
sudo cp deploy/Caddyfile /etc/caddy/Caddyfile
sudo systemctl reload caddy
```

ถ้า `systemctl status` ไม่เขียว ให้ดูสาเหตุที่ `journalctl -u drivegolight -n 50`
สาเหตุที่พบบ่อยที่สุดคือ `DATABASE_URL` ผิด หรือยังไม่ได้รัน `db/app-role.sql`

**ติดตั้งใหม่กับอัปเกรดใช้คนละคำสั่ง**

| สถานการณ์ | คำสั่ง |
|---|---|
| ฐานข้อมูลยังว่างเปล่า | `tools/migrate.sh --fresh` |
| อัปเกรดของเดิมหลังดึงโค้ดใหม่ | `tools/migrate.sh` |
| อยากดูก่อนว่าจะรันอะไร | `tools/migrate.sh --dry-run` |
| ดูว่ารันอะไรไปแล้ว | `tools/migrate.sh --status` |

ตัวรันเป็น Node ใช้ไลบรารี `pg` ที่โปรเจกต์มีอยู่แล้ว **ไม่ต้องมี `psql` บนเครื่อง**
จึงใช้คำสั่งเดียวกันได้ทั้งบนเซิร์ฟเวอร์ที่เราดูแลเอง บนแพลตฟอร์ม และบนเครื่องพัฒนา

`001_init.sql` เก็บสคีมาปัจจุบันไว้ครบ ไฟล์ `003` เป็นต้นไปจึงเป็น "ทางเดินจากของเก่ามาหาปัจจุบัน"
ไม่ใช่ของที่ต้องรันซ้ำ — ฐานใหม่ที่รัน `001` แล้วไปรัน `007` ต่อจะพังทันที
`--fresh` จึงรันเฉพาะไฟล์ฐานแล้วจดที่เหลือว่ารันแล้ว
มีชุดทดสอบพิสูจน์ว่าสองทางให้ฐานข้อมูลเหมือนกันทุกคอลัมน์ ทุกเงื่อนไข ทุกดัชนี

ตัวรันจดสิ่งที่รันไปแล้วไว้ใน `ops.migrations` พร้อมลายนิ้วมือของไฟล์
**ถ้าไฟล์ที่ขึ้นเครื่องจริงไปแล้วถูกแก้ ตัวรันจะหยุดทันที** — ให้เขียนไฟล์ใหม่ต่อท้ายแทน

`db/app-role.sql` รันซ้ำได้ และ**ต้องรันซ้ำทุกครั้งหลังกู้ฐานข้อมูล** เพราะสิทธิ์บนตารางหายไปพร้อมฐานข้อมูลเก่า
ส่วนตัว role อยู่ระดับคลัสเตอร์จึงยังอยู่

## 3.1 ตรวจว่าระบบพร้อมจริง

```bash
curl -s https://<โดเมนจริง>/healthz
```

ตอบ `{"ok":true,"failed":[]}` เท่านั้นจึงถือว่าพร้อม ตรวจสามอย่างที่เคยพังจริง

| ชื่อ | ตรวจอะไร |
|---|---|
| `db` | ต่อฐานข้อมูลได้ และ role **ไม่ได้** ข้าม RLS |
| `clock` | เขตเวลาแอปกับฐานข้อมูลเห็นวันเดียวกัน |
| `migrations` | ไมเกรชันที่โค้ดต้องการรันครบแล้ว |

ตอบ 503 เมื่อข้อใดข้อหนึ่งตก ใช้เป็นตัวตรวจสุขภาพของระบบเฝ้าระวังได้เลย

## 3.2 ดูข้อผิดพลาดที่เกิดขึ้นจริง

ข้อผิดพลาดถูกบันทึกลง `ops.errors` **ในฐานข้อมูลของเราเอง ไม่ส่งออกนอกประเทศ**
เพราะข้อความผิดพลาดมีข้อมูลลูกค้าติดไปด้วยเสมอ

```bash
tools/errors.sh            # 20 รายการล่าสุด
tools/errors.sh 100        # 100 รายการล่าสุด
```

รหัสผ่าน token และ connection string ถูกกรองออกก่อนบันทึกเสมอ (มีชุดทดสอบยืนยัน)
ลบของเกิน 90 วันด้วย `select ops.prune_errors(90)` ให้ตรงกับที่หน้า PDPA บอกไว้

**ยังไม่มีการแจ้งเตือน** — ต้องเปิดดูเอง จนกว่าจะทำระบบส่งอีเมลเสร็จ

## 4. เปิดอู่ใหม่หนึ่งราย

```bash
DATABASE_URL=... node packages/importer/dist/cli.js <ไฟล์สำรองของอู่>.json \
  --owner-email=<อีเมลเจ้าของอู่> \
  --app-url=https://<โดเมนจริง>
```

คำสั่งนี้ **ก๊อปลิงก์ตั้งรหัสผ่านลงคลิปบอร์ดให้** แล้วพิมพ์แค่รูปร่างออกมาให้ตรวจ
ใช้ได้ครั้งเดียว หมดอายุ 7 วัน — เครื่องที่ไม่มีคลิปบอร์ดให้สั่ง `--show`
ส่งให้เจ้าของอู่ทางช่องทางที่ยืนยันตัวตนได้ (โทรบอก หรือไลน์ที่คุยกันอยู่แล้ว) ไม่ใช่โพสต์ในกลุ่ม

อู่ที่เพิ่งเปิดจะได้ทดลองใช้ฟรี 15 วันนับจากวันเปิด หลังจากนั้นบันทึกรายการใหม่ไม่ได้จนกว่าจะบันทึกการต่ออายุ
ที่เมนู 08 — แต่ยังเปิดดู พิมพ์ และดาวน์โหลดไฟล์สำรองได้เสมอ

## 5. สำรองข้อมูลอัตโนมัติ

```bash
0 3 * * * DATABASE_URL=postgresql://postgres@127.0.0.1/dgl BACKUP_DIR=/srv/dgl-backups \
  /srv/drivegolight/tools/backup-db.sh >> /var/log/dgl-backup.log 2>&1
```

`tools/backup-db.sh` ทำสามอย่างที่สคริปต์สำรองทั่วไปมักลืม

1. อ่านสารบัญไฟล์กลับทันทีหลังดัมป์ ไฟล์ที่เปิดไม่ออกไม่นับว่าสำเร็จ
2. เขียน `.sha256` คู่ไว้ ใช้เทียบตอนคัดลอกไปเก็บที่อื่น
3. ล้มถ้าไฟล์เล็กกว่า 10 KB — อาการของการต่อผิดฐานข้อมูล

เก็บย้อนหลัง 90 วัน (`KEEP_DAYS`) ให้ตรงกับที่เขียนไว้ในหน้านโยบายข้อมูลส่วนบุคคล
**ต้องคัดลอกออกไปเก็บนอกเครื่องด้วย** ไฟล์สำรองที่อยู่บนเครื่องเดียวกับฐานข้อมูลไม่ช่วยอะไรตอนดิสก์พัง

## 6. ซ้อมกู้คืน

ซ้อมทุกไตรมาส และซ้อมก่อนย้ายลูกค้าเข้าใหม่ทุกครั้ง ห้ามซ้อมด้วยการอ่านคู่มือเฉย ๆ

```bash
createdb dgl_drill
tools/restore-db.sh /srv/dgl-backups/dgl-<วันที่>.dump \
  postgresql://postgres@127.0.0.1/dgl_drill
```

สคริปต์จะเทียบลายนิ้วมือไฟล์ ล้าง schema ปลายทาง กู้ข้อมูล แล้วตรวจสองอย่าง
คือจำนวนแถวกับยอดขายรวม และว่า **ทุกตารางที่มี `tenant_id` ยังเปิด row level security แบบ force อยู่**
ข้อหลังสำคัญที่สุด เพราะฐานข้อมูลที่กู้มาแล้ว RLS ไม่ติดคือรอยรั่วข้ามอู่ที่ไม่มีอาการให้เห็น

ตรวจต่ออีกสองข้อด้วยมือ

```bash
# 1. role ของแอปใช้งานได้จริงหลังกู้
psql "$ADMIN_URL/dgl_drill" -f db/app-role.sql
psql "postgresql://dgl_app:<รหัส>@127.0.0.1/dgl_drill" \
  -c "select set_config('app.tenant_id','<รหัสอู่>',false)" \
  -c "select count(*), sum(grand_total) from documents"

# 2. ตั้งรหัสอู่ปลอมแล้วต้องได้ 0 แถว
psql "postgresql://dgl_app:<รหัส>@127.0.0.1/dgl_drill" \
  -c "select set_config('app.tenant_id','00000000-0000-0000-0000-000000000000',false)" \
  -c "select count(*) from documents"
```

ผลการซ้อมรอบล่าสุด — 31 สิงหาคม 2569 กับข้อมูลชุดทดสอบ 734 เอกสาร

| ตรวจ | ต้นทาง | ที่กู้มา |
|---|---|---|
| อู่ / ผู้ใช้ / สินค้า | 1 / 1 / 200 | 1 / 1 / 200 |
| ลูกค้า / รถ | 30 / 33 | 30 / 33 |
| เอกสาร / รายการ / การชำระ | 734 / 2275 / 424 | 734 / 2275 / 424 |
| ยอดรวมเอกสาร | 7,282,074.60 | 7,282,074.60 |
| ยอดชำระรวม | 3,808,520.23 | 3,808,520.23 |
| สต๊อกคงเหลือรวม | 3,177.000 | 3,177.000 |
| ชุดเลขที่เอกสาร | 6 | 6 |

หลังกู้ `dgl_app` อ่านข้อมูลของอู่ตัวเองได้ครบ 734 เอกสาร ตั้งรหัสอู่ปลอมได้ 0 แถว
และ `select ... from auth.sessions` ถูกปฏิเสธ ซึ่งถูกต้อง — แอปต้องแตะสคีมา `auth` ผ่านฟังก์ชันเท่านั้น

ลบฐานข้อมูลซ้อมทิ้งเมื่อเสร็จ `dropdb dgl_drill`

## 7. ย้ายลูกค้าเดิมจากโปรแกรม HTML

โปรแกรมเดิมเก็บข้อมูลไว้ในเครื่องลูกค้า ไม่มีใครถือสำเนากลาง การย้ายจึงต้องให้อู่ส่งไฟล์สำรองมาเอง
และต้องย้ายทีละราย ไม่ย้ายพร้อมกันทั้งหมด

**ลำดับการย้าย**

1. **รอบที่ 1 — อู่ของเราเอง** ใช้จริงหนึ่งเดือนเต็มก่อนชวนใครมา ปิดรอบภาษีบนระบบใหม่ให้ได้หนึ่งรอบ
2. **รอบที่ 2 — อู่ที่ยินดีเป็นคนแรก 2–3 ราย** เลือกรายที่เอกสารไม่เยอะและติดต่อได้ง่าย
   นัดย้ายวันจันทร์ต้นเดือน จะได้มีทั้งสัปดาห์ให้แก้ถ้าติดปัญหา
3. **รอบที่ 3 — ที่เหลือ** เมื่อรอบที่ 2 ผ่านหนึ่งเดือนโดยไม่ต้องย้อนกลับ

**ขั้นตอนของอู่หนึ่งราย**

1. อู่กดสำรองข้อมูลจากโปรแกรมเดิม ส่งไฟล์ `.json` มาให้
2. **เก็บไฟล์นั้นไว้ไม่แตะต้อง** เป็นจุดย้อนกลับ ตั้งชื่อ `<ชื่ออู่>-ก่อนย้าย-<วันที่>.json`
3. นำเข้าด้วยคำสั่งในข้อ 4 บนฐานข้อมูลจริง
4. อ่านรายการเตือนที่ตัวนำเข้าพิมพ์ออกมาให้ครบ โดยเฉพาะเลขที่เอกสารซ้ำหรือว่าง ซึ่งตัวนำเข้าเติมให้เอง
5. เทียบกับหน้าจอโปรแกรมเดิมสามตัวเลขต่อหน้าอู่ — ยอดขายรวมทั้งปี ยอดลูกหนี้คงค้าง และจำนวนเอกสาร
6. ให้อู่ลองออกใบกำกับภาษีใหม่หนึ่งใบแล้วพิมพ์ ดูว่าเลขที่ต่อจากของเดิมและหน้าตากระดาษใช้ได้
7. **บอกให้ชัดว่าตั้งแต่นี้ให้บันทึกงานในระบบใหม่เท่านั้น** ห้ามบันทึกสองที่พร้อมกัน
   ข้อมูลสองชุดที่ไม่ตรงกันแก้ยากกว่าการย้ายใหม่ทั้งหมด
8. บอกให้เก็บไฟล์ HTML เดิมไว้ก่อน อย่าเพิ่งลบ ใช้เปิดดูย้อนหลังได้

**ถ้าอู่ไม่พอใจและจะย้อนกลับ**

ภายใน 30 วันแรกทำได้ไม่ยาก เพราะโปรแกรมเดิมยังอยู่ในเครื่องเขา

1. อู่ดาวน์โหลดไฟล์สำรองจากระบบใหม่ที่เมนู 07 — เป็นรูปแบบเดียวกับโปรแกรมเดิมทุกประการ
   (มีชุดทดสอบตรวจว่าส่งออกแล้วนำกลับเข้าได้ยอดตรงทุกบาท)
2. เปิดโปรแกรม HTML เดิม กดนำเข้าไฟล์นั้น งานที่ทำในระบบใหม่ระหว่างนั้นตามกลับไปด้วย
3. ถ้าไฟล์มีปัญหา ใช้ไฟล์ `<ชื่ออู่>-ก่อนย้าย-<วันที่>.json` ที่เก็บไว้ในข้อ 2 แทน
   เสียแค่งานช่วงที่อยู่บนระบบใหม่ ไม่เสียของเก่า
4. ลบข้อมูลของอู่ออกจากระบบใหม่ที่เมนู 08 หรือให้เราลบให้

ทางกลับต้องใช้งานได้จริงเสมอ ไม่ใช่ข้อความปลอบใจ — ถ้าวันไหนไฟล์สำรองนำกลับเข้าโปรแกรมเดิมไม่ได้
แปลว่าเราผิดสัญญาที่ให้ไว้ว่าข้อมูลเป็นของอู่ ให้ถือเป็นข้อบกพร่องระดับต้องแก้ก่อนงานอื่น

## 8. รายการตรวจก่อนเปิดให้คนนอกใช้

- [ ] HTTPS ใช้ได้และเปลี่ยนเส้นทางจาก HTTP อัตโนมัติ
- [ ] `DATABASE_URL` ใช้ role `dgl_app` และเปลี่ยนรหัสผ่านจากค่าในไฟล์ตัวอย่างแล้ว
- [ ] Postgres ไม่เปิดพอร์ตออกอินเทอร์เน็ต ฟังเฉพาะ 127.0.0.1
- [ ] cron สำรองข้อมูลรันจริง และมีไฟล์เกิดขึ้นจริงในสองวันติดกัน
- [ ] คัดลอกไฟล์สำรองออกนอกเครื่องแล้ว
- [ ] ซ้อมกู้คืนผ่านหนึ่งรอบ ตามข้อ 6
- [ ] `npm test` ผ่านทั้งหมดบนโค้ดชุดที่กำลังจะขึ้น
- [ ] เปิดหน้าใดก็ได้แล้วไม่ขึ้นข้อผิดพลาดเรื่องเขตเวลา (แอปตรวจให้เองตอนต่อฐานข้อมูล)
- [ ] หน้านโยบายข้อมูลส่วนบุคคลเปิดได้โดยไม่ต้องเข้าสู่ระบบ
- [ ] ข้อมูลติดต่อในหน้า `/privacy` และหน้าต่ออายุตรงกับความจริง
- [ ] `curl https://<โดเมน>/healthz` ตอบ `{"ok":true,"failed":[]}`
- [ ] ปิดล็อกอินด้วยรหัสผ่านและปิด root แล้ว (`ssh root@<IP>` ต้องถูกปฏิเสธ)
- [ ] `sudo ufw status` เปิดแค่ 22 · 80 · 443
- [ ] `sudo systemctl is-enabled drivegolight caddy postgresql` ได้ enabled ทั้งสาม
      (รีบูตเครื่องแล้วต้องกลับมาเองโดยไม่ต้องสั่ง)
- [ ] `/etc/drivegolight/env` เป็น `root:dgl` และ `chmod 640`

## 9. ยังไม่ได้ทำ

- **รับชำระเงินออนไลน์** ต้องเปิดบัญชีร้านค้ากับผู้ให้บริการรับชำระเงินก่อน ซึ่งเป็นเรื่องนอกโค้ด
  ตอนนี้อู่โอนเงินมาแล้วเราบันทึกการต่ออายุให้ที่เมนู 08
- **แจ้งเตือนก่อนหมดอายุทางอีเมลหรือไลน์** ตอนนี้เตือนบนหน้าจอเมื่อเหลือไม่ถึง 7 วันเท่านั้น
- **ตั้งค่าหน้าเว็บให้ทำงานหลายเครื่องพร้อมกัน** ตอนนี้ออกแบบไว้สำหรับเครื่องเดียว
