# แผนไล่ให้ทันรุ่น 6.4 cloud

เอกสารนี้เป็นแผนทำงานจริง ไม่ใช่เอกสารขาย ทุกช่วงมีของที่ส่งมอบได้และวิธีตรวจว่าเสร็จจริง
ปรับได้ระหว่างทาง แต่ให้แก้ที่ไฟล์นี้ ไม่ใช่จำไว้ในหัว

อ้างอิง: `legacy/drivegolight-6.4-cloud.html` (995 KB · 26 ส.ค. 2569)

---

## หลักที่ใช้ตัดสินใจ

**1. ไม่เริ่มใหม่** — 12,373 บรรทัดไม่ผูกกับรุ่นของโปรแกรมเดิม (ฐานข้อมูล การแยกอู่
เข้าสู่ระบบ ลิขสิทธิ์ PDPA สำรอง/กู้คืน หน้าพิมพ์เอกสาร และชุดทดสอบ 244 ข้อ)
ส่วนที่ผูกกับหน้าตาคือ 6,298 บรรทัด และงานโมดูลใหม่เท่ากันทั้งสองทาง

**2. เปลือก UI มาก่อนโมดูลใหม่** — ทุกโมดูลที่ทำหลังจากนั้นจะเขียนบนหน้าตาชุดสุดท้ายเลย
ไม่ต้องรื้อสองรอบ

**3. เรื่องที่ย้อนหลังไม่ได้ต้องมาก่อนข้อมูลจริง** — วิธีคิดต้นทุนเปลี่ยนแล้วคำนวณย้อนหลังไม่ได้
จึงต้องลงก่อนอู่แรกเริ่มบันทึก

**4. ไม่ยกข้อบกพร่องของต้นฉบับมา** — ที่เจอแล้วสองจุด (กฎ Max ที่เตือนทุกตัว
และการเก็บล็อตแบบทับข้อมูลเดิม) ทำให้ถูกและเขียนคอมเมนต์บอกว่าทำไมไม่เหมือนต้นฉบับ

---

## ช่วงที่ 0 — กันข้อมูลหายตอนนำเข้า · ครึ่งวัน · **เสร็จแล้ว**

ทำก่อนทุกอย่างเพราะถูกที่สุดและกันความเสียหายที่แก้คืนไม่ได้

ตอนนี้ไฟล์สำรองจาก 6.4 ที่มี `billnotes` `claims` `counts` ผ่านการตรวจโดยไม่มีคำเตือน
แล้วสามกลุ่มนั้นหายไปเงียบ ๆ

**ส่งมอบ**
- ตัวนำเข้าตรวจเจอกลุ่มที่ยังรองรับไม่ได้ แล้วรายงานเป็นคำเตือนพร้อมจำนวนแถว
- CLI แสดงคำเตือนเด่นชัด และหน้ากู้คืนบนเว็บแสดงให้ผู้ใช้เห็นก่อนกดยืนยัน
- เลือกได้ว่าจะยกเลิกหรือยืนยันว่ารับทราบว่าข้อมูลส่วนนั้นจะไม่ถูกนำเข้า

**เสร็จเมื่อ** มีเทสต์ที่ป้อนไฟล์ซึ่งมีสามกลุ่มนั้นแล้วยืนยันว่าได้คำเตือนพร้อมจำนวนถูกต้อง

**ผลที่ทำจริง** — `packages/importer/src/support.ts`
- ตรวจสี่กลุ่ม แยกเป็น "หายทั้งกลุ่ม" (ใบวางบิล ใบเคลม ใบตรวจนับ) กับ
  "ยอดยังถูกแต่ประวัติหาย" (การเคลื่อนไหวสต๊อก) เพราะสองอย่างนี้ผู้ใช้ตัดสินใจไม่เหมือนกัน
- CLI หยุดด้วย exit 2 ถ้ามีข้อมูลจะหาย ต้องใส่ `--accept-data-loss` ถึงจะไปต่อ
- หน้าเว็บแยกเป็นสองขั้น ตรวจไฟล์ก่อนแล้วค่อยยืนยัน และเซิร์ฟเวอร์ตรวจซ้ำเอง
  ไม่เชื่อว่าหน้าเว็บตรวจมาแล้ว
- เทสต์ 13 ข้อ รวมข้อที่ยืนยันว่า **การตรวจไฟล์ไม่แตะข้อมูลเดิมแม้แต่แถวเดียว**

---

## ช่วงที่ 1 — ล็อตสินค้าและต้นทุนขาย · 4–5 วัน

ต้องมาก่อนข้อมูลจริง เพราะเปลี่ยนวิธีคิดต้นทุนแล้วย้อนหลังไม่ได้

**6.4 ทำอย่างไร** เก็บ `p.lots = [{q, c, d}]` บนตัวสินค้า ตัดแบบเข้าก่อนออกก่อน
(`lotConsume` เดินจากล็อตหน้าสุด) ของที่คืนกลับเข้าล็อตหน้าสุดด้วยต้นทุนจริง
ถ้าล็อตหมดก่อนก็ใช้ `p.cost` เป็นตัวสำรอง

**เราจะทำอย่างไร** คิดแบบเข้าก่อนออกก่อนจาก `stock_moves` ที่มีอยู่แล้ว
ไม่เก็บอาเรย์ล็อตที่เขียนทับตัวเอง

เหตุผล: `stock_moves` เป็นบัญชีเดินสะพัดที่เพิ่มอย่างเดียวอยู่แล้ว และมี `unit_cost` ต่อแถว
การคิดล็อตจากบัญชีนี้ให้ผลเท่ากันแต่**เก็บประวัติไว้ครบ** ต่างจากต้นฉบับที่พอตัดล็อตแล้ว
ข้อมูลล็อตเดิมหายไป ตรวจย้อนไม่ได้ว่าต้นทุนที่คิดไปมาจากไหน

**แตะฐานข้อมูล**
- `stock_moves` เพิ่ม `cost_amount numeric(14,2)` — ต้นทุนรวมของการตัดครั้งนั้น
- เพิ่มชนิดใน `stock_reason`: `use` (เบิกใช้ในอู่) · `count` (ปรับตามตรวจนับ) · `set` (ตั้งยอด)
  ให้ตรงกับ `MOVE_KIND_LABEL` ของ 6.4

**ส่งมอบ**
- ฟังก์ชัน `fifoCost()` ใน `packages/core` — รับรายการรับเข้าและจำนวนที่ตัด คืนต้นทุน
- ทุกจุดที่ตัดสต๊อก (ขาย เคลม เบิกใช้ ตรวจนับ) บันทึกต้นทุนที่คิดได้ลงแถวนั้น
- งบกำไรขาดทุนใช้ต้นทุนจากบัญชีนี้แทนต้นทุนล่าสุด
- หน้าสินค้าแสดงล็อตคงเหลือพร้อมต้นทุนต่อล็อต

**เสร็จเมื่อ** ชุดทดสอบ differential เทียบ `fifoCost()` กับ `lotConsume` ของ 6.4
ด้วยลำดับรับเข้า/ตัดออกแบบสุ่มอย่างน้อย 2,000 ชุด แล้วได้ต้นทุนตรงกันทุกสตางค์

---

## ช่วงที่ 2 — เปลือก UI ให้เป็น 6.4 · 5–7 วัน

**ที่เปลี่ยน**
- ชุดสีจากน้ำเงินเทา `#17607A` เป็นเขียว `#1D8A5F` + `#0F5C3E` + navy `#1B2A4A`
- เมนูแบบการ์ด (`menuCard` `actCard` `pageBoxes` `cardLabels`)
- การนำทางแบบมีประวัติย้อนกลับ (`navPush` `navBack` `navSpot`) พร้อมปุ่มย้อนกลับลอยบนมือถือ
- หน้าต่างเอกสาร (`docwinOpen` / `docwinRequestClose`) แทนการเปิดเป็นหน้าเต็ม
- ตัวเลือกเดือนแบบใหม่ (`drawMonthPick`) และเมนูหล่น (`bindDrops` `paintDrop`)
- แถบแท็บย่อยแบบมือถือ (`subtabBarM`) · เรียงตารางด้วยการกดหัวคอลัมน์ (`sortCellVal`)

**ที่ไม่เปลี่ยน** เส้นทาง server action การตรวจฟอร์ม และหน้าพิมพ์เอกสารทั้งหมด
(6.4 ไม่ได้แก้หน้ากระดาษ) — ยืนยันด้วยการที่ชุดทดสอบหน้าพิมพ์ต้องผ่านเหมือนเดิม

**ส่งมอบ**
- โทเคนสีชุดใหม่ใน `globals.css` ที่รองรับทั้งโหมดสว่างและมืด
- คอมโพเนนต์ `MenuCard` `BackFab` `DocWindow` `MonthPick` `SortableTh`
- ทุกหน้าที่มีอยู่ใช้เปลือกใหม่ครบ

**เสร็จเมื่อ** เปิดเทียบกับ 6.4 ทีละเมนูบนจอ 1440 และ 375 แล้วโครงหน้าตรงกัน
ชุดทดสอบ 244 ข้อยังผ่านครบ

---

## ช่วงที่ 3 — ใบวางบิล (03.4) · 3–4 วัน

รวมใบแจ้งหนี้หลายใบของลูกค้ารายเดียวเป็นใบวางบิลใบเดียว สำหรับลูกค้าองค์กรที่วางบิลเป็นรอบ

**แตะฐานข้อมูล**
- เพิ่ม `BN` ใน `doc_kind` และชุดเลขที่ `BN-YYYYMM-NNN`
- ตาราง `billnote_docs (billnote_id, doc_id)` เชื่อมใบวางบิลกับใบแจ้งหนี้
- ข้อบังคับ: ใบแจ้งหนี้หนึ่งใบอยู่ในใบวางบิลที่ยังไม่ถูกยกเลิกได้ครั้งเดียว

**ส่งมอบ**
- หน้าเลือกใบแจ้งหนี้ที่ยังค้างของลูกค้าหนึ่งราย แล้วออกใบวางบิล
- พิมพ์ใบวางบิล
- รับชำระทีเดียวหลายใบ แล้วกระจายเงินลงแต่ละใบตามลำดับ

**เสร็จเมื่อ** ยอดในใบวางบิลเท่ากับผลรวมยอดค้างของใบที่อยู่ในนั้นเสมอ มีเทสต์ยืนยัน
และรับชำระผ่านใบวางบิลแล้วยอดค้างของแต่ละใบลดถูกต้อง

---

## ช่วงที่ 4 — เคลม ลูกค้าและผู้ขาย (05.3 / 05.4) · 4–5 วัน

**แตะฐานข้อมูล** ตาราง `claims` + `claim_items`
เก็บทิศทาง (`customer` / `vendor`) ชนิด (`warranty` / `defect`) ผูกลูกค้าและรถ
เลขอ้างอิง เหตุผล ผู้รับผิดชอบ และธงว่าตัดสต๊อกแล้วหรือยัง

**ส่งมอบ** หน้ารายการและหน้าฟอร์มทั้งสองทิศทาง · เลือกอะไหล่จากทะเบียน ·
ตัดสต๊อกพร้อมคิดต้นทุนจากล็อต (ใช้ของช่วงที่ 1) · พิมพ์ใบเคลม

**เสร็จเมื่อ** ตัดสต๊อกจากใบเคลมแล้วยอดคงเหลือและต้นทุนขายขยับถูกต้อง
ยกเลิกใบเคลมแล้วคืนสต๊อกกลับล็อตเดิม

---

## ช่วงที่ 5 — ตรวจนับสต๊อก + บาร์โค้ด (05.5) · 4–5 วัน

**แตะฐานข้อมูล** ตาราง `stock_counts` + `stock_count_items`
พร้อมธง `applied` และประวัติการแก้ไข

**ส่งมอบ** เปิดใบตรวจนับ · สแกนบาร์โค้ดเพิ่มรายการ · พิมพ์ใบนับไปเดินนับ ·
ส่งออก CSV · ยืนยันผลนับแล้วปรับยอดเข้าบัญชีสต๊อกด้วยเหตุผล `count` · พิมพ์บาร์โค้ดสินค้า

**เสร็จเมื่อ** ยืนยันผลนับแล้วเกิดแถวใน `stock_moves` เท่ากับส่วนต่างจริง
และยืนยันซ้ำใบเดิมไม่ทำให้ยอดเพี้ยน

---

## ช่วงที่ 6 — สิทธิ์ละเอียด + ทะเบียนพนักงาน (07.2) · 3–4 วัน

**6.4 เก็บอย่างไร** `perms.{cost, edit, export, home, sub, tabs}`
โดย `tabs['stock.count'] = true` ให้สิทธิ์ระดับแท็บย่อย
และมีกติกาพิเศษ: ผู้ที่ถูกซ่อนต้นทุนเข้าหน้ากำไรขาดทุนไม่ได้

**แตะฐานข้อมูล** เปลี่ยน `users.perms` จากอาเรย์ของเมนูเป็น jsonb
พร้อมตัวแปลงข้อมูลเดิมให้อัตโนมัติ

**ส่งมอบ** ซ่อนต้นทุนทั้งระบบเมื่อไม่มีสิทธิ์ (ตาราง เอกสาร รายงาน และไฟล์ที่ส่งออก) ·
สิทธิ์แก้เอกสารแยกจากสิทธิ์ดู · สิทธิ์ส่งออกแยกต่างหาก · หน้าจัดการพนักงานแบบใหม่

**เสร็จเมื่อ** มีเทสต์ที่สร้างผู้ใช้ซึ่งถูกซ่อนต้นทุน แล้วยืนยันว่าไม่มีเส้นทางไหน —
รวมทั้ง CSV ที่ส่งออกและหน้าที่พิมพ์ — ที่ทำให้เห็นตัวเลขต้นทุนได้

---

## ช่วงที่ 7 — ที่เหลือ · 11–14 วัน

ไม่มีอันไหนกันอันอื่น ทำเรียงตามนี้ได้

| งาน | ประเมิน | หมายเหตุ |
|---|---|---|
| นำข้อมูลเข้าระบบ (07.3) | 2 วัน | นำเข้าลูกค้า/ผู้ขายจาก CSV — เรามีของสินค้าแล้ว |
| หน้ารายละเอียดสินค้า + พิมพ์บาร์โค้ด | 2 วัน | |
| ส่งออกการเงินหลายรูปแบบ | 2 วัน | เรามี CSV แล้ว ขาด PDF และตัวเลือกช่วง |
| รูปภาพแนบเอกสาร | 3–4 วัน | ย่อรูปที่ด้านยาว 800px · **ต้องตั้ง object storage** |
| ประวัติการแก้ไข | 2–3 วัน | ตาราง `doc_edits` — ใครแก้อะไรเมื่อไหร่ |

---

## งานขึ้นใช้งานจริงที่ยังค้างจากก่อนหน้า

ไม่เกี่ยวกับ 6.4 แต่กันไม่ให้เปิดใช้จริงได้ ต้องแทรกเข้ามาก่อนอู่แรก

| งาน | ประเมิน | ทำไมต้องมี |
|---|---|---|
| push ขึ้น GitHub | นาทีเดียว | ตอนนี้งานทั้งหมดอยู่บนเครื่องเดียว |
| deploy จริงตาม DEPLOY.md | 1–2 วัน | ยังไม่เคยรันที่ไหนนอกจากเครื่องนี้ |
| เจ้าของอู่ลืมรหัสผ่าน | 1–2 วัน | ต้องมีระบบส่งอีเมลก่อน |
| คอนโซลผู้ให้บริการ | 2 วัน | สร้างอู่ใหม่ + ดูว่าใครหมดอายุเมื่อไหร่ |
| เก็บ error + แจ้งผลสำรองข้อมูล | ครึ่งวัน | ไม่งั้นรู้ว่าพังตอนลูกค้าโทรมา |

---

## เส้นทางสองแบบ

**เปิดให้อู่แรกใช้เร็วที่สุด — ราว 4–5 สัปดาห์**
ช่วง 0 → 1 → 2 → 3 → งานขึ้นใช้งานจริงทั้งหมด
ได้ระบบที่หน้าตาตรง 6.4 ต้นทุนถูกต้อง มีใบวางบิล และเปิดใช้จริงได้
ยังไม่มีเคลมกับตรวจนับ ซึ่งอู่ใช้ไม่บ่อยเท่า

**ครบเท่า 6.4 ทั้งหมด — ราว 7–9 สัปดาห์**
ทำช่วง 4 ถึง 7 ต่อระหว่างที่อู่แรกใช้งานอยู่ ได้ผลตอบกลับจากของจริงไปด้วย

แนะนำแบบแรก เพราะข้อมูลจากอู่ที่ใช้จริงหนึ่งเดือนมีค่ากว่าการเดาว่าหน้าเคลมควรเป็นอย่างไร

---

## ความเสี่ยงและวิธีรับมือ

| ความเสี่ยง | รับมือ |
|---|---|
| ทำ UI ไปแล้ว 6.4 ออกรุ่นใหม่อีก | ตรึงไฟล์ 6.4 ไว้ใน repo แล้ว ถ้ามีรุ่นใหม่ให้เทียบด้วยวิธีเดียวกับที่ทำหน้านี้ก่อนตัดสินใจ |
| ต้นทุนแบบล็อตคำนวณไม่ตรงต้นฉบับ | เทียบด้วยชุดทดสอบ differential แบบเดียวกับที่ใช้กับภาษี ซึ่งจับได้ตั้งแต่บรรทัดแรกที่ต่าง |
| ประเมินเวลาพลาด | ทุกช่วงส่งมอบของที่ใช้ได้จริง หยุดตรงไหนก็ยังได้ระบบที่เดินได้ ไม่ใช่ครึ่ง ๆ กลาง ๆ |
| อู่ที่ใช้ 6.4 อยู่ย้ายเข้ามาแล้วข้อมูลหาย | ช่วงที่ 0 แก้เรื่องนี้ก่อนอย่างอื่น |

## ที่ตั้งใจไม่ทำ

- ไม่ยกกฎ Max ของต้นฉบับที่เตือนสินค้าทุกตัวที่มีของ (ข้อบกพร่อง ไม่ใช่พฤติกรรม)
- ไม่เก็บล็อตแบบเขียนทับตัวเองอย่างต้นฉบับ — ใช้บัญชีเดินสะพัดที่ตรวจย้อนได้แทน
- ไม่ทำการรับชำระเงินออนไลน์ — ต้องเปิดบัญชีร้านค้ากับผู้ให้บริการรับชำระก่อน
