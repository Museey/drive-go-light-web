# งานขึ้นใช้งานจริง ช่วงที่ 1 — ขึ้นเซิร์ฟเวอร์จริง และเก็บข้อผิดพลาด

## Context

โค้ดครบเท่ารุ่น 6.4 แล้วในส่วนที่อู่ใช้ทำงานประจำวัน (เหลือของช่วงที่ 7 ซึ่งไม่กันการใช้งาน)
แต่ **ยังไม่เคยรันที่ไหนนอกจากเครื่องพัฒนา** และ **ยังไม่มีทางรู้ว่าระบบพัง**
นอกจากรอให้อู่โทรมาบอก

สองอย่างนี้ทำพร้อมกันเพราะพันกัน — ที่เก็บข้อผิดพลาดต้องมีเครื่องให้รัน
และการขึ้นเครื่องจริงครั้งแรกคือช่วงที่มีโอกาสพังมากที่สุด ถ้าไม่มีที่เก็บไว้ก่อน
เราจะไล่ปัญหาตอน deploy ด้วยการเดา

`DEPLOY.md` เขียนไว้ครบแล้วว่า**ต้องทำอะไร** ช่วงนี้คือทำให้**ทำได้จริงโดยไม่ต้องจำ** —
เติมของที่ยังขาดในโปรเจกต์ แล้วเอาขึ้นเครื่องจริงหนึ่งเครื่อง

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. เครื่องเดียว (VPS) ไม่ใช่ serverless

`DEPLOY.md` เขียนบนสมมติฐานนี้อยู่แล้ว และมีเหตุผลทางเทคนิคหนักกว่าความเคยชิน

- **RLS ผูกกับทรานแซกชัน** — `set_config('app.tenant_id', …, true)` มีผลเฉพาะในทรานแซกชัน
  ต้องใช้ connection pool ที่เราคุมเอง แพลตฟอร์ม serverless ต้องมี pooler คั่นอีกชั้น
  ซึ่งเป็นจุดที่ถ้าตั้งผิดแล้ว**ข้อมูลรั่วข้ามอู่โดยไม่มีอาการ**
- **แอปตรวจ RLS และเขตเวลาตอนเปิดการเชื่อมต่อ** — ออกแบบไว้สำหรับ pool ที่อยู่นาน
  ไม่ใช่การต่อใหม่ทุก request
- **cron สำรองข้อมูล กับ importer CLI ต้องแตะฐานข้อมูลโดยตรง** — ต้องมีเครื่องอยู่ดี
- 20–30 อู่ใช้ทรัพยากรน้อยมาก เครื่องเดียวเหลือเฟือไปอีกนาน

**สเปกขั้นต่ำ** — 2 vCPU · 4 GB RAM · SSD 60 GB · Ubuntu 24.04 LTS
เลือกศูนย์ข้อมูล**ในไทย** เพราะผู้ใช้อยู่ไทยทั้งหมด และข้อมูลลูกค้าอยู่ในประเทศ
ช่วยเรื่อง PDPA ที่หน้า `/privacy` พูดไว้

ราคาตลาดตอนนี้อยู่ราว **$8–20 ต่อเดือน** สำหรับสเปกนี้ในศูนย์ข้อมูลกรุงเทพ
(ตัวเลขคร่าว ๆ จากการสำรวจ — **ต้องเช็คหน้าเว็บผู้ให้บริการก่อนซื้อจริง**
เพราะราคาระดับนี้เปลี่ยนบ่อยและมักมีเงื่อนไขจ่ายล่วงหน้าทั้งปี)

> **นี่คือข้อเดียวที่ต้องตัดสินใจก่อนเริ่ม** — ผมทำส่วนที่เหลือได้ทันที
> แต่ขั้นตอนขึ้นเครื่องจริงต้องรอชื่อผู้ให้บริการกับสิทธิ์เข้าเครื่อง

### 2. ตัวรันไมเกรชันที่**จดว่ารันอะไรไปแล้ว**

ตอนนี้มีไฟล์ `db/001` ถึง `db/007` ที่ต้องรันด้วยมือเรียงกัน โดยไม่มีอะไรจดไว้ว่ารันถึงไหน

บนเครื่องพัฒนาไม่เป็นไรเพราะล้างฐานทิ้งได้ทุกวัน **บนเครื่องจริงล้างไม่ได้** —
และวิธีที่คนพลาดคือรัน `005` ซ้ำ หรือข้าม `006` แล้วไม่รู้ตัวจนกว่าจะมีหน้าพัง

```sql
create schema if not exists ops;
create table ops.migrations (
  filename    text primary key,
  checksum    text not null,        -- sha256 ของไฟล์ตอนที่รัน
  ran_at      timestamptz not null default now(),
  ran_by      text not null default current_user
);
```

`tools/migrate.sh` — รันเฉพาะไฟล์ที่ยังไม่เคยรัน เรียงตามชื่อ และ
**ปฏิเสธทันทีถ้าไฟล์ที่เคยรันแล้วมีเนื้อหาเปลี่ยนไป** เพราะแปลว่ามีคนแก้ไมเกรชันที่ขึ้นไปแล้ว
ซึ่งทำให้เครื่องจริงกับโค้ดไม่ตรงกันโดยไม่มีใครรู้

`001` กับ `002` ที่รันไปแล้วบนฐานเดิมให้บันทึกย้อนหลังได้ด้วย `--mark-only`

### 3. ที่เก็บข้อผิดพลาดอยู่ในฐานข้อมูลของเราเอง ไม่ส่งออกนอกประเทศ

ทางเลือกปกติคือส่งไปผู้ให้บริการภายนอก (Sentry และคล้ายกัน) ซึ่งได้การจัดกลุ่ม
และการแจ้งเตือนมาฟรี แต่**ข้อความผิดพลาดมีข้อมูลลูกค้าติดไปด้วยเสมอ** —
ชื่อ ทะเบียนรถ ยอดเงิน บางทีทั้งแถว

หน้า `/privacy` ของเราบอกลูกค้าว่าข้อมูลอยู่ที่ไหน การส่งข้อมูลอู่ออกไปนอกประเทศ
เพื่อความสะดวกของเราเองไม่คุ้มกับการต้องไปแก้คำพูดนั้น

```sql
create table ops.errors (
  id          bigserial primary key,
  at          timestamptz not null default now(),
  kind        text not null,          -- 'server' | 'client' | 'action' | 'job'
  digest      text,                   -- ตัวจัดกลุ่มของ Next.js
  message     text not null,
  stack       text,
  path        text,
  tenant_id   uuid,                   -- รู้บ้างไม่รู้บ้าง เก็บเท่าที่รู้
  user_id     uuid,
  seen        boolean not null default false
);
```

อยู่ในสคีมา `ops` ไม่ใช่ `public` — **ไม่ผูกกับ RLS** เพราะข้อผิดพลาดเกิดก่อนที่จะรู้ว่าเป็นอู่ไหนได้
และเราต้องอ่านได้ข้ามอู่ตอนไล่ปัญหา

**ลบอัตโนมัติเมื่อเกิน 90 วัน** ให้ตรงกับที่หน้า PDPA บอกไว้เรื่องไฟล์สำรอง

### 4. ยังไม่แจ้งเตือน แค่บันทึก — เพราะยังส่งอีเมลไม่ได้

การแจ้งเตือนต้องรอระบบส่งอีเมลซึ่งเป็นงานถัดไป ช่วงนี้ทำให้**มีของให้ดู**ก่อน
แล้วงานถัดไปแค่ต่อท่อออก

ระหว่างนี้ดูผ่านหน้าเว็บ `/ops` ที่เข้าได้เฉพาะเจ้าของอู่ที่เป็นบัญชีของเราเอง
บวก `tools/errors.sh` ไว้ดูจาก terminal ตอนอยู่บนเครื่อง

### 5. หน้าแสดงข้อผิดพลาดต้องบอกผู้ใช้ว่าทำอะไรต่อได้

ตอนนี้ยังไม่มี `error.tsx` เลย — พังแล้วผู้ใช้เห็นหน้าเปล่าของ Next.js
ที่ไม่บอกอะไรและไม่มีปุ่มอะไร

สามหน้าที่ต้องมี
- `error.tsx` — พังในหน้าใดหน้าหนึ่ง · มีปุ่มลองใหม่ · แสดงรหัสอ้างอิงให้แจ้งเราได้
- `global-error.tsx` — พังตั้งแต่ layout · เป็นหน้าสุดท้ายที่กันจอขาว
- `not-found.tsx` — พิมพ์ URL ผิด · พากลับหน้าแรก

ทั้งสามหน้า**ไม่แสดง stack trace ให้ผู้ใช้เห็น** แสดงแค่รหัสอ้างอิงที่ตรงกับแถวใน `ops.errors`

### 6. `/healthz` ตรวจของจริง ไม่ใช่แค่ตอบ 200

ตัวตรวจสุขภาพที่ตอบ 200 เสมอไม่มีประโยชน์ ต้องตรวจสามอย่างที่เคยพังจริงในโปรเจกต์นี้

1. ต่อฐานข้อมูลได้ และ role **ไม่ได้** ข้าม RLS
2. เขตเวลาแอปกับฐานข้อมูลตรงกัน (เคยพังมาแล้วในช่วงประเมินความพร้อม)
3. ไมเกรชันที่โค้ดต้องการรันครบแล้ว

ไม่ต้องล็อกอิน แต่**ตอบแค่ ok/ไม่ ok กับชื่อการตรวจที่ตก** ไม่บอกรายละเอียดระบบ

---

## ไฟล์ที่จะเพิ่ม

| ไฟล์ | ทำอะไร |
|---|---|
| `db/008_ops.sql` | สคีมา `ops` · ตาราง `migrations` และ `errors` |
| `tools/migrate.sh` | รันไมเกรชันที่ยังไม่เคยรัน · ตรวจ checksum · `--mark-only` · `--dry-run` |
| `tools/errors.sh` | ดูข้อผิดพลาดล่าสุดจาก terminal |
| `apps/web/src/lib/ops.ts` | `recordError()` · `listErrors()` · `healthChecks()` |
| `apps/web/src/app/error.tsx` | หน้าพังรายหน้า |
| `apps/web/src/app/global-error.tsx` | หน้าพังระดับ layout |
| `apps/web/src/app/not-found.tsx` | หน้าไม่พบ |
| `apps/web/src/app/healthz/route.ts` | ตัวตรวจสุขภาพ |
| `apps/web/src/app/ops/page.tsx` | หน้าดูข้อผิดพลาด (เฉพาะบัญชีผู้ให้บริการ) |
| `deploy/drivegolight.service` | systemd unit — restart อัตโนมัติ · จำกัดสิทธิ์ · อ่าน env จากไฟล์ |
| `deploy/Caddyfile` | HTTPS อัตโนมัติ · redirect จาก HTTP · security headers |
| `deploy/backup.cron` | cron สำรองข้อมูลตามที่ `DEPLOY.md` เขียนไว้ |

เลือก **Caddy** แทน nginx เพราะขอใบรับรอง HTTPS และต่ออายุให้เอง
ไม่ต้องตั้ง certbot แยก — ข้อ HTTPS ในรายการตรวจก่อนเปิดใช้จะได้ไม่พลาด

---

## จุดที่ต้องต่อสาย

**`instrumentation.ts` ของ Next** — จับข้อผิดพลาดฝั่งเซิร์ฟเวอร์ที่หลุดออกมาทั้งหมด
เป็นที่เดียวที่ดักได้ครบโดยไม่ต้องไปแก้ทุก action

**`mutate()` และ `query()`** — ข้อผิดพลาดที่โยนออกจากสองตัวนี้ผ่านตาผู้ใช้อยู่แล้ว
แต่ต้องบันทึกไว้ด้วย เพราะข้อความที่ผู้ใช้เห็นถูกแปลงเป็นภาษาคนแล้ว
ของจริงที่ต้องใช้ไล่ปัญหาอยู่ในต้นฉบับ

**`tools/backup-db.sh`** — เขียนผลลงตาราง `ops.errors` เมื่อล้มเหลว
(งานขึ้นใช้งานจริงข้อ "แจ้งผลสำรองข้อมูล" เริ่มจากตรงนี้)

---

## ชุดทดสอบ

**`apps/web/test/ops.test.ts` (ไฟล์ใหม่ · ฐานข้อมูลจริง)**
- บันทึกข้อผิดพลาดแล้วอ่านกลับได้ · ข้ามอู่ได้ (ไม่ติด RLS)
- **ข้อมูลอ่อนไหวไม่ถูกบันทึกลงไป** — ส่ง object ที่มีรหัสผ่านหรือ token เข้าไป
  แล้วยืนยันว่าไม่โผล่ในแถวที่บันทึก
- ลบของเกิน 90 วันทิ้งจริง
- `healthChecks()` แดงเมื่อเขตเวลาไม่ตรง · แดงเมื่อไมเกรชันไม่ครบ

**`apps/web/test/migrate.test.ts` (ไฟล์ใหม่)**
- รันสองรอบติดกัน รอบที่สองต้องไม่ทำอะไรเลย
- ไฟล์ที่เคยรันแล้วถูกแก้เนื้อหา → ปฏิเสธพร้อมบอกชื่อไฟล์
- ไฟล์ใหม่ที่แทรกเข้ามาตรงกลางลำดับ → รันแล้วบันทึกถูกต้อง
- `--mark-only` บันทึกโดยไม่รัน

**ตรวจว่าไมเกรชันทั้งชุดสร้างฐานที่เหมือน `001` ทุกประการ**
สร้างฐาน A ด้วย `001+002` แล้วรัน `003`–`008`
สร้างฐาน B ด้วย `001+002+008` ตรง ๆ (ซึ่ง `001` รวมทุกอย่างไว้แล้ว)
แล้ว**เทียบโครงสร้างสองฐานให้ตรงกันทุกคอลัมน์ ทุก constraint ทุก index**

ข้อนี้สำคัญที่สุดของช่วงนี้ — ที่ผ่านมาเราแก้ `001` กับเขียนไฟล์ไมเกรชันคู่กันมาห้าช่วง
โดยไม่เคยพิสูจน์ว่าสองทางให้ผลเหมือนกัน อู่ที่ย้ายมาก่อนกับอู่ที่เปิดใหม่
จะได้ฐานข้อมูลคนละแบบโดยไม่มีใครรู้

**เดิมต้องไม่พัง** — 428 ข้อผ่านครบ

---

## ขั้นตอนขึ้นเครื่องจริง (หลังโค้ดพร้อม)

1. เช่าเครื่อง · ตั้ง SSH key · ปิด password login · เปิดไฟร์วอลล์เฉพาะ 22/80/443
2. ติดตั้ง Postgres 16 · ตั้งให้ฟังเฉพาะ 127.0.0.1
3. `tools/migrate.sh` แล้ว `db/app-role.sql` (เปลี่ยนรหัสผ่านก่อน)
4. build แล้วเปิดผ่าน systemd · ตั้ง Caddy ชี้โดเมนจริง
5. ตั้ง cron สำรองข้อมูล · **รอสองวันแล้วเช็คว่ามีไฟล์เกิดจริงสองวันติด**
6. ซ้อมกู้คืนหนึ่งรอบตาม `DEPLOY.md` ข้อ 6 · บันทึกผลลงเอกสาร
7. เปิดอู่ของเราเองเป็นรายแรก แล้ว**ใช้จริงหนึ่งเดือนก่อนชวนใครมา**
8. เดินรายการตรวจใน `DEPLOY.md` ข้อ 8 ให้ครบทุกช่อง

---

## ที่ไม่ทำในช่วงนี้

- **แจ้งเตือนเมื่อพัง** — ต้องมีอีเมลก่อน อยู่ในงานถัดไป (ลืมรหัสผ่าน)
- **หลายเครื่องพร้อมกัน** — ออกแบบไว้เครื่องเดียว 20–30 อู่ไม่ต้องใช้
- **รับชำระเงินออนไลน์** — เรื่องนอกโค้ด ต้องเปิดบัญชีร้านค้าก่อน
- **source map ของข้อผิดพลาดฝั่งเบราว์เซอร์** — เก็บ digest ไว้ก่อน
  ถ้าวันหนึ่งไล่ไม่ได้จริงค่อยเพิ่ม
