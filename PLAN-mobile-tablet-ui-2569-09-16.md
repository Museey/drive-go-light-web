# แผน — ยกการแสดงผลมือถือ/แท็บเล็ตตามต้นแบบของทีม

branch `feat/mobile-tablet-ui-2569-09-16` (แตกจาก `main` = `a164f8f`) · ทำทีละเฟส ขออนุมัติก่อนเริ่มแต่ละเฟส

ต้นแบบ: `~/Downloads/dgl-prototype-2569-09-13-mobile.html`
**เอาเฉพาะการแสดงผล ตำแหน่งปุ่ม และการจัดวาง — ไม่แตะตรรกะของระบบ** (ผู้ใช้กำหนด)

## สิ่งที่อ่านจากต้นแบบ

- ปุ่มสลับอุปกรณ์ตั้งคลาสเป็น `m mt` เมื่อกด "แท็บเล็ต" → **แท็บเล็ตใช้เลย์เอาต์มือถือทั้งหมด** กว้าง 720px เนื้อหาไม่เกิน 560px
  (CSS `.frame.t` ในไฟล์เป็นโค้ดที่ไม่ถูกใช้)
- มือถือเปลี่ยนตารางเป็นการ์ดทุกหน้า · เมนูย่อยกลายเป็นการ์ด "ประเภทเอกสาร" · ตัวกรองช่วงเวลาเหลือ dropdown เดียว
- แถบล่าง 6 เมนู: หน้าแรก · ลูกค้า · รายรับ · สินค้า · รายจ่าย · เพิ่มเติม
- ต้นแบบซ่อนปุ่มพิมพ์รายงาน/CSV และเมนูย่อยประเภทเครื่องมือบนมือถือ

## ตัดสินใจแล้ว (ผู้ใช้เลือก)

- ปุ่มพิมพ์รายงาน/CSV **ย้ายเข้าลิ้นชัก "เพิ่มเติม"** ไม่ซ่อนทิ้ง — มือถือยังสั่งพิมพ์/โหลดไฟล์ได้
- แท็บเล็ตจำกัดความกว้างเนื้อหา 560px กึ่งกลางจอ

## เฟส

| เฟส | ขอบเขต |
|---|---|
| 1 | โครงหน้าจอ: แท็บเล็ตใช้เลย์เอาต์มือถือ (จุดตัดใหม่ที่ 1280) · แถบล่าง 6 เมนู · เนื้อหา 560px บนแท็บเล็ต · ย้ายปุ่มเครื่องมือของหน้าเข้าลิ้นชัก · ขนาดหัวหน้าจอ |
| 2 | รายการเอกสาร (รายรับ · รายจ่าย · ใบวางบิล) เป็นการ์ด + ตัวกรองช่วงเวลาแบบ dropdown + ปุ่ม "สร้างใหม่" ในหัวข้อกลุ่ม |
| 3 | สินค้า: การ์ดสินค้า · ช่องค้นหา · ตัวกรอง · แบ่งหน้า |
| 4 | ลูกค้า/ผู้ขาย: การ์ดผู้ติดต่อ · หน้ารายละเอียดแบบการ์ด |
| 5 | หน้าแรก + การเงิน: การ์ดตัวเลข 2 คอลัมน์ · การ์ดลูกหนี้/เจ้าหนี้รายราย · การ์ดภาษี 3 ใบ |
| 6 | ฟอร์มและเอกสาร: ช่องกรอกบนจอแคบ · ปุ่มลอยย้อนกลับ · หน้ากระดาษพิมพ์ |

## เฟส 1 — รายละเอียด

1. **จุดตัดจอ**: กฎที่ตอนนี้เปิดโหมดเดสก์ท็อปที่ 768px ย้ายไป 1280px — แท็บเล็ตจึงได้แถบล่าง + ลิ้นชัก เหมือนมือถือ
   (สเปก §2 เดิมมีจุดตัด 768/1280 อยู่แล้ว ไม่เพิ่มจุดตัดใหม่)
2. **แถบล่าง 6 เมนู** ตามลำดับต้นแบบ: หน้าแรก · ลูกค้า · รายรับ · สินค้า · รายจ่าย · เพิ่มเติม
3. **แท็บเล็ต**: เนื้อหากว้างสุด 560px กึ่งกลาง · ตัวอักษรแถบล่างใหญ่ขึ้นเล็กน้อยตามต้นแบบ
4. **ปุ่มเครื่องมือของหน้า** (พิมพ์รายงาน · CSV · ปุ่มใน `actions` ของ Shell): จอ < 1280 ไม่แสดงในหัวหน้า แต่ไปอยู่ในลิ้นชัก "เพิ่มเติม" ใต้หัวข้อ "เครื่องมือของหน้านี้"
5. **หัวหน้าจอ**: ชื่อ 18px · คำบรรยาย 12.5px บนจอ < 1280

**เทสต์เฟส 1** (`e2e/mobile-shell.spec.ts` 375 + 768):
- แท็บเล็ตต้องมีแถบล่าง ไม่มีแถบเมนูหลักแบบเดสก์ท็อป · เดสก์ท็อป 1280 ยังเหมือนเดิมทุกอย่าง
- แถบล่างมี 6 เมนูเรียงตามที่กำหนด · เมนูที่อยู่ปัจจุบันมีขีดบอก
- ที่ 768 เนื้อหากว้างไม่เกิน 560px และอยู่กึ่งกลาง
- ปุ่มพิมพ์รายงานไม่อยู่ในเนื้อหาหลักบนจอ < 1280 แต่กดจากลิ้นชักได้และลิงก์ถูกหน้า
- ย้อนการแก้ทีละจุดต้องแดง

## ตรวจรวมทุกเฟส
typecheck · `npm test` · e2e ครบบน production build · ภาพก่อน/หลังเทียบต้นแบบ · อัปเดตสเปก §5.2

## เฟส 2 — รายละเอียด (16 ก.ย. 2569)

branch `feat/doc-cards-2569-09-16` (แตกจาก `main` = `0ac6fbe` ซึ่งรวมเฟส 1 แล้ว)

### หน้าที่อยู่ในขอบเขต
- `/income` (ประวัติเอกสารขาย) — ตารางเดียวกับ `/income/walkin` จึงได้การ์ดพร้อมกัน
- `/expense` (ใบซื้อ · ค่าใช้จ่าย)
- `/income/billing` (ใบวางบิล)

### สิ่งที่ทำ

1. **การ์ดเอกสาร** — คอมโพเนนต์ใหม่ `DocCards` + โมดูลบริสุทธิ์ `lib/doc-card.ts`
   เค้าโครงตามต้นแบบ `a.mdoc`:
   - แถวบน: ชิปชนิด (`QT/IVT/IV/RC/BN/PO/EX` สีตามต้นแบบ) · เลขที่เอกสาร (mono หนา) · ชิปสถานะชิดขวา
   - ชื่อลูกค้า/ผู้ขาย (16px หนา) · ทะเบียนรถบรรทัดรอง (mono เทา)
   - แถวล่างคั่นด้วยเส้นประ: วันที่ (ซ้าย) · ยอดรวม (ขวา ตัวใหญ่) + "คงค้าง N" สีแดง หรือ "ชำระครบ" สีเขียว
   - ใบเสนอราคาไม่มีบรรทัดคงค้าง (ตามต้นแบบ — สถานะค้างส่งมอบอยู่ในชิปแล้ว) · ใบที่ยกเลิกได้ชิป "ยกเลิก" และการ์ดจางเหมือนแถว `tr.voided`
   - แต่ละหน้าแปลงแถวของตัวเองเป็นรูปแบบกลางเดียวกัน (income / expense / billing มีคนละฟิลด์)

2. **สลับการ์ด ↔ ตาราง ด้วย CSS** แพตเทิร์นเดียวกับทะเบียนสินค้าที่มีอยู่แล้ว
   (`.stock-cards` / `.stock-table`) — `.doc-cards` แสดงต่ำกว่า 1280 · ตารางซ่อน
   บล็อกสลับเขียน `@media (min-width: 1280px), print` เพื่อให้ **สั่งพิมพ์แล้วยังได้ตาราง** ไม่ใช่การ์ด

3. **ตัวกรองช่วงเวลาเป็น dropdown** (ผู้ใช้เลือก: dropdown อย่างเดียว ตามต้นแบบ)
   - ต่ำกว่า 1280: แถวเดียว `ช่วงเวลา [เลือก▾]` (`.mdate`) แทนแถวชิป + ช่องกรอกวันที่
   - ตัวเลือก = พรีเซ็ตเดิมของเราหกตัว (ทั้งหมด · วันนี้ · เดือนนี้ · เดือนที่แล้ว · ปีนี้ · ปีที่แล้ว)
     ต้นแบบมีห้าตัว (ไม่มี "วันนี้") — เก็บหกตัวไว้เพราะเป็นพรีเซ็ตที่เดสก์ท็อปมีอยู่แล้ว ไม่ได้เพิ่มของใหม่
   - ถ้า `from/to` ที่ติดมาไม่ตรงพรีเซ็ตใด (เช่นลิงก์ที่กรองเองมาจากเดสก์ท็อป) เพิ่มตัวเลือก "ช่วงที่เลือกไว้" ให้เห็นค่าจริง ไม่แสดงค่าผิด
   - เรนเดอร์ทั้งสองแบบเสมอแล้วซ่อนด้วย CSS — **ไม่วัดความกว้างจอใน JS** ตามกติกาเดิม (กันเมนูกระพริบตอนโหลด)
   - จอ ≥ 1280 ทุกอย่างเหมือนเดิมทุกจุด

4. **ปุ่ม "＋ สร้างใหม่" ในหัวข้อกลุ่ม** (`.m-sec h2` + `.m-newbtn`) เหนือรายการการ์ด เฉพาะจอ < 1280
   ปลายทางตามชนิดที่กรองอยู่: QT/IVT/IV/RC → `/income/new?kind=…` · PO/EX → `/expense?kind=…`
   · ใบวางบิล → `/income/billing/new` · ไม่ได้เลือกชนิด → ไม่แสดงปุ่ม (ยังมีไทล์ "＋ สร้าง" ด้านบน)

5. **หัวข้อกลุ่มบอกจำนวน** — `ประวัติใบเสร็จ  <span class="cnt">N เอกสาร</span>` ตามต้นแบบ

### ไม่แตะ
ตรรกะการกรอง/สิทธิ์/การแบ่งหน้า · ตารางเดสก์ท็อปทุกคอลัมน์และปุ่มในแถว · การแบ่งหน้ายังเป็น `.pager` เดิม
(ต้นแบบมีปุ่มแบ่งหน้าแบบลอยเฉพาะหน้าสินค้า อยู่ในเฟส 3)

### ผลที่ผู้ใช้ยอมรับแล้ว (เลือกไว้ 16 ก.ย. 2569)
- จอต่ำกว่า 1280 **ไม่มีปุ่มรับชำระ/แก้ไข/เมนู/ลบ ในรายการ** — กดการ์ดเข้าไปทำในหน้าเอกสาร
- จอต่ำกว่า 1280 **ไม่มีช่องกรอกช่วงวันที่เอง** — ใช้พรีเซ็ตใน dropdown

### เทสต์เฟส 2
`test/doc-card.test.ts` (โมดูลบริสุทธิ์): ชิปชนิด · สถานะ · คงค้าง/ชำระครบ · ใบเสนอราคาไม่มีคงค้าง · ใบยกเลิก
`e2e/doc-cards.spec.ts` (375 · 768 · 1280):
- ต่ำกว่า 1280 เห็นการ์ด ไม่เห็นตาราง · ที่ 1280 เห็นตาราง ไม่เห็นการ์ด (ทั้งสามหน้า)
- จำนวนการ์ด = จำนวนที่หัวข้อกลุ่มบอก · การ์ดใบแรกมีครบ: ชิปชนิด · เลขที่ · ชื่อ · วันที่ · ยอด
- กดการ์ดแล้วไปหน้าเอกสารใบนั้น (`/income/<id>`)
- เลือก "เดือนที่แล้ว" ใน dropdown แล้ว URL มี `from`/`to` และจำนวนเอกสารเปลี่ยนตาม
- ที่ 1280 ไม่มี dropdown · ชิปช่วงวันที่และช่องกรอกวันที่ยังอยู่
- ปุ่ม "＋ สร้างใหม่" ลิงก์ถูกหน้าตามชนิด (QT · RC · PO · EX · ใบวางบิล)
- ใบเสนอราคาไม่มีบรรทัดคงค้าง · ใบชำระครบขึ้น "ชำระครบ" · ใบค้างขึ้น "คงค้าง"
- เทสต์เดิมทั้งหมดต้องเขียว โดยเฉพาะ `responsive.spec` (ไม่มีอะไรล้นจอ) และการพิมพ์รายงานยังได้ตาราง

**กลายพันธุ์ที่ต้องทำให้แดง**: สลับด้านของจุดตัดการ์ด/ตาราง · ตัดบรรทัดคงค้างทิ้ง · ปุ่มสร้างใหม่ลิงก์ผิดชนิด ·
dropdown ไม่ส่ง from/to · การ์ดไม่ลิงก์ไปเอกสาร · ใบเสนอราคาแสดงคงค้าง

### เอกสาร
สเปก §5.1 (รายการเอกสารบนจอแคบ) · §13.9 ตารางเหตุผลของรอบนี้

## เฟส 3 — รายละเอียด (16 ก.ย. 2569)

branch `feat/stock-cards-2569-09-16` (แตกจาก `main` = `7969d22` ซึ่งรวมเฟส 2 แล้ว)
ต้นแบบ: `mStock()` · `.mseg` · `.msearch` · `.mfilter` · `.mprod`/`a.mpcard` · `.mpager`

### ปัญหาที่วัดได้ตอนนี้
หน้า `/stock` บนจอ 375 ต้องเลื่อนผ่าน **ไทล์เมนูย่อย 10 ใบ + ไทล์ตัวกรอง 8 ใบ + ฟอร์มค้นหา**
รวมสูงราว 700px ก่อนถึงสินค้ารายการแรก — เปิดหน้ามาไม่เห็นของที่มาหา

### สิ่งที่ทำ

1. **ปุ่มสลับสองช่อง** (`.mseg`) บนสุดของหน้า เฉพาะจอ < 1280 (ผู้ใช้เลือก)
   - `🔎 ค้นหาสินค้า` (ค่าเริ่มต้น) = ช่องค้นหา + รายการสินค้า
   - `▤ เมนูและตัวกรอง` (`?menu=1`) = ไทล์เมนูย่อย 05.1–05.11 + ไทล์ตัวกรอง (ทั้งหมด · ถึงจุดสั่งซื้อ · Min · Max · ไม่เคลื่อนไหว · ใกล้หมดอายุ · หมดอายุแล้ว) + ตั้งค่าจำนวนต่อหน้า
   - **ไม่มีอะไรหาย** — ของที่เคยอยู่บนสุดย้ายไปอยู่หลังปุ่มเดียว · จอ ≥ 1280 ไม่เปลี่ยนอะไรเลย
2. **ช่องค้นหาเต็มความกว้าง** (`.msearch`) สูง 50px · ใต้ช่องมีแถวเล็ก: หมวดหมู่ ▾ · ☐ รวมที่ปิดใช้งาน
   (ต้นแบบมีแค่ช่องค้นหา — เก็บสองตัวนี้ไว้เพราะเป็นตัวกรองที่หน้าสต๊อกใช้จริง ไม่ใช่ของประดับ)
   ไม่มีปุ่ม "ค้นหา" แยก — Enter ส่งฟอร์ม (กติกาเดิมของระบบ · ใช้ `data-enter="own"`)
3. **แถบบอกตัวกรองที่เปิดอยู่** (`.mfilter`) เช่น `กรอง: ถึงจุดสั่งซื้อ   ✕ ล้าง`
   โผล่เมื่อมี `reorder=1` หรือ `flag=…` — คนกดมาจากหน้าแรกจะได้รู้ว่าทำไมเห็นไม่ครบ
4. **หัวข้อกลุ่มบอกจำนวน** `รายการสินค้า  <span class="cnt">N รายการ · หน้า x/y</span>`
5. **การ์ดสินค้าตามต้นแบบ** — จาก `.scard` (สองบรรทัดคู่ป้าย/ค่า) เป็น `a.mpcard` สามคอลัมน์:
   รูป 56px | ชื่อ (ตัดสองบรรทัด) + รหัส | ราคาขาย + คงเหลือ (ถึงจุดสั่งซื้อเป็นสีแดง)
   เพิ่มชิปสถานะที่ตารางมีแต่การ์ดยังไม่มี: ปิดใช้งาน · ใกล้หมดอายุ/หมดอายุ (`p.flags`)
6. **ปุ่มแบ่งหน้าแบบลอย** (`.mpager`) มุมซ้ายล่างเหนือแถบล่าง (ผู้ใช้เลือก)
   `‹ ก่อนหน้า | x / y | ถัดไป ›` · ปุ่มที่ไปต่อไม่ได้เป็นสีจาง กดไม่ได้ · มีตัวเว้นท้ายหน้าไม่ให้บังการ์ดใบสุดท้าย
   แถวแบ่งหน้าเดิมท้ายรายการซ่อนบนจอแคบ — ตัวเลือก "จำนวนต่อหน้า" ย้ายไปแท็บเมนู (ข้อ 1)

### ไม่แตะ
- หน้าสต๊อกอื่น (`/stock/expiry` `/stock/pending` `/stock/claim` `/stock/count` `/stock/kits`) ยังเป็นตารางเดิม
  — รายการสั้นกว่ามากและเป็นงานเฉพาะกิจ ไม่ใช่หน้าที่เปิดทุกวัน (ถ้าต้องการค่อยทำรอบแยก)
- ตรรกะการค้นหา/ตัวกรอง/สิทธิ์/จำนวนต่อหน้า · ตารางและ ColPicker บนเดสก์ท็อปเหมือนเดิมทุกจุด
- รูปสินค้ายังดึงจาก `/pics/<id>/<sha>` เหมือนเดิม

### เทสต์เฟส 3
`e2e/stock-cards.spec.ts` (375 · 768 · 1280):
- จอแคบ: เปิด `/stock` มาเห็นการ์ดสินค้าใบแรก **ภายในหน้าจอแรก** (วัดจาก boundingBox ว่าอยู่สูงไม่เกินความสูงจอ)
- ปุ่มสลับ: แท็บ "เมนูและตัวกรอง" แสดงไทล์เมนูย่อยครบและตัวกรองครบ · กดตัวกรองแล้วกลับมาที่รายการพร้อมแถบ `.mfilter`
- แถบตัวกรอง: `?reorder=1` ต้องมีแถบบอก + ปุ่ม "ล้าง" กลับไป `/stock` เปล่า
- การ์ด: ชื่อ · รหัส · ราคา · คงเหลือ ครบทุกใบ · สินค้าที่ถึงจุดสั่งซื้อคงเหลือเป็นสีแดง (เทียบ computed color)
- กดการ์ด → `/stock/<id>`
- ปุ่มแบ่งหน้าลอย: หน้าแรกปุ่ม "ก่อนหน้า" กดไม่ได้ · กด "ถัดไป" แล้ว `page=2` และรายการเปลี่ยน · ปุ่มไม่บังการ์ดใบสุดท้าย (วัดกรอบชนกัน)
- จำนวนต่อหน้าอยู่ในแท็บเมนู และเปลี่ยนแล้วมีผลจริง
- เดสก์ท็อป 1280: ไม่มี `.mseg` `.mfilter` `.mpager` · ตาราง + ไทล์ + ColPicker เหมือนเดิม
`test/stock-card.test.ts` (โมดูลบริสุทธิ์ `lib/stock-card.ts`): แปลงแถวสินค้า → การ์ด · ชิปสถานะ · เงื่อนไขคงเหลือสีแดง

**กลายพันธุ์ที่ต้องทำให้แดง**: ซ่อนการ์ดผิดด้านของจุดตัด · แท็บเมนูไม่มีไทล์ตัวกรอง · แถบ `.mfilter` ไม่ขึ้นตอนมีตัวกรอง ·
ปุ่ม "ถัดไป" ไม่ส่ง `page` · คงเหลือไม่เป็นสีแดงตอนถึงจุดสั่งซื้อ · การ์ดไม่ลิงก์ไปหน้าสินค้า

### เอกสาร
สเปก §5.1 (รายการสินค้าบนจอแคบ) · §13.10 ตารางเหตุผลของรอบนี้

## เฟส 4 — รายละเอียด (16 ก.ย. 2569)

branch `feat/contact-cards-2569-09-16` (แตกจาก `main` = `e46a629` ซึ่งรวมเฟส 3 แล้ว)
ต้นแบบ: `mCustomers()` · `mCustomerDetail()` — `.mcbar` · `a.mparty` · `.mback` · `.mdetail`/`.mkv` · `.mstat-grid`

### ส่วนที่ 1 — หน้ารายชื่อ `/customers` (จอ < 1280)

1. **แถบค้นหา + ปุ่มเพิ่ม** (`.mcbar`): ช่องค้นหาเต็มความกว้างสูง 52px + ปุ่ม `＋ เพิ่ม` สีอำพันข้างกัน
   ปุ่มเพิ่มพาไปฟอร์มของชนิดที่เปิดอยู่ (ทะเบียนลูกค้า → `kind=customer` · ทะเบียนผู้ขาย → `kind=vendor`)
2. **การ์ดผู้ติดต่อ** (`a.mparty`): ชื่อตัวหนา + บรรทัดรอง `ชิปลูกค้า/ผู้ขาย · รหัส · โทร · ทะเบียนรถ (สองคันแรก) หรือเลขผู้เสียภาษี`
   ปิดท้ายด้วยลูกศร `›` อย่างเดียว ตามต้นแบบ (ผู้ใช้เลือก)
3. **ชิปกรองประเภท** (ทุกรูปแบบ · บุคคลธรรมดา · นิติบุคคล) ยกมาอยู่ใต้แถบค้นหาเป็นแถวเดียว
4. **ตั้งค่าคอลัมน์ (ColPicker)** ซ่อนบนจอแคบ — จอแคบไม่มีตารางให้ตั้งค่า
5. **หัวข้อกลุ่ม + จำนวน** (`N ราย · หน้า x/y`) และ **ปุ่มแบ่งหน้าลอย** ใช้ของเฟส 3 ซ้ำ
   แถวแบ่งหน้าเดิมบนจอแคบเหลือเฉพาะตัวเลือก "แสดงต่อหน้า" (เลขหน้าและปุ่มก่อนหน้า/ถัดไปอยู่ที่ปุ่มลอยแล้ว)
6. **เมนูย่อย 02.x ยังอยู่ด้านบนเหมือนเดิม** — มีแค่สองแท็บกับการ์ดเครื่องมือ ไม่ยาวเหมือนหน้าสินค้า จึงไม่ต้องมีปุ่มสลับ

### ส่วนที่ 2 — หน้าผู้ติดต่อรายคน `/customers/[id]` (จอ < 1280)

**เปิดมาเป็นการ์ดอ่านอย่างเดียว แล้วค่อยกดแก้ไข** (ผู้ใช้เลือก · ตามต้นแบบ)

1. ลิงก์ `← กลับรายชื่อ` (`.mback`) บนสุด
2. **การ์ดข้อมูล** (`.mdetail`): หัวการ์ด = ชื่อ + ชิปลูกค้า/ผู้ขาย · แถว `.mkv` คู่ป้าย/ค่า:
   รหัส · โทรศัพท์ · โทรศัพท์สำรอง · อีเมล · ทะเบียนรถ (ทุกคัน) · เลขผู้เสียภาษี · เครดิต · ที่อยู่ · หมายเหตุ
   (ค่าว่างไม่แสดงแถวนั้น ตามต้นแบบ)
3. **การ์ดตัวเลขสองใบ** (`.mstat-grid`): จำนวนเอกสาร · ยอดคงค้าง (แดงเมื่อมากกว่าศูนย์)
   ทั้งสองค่าคิดจาก `history` ที่หน้านี้ดึงมาอยู่แล้ว — ไม่เพิ่มคิวรีใหม่
4. **ปุ่ม `✎ แก้ไขข้อมูลผู้ติดต่อ`** → `?edit=1` แล้วฟอร์มเดิมโผล่แทนการ์ดอ่าน (สลับด้วย CSS เหมือนแท็บของเฟส 3)
5. **ประวัติซื้อขาย** เปลี่ยนเป็นการ์ดเอกสารของเฟส 2 (`DocCards`) บนจอแคบ · ตารางเดิมบนเดสก์ท็อปและตอนพิมพ์
6. **การ์ด "ลบผู้ติดต่อ"** อยู่ในโหมดแก้ไขเท่านั้นบนจอแคบ — ปุ่มลบไม่ควรอยู่ในหน้าที่เปิดมาเพื่อ "ดู"
7. หน้า **เพิ่มผู้ติดต่อใหม่** (`/customers/new`) เปิดฟอร์มทันทีเหมือนเดิมทั้งสองขนาดจอ — ยังไม่มีข้อมูลให้ดู

### ไม่แตะ
- เดสก์ท็อป ≥ 1280 เหมือนเดิมทุกจุด: ตารางทะเบียน · ColPicker · ฟอร์มแก้ไขเปิดมาเลย · ตารางประวัติ · การ์ดลบ
- ตรรกะค้นหา/กรอง/สิทธิ์/บันทึก/ลบ · คิวรีเดิมทั้งหมด (เฟสนี้เอาแค่การแสดงผล)
- หน้าพิมพ์ทะเบียนผู้ติดต่อ (`/customers/print`)

### เทสต์เฟส 4
`test/contact-card.test.ts` (โมดูลบริสุทธิ์ `lib/contact-card.ts`): แปลงผู้ติดต่อ → การ์ด · บรรทัดรอง
(ชิปชนิด · รหัส · โทร · ทะเบียน/เลขภาษี) · แถวคู่ป้าย/ค่าที่ค่าว่างต้องหายไป · แปลงเอกสารในประวัติ → การ์ด

`e2e/contact-cards.spec.ts` (375 · 768 · 1280):
- จอแคบ: มี `.mcbar` + ปุ่มเพิ่มที่ลิงก์ถูก kind · การ์ดใบแรกอยู่ในหน้าจอแรก · ตารางทะเบียนซ่อน
- การ์ด: ชื่อ · ชิปชนิด · รหัส · ทะเบียน/เลขภาษี ครบ · กดแล้วไป `/customers/<id>`
- หน้าผู้ติดต่อบนจอแคบ: เห็นการ์ดข้อมูล **ไม่เห็นฟอร์ม** · กด "แก้ไข" แล้วได้ `?edit=1` ฟอร์มโผล่ การ์ดอ่านหาย
  · กด "กลับรายชื่อ" กลับไป `/customers`
- ค่าบนการ์ดตัวเลขตรงกับประวัติจริง (จำนวนใบ · ยอดค้าง) — เทียบกับตัวเลขที่เดสก์ท็อปแสดงในหัวการ์ดประวัติ
- ประวัติซื้อขายบนจอแคบเป็นการ์ดเอกสาร · เดสก์ท็อปเป็นตาราง
- ปุ่มแบ่งหน้าลอยทำงานและไม่บังการ์ดใบสุดท้าย · "แสดงต่อหน้า" ยังกดได้บนจอแคบ
- เดสก์ท็อป 1280: ไม่เห็น `.mcbar` `.mback` `.mdetail` `.mpager` · ฟอร์มแก้ไขเปิดมาเลย · ตารางทั้งสองที่ยังอยู่

**กลายพันธุ์ที่ต้องทำให้แดง**: ซ่อนการ์ด/ตารางผิดด้านของจุดตัด · ปุ่มเพิ่มลิงก์ผิดชนิด · การ์ดไม่ลิงก์ไปผู้ติดต่อ ·
ปุ่มแก้ไขไม่พา `edit=1` · ฟอร์มโผล่ทั้งที่ยังไม่กดแก้ไข · ทะเบียนรถหายจากบรรทัดรองของการ์ด

### เอกสาร
สเปก §5.1 (รายชื่อผู้ติดต่อและหน้ารายคนบนจอแคบ) · §13.11 ตารางเหตุผลของรอบนี้
