# แผน — นำชุดแก้ไข 13–14 ก.ย. 2569 เข้าระบบ

branch `integrate/final-2569-09-13` · แตกจาก `bf7301e` · **ไม่แตะ `main` จนกว่าคุณอนุมัติ merge**

ยึด **เมนูแบบใหม่** ตาม `dgl-ui-layout-spec.md` (แทนแผน `PLAN-ux-nav.md` เดิม)

---

## สถานะที่ตรวจแล้ว (วางชุดแก้ทับในสำเนาแยก ยังไม่ commit อะไร)

| ตรวจ | ผล |
|---|---|
| ฐานของชุดแก้ | ทำต่อจากโค้ดล่าสุดของเรา — ของที่แก้ใน `9d3bd59` `bf7301e` ยังอยู่ครบ |
| typecheck | ผ่าน |
| เทสต์หน่วย **รวมเทสต์ที่ใช้ฐานข้อมูล** | 722 / 729 ผ่าน · ล้ม 7 |
| e2e 3 ขนาดจอ | 58 ผ่าน · **ล้ม 19** · ข้าม 4 |
| ไฟล์ generated ของไมเกรชัน | ตรงกับโฟลเดอร์ `db/` (28 ไฟล์) |
| server action ใหม่ 2 ตัว | อ่านผ่าน `query()` ที่ผูกกับอู่ของผู้ใช้ — ปลอดภัย |
| เลขที่เอกสารรูปแบบใหม่ | ฐานข้อมูลยังบังคับไม่ซ้ำ `(tenant, kind, doc_no)` ไม่ชนเลขเก่า |

---

## ขั้น 0 · นำเข้าตามต้นฉบับ

- วางไฟล์ของชุดแก้ทั้งหมด **ยกเว้น** `tsconfig.tsbuildinfo` (ไฟล์แคชของเครื่องคนทำ) และเอกสาร 3 ไฟล์
- เพิ่ม `*.tsbuildinfo` ใน `.gitignore`
- commit แรก = ชุดแก้ **ไม่ดัดแปลง** — ตั้งใจให้ commit นี้ยังแดงอยู่ (7 + 19 ข้อข้างบน)
  เพื่อให้ทุกอย่างที่เราแก้ต่อจากนี้ดู diff แยกได้ชัด ไม่ปนกับงานของคนทำ

## ขั้น 1 · ความปลอดภัยและฐานข้อมูล

**1.1 `product_suppliers` ต้อง force RLS** — ตารางข้อมูลอู่ตัวอื่นทุกตัวตั้งไว้ ตัวนี้ไม่ได้ตั้ง
- `025` เพิ่ม `force row level security` + grant แบบมีเงื่อนไขเหมือน `006` `009` `010`
- `001_init.sql` เพิ่มชื่อตารางเข้า loop ที่ force
- ผ่านเมื่อ: `tenant-isolation` `migrations` `auth-managed-db` เขียว (ล้มอยู่ 5 ข้อ)

**1.2 ดัชนี `documents_purged_idx`** มีใน `028` แต่ไม่มีใน `001` → อู่ที่เปิดใหม่ได้สคีมาไม่เหมือนอู่เดิม เพิ่มใน `001`

**1.3 หน้ารายใบรับรหัสที่ไม่ใช่ uuid แล้วพัง 500** (มีมาก่อนชุดแก้) — `/income/walkin` ตอนนี้ตกไปที่ `/income/[id]`
แล้ว Postgres ปฏิเสธ `"walkin"` → ทุกหน้า `[id]` (รายรับ รายจ่าย สินค้า ลูกค้า ใบวางบิล ใบเคลม ใบตรวจนับ รวมหน้าแก้ไข/พิมพ์)
ตรวจรูปแบบก่อน query แล้วคืน 404 · มีเทสต์

## ขั้น 2 · สำรอง / กู้ข้อมูล ให้ครบข้อมูลใหม่

**ปัญหาที่ยืนยันแล้ว** — `backup.ts` และตัวนำเข้า (ชุดแก้ไม่ได้แตะ) ไม่รู้จักข้อมูลใหม่เลยสักตัว
กู้ข้อมูลแล้ว **ยอดของเอกสารที่มีส่วนลดรายบรรทัดจะเปลี่ยน** (`line_total` คำนวณใหม่โดยไม่มีส่วนลด)
และเอกสารที่ลบถาวรจะกลับมาโผล่

**2.1 ตัวนับเลขที่รายเดือน**
- ตอนนี้แอปใช้คีย์ `ปปดด` · ตัวนำเข้าใช้ `YYYYMM` · ใบวางบิล/ใบเคลมนำเข้าเป็นคีย์ว่างเสมอ · ไฟล์สำรองยุบทุกเดือนเหลือเลขเดียว
- ส่งออก: `seq` เดิมคงไว้ให้รุ่น 6.4 อ่านได้ + คีย์เสริม `_seqPeriods` เก็บทุกแถวพร้อมเดือน (เอกสาร ใบวางบิล ใบเคลม ใบตรวจนับ)
- นำเข้า: ถ้ามี `_seqPeriods` ใช้ทุกแถวตามจริง · ถ้าไม่มี (ไฟล์เก่า/รุ่น 6.4) ทำแบบเดิม
- ตัวนำเข้าใช้ `docNoPeriod()` ตัวเดียวกับแอป เลิกใช้ `YYYYMM`

**2.2 ข้อมูลใหม่ทุกช่อง** ใส่เป็นคีย์ที่รุ่น 6.4 ไม่รู้จักและข้ามไปเอง (ธรรมเนียมเดียวกับ `_picthumbs`)
- รายการ: ส่วนลด % · เอกสาร: ส่วนลดท้ายบิลแบบ %/บาท, วันที่ลบถาวร
- สินค้า: วิธีคิดต้นทุน, ระบบบาร์โค้ด, ผู้ขายของสินค้า · รถ: ช่อง "อื่นๆ"
- ร้าน: บัญชีรับโอนหลายธนาคาร, ชื่อเจ้าของ, รูปลายเซ็น, หมายเหตุมาตรฐาน
- `restore-core` เพิ่ม `product_suppliers` ในลำดับล้างข้อมูลให้ชัด (ตอนนี้หายไปเพราะ cascade)

**2.3 เทสต์** — ขยาย `backup-roundtrip` ให้ส่งออก→นำเข้าแล้วทุกช่องข้างบนกลับมาครบ
ยอดเอกสารที่มีส่วนลดเท่าเดิมทุกสตางค์ · ตัวนับข้ามเดือนอย่างน้อย 2 เดือน · ออกใบใหม่หลังกู้แล้วไม่ซ้ำ
เทสต์ 2 ข้อที่ล้มอยู่ **ปรับให้รู้จักเดือน ไม่ผ่อนเกณฑ์** · ลองย้อนการแก้ให้เทสต์แดงทุกจุด

## ขั้น 3 · สองหน้าที่ขาดไฟล์

**3.1 `/income/walkin` — 03.5 ขายหน้าร้าน** (ตาม SUMMARY ข้อ 3 และต้นแบบ)
- ไทล์ [ประวัติขายหน้าร้าน] [+ ขายหน้าร้าน] · ฟอร์มใบเสร็จ ลูกค้า "ลูกค้าขาจร (เงินสด)" เงินสดตามยอด
- ใช้ของที่มีอยู่แล้ว: preset ของ `/income/new?kind=RC&walkin=1` และ `FormBlock` ของหน้ารายรับ
- ประวัติ = ใบเสร็จที่ไม่มีใบอ้างอิง ไม่รวมที่ลบถาวร พร้อมตัวกรองช่วงเวลา · บันทึกแล้วกลับหน้าเดิม + แถบบันทึกเรียบร้อย

**3.2 `/settings/trash` — 07.5 เอกสารที่ลบ/ยกเลิก** (ต้นแบบมีหน้าตาครบ)
- ตัวกรองช่วงเวลาที่ยกเลิก · ตาราง เลขที่ · ชนิด · วันที่ · ลูกค้า/ผู้ขาย · ยอด · ยกเลิกเมื่อ · [กู้คืน] [ลบถาวร]
- **server action ยังไม่มี** ต้องเขียน — ผ่าน `mutate('settings')` ที่ `lib/trash.ts` ใช้อยู่ · ลบถาวรยืนยันสองชั้น
- เทสต์ฐานข้อมูล: กู้คืนใบเสร็จตัดสต๊อกใหม่ · กู้คืนใบซื้อรับของกลับ · ลบถาวรแล้วหายจากทุกหน้ารวมถังขยะ · อู่อื่นกู้/ลบของเราไม่ได้

## ขั้น 4 · หน้าจอ — บั๊กจริงที่เทสต์ `nav.spec` ของชุดแก้เองจับได้

| # | อาการ | แก้ตามสเปก |
|---|---|---|
| 4.1 | เดสก์ท็อป 1280px ปุ่มเมนูหลัก 2 ปุ่มหลุดขอบขวา (ขวาสุดที่ 1318px) | §5.3 เมนูเต็มแถบเดียว · บีบระยะ/ขนาดในบล็อก ≥1280 **ห้ามเพิ่มจุดตัด** · ตรวจที่ 1280 · 1366 · 1920 |
| 4.2 | มือถือ ปุ่มย้อนกลับไม่ลอย (อยู่ที่ y = 2299 ในเนื้อหา) | §3 G `fixed` ขวาล่าง เหนือแถบล่าง `bottom: 58 + 14px` |
| 4.3 | มือถือ/แท็บเล็ต ช่องกรอกในตารางรายการสูง 36px ปุ่มลบ 32px (37–38 จุด) | §2 §10 ปุ่มและช่องกดได้ ≥ 44px ทุกโหมด |
| 4.4 | แถบล่างมือถือ | §5.1 กำหนด หน้าแรก · รายรับ · สินค้า · ลูกค้า · เพิ่มเติม — ตรวจของจริง ถ้าไม่ตรง (เดิมเป็น รายจ่าย) แก้ให้ตรง |
| 4.5 | สเปก §12 อ้างไฟล์ที่ไม่มีจริง (`nav-rail.tsx` `drawer.tsx` `back-fab.tsx`) | แก้ตารางในสเปกให้ชี้ไฟล์จริงตอนนำเข้า repo |

## ขั้น 5 · เทสต์ที่ต้องปรับ และบั๊กเก่าที่อาจกลับมา

**5.1 ใบตรวจนับ ยิงรัว 3 นัดได้ 1** — **ตรวจก่อนว่าบั๊ก "ของหาย" กลับมา หรือแค่คอลัมน์เปลี่ยน**
ถ้ากลับมา แก้ให้ล้างช่องก่อนยิงคำขอ + ไม่ disabled ตามเดิม · เทสต์ยังเข้มเท่าเดิม

**5.2 ยิงบาร์โค้ดในเอกสาร** — ตารางใหม่มี 5 บรรทัดว่างเสมอ เทสต์เดิมนับแถว/อ่านช่องตามลำดับเลยผิดที่
- หาช่องจำนวนจากหัวคอลัมน์ ไม่ใช่ลำดับ `nth(3)` · "ยิงไม่เจอ" ตรวจว่าไม่มีรหัสนั้นในตาราง แทนการนับแถว
- ข้อ "ยิงซ้ำเพิ่มจำนวน" อ่านได้ช่องหน่วยว่าง — ต้องตรวจพฤติกรรมจริง ไม่ใช่แค่แก้ selector

**5.3 ตัวหนังสือกลางกรอบ** — หน้ารายรับเปิดมาเป็นฟอร์ม ต้องหาลิงก์รายใบจาก `?hist=1`
แล้ววัดทั้งระบบใหม่ทุกหน้า × 3 จอ รวมสองหน้าใหม่ (ชุดแก้เปลี่ยนหน้าตาเกือบทั้งหมด)

**กติกา** — ห้ามผ่อนเทสต์ให้ผ่าน ทุกเทสต์ที่แก้เขียนเหตุผลไว้ในไฟล์ · การแก้ทุกจุดลองย้อนแล้วเทสต์ต้องแดง

## ขั้น 6 · ตรวจรวมก่อนส่งให้ดู

- `npm test` พร้อมฐานข้อมูล · typecheck · e2e ครบ 3 จอ · วัดตัวหนังสือกลางกรอบทั้งระบบ
- เปิดของจริง: ใบเสนอราคา → ใบส่งมอบ → ใบเสร็จ · ขายหน้าร้านเครดิต → วางบิล · รับชำระบางส่วน
  · ถังขยะ กู้คืน/ลบถาวร · ส่งออกสำรอง → กู้คืนทับ → ยอดเท่าเดิม · พิมพ์ A4 หลายหน้า
- README "ที่ยังไม่ได้ทำ" · DIVERGE ของผังเมนูใหม่ · `DEPLOY.md` เพิ่มหมายเหตุไมเกรชัน 020–028

**ลำดับ commit** นำเข้าตามต้นฉบับ → ความปลอดภัย/ฐานข้อมูล → สำรองข้อมูล → สองหน้าที่ขาด → หน้าจอ → เทสต์ → เอกสาร

---

## ขึ้นเครื่องจริง (หลัง merge · คุณเป็นคนรัน)

- สำรองฐานข้อมูลก่อน · ไมเกรชัน `020` เขียนตาราง `doc_items` ใหม่ทั้งตาราง (ล็อกช่วงสั้น ราววินาทีที่ขนาด 20–30 อู่) → ทำนอกเวลาทำการ
- **เลขที่เอกสารรูปแบบใหม่นับใหม่ทุกเดือน** — ขึ้นกลางเดือนเลขจะเริ่ม 0001 กลางเดือน
- ไฟล์สำรองที่ส่งออกก่อนขึ้นยังกู้ได้ (ตัวนำเข้ารองรับไฟล์ที่ไม่มีคีย์ใหม่)

## ต้องให้คุณตัดสินใจ

1. **ส่วนลดรายบรรทัดในไฟล์สำรองที่เปิดด้วยรุ่น 6.4** — รุ่น 6.4 ไม่มีส่วนลดรายบรรทัด
   - **ก. (แนะนำ)** ช่องราคาที่รุ่น 6.4 อ่าน = ราคาหลังหักส่วนลด · ราคาเต็ม + % เก็บในคีย์เสริม → รุ่น 6.4 ยอดถูก เว็บกู้ได้ครบ
   - ข. ช่องราคา = ราคาเต็ม · % ในคีย์เสริม → รุ่น 6.4 ยอดสูงกว่าจริง
2. **วันขึ้นเครื่องจริง** — แนะนำต้นเดือน (เช่น 1 ต.ค. 2569) ให้เลขที่รูปแบบใหม่เริ่มสะอาด และแจ้งอู่/นักบัญชีว่ารูปแบบเลขเปลี่ยน
3. **เอกสารของชุดแก้** — แนะนำเก็บ `dgl-ui-layout-spec.md` และ `SUMMARY-FINAL.md` ไว้ที่ `docs/` เป็นสเปกอ้างอิง ส่วน `CHANGES.md` (บันทึกรายรอบ) ไม่ต้องเก็บ

## ไม่ทำในรอบนี้

อีเมล · คิวอาร์รับเงิน · ปิดงวด ภ.พ.30 · ค่าเสื่อมราคา (ตามรายการเดิมใน README)
ปัญหาหัวหน้าบนมือถือกินจอ 46% ที่เจอรอบก่อน — ชุดแก้เปลี่ยนหัวหน้าทั้งหมดแล้ว จะวัดใหม่ในขั้น 6 ถ้ายังเป็นอยู่จะแจ้งแยก
