# การเปิดอู่ใหม่และการให้สิทธิ์เข้าใช้งาน

เอกสารนี้อธิบายว่า **อู่หนึ่งอู่เริ่มใช้ระบบได้อย่างไร** ตั้งแต่ยังไม่มีอะไรเลย
จนถึงเจ้าของกิจการกับพนักงานเข้าใช้งานได้จริง

เขียนไว้ให้คนที่ต้องเปิดอู่ให้ลูกค้ารายใหม่ทำตามได้ทันที และให้คนที่กลับมาอ่านโค้ด
เข้าใจว่า **ลิขสิทธิ์กับบัญชีผู้ใช้เป็นคนละเรื่องกัน**

> สถานะ ณ ตอนนี้: ยังไม่มีหน้าสมัครเปิดอู่ด้วยตัวเอง อู่ใหม่เกิดจากผู้ให้บริการรันคำสั่ง
> เหมาะกับช่วง 20–30 อู่แรก · คอนโซลผู้ให้บริการอยู่ในรายการงานที่ยังไม่ได้ทำ

---

## สองสิ่งที่มักถูกเข้าใจว่าเป็นเรื่องเดียวกัน

|  | ลิขสิทธิ์การใช้งาน | บัญชีเข้าระบบ |
|---|---|---|
| ตาราง | `subscriptions` | `users` |
| ผูกกับ | **อู่** (`tenants`) | **คน** |
| ตอบคำถาม | อู่นี้จ่ายเงินหรือยัง | คนนี้คือใคร ทำอะไรได้บ้าง |
| จำนวน | 1 อู่ = 1 สาย ต่ออายุเป็นแถว ๆ | 1 อู่ = หลายคน |
| ตรวจที่ไหน | `mutate()` ตอนจะ**เขียน**ข้อมูล | `requirePerm()` ตอนจะ**เข้า**หน้า |
| ถ้าขาด | เขียนรายการใหม่ไม่ได้ แต่ยังดู พิมพ์ และส่งออกข้อมูลได้ | เข้าระบบไม่ได้เลย |

**ด่านสองใบซ้อนกันโดยตั้งใจ** — จ่ายเงินแล้วหรือยัง แล้วค่อยถามว่าคนนี้มีสิทธิ์ไหม
อู่ที่ขาดต่ออายุต้องเอาข้อมูลของตัวเองออกไปได้เสมอ ไม่ใช่ถูกจับเป็นตัวประกัน

จุดเดียวที่สองเรื่องนี้เกี่ยวกันคือ **จำนวนบัญชีพนักงานที่แพ็กเกจเปิดให้**
(`subscriptions.max_users` — ดูหัวข้อ 5)

---

## 1. เปิดอู่ใหม่ พร้อมบัญชีเจ้าของกิจการ

ผู้ให้บริการรันตัวนำเข้าจากเครื่องที่ต่อฐานข้อมูลได้

```bash
node packages/importer/dist/cli.js ไฟล์สำรองของอู่.json \
  --url="$DATABASE_URL" \
  --name="อู่ ช่างเอ ออโต้เซอร์วิส" \
  --owner-email=owner@example.com \
  --owner-name="สมชาย ใจดี" \
  --app-url=https://app.drivebizbegin.com
```

อู่ที่ยังไม่เคยใช้โปรแกรมเดิมมาก่อน ให้ก๊อป [`tools/new-shop.template.json`](tools/new-shop.template.json)
มาแก้ชื่อร้านแล้วส่งเข้าไปแทนไฟล์สำรอง

```bash
cp tools/new-shop.template.json /tmp/อู่ใหม่.json
# แก้ shop.name แล้วรันคำสั่งข้างบนโดยชี้ไปที่ไฟล์นี้
```

ไฟล์ที่มีแค่ `shop` อย่างเดียวใช้ไม่ได้ — ตัวตรวจบังคับว่าต้องมี array ครบทุกกลุ่ม
ถึงจะว่างก็ต้องมี เพื่อไม่ให้ไฟล์สำรองที่ขาดหายไปครึ่งหนึ่งถูกนำเข้าเงียบ ๆ
โดยที่ไม่มีใครรู้ว่าข้อมูลหาย

ทำงานในทรานแซกชันเดียว ได้ผลสามอย่าง

| ตาราง | ได้อะไร |
|---|---|
| `tenants` | แถวอู่ใหม่ · ข้อมูลร้าน · อัตราภาษี |
| `users` | เจ้าของ · `role='owner'` · `code='OWNER'` · **`password_hash` ยังว่าง** |
| `auth.setup_tokens` | โทเคนตั้งรหัสผ่าน อายุ **7 วัน** — ฐานข้อมูลเก็บแค่ SHA-256 ของมัน |

แล้วพิมพ์ลิงก์ออกมา

```
tenant_id = f08cb3b9-…
https://app.drivebizbegin.com/setup/z_-d1JInXjJWSoJSqXCZiGWSlJweBw8EFxfkaTzRtOM
```

**ส่งลิงก์นี้ให้เจ้าของอู่** — ระบบยังส่งอีเมลเองไม่ได้

เจ้าของได้สิทธิ์ครบทั้งหกเมนูอัตโนมัติ และ `role='owner'` ทำให้**ข้ามด่านสิทธิ์ทุกด่านตลอดไป**
ไม่ต้องติ๊กอะไรให้ และไม่มีทางล็อกตัวเองออกจากระบบด้วยการตั้งสิทธิ์ผิด

---

## 2. เจ้าของกิจการตั้งรหัสผ่านครั้งแรก

เปิดลิงก์ → ตั้งรหัสผ่าน (อย่างน้อย 10 ตัวอักษร) → ระบบทำสามอย่าง

1. เก็บรหัสผ่านเป็น argon2id — **ไม่เคยเก็บรหัสผ่านดิบ**
2. **ลบ session ทุกเครื่องที่เคยเข้าไว้** เผื่อลิงก์เคยหลุดไปถึงมือคนอื่น
3. ล็อกอินให้ทันที · โทเคนใช้ได้ครั้งเดียวแล้วหมดอายุ

ตั้งแต่นี้ไปเข้าระบบด้วย **อีเมล + รหัสผ่าน** เท่านั้น
session อยู่ได้ 14 วัน · กรอกรหัสผิด 5 ครั้งติดกันถูกล็อก 15 นาที

---

## 3. ลิขสิทธิ์เริ่มนับเอง ไม่ต้องลงทะเบียนอะไร

ยังไม่มีแถวใน `subscriptions` → ระบบถือว่าเป็น **ทดลองใช้ 15 วัน**
นับจากวันที่เปิดอู่ (`tenants.created_at`)

| สถานะ | เกิดเมื่อ | เขียนข้อมูลใหม่ | ดู / พิมพ์ / ส่งออก |
|---|---|---|---|
| `trial` | ยังไม่เคยจ่าย และยังไม่เกิน 15 วัน | ได้ | ได้ |
| `active` | มีแถวต่ออายุที่ยังไม่หมด | ได้ | ได้ |
| `expired` | เลยวันหมดอายุ | **ไม่ได้** | **ได้** |

ต่ออายุที่เมนู **08 ลิขสิทธิ์การใช้งาน** (ต้องมีสิทธิ์ตั้งค่าร้าน)
ต่อล่วงหน้าจะนับต่อจากวันหมดอายุเดิม ไม่เสียวันที่จ่ายไปแล้ว
ส่วนที่ขาดไปแล้วเริ่มนับใหม่จากวันนี้ ไม่ต้องจ่ายชดเชยวันที่ไม่ได้ใช้

งานที่ยังทำได้แม้หมดอายุ — ต่ออายุ · ตั้งค่าร้าน · จัดการผู้ใช้ · ส่งออกและกู้คืนข้อมูล ·
ขอลบข้อมูลตาม PDPA

---

## 4. เจ้าของเพิ่มพนักงาน

**ตั้งค่าร้าน → ตั้งค่าพนักงาน (07.2) → + เพิ่มผู้ใช้**

```
ชื่อ     สมหญิง ใจดี
อีเมล    somying@example.com     ← ใช้เข้าระบบ
รหัส     U0001                    ← รหัสพนักงาน ไม่ได้ใช้เข้าระบบ
สิทธิ์    ติ๊กทุกเมนู ยกเว้น "ตั้งค่าร้าน" (ค่าปริยาย)
```

> **`code` ไม่ใช่รหัสเข้าระบบ** — ในโปรแกรมรุ่น HTML เดิมไม่มีการล็อกอินจริง
> `code` + `pass` ใช้ "สลับผู้ใช้" ในไฟล์เดียวกัน ใครเปิดไฟล์ได้ก็สลับเป็นใครก็ได้
> พอย้ายมาเว็บที่มี session จริง `code` จึงเหลือหน้าที่เดียวคือ**รหัสอ้างอิงพนักงาน**
> สำหรับใช้ในเอกสารและประวัติการแก้ไข

กดบันทึกแล้ว **พนักงานยังเข้าระบบไม่ได้** เพราะยังไม่มีรหัสผ่าน

เจ้าของต้องกด **“ออกลิงก์ตั้งรหัสผ่าน”** แล้ว**ส่งลิงก์ให้พนักงานเอง** (ทางไลน์หรืออื่น ๆ)
พนักงานเปิดลิงก์ ตั้งรหัสของตัวเอง แล้วเข้าใช้งานได้

ขั้นตอนเดียวกันนี้ใช้ตอน **พนักงานลืมรหัสผ่าน** ด้วย — เลือกแบบ “รีเซ็ต”

---

## 5. จำนวนบัญชีพนักงานที่เปิดได้

`subscriptions.max_users` — ค่าปริยาย 3 คนให้ตรงกับโปรแกรมเดิม · `null` แปลว่าไม่จำกัด

- **เจ้าของกิจการไม่ถูกนับ** — ที่นั่งที่ขายคือที่นั่งพนักงาน
- **ตรวจตอนสร้างบัญชีใหม่เท่านั้น ไม่ตรวจตอนล็อกอิน**
  ลดแพ็กเกจแล้วต้องไม่มีใครถูกล็อกออกจากระบบกลางดึกโดยไม่รู้ตัว
- ปิดการเข้าใช้งานของพนักงานที่ลาออกแล้ว ที่นั่งคืนมาทันที

เพิ่มที่นั่งให้อู่หนึ่งอู่โดยไม่ต้อง deploy ใหม่

```sql
-- แถวล่าสุดของอู่นั้น (Postgres ไม่รับ order by/limit ใน update ตรง ๆ)
update subscriptions set max_users = 6
 where id = (select id from subscriptions
              where tenant_id = 'f08cb3b9-…'
              order by expires_on desc limit 1);
```

---

## สรุปเป็นภาพเดียว

```
ผู้ให้บริการ                เจ้าของอู่                    พนักงาน
     │
     │ 1. รัน importer
     ├──────────────────────▶ tenants + users(owner)
     │                        + ลิงก์ตั้งรหัสผ่าน (7 วัน)
     │ ส่งลิงก์ให้
     └──────────────────────▶│
                             │ 2. ตั้งรหัสผ่าน → เข้าระบบได้
                             │    (ทดลองใช้ 15 วันเริ่มนับเอง)
                             │
                             │ 3. เพิ่มพนักงาน + ติ๊กสิทธิ์
                             ├────────────────────────────▶ users(staff)
                             │                              ยังไม่มีรหัสผ่าน
                             │ 4. ออกลิงก์ แล้วส่งให้
                             └────────────────────────────▶│
                                                           │ 5. ตั้งรหัสผ่าน
                                                           │    → เข้าระบบได้
```

ทุกครั้งที่กดบันทึกอะไรก็ตาม

```
        ด่าน 1 · ลิขสิทธิ์ (ผูกกับอู่)    →  ขาดต่ออายุ = เขียนไม่ได้ แต่ยังอ่านได้
        ด่าน 2 · สิทธิ์ (ผูกกับคน)        →  ไม่มีสิทธิ์เมนูนั้น = เด้งกลับ
```

---

## ที่ยังไม่ได้ทำ และต้องรู้ก่อนเปิดใช้จริง

| เรื่อง | ผลกระทบตอนนี้ |
|---|---|
| ระบบส่งอีเมล | ลิงก์ตั้งรหัสผ่านต้องคัดลอกส่งเอง ทั้งของเจ้าของและของพนักงาน |
| **เจ้าของลืมรหัสผ่าน** | รีเซ็ตเองไม่ได้ ต้องให้ผู้ให้บริการเข้าฐานข้อมูลออกโทเคนให้ |
| หน้าสมัครเปิดอู่เอง | ไม่มี — ผู้ให้บริการรัน CLI ให้ทุกอู่ |
| คอนโซลผู้ให้บริการ | ยังไม่มี — ดูว่าอู่ไหนหมดอายุเมื่อไหร่ต้องเปิดฐานข้อมูลดู |

ดูรายการเต็มและวิธี deploy ที่ [`DEPLOY.md`](DEPLOY.md) · แผนงานที่เหลือที่ [`PLAN-6.4.md`](PLAN-6.4.md)
