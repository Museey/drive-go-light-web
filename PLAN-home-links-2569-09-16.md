# แผน — ลิงก์หน้าแรกพาไปประวัติ ไม่ใช่ฟอร์มสร้างใหม่

branch `fix/home-links-history-2569-09-16` (แตกจาก `main` = `6ef8dd1`) · **อนุมัติแล้ว 16 ก.ย. 2569** · ไม่ merge จนกว่าอนุมัติอีกรอบ

กติกาเดิม: แก้ที่ต้นเหตุ · เทสต์ใหม่ต้องแดงก่อนแก้ ย้อนการแก้แล้วแดง · ห้ามผ่อนเทสต์ · e2e บน production build · ภาพ 1–2 รูป

## อาการ (ผู้ใช้แจ้ง)

หน้าแรก การ์ด "สรุปยอดขาย" มีปุ่ม "ดูใบเสร็จทั้งหมด" กดแล้วไปหน้า**สร้างใบเสร็จใหม่** ที่ถูกคือ**ประวัติใบเสร็จ**

**ต้นเหตุ**: ทุกเมนูย่อยของรายรับ/รายจ่ายเปิดมาเป็นฟอร์มสร้างใหม่ ประวัติต่อด้านล่าง (`histFirst = sp.hist === '1' || !formKind`)
ลิงก์จากหน้าแรกส่ง `?kind=` อย่างเดียว จึงได้ฟอร์ม ไม่ใช่ประวัติ

## จุดที่แก้ (หน้าแรก `src/app/page.tsx` ทั้งหมด)

| จุด | ตอนนี้ | แก้เป็น |
|---|---|---|
| การ์ดสรุปยอดขาย → "ดูใบเสร็จทั้งหมด" | `/income?kind=RC` | `/income?kind=RC&hist=1` |
| การ์ดงานค้างส่งมอบ → "ดูรายการ" | `/income?kind=QT&open=1` | + `hist=1` (ยังกรองเฉพาะงานค้างเหมือนเดิม) |
| ตาราง "เอกสารในระบบ" → "ดูรายการ" ทุกแถว | `/income?kind=…` · `/expense?kind=…` | + `hist=1` ทั้งสองฝั่ง |

ไม่แตะกติกา "เปิดเมนูย่อยมาเป็นฟอร์ม" — เมนู 03/04 ยังเปิดมาเป็นฟอร์มเหมือนเดิม เปลี่ยนเฉพาะปลายทางของลิงก์หน้าแรก

## เทสต์ (`e2e/home-links.spec.ts`)

- กดปุ่มทั้ง 3 จุดจากหน้าแรกจริง ต้องได้หน้าประวัติ: มีตารางประวัติ · ไม่มีฟอร์มสร้างเอกสารใหม่ · URL มี `hist=1`
- ตัวกรองยังติดไป: ใบเสร็จได้เฉพาะ RC · งานค้างส่งมอบยังมี `open=1` และเห็นเฉพาะใบที่ค้าง
- ตาราง "เอกสารในระบบ" ทั้งฝั่งรายรับและรายจ่าย
- ย้อนการแก้ทีละจุดแล้วต้องแดง

## ตรวจรวม
typecheck · `npm test` · e2e ครบบน production build · ย้อนการแก้ · ภาพก่อน/หลัง · §13.5 ในสเปก
