# แผน — ปุ่ม "จ่ายชำระ" หน้าเจ้าหนี้ถูกตัดขอบขวา (17 ก.ย. 2569)

branch `fix/ap-pay-button-clip-2569-09-17` (แตกจาก `main` = `e684aef`)

## ต้นเหตุ (วัดจากหน้าจริง)
| จอกว้าง | ตารางเจ้าหนี้ | กรอบ | ล้น |
|---|---|---|---|
| 1280 | 1,273px | 1,234px | **39px** → ตารางเลื่อนข้าง ปุ่มท้ายแถวโดนตัด |
| 1366 · 1440 · 1920 | พอดีกรอบ | | ไม่ล้น |

- ช่อง "ครบกำหนด" กว้าง 194px เพราะชิป **"เกิน N วัน"** ต่อท้ายวันที่ในบรรทัดเดียวกัน (ตัดบรรทัดไม่ได้)
- หน้าเจ้าหนี้มีคอลัมน์ "ชนิด" ที่ชื่อหมวดยาว (เช่น "ค่าโทรศัพท์ / อินเทอร์เน็ต") จึงล้น · หน้าลูกหนี้พอดี 1,234px เป๊ะ (เสี่ยงล้นถ้าชื่อยาวขึ้น)
- ตรวจตารางอื่นที่ 1280 แล้ว 17 หน้า (รายรับ รายจ่าย ใบวางบิล ยอดขาย ลูกค้า สินค้า ใบเคลม ใบตรวจนับ ถังขยะ ผู้ใช้ ฯลฯ) — **ล้นหน้าเดียวคือเจ้าหนี้**

## ลองแล้ว
| ทาง | ผล |
|---|---|
| ให้ช่อง "ชนิด" ตัดบรรทัดได้ | แย่ลง (ล้น 69px) — ช่องที่ตัดบรรทัดได้มีความกว้างขั้นต่ำ 200px |
| **ชิป "เกิน N วัน" ลงบรรทัดใต้วันที่** | ช่องครบกำหนดเหลือ 113px · **ไม่ล้นทุกขนาดจอ** · แถวที่เกินกำหนดสูงขึ้น 3px |

## จะทำ
- ชิป "เกิน N วัน" อยู่ใต้วันที่ครบกำหนด **ทั้งหน้าเจ้าหนี้และลูกหนี้** (สองหน้าหน้าตาเดียวกัน · ลูกหนี้ได้ที่เผื่อไว้ด้วย)
- ปุ่ม "จ่ายชำระ" / "รับชำระ" สี ขนาด ตำแหน่งเดิม
- จอแคบ (การ์ดรายคน) ไม่แตะ

## เทสต์
e2e เดสก์ท็อป 1280 · 1366 · 1920: ตารางเจ้าหนี้/ลูกหนี้ไม่ล้นกรอบ · ปุ่มท้ายแถวอยู่ในกรอบทั้งปุ่ม · ชิปเกินกำหนดอยู่ใต้วันที่ (ไม่ใช่ข้าง ๆ) ·
ทั้งชุดเดิมต้องเขียว · กลายพันธุ์: ชิปกลับไปต่อท้ายวันที่ → ข้อ 1280 ต้องแดง

---

## ผล (17 ก.ย. 2569)
- e2e ทั้งชุดบน production build 548 ผ่าน · 97 ข้ามตามออกแบบ · `e2e/ap-pay-clip.spec.ts` 6 ข้อ (เจ้าหนี้/ลูกหนี้ × 1280/1366/1920)
- เทสต์ใหม่แดงบนโค้ดเดิม 6/6 (เจ้าหนี้ 1280 ล้น 39px · ที่เหลือชิปอยู่ข้างวันที่)
- ย้อนการแก้ (เอากฎ CSS ออก) → แดง 6/6 ตรงอาการ (ล้น 33px · ชิปกลับไปข้างวันที่)
- เจอระหว่างทำ: รอบแรกใช้ `display: table` ตารางพอดีแล้วแต่ชิปเสียทรงในภาพหน้าจริง → เปลี่ยนเป็น flex แล้วถ่ายภาพยืนยันใหม่
- เครื่องใกล้เต็ม (เหลือ 2.8 GB) ไฟล์ build ค้างไม่ครบจนเปิดเซิร์ฟเวอร์ไม่ขึ้น → ลบ `.next` ที่ระบบสร้างเองแล้ว build ใหม่
