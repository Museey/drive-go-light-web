# ช่วงที่ 6 — สิทธิ์ละเอียดและทะเบียนพนักงาน (07.2)

## Context

ตอนนี้สิทธิ์ของเรามีชั้นเดียว — `users.perms` เป็นอาเรย์ของชื่อเมนูหกตัว
ใครมีสิทธิ์ `stock` ก็ทำได้ทุกอย่างในหมวดสินค้า ตั้งแต่ดูจนถึงลบทิ้ง
และเห็นต้นทุนของทุกชิ้น

รุ่น 6.4 แยกเป็นสามแกนต่อเมนูย่อยหนึ่งอัน — **ดูได้ · แก้ไขได้ · พิมพ์หรือส่งออกได้** —
บวกสองสวิตช์ระดับคน คือ **เห็นต้นทุน** กับ **เห็นรายงานสรุปหน้าแรก**

นี่เป็นช่วงสุดท้ายที่แตะโครงสร้างข้อมูล และเป็นช่วงที่**พลาดแล้วเจ็บเงียบ** —
ระบบยังทำงานได้ปกติทุกอย่าง แค่ช่างคนหนึ่งเห็นต้นทุนอะไหล่ทั้งร้าน
แล้วไม่มีใครรู้จนกว่าจะไปเปิดร้านแข่ง

**ข้อสำคัญที่สุดของช่วงนี้จึงไม่ใช่การซ่อนปุ่ม แต่คือการพิสูจน์ว่าไม่มีทางลอด**

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. `users.perms` เปลี่ยนจาก `text[]` เป็น `jsonb` พร้อมตัวแปลงอัตโนมัติ

รูปแบบใหม่ — จัดใหม่ให้ตรงไปตรงมากว่า 6.4 แต่ความหมายเหมือนกัน

```jsonc
{
  "menus":  { "customer": true, "income": true, "stock": true },  // ระดับเมนูหลัก
  "tabs":   { "stock.list": true, "stock.count": false },         // ดูได้ รายเมนูย่อย
  "edit":   { "stock.list": false },                              // แก้ไขได้
  "export": { "stock.list": false },                              // พิมพ์ทั้งชุด / ส่งออกไฟล์
  "cost": false,                                                  // เห็นต้นทุน
  "homeReport": true                                              // เห็นสรุปหน้าแรก
}
```

**ไม่พอร์ตช่องทางเก่าสองอันของ 6.4** — `perms.sub` (จำกัดให้เข้าได้แท็บเดียว)
กับ `perms.exportData` (สวิตช์ส่งออกตัวเดียวทั้งระบบ) มีไว้รองรับไฟล์รุ่นก่อน 6.4 ของมันเอง
เรารับไฟล์เข้ามาแปลงครั้งเดียวตอนนำเข้า ไม่ต้องแบกทางเลือกสองทางไว้ตลอดกาล
— แต่**ตัวนำเข้าต้องอ่านทั้งสองแบบให้ถูก** เพราะไฟล์จริงมีทั้งสองรูปแบบปนกัน

ตัวแปลงในไมเกรชันแปลงอาเรย์เดิมเป็น `menus` ทั้งหมด และตั้ง `cost`/`homeReport` เป็น `true`
— คนที่ใช้อยู่ตอนนี้ต้องไม่เสียสิทธิ์อะไรจากการอัปเกรด

### 2. ค่าที่ไม่ได้ตั้งไว้ = อนุญาต ยกเว้น `tabs` ที่ทำตาม 6.4

`edit` · `export` · `cost` · `homeReport` ที่ไม่มีคีย์ ถือว่าอนุญาต (`!== false`)
เหมือน 6.4 ทุกตัว

`tabs` ต่างออกไป — 6.4 ใช้กติกา *"ถ้ามีคีย์ขึ้นต้นด้วยเมนูนี้อยู่บ้าง ที่ไม่มี = ห้าม"*
ซึ่งจำเป็นเพราะไฟล์เก่าไม่มีคีย์เลย ทำตามเพื่อให้ไฟล์ที่นำเข้ามาได้สิทธิ์เท่าเดิม
**แต่หน้าจอของเราจะเขียนครบทุกแท็บเสมอตอนบันทึก** ค่าที่ "ไม่มี" จึงเกิดได้เฉพาะกับ
ข้อมูลที่ย้ายเข้ามา ไม่ใช่ข้อมูลที่ระบบเราสร้างเอง

### 3. เจ้าของกิจการข้ามทุกด่านเสมอ

`role = 'owner'` ผ่านหมด ไม่ต้องอ่าน `perms` เลย — เหมือนที่ `can()` ทำอยู่ตอนนี้
คนที่ตั้งค่าสิทธิ์ให้คนอื่นต้องไม่มีทางล็อกตัวเองออกจากระบบ

### 4. ซ่อนต้นทุน = ปิดหน้ากำไรขาดทุนด้วย — ตามต้นฉบับ

6.4 เขียนคอมเมนต์ไว้ตรง ๆ ว่า *"งบกำไรขาดทุนแสดงต้นทุนขายเป็นหลัก
ผู้ที่ถูกซ่อนต้นทุนจึงเข้าไม่ได้"* ทำตาม เพราะทั้งหน้าคือรายงานต้นทุน
ซ่อนทีละตัวเลขแล้วเหลือแต่กรอบว่างไม่มีประโยชน์กับใคร

### 5. **ส่งออกไฟล์สำรองต้องมีสิทธิ์เห็นต้นทุน**

ไฟล์สำรองมีต้นทุนของสินค้าทุกตัวและทุกใบซื้ออยู่ในนั้น
ถ้ากันแค่หน้าจอแต่ปล่อยให้กดดาวน์โหลดได้ การซ่อนต้นทุนก็ไม่มีความหมายเลย

ตอนนี้ `/settings/export` กันด้วย `settings` อย่างเดียว — เพิ่มเงื่อนไข `cost` เข้าไป
นี่เป็นข้อที่ 6.4 ไม่ได้ทำ แต่เป็นรูรั่วจริงที่เห็นได้ทันทีเมื่อแยกสิทธิ์ต้นทุนออกมา

เหตุผลเดียวกันกับ `/stock/csv` (มีคอลัมน์ต้นทุน) และ `/finance/csv`

### 6. ต้นทุนที่ถูกซ่อนแสดงเป็น `•••••` ไม่ใช่ `0` หรือช่องว่าง

`costMoney()` ของ 6.4 คืน `'•••••'` — ตั้งใจให้ผู้ใช้รู้ว่า**มีตัวเลขอยู่แต่ไม่ได้สิทธิ์เห็น**
ต่างจากศูนย์ซึ่งอ่านว่าของฟรี และต่างจากช่องว่างซึ่งอ่านว่ายังไม่ได้กรอก

### 7. `users.code` เปลี่ยนความหมายเป็น **รหัสพนักงาน** ไม่ใช่รหัสเข้าระบบ

ตอนนี้พนักงานหนึ่งคนมีตัวระบุสองอัน แต่ใช้จริงอันเดียว —
`code` (`U0001`) ตั้งค่าได้และโผล่ในตาราง **แต่ไม่เคยถูกใช้ยืนยันตัวตนเลย**
ส่วน `email` คือสิ่งที่ล็อกอินจริง

สาเหตุคือรุ่น 6.4 **ไม่มีการล็อกอินจริง** — `code` + `pass` ใช้ "สลับผู้ใช้" ในไฟล์เดียวกัน
ใครเปิดไฟล์ได้ก็สลับเป็นใครก็ได้ พอย้ายมาเว็บที่มี session จริง `code` เลยเหลือแค่ป้ายชื่อ
ที่ตกค้างมาเพราะไฟล์สำรองยังมีคอลัมน์นี้

**ไม่ทิ้ง แต่เลิกทำให้มันดูเหมือนรหัสล็อกอิน** — เก็บไว้เป็นรหัสอ้างอิงพนักงาน
ซึ่งช่วงที่ 7 ต้องใช้อยู่แล้วตอนทำประวัติการแก้ไข (`created_by` ชี้มาที่ผู้ใช้
แต่แสดง `U0003` อ่านง่ายกว่าอีเมลยาว ๆ บนตารางประวัติ)

สิ่งที่ต้องทำ
- หน้า 07.2 ย้ายช่องรหัสไปอยู่กลุ่ม "ข้อมูลพนักงาน" ไม่ใช่กลุ่ม "การเข้าใช้งาน"
  พร้อมคำกำกับว่า *ใช้อ้างอิงในเอกสารและประวัติการแก้ไข — เข้าระบบด้วยอีเมล*
- หน้าล็อกอินไม่ต้องแก้ (ถามอีเมลอยู่แล้ว)
- คอลัมน์ในฐานไม่ต้องแก้ — `unique (tenant_id, code)` ยังถูกต้องสำหรับรหัสพนักงาน

### 8. จำนวนผู้ใช้เป็นค่าของ**การสมัครใช้บริการ** ไม่ใช่ค่าคงที่ในโค้ด

6.4 ฝัง `MAX_USERS = 3` ไว้ในไฟล์ ซึ่งเป็นกติกาเชิงธุรกิจที่ตอนนี้ยังไม่ได้ตั้งราคาแน่นอน
และเราไม่มีคอนโซลผู้ให้บริการให้แก้รายอู่

```sql
alter table subscriptions add column max_users integer;   -- null = ไม่จำกัด
```

- **ตรวจตอนสร้างบัญชีใหม่เท่านั้น** ไม่ตรวจตอนล็อกอิน —
  ลดแพ็กเกจแล้วต้องไม่มีใครถูกล็อกออกจากระบบกลางดึกโดยไม่รู้ตัว
- **เจ้าของกิจการไม่ถูกนับ** — ที่นั่งที่ขายคือที่นั่งพนักงาน
- ค่าปริยายของอู่ใหม่ตั้งที่ 3 ให้ตรงกับ 6.4 แต่ปลดล็อกรายอู่ได้ด้วยการ update แถวเดียว
  โดยไม่ต้อง deploy ใหม่ — สำคัญเพราะยังไม่มีคอนโซลผู้ให้บริการ
- ข้อความตอนเต็มต้องบอกทางออก ไม่ใช่แค่ปฏิเสธ:
  *"แพ็กเกจนี้เปิดบัญชีพนักงานได้ 3 คน — ปิดการเข้าใช้งานของคนที่ไม่ได้ทำงานแล้ว
  หรือติดต่อผู้ให้บริการเพื่อเพิ่มจำนวน"*

**นี่คือจุดเดียวที่ลิขสิทธิ์กับบัญชีผู้ใช้เกี่ยวกัน** — ที่เหลือแยกกันสนิท
ลิขสิทธิ์ผูกกับอู่และตอบว่า "จ่ายเงินหรือยัง" · บัญชีผูกกับคนและตอบว่า "คนนี้ทำอะไรได้"
ด่านสองใบซ้อนกันโดยตั้งใจ ตรวจคนละที่ ( `mutate()` กับ `requireEdit()` )

---

## แก้ฐานข้อมูล — `db/007_perms.sql`

```sql
alter table users add column perms_json jsonb not null default '{}'::jsonb;

-- แปลงของเดิม: อาเรย์ชื่อเมนู → menus · ที่เหลือเปิดหมดเพื่อไม่ให้ใครเสียสิทธิ์
update users set perms_json = jsonb_build_object(
  'menus', (select coalesce(jsonb_object_agg(k, true), '{}'::jsonb)
            from unnest(perms) k),
  'cost', true, 'homeReport', true);

alter table users drop column perms;
alter table users rename column perms_json to perms;

-- โครงต้องเป็นออบเจกต์เสมอ ไม่ใช่อาเรย์หรือ null
alter table users add constraint users_perms_object check (jsonb_typeof(perms) = 'object');

-- จำนวนบัญชีพนักงานที่แพ็กเกจนี้เปิดได้ · null = ไม่จำกัด (ข้อ 8)
alter table subscriptions add column max_users integer check (max_users is null or max_users > 0);

comment on column users.code is
  'รหัสพนักงานสำหรับอ้างอิงในเอกสารและประวัติการแก้ไข — ไม่ใช่รหัสเข้าระบบ (เข้าด้วยอีเมล)';
```

ทิ้ง `users_perms_valid` เดิม แล้วย้ายการตรวจชื่อเมนูไปไว้ที่ชั้นแอป
เพราะตอนนี้คีย์มีสามชั้นและมีชื่อแท็บปนอยู่ด้วย — เขียนเป็น CHECK แล้วอ่านไม่ออก

---

## แก้ `apps/web/src/lib/perms.ts` — ใจกลางของช่วงนี้

ไฟล์เดียวที่ตอบคำถามสิทธิ์ทั้งหมด **ฟังก์ชันบริสุทธิ์ ไม่แตะฐานข้อมูล** ทดสอบได้ตรง ๆ

```ts
export interface Perms {
  menus?: Partial<Record<PermKey, boolean>>;
  tabs?: Record<string, boolean>;
  edit?: Record<string, boolean>;
  export?: Record<string, boolean>;
  cost?: boolean;
  homeReport?: boolean;
}

export function canMenu(s: Session, menu: PermKey): boolean
export function canTab(s: Session, menu: PermKey, sub: string): boolean
export function canEdit(s: Session, menu: PermKey, sub: string): boolean
export function canExport(s: Session, menu: PermKey, sub: string): boolean
export function canCost(s: Session): boolean
export function canHomeReport(s: Session): boolean

/** ต้นทุนที่ถูกซ่อน — ตรงกับ costMoney() ของรุ่น 6.4 */
export const HIDDEN_COST = '•••••';
export function costText(v: number, allowed: boolean): string

/** ที่นั่งพนักงานเหลือกี่คน — null = ไม่จำกัด · เจ้าของไม่ถูกนับ (ข้อ 8) */
export function seatsLeft(maxUsers: number | null, staffCount: number): number | null
```

**`canTab` ต้องบังคับกฎข้อ 4 ในตัวเอง** — `finance.pl` คืน `false` เสมอเมื่อไม่มีสิทธิ์ต้นทุน
ไม่ใช่ให้แต่ละหน้าจำเอง ที่เดียวที่ตัดสินใจคือที่เดียวที่ลืมไม่ได้

**`requirePerm` เดิมยังอยู่** แต่เพิ่มญาติสามตัวที่ redirect เหมือนกัน

```ts
requireTab(menu, sub)      // เข้าหน้าไม่ได้ → /?denied=…
requireEdit(menu, sub)     // เรียกในทุก action ที่เขียนข้อมูล
requireExport(menu, sub)   // เรียกในทุก route ที่ส่งไฟล์ออก
requireCost()              // เรียกในหน้าและไฟล์ที่มีต้นทุนล้วน
```

---

## จุดที่ต้องต่อสาย

### หน้าที่ต้องกันการเข้า (`requireTab`)
ทุกหน้าใต้ `customer` `income` `expense` `stock` `finance` `settings`
ที่ตอนนี้เรียก `requirePerm(menu)` เฉย ๆ — ราว 30 หน้า

### Action ที่ต้องกันการเขียน (`requireEdit`)
ทุก `'use server'` ที่บันทึก ลบ หรือยกเลิก — ราว 40 ตัว
`mutate()` รับ `sub` เพิ่มเข้ามาแล้วตรวจให้ในที่เดียว จะได้ไม่ต้องไล่แก้ทีละตัว

### เส้นทางที่ส่งไฟล์ออก (`requireExport` + `requireCost` ตามข้อ 5)

| เส้นทาง | สิทธิ์ที่ต้องมี |
|---|---|
| `/settings/export` (ไฟล์สำรองทั้งอู่) | `settings` + **`cost`** |
| `/stock/csv` | `stock.list` ส่งออก + **`cost`** (มีคอลัมน์ต้นทุน) |
| `/stock/sheet/csv` | `stock.list` ส่งออก (ไม่มีต้นทุน ไม่ต้องใช้ `cost`) |
| `/finance/csv` | `finance.sales` ส่งออก + **`cost`** |

### หน้าที่พิมพ์ทั้งชุด (`requireExport`)
`stock/print` · `stock/pending/print` · `customers/print` · `income/print` · `expense/print`

**ใบรายฉบับไม่กัน** — 6.4 เขียนไว้ว่า *"การพิมพ์เอกสารรายใบ เช่น ใบเสร็จของลูกค้า
ทำได้เสมอตามสิทธิ์เมนู"* เพราะเป็นงานประจำวัน ไม่ใช่การเอาข้อมูลทั้งชุดออกไป

### ที่ที่ต้องซ่อนต้นทุน

| ไฟล์ | สิ่งที่ต้องซ่อน |
|---|---|
| `stock/page.tsx` | คอลัมน์ต้นทุนล่าสุด · การ์ดมูลค่าสต๊อกรวม |
| `stock/[id]/page.tsx` | ต้นทุน · ล็อตคงเหลือ · คอลัมน์ต้นทุนในประวัติ |
| `stock/print/page.tsx` | เหมือนหน้าจอ |
| `stock/product-form.tsx` | ช่องต้นทุนล่าสุด (ตาม 6.4 ที่ตัดช่องออกทั้งช่อง) |
| `stock/claim/*` | คอลัมน์ต้นทุน/หน่วย · มูลค่ารวม · ยอดที่จ่ายออก |
| `stock/count/*` | ต้นทุน · มูลค่าส่วนต่าง · การ์ดมูลค่าสะสม |
| `finance/sales` | ต้นทุนขายและกำไรขั้นต้น |
| `finance/pl` | ปิดทั้งหน้า (ข้อ 4) |
| `app/page.tsx` | การ์ดสรุปทั้งแถบเมื่อไม่มี `homeReport` |
| `lib/products-csv.ts` | คอลัมน์ต้นทุนตอนส่งออก |

### หน้าตั้งค่าพนักงาน (07.2)
ตารางติ๊กสามคอลัมน์ต่อเมนูย่อย ตามผัง 6.4 —
**ช่องแก้ไข/ส่งออกกดไม่ได้ถ้ายังไม่ติ๊กดูได้** (6.4 มี `syncEd` ทำข้อนี้)
กันตั้งค่าที่ขัดกันเองตั้งแต่ตอนกรอก

อ่านผังเมนูย่อยจาก `components/menu-map.ts` ที่มีอยู่แล้ว — ที่เดียวที่รู้ว่าระบบมีแท็บอะไรบ้าง

---

## ชุดทดสอบ — หัวใจของช่วงนี้

**`packages/core` ไม่แตะ** สิทธิ์เป็นเรื่องของแอป ไม่ใช่สูตรคำนวณ

**`apps/web/test/perms.test.ts` (ไฟล์ใหม่ · ฟังก์ชันล้วน)**
- ค่าที่ไม่ได้ตั้ง = อนุญาต ทุกแกนยกเว้น `tabs`
- `tabs` ว่างทั้งเมนู = เข้าได้ทุกแท็บ · มีบางคีย์ = ที่ไม่มีคือห้าม
- เจ้าของผ่านทุกด่านแม้ `perms` จะว่างเปล่า
- **ไม่มีสิทธิ์ต้นทุน → `canTab('finance','pl')` เป็น false เสมอ** แม้ติ๊ก tabs ไว้
- แปลงรูปแบบเก่า (`sub`, `exportData`) ได้ถูกต้อง

**`apps/web/test/perms-db.test.ts` (ไฟล์ใหม่ · ฐานข้อมูลจริง)**

ข้อที่ทั้งช่วงนี้ตั้งอยู่บนมัน — **ไล่ทุกเส้นทางที่ส่งข้อมูลออก แล้วพิสูจน์ว่าไม่มีต้นทุนหลุด**

```
1. สร้างอู่ที่มีสินค้าต้นทุน 1,234.56 · ใบซื้อ · ใบเคลม · ใบตรวจนับ
2. สร้างผู้ใช้ที่ปิดสิทธิ์ต้นทุน แต่เปิดเมนูอื่นหมด
3. ไล่ทุก route และทุกหน้าพิมพ์ **จากรายชื่อไฟล์จริงในโปรเจกต์** ไม่ใช่รายการที่พิมพ์ไว้
   — หน้าใหม่ที่ลืมกันจะทำให้เทสต์แดงเอง แบบเดียวกับ tenant-isolation.test.ts
4. ยืนยันว่าไม่มีผลลัพธ์ไหนมีสตริง "1234.56" หรือ "1,234.56"
```

- ไฟล์สำรองดาวน์โหลดไม่ได้เมื่อไม่มีสิทธิ์ต้นทุน
- `/stock/csv` ปฏิเสธ · `/stock/sheet/csv` ยังผ่าน (ไม่มีต้นทุน)
- ผู้ใช้ที่ดูสต๊อกได้แต่แก้ไม่ได้ → เปิดหน้าได้ แต่ action บันทึกถูกปฏิเสธ
- **และยังออกใบเสร็จที่ดึงสินค้าได้ตามปกติ** — 6.4 ย้ำข้อนี้ไว้ชัดเจน
  ("การดึงสินค้าเข้าเอกสารขายแล้วสต๊อกลดตามปกติ ไม่นับเป็นการแก้ทะเบียนสินค้า")
- ไมเกรชัน: ผู้ใช้ที่มี `perms = {stock,income}` เดิม ต้องได้ `menus` เท่าเดิม
  และได้ `cost`/`homeReport` เป็น true
- **ที่นั่งเต็มแล้วสร้างบัญชีใหม่ไม่ได้ แต่คนที่มีอยู่ยังเข้าระบบได้ตามปกติ**
  (ลดแพ็กเกจแล้วต้องไม่ล็อกใครออกกลางดึก)
- เจ้าของไม่ถูกนับเป็นที่นั่ง · `max_users = null` แปลว่าไม่จำกัด
- ปิดการเข้าใช้งานของพนักงานคนหนึ่งแล้ว ที่นั่งต้องคืนมา

**`apps/web/test/menu-map.test.ts`** — เพิ่มข้อว่าทุกแท็บในผังต้องมีที่ให้ติ๊กในหน้า 07.2
(ไม่งั้นแท็บใหม่จะไม่มีทางตั้งสิทธิ์ได้ แล้วไปตกที่ค่าปริยายเงียบ ๆ)

**`packages/importer/test`** — ไฟล์ 6.4 ที่มี `perms` ทั้งสองรูปแบบเข้ามาแล้วได้สิทธิ์ตรง

**กลายพันธุ์แล้วต้องแดง**
1. ถอด `requireCost()` ออกจาก `/settings/export`
2. ให้ `canTab` ไม่สนใจกฎ `finance.pl`
3. ให้ `tabs` ที่ว่างแปลว่าห้ามทั้งหมด (สลับกติกา)
4. ให้ `canEdit` คืน true เสมอ
5. ให้จำนวนที่นั่งนับเจ้าของรวมไปด้วย

**เดิมต้องไม่พัง** — 394 ข้อผ่านครบ

---

## วิธีตรวจว่าใช้ได้จริง

```bash
export PATH=~/.nvm/versions/node/v22.23.2/bin:$PATH
DATABASE_URL="postgresql://postgres:x@localhost:5433/dgl" npm test
npm run build -w @drivegolight/web
```

ตรวจด้วยมือ — สร้างพนักงานหนึ่งคน ปิดสิทธิ์ต้นทุน เปิดเมนูสินค้าแบบดูอย่างเดียว
แล้วเข้าระบบด้วยบัญชีนั้น
1. ทะเบียนสินค้า → เห็นรายการ แต่คอลัมน์ต้นทุนเป็น `•••••` และไม่มีการ์ดมูลค่าสต๊อก
2. กดแก้ไขสินค้า → ปุ่มไม่มี · พิมพ์ URL ตรงเข้าไป → เด้งกลับ
3. เมนู 06.4 กำไรขาดทุน → ไม่มีในเมนูย่อย · พิมพ์ URL ตรง → เด้งกลับ
4. หน้าตั้งค่า → ดาวน์โหลดไฟล์สำรองไม่ได้
5. ออกใบเสร็จ ดึงอะไหล่เข้าใบ → **ทำได้ปกติ** และสต๊อกลดตามจริง
6. เข้าด้วยบัญชีเจ้าของอีกครั้ง → เห็นครบทุกอย่างเหมือนเดิม
7. เพิ่มพนักงานจนครบ 3 คน → ปุ่มเพิ่มหายไปพร้อมข้อความบอกทางออก
   · ปิดการเข้าใช้งานคนหนึ่ง → ปุ่มกลับมา
8. หน้า 07.2 → ช่องรหัสพนักงานอยู่กลุ่มข้อมูลพนักงาน มีคำกำกับว่าเข้าระบบด้วยอีเมล

---

## ที่ไม่ทำในช่วงนี้

- **บันทึกว่าใครแก้อะไรเมื่อไหร่** — อยู่ในช่วงที่ 7 (`doc_edits`)
- **สิทธิ์ระดับแถว** (ช่างเห็นเฉพาะงานของตัวเอง) — 6.4 ไม่มี และเป็นคนละเรื่องกัน
- **ผู้ใช้เปลี่ยนรหัสผ่านตัวเอง** — เป็นงานขึ้นใช้งานจริง ทำพร้อมระบบส่งอีเมล

---

# ผลที่ทำจริง

**ครบทุกข้อในแผน** · เทสต์ 428 ข้อผ่าน (เดิม 394) · build ผ่าน

## สิทธิ์แบบละเอียด

- `users.perms` เป็น `jsonb` แล้ว · `db/007_perms.sql` แปลงของเดิมให้อัตโนมัติ
  **ไม่มีใครเสียสิทธิ์จากการอัปเกรด** — อาเรย์เดิมกลายเป็น `menus`
  และทุกคนได้ `cost` กับ `homeReport` เป็น true เพราะของเดิมไม่เคยซ่อนอะไร
- `PERM_KEYS` · `Perms` · `permsFromLegacy` ย้ายไป `packages/core` เพราะตัวนำเข้าใช้ร่วม
  ส่วนการตัดสินใจที่ผูกกับผู้ใช้อยู่ที่ `apps/web/src/lib/perms.ts`
- `mutate()` รับ `sub` แล้วตรวจสิทธิ์แก้ไขให้ในที่เดียว — ต่อสายครบทั้ง 33 จุด
- **32 หน้าเปลี่ยนจาก `requirePerm` เป็น `requireTab`** ทั้งหมด
- หน้า 07.2 มีตารางติ๊กสามคอลัมน์ต่อเมนูย่อยครบ 20 แท็บ อ่านผังจาก `menu-map`
  แท็บใหม่ที่เพิ่มในผังจะมีที่ให้ติ๊กเองอัตโนมัติ

## สิ่งที่ตรวจกับของจริง ไม่ใช่แค่เทสต์

สร้างพนักงานที่ถูกซ่อนต้นทุนและแก้ทะเบียนสินค้าไม่ได้ แล้วเข้าระบบด้วยบัญชีนั้นจริง

| เส้นทาง | ผล |
|---|---|
| `/` `/stock` `/stock/print` `/finance/ar` `/income` | 200 — เข้าได้ตามสิทธิ์ |
| `/finance/pl` | redirect (ถูกซ่อนต้นทุน) |
| `/settings/users` | redirect (ไม่มีสิทธิ์เมนู) |
| `/settings/export` `/stock/csv` `/finance/csv` | redirect (ต้องมีสิทธิ์ต้นทุน) |
| `/stock/sheet/csv` | 200 — ใบนับเปล่าไม่มีต้นทุน จึงไม่ต้องกัน |

และไล่หาเลขต้นทุนจริง (5,325.00) ในทุกหน้าที่พนักงานคนนั้นเข้าได้ — ไม่พบสักที่
คอลัมน์ทุนหาย · มูลค่าสต๊อกรวมหาย · กล่องล็อตไม่มีคอลัมน์ต้นทุน ·
เจ้าของเข้าหน้าเดียวกันยังเห็นครบเหมือนเดิม

## สองจุดที่เจอตอนตรวจจริง ทั้งที่เทสต์เขียวหมด

**1. ซ่อนแท็บแล้วแต่พิมพ์ URL ตรงยังเข้าได้** — ตอนแรกเปลี่ยนแค่ `SubNav` ให้กรองแท็บ
แต่หน้ายังเรียก `requirePerm('finance')` อยู่ `/finance/pl` จึงเปิดได้ทั้งที่ไม่มีในเมนู
ตรงกับที่แผนเขียนไว้เองว่า "การซ่อนปุ่มไม่ใช่การป้องกัน" — แก้แล้วทั้ง 32 หน้า
**เจอเพราะลองพิมพ์ URL เอง ไม่ใช่เพราะเทสต์**

**2. ส่งออกไฟล์สำรองพัง 500** — `backup.ts` ยังแปลง `perms` เป็นอาเรย์อยู่
เจอตอนกดในนามเจ้าของ ไม่ได้อยู่ในเทสต์เดิมเลย แก้แล้วพร้อมเขียนเทสต์ round-trip
ที่ยืนยันว่าสิทธิ์แบบละเอียดรอดข้ามไฟล์สำรองและกลับเข้ามาได้เท่าเดิม

## ที่นั่งพนักงานกับรหัสพนักงาน

- `subscriptions.max_users` · null = ไม่จำกัด · **เจ้าของไม่ถูกนับ** ·
  ตรวจตอนสร้างบัญชีเท่านั้น
- `users.code` เปลี่ยนความหมายเป็นรหัสอ้างอิงพนักงาน มี comment กำกับในสคีมา
  และคำอธิบายใต้ช่องในหน้า 07.2 ว่าเข้าระบบด้วยอีเมล

## ชุดทดสอบ

- `perms.test.ts` 23 ข้อ (ฟังก์ชันล้วน) · `perms-db.test.ts` 10 ข้อ
  ซึ่งไล่ route จากไฟล์จริงในโปรเจกต์ — route ใหม่ที่ลืมกันจะแดงเอง
- `backup-roundtrip.test.ts` +1 · แก้ fixture ที่ยังใช้ `text[]` สองไฟล์
- **กลายพันธุ์แล้วเทสต์ต้องแดง** ตรวจครบห้าจุดตามแผน
- ระหว่างทางเทสต์เดิมจับได้ว่าลืมพอร์ต `migrateUserPerms` ของ 6.4
  (quote/receipt → income) ซึ่งจะทำให้ผู้ใช้จากไฟล์รุ่นเก่าเสียสิทธิ์เมนูรายรับทั้งหมด
