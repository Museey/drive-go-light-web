# แผน — กู้คืนจากไฟล์สำรอง ไม่แตะยอดหัก ณ ที่จ่ายของเอกสารเก่า (17 ก.ย. 2569)

branch `fix/restore-keep-old-wht-2569-09-17` (แตกจาก `main` = `ea4a7ac`) · เรื่องที่พักไว้ตั้งแต่ 16 ก.ย.

## ผู้ใช้กำหนด
"ถ้าเป็นของเก่าที่กู้คืนมาจากไฟล์ เราจะไม่ยุ่งเลย"

## ต้นเหตุ
- ไฟล์สำรอง (ทั้งของโปรแกรมเดิมและที่เว็บนี้ส่งออก) **เก็บแค่อัตรา** หัก ณ ที่จ่าย ไม่ได้เก็บยอด
- ตอนกู้คืน/นำเข้า ระบบคำนวณยอดใหม่ด้วยกติกา **ขั้นต่ำค่าแรง 1,000 บาท** (ตั้ง 16 ก.ย. 2569) — โปรแกรมเดิมไม่มีขั้นต่ำ
- ผล: ใบเก่าที่ค่าแรงต่ำกว่า 1,000 **ยอดหักหายไป** · ไฟล์ร้านกิจเจริญแอร์เซอร์วิส 7 ใบ 112.50 บาท · ไฟล์ตัวอย่าง 75 ใบ 1,366.95 บาท
- เทสต์ตัวนำเข้า `hard-cases` ("คำนวณภาษีหัก ณ ที่จ่ายจากค่าแรง" คาด 15 ได้ 0) แดงอยู่ด้วยเหตุนี้

## จะทำ
### 1. ไฟล์จากโปรแกรมเดิม → คิดแบบโปรแกรมเดิม (ไม่มีขั้นต่ำ)
- ตัวกู้คืนและตัวนำเข้าคิดยอดหัก ณ ที่จ่ายของใบส่งมอบ/ใบเสร็จ **ด้วยสูตรเดียวกับโปรแกรมเดิมทุกอย่าง** = ยอดที่ร้านเห็นตอนออกใบจริง
- สูตรอยู่ที่ core ตัวเดิม (`recTotals`) เพิ่มตัวเลือก "ไม่ใช้ขั้นต่ำ" — ปัดเศษลำดับเดิม ฐานค่าแรงเดิม
### 2. ไฟล์ที่เว็บนี้ส่งออก → ใช้ยอดที่บันทึกไว้ตรง ๆ
- ไฟล์สำรองของเว็บ **เพิ่มช่องยอดหัก ณ ที่จ่ายที่บันทึกไว้** ต่อเอกสาร (ไฟล์ยังเปิดในโปรแกรมเดิมได้ ช่องที่ไม่รู้จักถูกข้าม)
- กู้คืนเจอช่องนี้ → ใช้ค่านั้นเลย ไม่คำนวณใหม่ (ใบที่ออกหลังตั้งกติกา ค่าแรงไม่ถึง 1,000 ได้ 0 เหมือนตอนบันทึก)
### 3. ไม่แตะ
- ใบใหม่ที่ออกในระบบ · แก้ไขใบ → ใช้ขั้นต่ำ 1,000 เหมือนเดิม
- ค่าใช้จ่าย (อู่เป็นผู้หัก) ไม่เคยมีขั้นต่ำ คงเดิม · ยอดรวม/VAT คงเดิม

## ผลที่ควรรู้
- **เปิดใบเก่าที่กู้คืนมาแล้วกดแก้ไขบันทึก** → ระบบคิดใหม่ด้วยกติกาปัจจุบัน (ขั้นต่ำ 1,000) เหมือนการแก้ใบทุกใบ
- **ไฟล์ที่เว็บนี้ส่งออกก่อนแก้รอบนี้** ยังไม่มีช่องยอด → คิดแบบโปรแกรมเดิม · ใบที่ออกในเว็บหลัง 16 ก.ย. ค่าแรงต่ำกว่า 1,000 จะได้ยอดหักกลับมา
  (ดาวน์โหลดไฟล์สำรองใหม่หลัง deploy รอบนี้จะไม่มีปัญหานี้)

## เทสต์
- core: ตัวเลือกไม่ใช้ขั้นต่ำ = สูตรเดิมทุกกรณี (เทียบของโปรแกรมเดิมที่มีอยู่แล้ว) · ค่าตั้งต้นยังมีขั้นต่ำ
- ตัวนำเข้า: `hard-cases` กลับมาเขียว (ค่าแรง 500 หัก 15) · `import.test` ยอดทุกใบเท่าสูตรโปรแกรมเดิม
- กู้คืน (ฐานข้อมูลจริง): ไฟล์ตัวอย่าง — ใบที่ค่าแรงต่ำกว่า 1,000 ได้ยอดหักครบ (ไม่หาย 75 ใบ) ·
  ส่งออกแล้วกู้คืน (ใบที่ออกในเว็บยอดหัก 0 เพราะไม่ถึงขั้นต่ำ) → กลับมาเป็น 0 ไม่ใช่ถูกคิดใหม่ · ยอดรวมทุกตารางเท่าเดิม
- ทั้งชุดเดิมต้องเขียว (npm test พร้อมฐานข้อมูล · e2e)
- กลายพันธุ์: ตัวนำเข้ากลับไปใช้ขั้นต่ำ · กู้คืนไม่ใช้ยอดที่เก็บไว้ · ส่งออกไม่เขียนยอด
