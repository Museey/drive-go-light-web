# ทำงานจบจากหน้ารายการ — ใบวางบิล (03.4) และรายจ่าย (04)

รายงานตรวจช่วงที่ 10 สรุปไว้ว่า **"ความต่างที่ใหญ่ที่สุดกับ 6.4 ไม่ใช่คอลัมน์ แต่คือทางเดิน
— 6.4 ทำงานจบได้จากหน้ารายการ ของเราต้องเปิดเอกสารเข้าไปก่อนแทบทุกอย่าง"**
ช่วงนั้นแก้ให้หน้ารายรับ (03.1–03.3) แล้ว เหลือสองเมนูนี้ที่ยังเป็นแบบเดิม

ไม่แตะสคีมา ไม่แตะสูตรเงิน ไม่ต้องไมเกรชัน

---

## คำตอบของคำถามที่ค้างอยู่ — "ยกเลิกการยกเลิกเอกสาร"

ผมเคยเสนอกติกาของตัวเองไว้แล้วรอคุณตัดสิน **ตอนนี้ไปเจอว่า 6.4 ตอบไว้แล้ว
และตอบคนละแบบกับที่ผมเสนอ** — ขอถอนข้อเสนอเดิมแล้วทำตาม 6.4 แทน

| เอกสาร | 6.4 ทำอย่างไร | ของเรา |
|---|---|---|
| รายรับ 03 ทั้งหมด | **กู้คืนไม่ได้** มีแต่ปุ่ม "คัดลอกใบใหม่" | ✅ มีแล้ว |
| ใบซื้อ (04.1) | **กู้คืนได้** ปุ่ม "กู้คืน" ในแถว | ❌ ไม่มี |
| ค่าใช้จ่าย (04.2) | **กู้คืนได้** ปุ่ม "กู้คืน" ในแถว | ❌ ไม่มี |

เหตุผลที่ 6.4 เขียนกำกับไว้ในโค้ดตรงจุดนั้นเอง

> ใบที่ยกเลิกแล้วกู้คืนไม่ได้ เพราะยอดขาย ภาษี และสต๊อกถูกปรับกลับไปแล้ว
> การกู้คืนจึงเสี่ยงทำให้ตัวเลขย้อนหลังเพี้ยน ใบใหม่จะได้เลขที่ใหม่
> แต่ยังอ้างอิงใบเสนอราคาเดิม ทำให้ตามรอยกลับไปได้ว่ามาจากงานไหน

เหตุผลนี้ใช้ได้กับฝั่งรายรับจริง เพราะใบเสร็จที่ยกเลิกไปแล้วมีภาษีขายและใบกำกับภาษี
ที่ส่งออกไปแล้วพัวพันอยู่ ส่วนฝั่งรายจ่ายไม่มีเอกสารที่ออกไปข้างนอก
การกดยกเลิกผิดใบจึงเป็นแค่ความผิดพลาดในการคีย์ ไม่ใช่เรื่องที่ต้องมีร่องรอยใบใหม่

**ผมจึงเสนอให้ทำตาม 6.4 ทุกประการ** — กู้คืนได้เฉพาะรายจ่าย

---

## งานที่ 1 — ใบวางบิลทำงานจบจากหน้ารายการ

ตอนนี้แถวใบวางบิลมีแค่ปุ่ม **พิมพ์** กับลิงก์ที่เลขที่

| เพิ่ม | ไปไหน |
|---|---|
| **เปิด** | `/income/billing/<id>` (ตอนนี้ต้องกดที่เลขที่ ซึ่งไม่เหมือนหน้าอื่น) |
| **ยกเลิก** | `/income/billing/<id>?void=1` เปิดแผงกรอกเหตุผลให้เลย |
| **คัดลอกใบใหม่** | `/income/billing/new?from=<id>` เฉพาะใบที่ยกเลิกแล้ว |

**ยกเลิกไม่ทำทันทีจากแถว** — พาไปแผงยืนยันที่ต้องกรอกเหตุผล เหมือนหน้ารายรับ
(`VoidBillnote` มีอยู่แล้ว เพิ่มแค่ prop `startOpen` แบบเดียวกับ `startVoiding` ของ 03)

**คัดลอกใบวางบิลที่ยกเลิก** — ตั้งต้นใบใหม่ด้วยลูกค้าเดิม วันที่วันนี้ เลขที่ใหม่
แล้วติ๊กใบแจ้งหนี้เดิมกลับมาให้ **เฉพาะใบที่ยังค้างชำระอยู่และยังไม่ไปอยู่บนใบวางบิลอื่น**
ใบที่เก็บเงินครบไปแล้วระหว่างนั้นต้องไม่ติ๊กกลับมา ไม่งั้นใบใหม่จะแจ้งเก็บซ้ำ

## งานที่ 2 — ตัวเลขหัวหน้าใบวางบิล

6.4 มีแถบสรุปสามช่องที่บอกว่า **ยังมีงานวางบิลค้างอยู่เท่าไร** ของเราบอกแค่ยอดของใบที่ออกไปแล้ว

| ช่อง | ที่มา |
|---|---|
| ใบวางบิลที่ใช้งาน | นับใบที่ยังไม่ยกเลิก (มีแล้ว) |
| ใบที่ยังเก็บเงินไม่ครบ · **ยังไม่ได้วางบิล** | `openInvoices()` ที่ `in_billnote_no` ว่าง |
| ยอดค้างจากใบที่ยังไม่ได้วางบิล | รวมยอดค้างของกลุ่มนั้น |

*ต่างจาก 6.4 นิดเดียว* — 6.4 นับใบค้างชำระทั้งหมดรวมที่วางบิลไปแล้วด้วย
ผมเสนอให้นับเฉพาะที่ยังไม่ได้วางบิล เพราะตัวเลขบนหน้านี้ควรตอบว่า "ต้องทำอะไรต่อ"
ไม่ใช่ตัวเลขลูกหนี้ซึ่งมีที่ของมันอยู่แล้วที่ 06.2 — และจะเขียนกำกับไว้ว่ายอดลูกหนี้ทั้งหมดดูที่ 06.2
ตามที่ 6.4 เขียนไว้

## งานที่ 3 — แบ่งหน้าใบวางบิล

`listBillnotes()` **ไม่มี LIMIT เลย** เปิดหน้านี้ทีเดียวดึงใบวางบิลทั้งหมดที่เคยออก
อู่ที่วางบิลทุกเดือนสามปีก็สามสิบกว่าใบ ยังไม่เจ็บ แต่มันโตทางเดียวและไม่มีเพดาน
ใส่ `limit/offset` กับตัวนับรวม แล้วใช้ `PageSize` ตัวเดียวกับหน้าอื่น (6.4 ก็แบ่งหน้า)

## งานที่ 4 — หน้ารายจ่ายทำงานจบจากหน้ารายการ

**ตอนนี้แถวรายจ่ายไม่มีคอลัมน์ปุ่มเลยสักปุ่ม** ต้องกดที่เลขที่เพื่อเปิดเข้าไปเท่านั้น

เพิ่มคอลัมน์ปุ่มแบบเดียวกับหน้ารายรับ — **เปิด · พิมพ์ · ยกเลิก** และ **กู้คืน** สำหรับใบที่ยกเลิกแล้ว
ยกเลิกพาไปแผงกรอกเหตุผลที่ `/expense/<id>?void=1` (เพิ่ม prop `startVoiding` ให้ `BuyDocActions`)

## งานที่ 5 — กู้คืนใบซื้อและค่าใช้จ่าย

นี่คืองานเดียวในก้อนนี้ที่แตะข้อมูล จึงต้องระวังที่สุด

**ตอนยกเลิกใบซื้อ ระบบตัดของที่รับเข้ามาออกไปด้วยต้นทุนตามล็อต** (ไม่ใช่คืนที่ราคาซื้อ
เพราะของอาจถูกขายไปบางส่วนแล้ว) การกู้คืนจึงต้อง **รับของกลับเข้าด้วยต้นทุนก้อนที่ตัดออกไปตอนยกเลิก**
ไม่ใช่ราคาซื้อบนใบ — สองค่านี้ไม่เท่ากันเมื่อราคาซื้อเปลี่ยนระหว่างทาง

กติกาที่จะเขียน

- อ่านแถว `reason='return'` ที่ผูกกับใบนั้น แล้วรับเข้ากลับด้วย `cost_amount` เดิมทีละแถว
- **มูลค่าสต๊อกก่อนยกเลิกต้องเท่ากับหลังกู้คืนทุกสตางค์** — ข้อนี้เป็นเทสต์
- ล้าง `voided_at` และ `voided_reason` พร้อมกับเปลี่ยนสถานะ (สคีมามี CHECK บังคับให้สองอย่างนี้ตรงกัน)
- บันทึกลง `doc_edits` ว่ากู้คืนเมื่อไรโดยใคร — ใบที่ยกเลิกแล้วกลับมามีผลทางบัญชีต้องมีร่องรอย
- ยอดเจ้าหนี้กลับมาเองเพราะคิดจากสถานะเอกสาร ไม่ต้องแตะ
- ค่าใช้จ่าย (EX) ไม่มีสต๊อก กู้คืนคือเปลี่ยนสถานะอย่างเดียว

**ถามยืนยันก่อนเสมอ** พร้อมข้อความว่ายอดของใบนี้จะกลับไปนับในเจ้าหนี้ ภาษีซื้อ และสต๊อกตามเดิม
ตามที่ 6.4 ถาม

---

## เทสต์ที่ต้องมี

| เรื่อง | พิสูจน์อะไร |
|---|---|
| กู้คืนใบซื้อแล้วสต๊อกกลับมา | จำนวนคงเหลือเท่าก่อนยกเลิก |
| **มูลค่าสต๊อกกลับมาเท่าเดิมทุกสตางค์** | ยกเลิก → กู้คืน แล้วมูลค่าตามบัญชีเท่าก่อนยกเลิก **ข้อสำคัญที่สุด** |
| ราคาซื้อเปลี่ยนระหว่างยกเลิกกับกู้คืน | ยังคืนด้วยต้นทุนก้อนที่ตัดออกไป ไม่ใช่ราคาใหม่ |
| กู้คืนซ้ำสองครั้ง | ครั้งที่สองต้องถูกปฏิเสธ ของไม่งอกสองเท่า |
| กู้คืนค่าใช้จ่าย | สถานะกลับมา ไม่มีแถวสต๊อกเกิดขึ้น |
| ประวัติ | `doc_edits` มีแถวการกู้คืน |
| สิทธิ์ | พนักงานที่แก้ไขรายจ่ายไม่ได้ กู้คืนไม่ได้ |
| คัดลอกใบวางบิลที่ยกเลิก | ติ๊กกลับมาเฉพาะใบที่ยังค้างและยังไม่อยู่บนใบวางบิลอื่น |
| แบ่งหน้าใบวางบิล | หน้าละเท่าที่เลือก และตัวนับรวมตรง |

## ที่ไม่ทำในก้อนนี้

- **กู้คืนเอกสารรายรับ** — ทำตาม 6.4 คือไม่ให้ทำ ใช้ "คัดลอกใบใหม่" ที่มีอยู่แล้ว
- **หน้าแรกสี่ตัวเลข · ตารางรายใบในหน้ายอดขาย** — งานคิวรีล้วน แยกก้อนถัดไป
- **ส่งออก CSV ที่ 06.2 / 06.3** — แยกก้อน

## เสร็จเมื่อ

- [ ] แถวใบวางบิลมี เปิด · พิมพ์ · ยกเลิก · คัดลอกใบใหม่
- [ ] หัวหน้าใบวางบิลบอกว่ายังมีใบที่ต้องวางบิลกี่ใบ ยอดเท่าไร และชี้ไป 06.2
- [ ] หน้าใบวางบิลแบ่งหน้าแล้ว
- [ ] แถวรายจ่ายมี เปิด · พิมพ์ · ยกเลิก · กู้คืน
- [ ] กู้คืนใบซื้อแล้วมูลค่าสต๊อกกลับมาเท่าเดิมทุกสตางค์
- [ ] `npm test` เขียวทั้งหมด · การกลายพันธุ์ที่สำคัญทำให้แดง
- [ ] เปิดหน้าจอดูของจริงทุกข้อ

---

## สรุปสิ่งที่ต้องการคำตอบ

1. **กู้คืนได้เฉพาะรายจ่าย ตามที่ 6.4 ทำ** (ถอนข้อเสนอเดิมของผมที่จะให้กู้คืนใบรายรับได้) — โอเคไหม
2. ตัวเลขหัวหน้าใบวางบิลนับ **เฉพาะใบที่ยังไม่ได้วางบิล** แทนที่จะนับใบค้างทั้งหมดแบบ 6.4 — โอเคไหม
