# ช่วงที่ 11 — เลขบัญชีบนเอกสาร และช่องรถในใบเคลม

สามข้อจากการไล่ช่องกรอกและหน้าพิมพ์ ใน [`PLAN-6.4-flow-audit.md`](PLAN-6.4-flow-audit.md)

---

## งานที่ 1 — บัญชีธนาคารของอู่

**ใบเสร็จที่ไม่มีเลขบัญชี คือใบที่ลูกค้าจ่ายเงินไม่ได้** ตอนนี้ขาดครบสามชั้น

**ฐานข้อมูล** — ไมเกรชัน `014_shop_bank.sql` เพิ่มสามคอลัมน์ใน `tenants`

| คอลัมน์ | เก็บอะไร |
|---|---|
| `bank_name` | ชื่อธนาคาร เช่น กสิกรไทย |
| `bank_account_no` | เลขที่บัญชี |
| `bank_account_name` | ชื่อบัญชี |

ทั้งสามเป็นข้อความว่างได้ ไม่มี CHECK รูปแบบ เพราะเลขบัญชีของแต่ละธนาคาร
เขียนไม่เหมือนกัน และการบังคับรูปแบบผิด ๆ จะทำให้อู่กรอกไม่ได้เลย

**หน้าตั้งค่า** — สามช่องต่อจากที่อยู่ร้าน

**หน้าพิมพ์** — บรรทัดบัญชีบน **ใบเสร็จ · ใบวางบิล · แบบฟอร์มเปล่า**
ตรงกับที่ `bankLine()` ของ 6.4 พิมพ์ไว้

**ข้อที่ 6.4 คิดมาดีและต้องทำตาม** — ถ้ายังไม่ได้ตั้งค่า ให้พิมพ์**เส้นว่างให้เขียนด้วยมือ**
ไม่ใช่ไม่พิมพ์อะไรเลย อู่ที่ยังไม่ได้กรอกจะได้ยังใช้กระดาษได้

**คิวอาร์รับเงินไม่ทำในช่วงนี้** — เป็นรูปภาพ ต้องใช้ทางเดียวกับโลโก้
(data URI + เพดานขนาด + ตัวตรวจชนิดไฟล์) เป็นงานคนละก้อนกับตัวเลขบัญชี
และตัวเลขบัญชีอย่างเดียวก็ทำให้โอนเงินได้แล้ว

## งานที่ 2 — ใบเคลมกรอกรถเองได้ พร้อมเลขไมล์

อาการเดียวกับฟอร์มเอกสารขายก่อนช่วงที่ 8 — ช่องรถเป็นตัวเลือกจากทะเบียนอย่างเดียว
และไม่มีช่องเลขไมล์เลย **ตอนนั้นผมแก้ไม่ครบ** แก้เฉพาะฟอร์มขายแล้วไม่ได้ไล่ดูฟอร์มอื่น

ใช้ `VehicleFields` ตัวเดียวกับที่ทำไว้แล้ว — ยี่ห้อ · รุ่น · ปี · สี · ทะเบียน ·
จังหวัด · **เลขไมล์** · เลขเครื่องยนต์ · เลขตัวถัง

**ไม่ต้องแตะสคีมา** `claims.vehicle` เป็น jsonb เหมือน `documents.vehicle`

**เขียนเลขไมล์กลับเข้าทะเบียนรถด้วยไหม** — ทำ ใช้ `syncVehicleFromDocWith()`
ตัวเดียวกับเอกสารขาย เพราะรถคันเดียวกันเข้ามาเคลมก็คือรถที่วิ่งมาถึงเลขไมล์นั้นจริง
กติกาเดิมทุกข้อ: เขียนกลับแค่เลขไมล์กับวันที่บริการล่าสุด และวันที่ขยับไปข้างหน้าอย่างเดียว

## งานที่ 3 — ป้ายช่องของใบเคลมผู้ขาย

6.4 ใช้ช่องเดียวกันแต่เปลี่ยนป้ายตามฝั่ง

| ฝั่ง | ป้าย |
|---|---|
| เคลมลูกค้า | รถที่เข้ารับบริการ |
| เคลมผู้ขาย | **เลขที่ใบซื้ออ้างอิง** |

เพราะเคลมผู้ขายคือการส่งของชำรุดคืนร้านอะไหล่ ไม่เกี่ยวกับรถของใคร
ป้ายที่เขียนว่า "รถของลูกค้า" บนใบเคลมผู้ขายอ่านแล้วสับสน

**ฝั่งผู้ขายไม่ต้องมีช่องรถทั้งชุด** — สคีมามี CHECK ห้ามใบฝั่งผู้ขายผูกรถอยู่แล้ว
(`ฝั่งผู้ขายไม่เกี่ยวกับรถลูกค้า` ใน `005_claims.sql`) แสดงแค่ช่องเลขที่ใบซื้ออ้างอิงพอ

---

## เสร็จเมื่อ

- [ ] กรอกบัญชีธนาคารในหน้าตั้งค่าแล้วบันทึกได้
- [ ] ใบเสร็จและใบวางบิลที่พิมพ์ออกมามีบรรทัดบัญชี
- [ ] อู่ที่ยังไม่ได้กรอก ได้เส้นว่างให้เขียนมือ ไม่ใช่บรรทัดหาย
- [ ] แบบฟอร์มเปล่ามีเส้นบัญชีให้เขียน
- [ ] ใบเคลมลูกค้ากรอกรถเองได้ครบทุกช่องรวมเลขไมล์
- [ ] แก้เลขไมล์บนใบเคลมแล้วทะเบียนรถอัปเดตตาม
- [ ] ใบเคลมผู้ขายเห็นป้าย "เลขที่ใบซื้ออ้างอิง" ไม่ใช่ช่องรถ
- [ ] `npm test` เขียวทั้งหมด · การกลายพันธุ์ที่สำคัญทำให้แดง
- [ ] **เปิดหน้าจอดูของจริงทุกข้อ** รวมถึงสั่งพิมพ์ออกมาดู

## เทสต์ที่ต้องมี

| เรื่อง | พิสูจน์อะไร |
|---|---|
| ไมเกรชัน | รันแล้วคอลัมน์ใหม่มีจริง และรันซ้ำไม่พัง |
| ค่าว่าง | อู่ที่ไม่ได้กรอกบัญชี ยังพิมพ์เอกสารได้และได้เส้นว่าง |
| เขียนกลับจากใบเคลม | เลขไมล์เข้าทะเบียนรถ · ยี่ห้อรุ่นไม่เขียนกลับ · วันที่ไม่ถอยหลัง |
| ฝั่งผู้ขาย | ใบเคลมผู้ขายไม่ผูกรถ และสคีมายังปฏิเสธถ้าพยายามผูก |
| ข้ามอู่ | บัญชีของอู่หนึ่งไม่โผล่บนเอกสารของอีกอู่ |

## ที่ไม่ทำในช่วงนี้

- **คิวอาร์รับเงิน** — งานรูปภาพ ทำต่อจากนี้ได้เลยแต่แยกก้อน
- **ตรวจรูปแบบเลขบัญชีตามธนาคาร** — บังคับผิดแล้วอู่กรอกไม่ได้
- **กู้คืนเอกสารที่ยกเลิก** — ยังรอตัดสินขอบเขต

---

# ผลที่ทำจริง

**ครบทั้งสามงาน** · ชุดทดสอบ **728 ข้อผ่าน** (เว็บ 543 · core 90 · ตัวนำเข้า 95)

## ตรวจด้วยตาบนหน้าจอจริงทุกข้อ

| ตรวจ | ผล |
|---|---|
| อู่ที่ยังไม่กรอกบัญชี | ใบเสร็จได้ **เส้นว่าง 3 เส้น** ให้เขียนมือ ตามที่ 6.4 ทำ |
| กรอกบัญชีแล้ว | ใบเสร็จขึ้น `ธนาคาร กสิกรไทย · เลขที่บัญชี 123-4-56789-0 · ชื่อบัญชี …` |
| ใบวางบิล | ขึ้นบรรทัดเดียวกัน (สร้าง BN-202609-002 ขึ้นมาตรวจ) |
| แบบฟอร์มเปล่า | **เฉพาะใบเสร็จเปล่า**ที่มีบรรทัดบัญชี ใบเสนอราคา/ใบงาน/ใบซื้อ ไม่มี — ถูกตามต้นฉบับ |
| หน้าตั้งค่า | สามช่องโหลดค่าเดิมขึ้นมาถูก |
| ใบเคลมลูกค้า | กรอกรถเองได้ครบ 9 ช่องรวม **เลขไมล์ (กม.)** |
| ใบเคลมผู้ขาย | ป้ายเป็น **เลขที่ใบซื้ออ้างอิง** และไม่มีช่องรถเลย |

## เส้นทางไมเกรชัน

ทดสอบทั้งสองทาง ไม่ใช่แค่ทางเดียว

- **ติดตั้งใหม่** — คอลัมน์มาจาก `001_init.sql` และ `014` ถูกข้ามเพราะรวมอยู่แล้ว
- **อัปเกรด** — ถอดคอลัมน์ออกจากฐานที่ติดตั้งแล้ว ถอนบันทึกไมเกรชัน แล้วรันใหม่
  ได้คอลัมน์กลับมาครบพร้อมค่าปริยายเป็นข้อความว่าง
- **รันซ้ำ** — บอกว่าไม่มีอะไรค้าง

## สิ่งที่ต่างจากแผน

**ย้าย `plateOf()` ไปไว้ที่ `doc-chain.ts`** เดิมอยู่ใน `sales.ts` ซึ่งใบเคลมเรียกไม่ได้
เพราะมี `server-only` ผลคือถ้าปล่อยไว้ ใบเคลมกับเอกสารขายจะประกอบทะเบียนคนละแบบ
แล้วค้นด้วยทะเบียนจะเจอเฉพาะบางชนิดเอกสาร ซึ่งผู้ใช้จะสรุปว่าใบนั้นหายไป

**เทสต์ `migrate-runner` แดงตอนเพิ่มไฟล์ 014** — เป็นการ์ดที่ตั้งใจให้แดง
บอกวิธีแก้มาในข้อความเลยว่าให้รัน `node tools/gen-migrations.mjs`

## ที่ยังไม่ได้ทำ

- **คิวอาร์รับเงิน** — ตัวเลขบัญชีอย่างเดียวก็โอนได้แล้ว คิวอาร์เป็นงานรูปภาพแยกก้อน
- **กู้คืนเอกสารที่ยกเลิก** — ยังรอตัดสินขอบเขต
