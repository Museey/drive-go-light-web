# ช่วงที่ 7 — งานที่เหลือของรุ่น 6.4

## Context

ช่วงที่ 0–6 เสร็จหมดแล้ว และตั้งแต่วันนี้ **ระบบขึ้นเครื่องจริงแล้ว**
(`/healthz` ตอบ `{"ok":true,"failed":[]}` บน Render สิงคโปร์)

นั่นเปลี่ยนกติกาของช่วงนี้ไปหนึ่งข้อที่สำคัญกว่าตัวฟีเจอร์ทั้งหมดรวมกัน —
**ไมเกรชันตั้งแต่ `009` เป็นต้นไปจะรันทับฐานข้อมูลที่มีข้อมูลจริงของอู่อยู่ข้างใน**
ก่อนหน้านี้ทุกไฟล์รันบนฐานเปล่าเสมอ ผิดก็แค่ลบทิ้งแล้วสร้างใหม่ ตั้งแต่นี้ไปไม่ใช่แล้ว

---

## ขอบเขตจริง หลังไปอ่านต้นฉบับซ้ำ

แผนแม่ (`PLAN-6.4.md`) ประเมินช่วงนี้ไว้ 11–14 วัน จากห้ารายการ
พอไปเปิดไฟล์ 6.4 อ่านทีละบรรทัดแล้ว **สามในห้าข้อนั้นเขียนไว้ผิด** ต้องแก้ก่อนเริ่ม

| ที่แผนแม่เขียนไว้ | ความจริง |
|---|---|
| หน้ารายละเอียดสินค้า + พิมพ์บาร์โค้ด · 2 วัน | **ทำไปแล้วทั้งคู่ในช่วงที่ 1 และ 5** — `renderProductDetail()` ของ 6.4 มีสี่ตัวเลขกับตารางการเคลื่อนไหว ส่วน `/stock/[id]` ของเรามีครบนั้นบวกล็อตคงเหลือและการปรับยอด · ฉลากบาร์โค้ดก็มีที่ `/stock/[id]/barcode` |
| ส่งออกการเงินหลายรูปแบบ ขาด PDF · 2 วัน | **6.4 ไม่มีตัวสร้าง PDF** — `finExportPdf()` เรียก `doPrint()` แล้วให้ผู้ใช้เลือก "บันทึกเป็น PDF" ในกล่องพิมพ์ของเบราว์เซอร์ ตามที่หน้าต่างของมันเขียนบอกไว้เอง เราไม่ต้องลงไลบรารี PDF |
| รูปภาพ**แนบเอกสาร** · 3–4 วัน | 6.4 มีแค่ **รูปสินค้า** `MAX_PICS = 1` รูปต่อสินค้าหนึ่งรายการ เอกสารไม่มีรูปแนบเลย |

อีกสองข้อตรงตามที่เขียนไว้ — นำเข้าลูกค้า/ผู้ขายจาก CSV และประวัติการแก้ไข

**ขอบเขตใหม่จึงเป็นหกชิ้น ประมาณ 6.5–7.5 วัน**

| # | งาน | ประเมิน | ทำไมอยู่ลำดับนี้ |
|---|---|---|---|
| **0** | **ทำให้ไมเกรชันหนึ่งไฟล์เป็นหนึ่งทรานแซกชัน** | **0.5 วัน** | **ตอนนี้ยังไม่ใช่ — ไฟล์ที่พังกลางคันทิ้งฐานไว้ครึ่ง ๆ ดูข้อ 1** |
| 1 | หน้า 07.3 นำข้อมูลเข้าระบบ + CSV ลูกค้า/ผู้ขาย | 2 วัน | อู่ที่กำลังจะเปิดใช้ต้องพิมพ์ทะเบียนลูกค้าเข้าไปทีละราย ถ้าไม่มีอันนี้ |
| 2 | ประวัติการแก้ไขเอกสาร | 1.5 วัน | ต้องมาก่อนอู่บันทึกเอกสารเยอะ เพราะประวัติที่ไม่ได้เก็บย้อนหลังไม่ได้ |
| 3 | ฉลากบาร์โค้ดหลายรายการในแผ่นเดียว | 0.5 วัน | ของใหม่ที่ 6.4 ทำไม่ได้ — อู่ที่มีอะไหล่ 300 ตัวไม่มีทางกดทีละตัว |
| 4 | พิมพ์รายงานการเงินลงกระดาษ / PDF | 0.5 วัน | เล็กลงมากเมื่อรู้ว่าไม่ต้องมีไลบรารี PDF |
| 5 | รูปสินค้า | 2 วัน | เป็นข้อเดียวที่ต้องตัดสินใจเรื่องที่ดินที่ทาง ดูข้อ 5 ข้างล่าง |

ข้อ 2 มาก่อนข้อ 3–4 เพราะเป็นข้อเดียวในสามข้อนั้นที่**ย้อนหลังไม่ได้**
ทุกวันที่ยังไม่ลง คือเอกสารอีกกองที่จะไม่มีวันรู้ว่าใครแก้

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. ไมเกรชันหนึ่งไฟล์ต้องเป็นหนึ่งทรานแซกชัน — ต้องแก้ก่อนเขียน `009`

ผมไปอ่านตัวรันจริงตอนเขียนแผนนี้แล้วพบว่า **ตอนนี้มันไม่ได้ครอบทรานแซกชันไว้เลย**
`runStatements()` ยิงทีละคำสั่ง แต่ละคำสั่ง commit เอง แล้วค่อยจด `ops.migrations` ทีหลัง

ตอนที่ฐานยังว่างเปล่านี่ไม่เป็นไร พังก็ลบฐานทิ้งสร้างใหม่ **ตั้งแต่วันนี้ไม่ใช่แล้ว**
ถ้า `009` มี 6 คำสั่งแล้วพังที่คำสั่งที่ 4 จะได้ฐานที่มีตารางแต่ไม่มี trigger
และ `ops.migrations` ไม่มีแถวของ `009` — deploy รอบหน้าจึงรันไฟล์เดิมซ้ำ
แล้วไปตายที่คำสั่งแรกเพราะตารางมีอยู่แล้ว **ติดค้างแบบที่แก้ได้ด้วยมือเท่านั้น**

แก้ที่ `tools/migrate.impl.mjs` — ครอบ `begin` / `commit` รอบทั้งไฟล์
**และรวมการจด `ops.migrations` เข้าไปในทรานแซกชันเดียวกัน**
ได้ผลสามอย่างพร้อมกัน

- ไฟล์ที่พังกลางคันไม่ทิ้งอะไรไว้เลย รันใหม่ได้ทันทีหลังแก้
- สิ่งที่ `ops.migrations` จดไว้ **ตรงกับสิ่งที่เกิดขึ้นจริงเสมอ** ไม่มีทางเหลื่อมกัน
- `set local` ใช้ได้ ซึ่งจำเป็นสำหรับข้อถัดไป

ตรวจแล้วว่า `db/*.sql` ทั้งแปดไฟล์ไม่มีคำสั่งที่รันในทรานแซกชันไม่ได้เลย
(ไม่มี `create index concurrently` · `vacuum` · `alter system`)
เขียนเทสต์ล็อกข้อนี้ไว้ด้วย เพราะวันหนึ่งจะมีคนอยากใช้ `concurrently`

### 1ข. กติกาของตัวไฟล์ SQL เอง

- **ห้ามเขียนตารางใหม่ทั้งตาราง** — `add column` ที่ default เป็นค่าคงที่ไม่ rewrite
  ตาราง (Postgres 11+) แต่การเปลี่ยนชนิดคอลัมน์ rewrite ทั้งใบและล็อกไว้ตลอด
- **ห้ามล็อกนาน** — `set local lock_timeout = '5s'` ที่หัวไฟล์ ถ้าจับล็อกไม่ได้ให้ล้มไปเลย
  ดีกว่าค้างคิวจนทั้งอู่ใช้งานไม่ได้ (ใช้ได้ต่อเมื่อทำข้อ 1 แล้ว)
- **ต้องรันซ้ำได้** — `create table if not exists` ทุกที่ ถึงจะมีทรานแซกชันคุ้มแล้วก็ตาม

เพิ่มเทสต์ที่บังคับทั้งสามข้อให้เป็นกฎ ไม่ใช่ความตั้งใจ — ดูหัวข้อชุดทดสอบ

### 2. นำเข้าผู้ติดต่อ — จับคู่ตามลำดับเดียวกับ 6.4 เป๊ะ

`findContact()` ของต้นฉบับหาของเดิมสามชั้นตามลำดับ **รหัส → เลขผู้เสียภาษี → ชื่อ**
โดยชื่อเทียบแบบ `normName` (ตัดหัวท้าย ยุบช่องว่างซ้ำ ตัวพิมพ์เล็ก)

ทำตามทุกชั้น เพราะอู่ที่ย้ายมาจาก 6.4 เคยนำเข้าไฟล์ชุดเดียวกันนี้แล้ว
ถ้าเราจับคู่ต่างออกไป ไฟล์เดิมของเขาจะสร้างลูกค้าซ้ำทั้งกอง

**ที่ทำต่างจากต้นฉบับหนึ่งข้อ** — 6.4 หาชื่อซ้ำด้วย `pool.find()` คือ**เจอตัวแรกแล้วหยุด**
ถ้ามีลูกค้าชื่อ "สมชาย ใจดี" สองคน มันจะรวมเข้าคนแรกเงียบ ๆ
เราจะนับก่อน ถ้าชื่อนั้นตรงมากกว่าหนึ่งราย ให้**ข้ามแถวนั้นแล้วรายงาน** ว่าชื่อซ้ำ
การรวมข้อมูลลูกค้าผิดคนแก้คืนยากกว่าการที่ผู้ใช้ต้องมาแก้เองสองแถว

### 3. หนึ่งลูกค้าหลายคัน — พิมพ์ชื่อซ้ำได้ ระบบรวมให้

ไฟล์ CSV เป็นหนึ่งแถวต่อหนึ่งคัน ลูกค้าที่มีสามคันก็สามแถว ชื่อเดียวกัน
`planContactImport()` ของ 6.4 รวมแถวที่คีย์ตรงกันเข้าเป็นรายการเดียวก่อน แล้วค่อยเขียน

**ห้ามใช้ `saveContact()` ที่มีอยู่** — มันลบรถทุกคันที่ไม่ได้อยู่ในฟอร์ม
ซึ่งถูกสำหรับหน้าแก้ไข แต่ผิดสำหรับ CSV ที่ส่งมาทีละคัน
ต้องมีทางเขียนแยกที่**เพิ่มและปรับปรุงเท่านั้น ไม่ลบอะไรเลย** ตาม `applyVehicle()` ของต้นฉบับ
ซึ่งจับคู่รถเดิมด้วย **เลขตัวถัง** หรือ **ทะเบียน (หมวดอักษร + เลข)**

และตามต้นฉบับ — **ช่องที่เว้นว่างในไฟล์คงค่าเดิมไว้ ไม่ทับด้วยค่าว่าง**
คนแก้เบอร์โทรของลูกค้า 20 รายไม่ควรต้องกรอกที่อยู่ซ้ำทั้ง 20 ราย

### 4. ประวัติการแก้ไขต้องอยู่ในฐานข้อมูล ไม่ใช่ในโค้ดแอป

6.4 เก็บ `doc.edits = [{at, by}]` โดยเรียก `stampEdit(doc)` ด้วยมือทุกจุดที่บันทึก
เราไม่ทำแบบนั้น เพราะจุดที่เขียนเอกสารมีหลายสิบจุดและจะโตขึ้นอีก
**ลืมจุดเดียวคือประวัติหายโดยไม่มีอาการ**

ใช้ **trigger บนตาราง `documents`** แทน — ไม่มีเส้นทางไหนหลบได้ รวมถึงตัวนำเข้าและ CLI

ตัว trigger ต้องรู้ว่าใครเป็นคนทำ จึงต้องเพิ่ม `app.user_id` เข้าไปคู่กับ `app.tenant_id`
ที่ `withTenant()` ตั้งอยู่แล้ว — **ตั้งแบบ `local` เหมือนกัน** เพื่อไม่ให้ค่าค้างข้าม request
ถ้าไม่มีค่า (ตัวนำเข้า · ไมเกรชัน · สคริปต์) ให้บันทึกเป็น `null` แล้วแสดงว่า "ระบบ"
**ห้าม trigger ล้มเหลวเพราะไม่รู้ว่าใครทำ** — การบันทึกเอกสารสำคัญกว่าการรู้ชื่อคนบันทึก

เก็บมากกว่า 6.4 หนึ่งอย่างคือ **`user_id` เป็น FK** ไม่ใช่ชื่อ เพราะพนักงานเปลี่ยนชื่อได้
แต่เก็บชื่อ ณ ตอนนั้นไว้ด้วยเป็นตัวสำรอง เผื่อบัญชีถูกลบ (`on delete set null`)

**เพดาน 100 แถวต่อเอกสารตามต้นฉบับ** ตัดแถวเก่าทิ้งเมื่อเกิน — เอกสารใบเดียว
ที่ถูกกดบันทึกพันครั้งไม่ควรลากตารางประวัติของทั้งอู่ให้โต

### 5. รูปสินค้า — เก็บใน Postgres ไม่ใช่ object storage

สามทางที่พิจารณา

| ทาง | ข้อดี | ข้อเสีย |
|---|---|---|
| **Postgres `bytea`** | สำรอง/กู้คืนได้ในไฟล์เดียวเหมือนเดิม · RLS คุ้มให้อัตโนมัติ · ไม่มีไฟล์กำพร้า · ไม่ต้องมีผู้ให้บริการรายที่สอง | กินพื้นที่ฐานข้อมูล · ทุกครั้งที่เสิร์ฟรูปกิน connection |
| Cloudflare R2 / S3 | เสิร์ฟผ่าน CDN ไม่แตะฐาน | ผู้ให้บริการรายที่สอง · ต้องเก็บกวาดไฟล์กำพร้าเอง · ไฟล์สำรองไม่ครบอีกต่อไป · ต้องกลับไปตอบเรื่องข้อมูลข้ามประเทศใหม่ |
| ไม่ทำ | — | 6.4 ทำได้ เราทำไม่ได้ |

**เลือก Postgres** เหตุผลชี้ขาดคือราคา — Render คิดพื้นที่ฐานข้อมูลที่ **$0.30 ต่อ GB ต่อเดือน**
รูปที่ย่อแล้วตามสเปกของ 6.4 (ด้านยาว 800px · JPEG คุณภาพ 0.62) อยู่ราว 50–80 KB
อู่ 30 รายที่ใส่รูปให้สินค้ารายละ 400 ตัว = ราว 840 MB = **ประมาณ 25 บาทต่อเดือนทั้งระบบ**
ความยุ่งยากของการมีผู้ให้บริการรายที่สองแพงกว่านั้นมาก

สามอย่างที่ต้องมีคู่กัน ไม่งั้นทางนี้จะพัง

1. **เก็บสองขนาด** — รูปย่อ 160px (~8 KB) สำหรับตารางรายการ และรูปเต็ม 800px
   สำหรับตอนกดดู หน้ารายการสินค้า 50 แถวจะได้ไม่ดึง 4 MB
2. **แคชถาวรที่เบราว์เซอร์** — URL ผูกกับ SHA-256 ของตัวไฟล์ แล้วส่ง
   `cache-control: public, max-age=31536000, immutable` รูปเดิมจึงถูกดึงครั้งเดียวต่อเครื่อง
   นี่คือสิ่งที่ทำให้ `DB_POOL_MAX = 5` ยังพอ
3. **โควตาต่ออู่** — จำกัดพื้นที่รูปต่ออู่ **200 MB** บังคับด้วย trigger ในฐานข้อมูล
   อู่เดียวที่อัปรูปกล้อง 12 ล้านพิกเซลรัว ๆ ต้องไม่ทำให้อู่อื่นล่ม

### 6. ย่อรูปที่เบราว์เซอร์ แต่ไม่เชื่อสิ่งที่เบราว์เซอร์ส่งมา

ย่อด้วย canvas ฝั่งผู้ใช้เหมือน 6.4 — ไม่ต้องลง `sharp` ซึ่งเป็นโมดูล native
ที่ทำให้ build บนเครื่องผู้ให้บริการเปราะขึ้นอีกชั้น (เพิ่งเจอมาแล้วรอบหนึ่ง)

แต่ฝั่งเซิร์ฟเวอร์ต้องตรวจเองทุกครั้ง เพราะ `<input type=file>` ส่งอะไรมาก็ได้

- ตรวจ **magic bytes** ว่าเป็น JPEG / PNG / WebP จริง ไม่ดู `file.type` ที่ผู้ใช้ตั้งเองได้
- จำกัดขนาด **1 MB ต่อรูป** (รูปที่ย่อแล้วไม่ควรเกิน 100 KB ด้วยซ้ำ)
- อ่านความกว้าง–สูงจากหัวไฟล์ ปฏิเสธถ้าเกิน 2000px — กัน decompression bomb
- ตอนเสิร์ฟกลับ ใช้ `content-type` ที่**เราสรุปเองจาก magic bytes** ไม่ใช่ที่ผู้ใช้ส่งมา
  บวก `x-content-type-options: nosniff` และ `content-disposition: inline`
  ไม่งั้นไฟล์ที่หน้าตาเป็นรูปแต่ข้างในเป็น HTML จะกลายเป็น XSS บนโดเมนของเราเอง

### 7. หน้า 07.3 จัดใหม่ให้ตรงกับต้นฉบับ

ตอนนี้ `/settings/backup` เอาสำรองข้อมูลไว้บนสุด แล้ว CSV สินค้าอยู่ล่างสุด
6.4 วางกลับกัน — สามช่องนำเข้า (สินค้า · ลูกค้า · ผู้ขาย) เรียงข้างกันอยู่บนสุด
แล้วสำรอง/กู้คืนอยู่ใต้ลงมา

ทำตาม เพราะคนที่ย้ายมาจาก 6.4 จำตำแหน่งได้ และเพราะการนำเข้าคือสิ่งที่คนเปิดหน้านี้มาทำ
ส่วนสำรองข้อมูลเป็นของที่ทำปีละครั้ง

แต่ละช่องมีสามปุ่มเหมือนต้นฉบับ — **ดาวน์โหลดข้อมูลปัจจุบัน · ดาวน์โหลดแบบฟอร์ม · นำเข้า**
โดยปุ่มดาวน์โหลดข้อมูลจะโผล่เฉพาะคนที่มีสิทธิ์ส่งออกเมนูย่อยนั้น ตามที่ 6.4 ทำ

### 8. เพดานนำเข้าครั้งละ 3,000 รายการ ตามต้นฉบับ

`IMPORT_LIMIT = 3000` ของ 6.4 มีเหตุผลที่ยังใช้ได้กับเรา — ไฟล์ที่ใหญ่กว่านั้น
แปลว่ามีอะไรผิดมากกว่าที่จะถูก และการนำเข้าทั้งหมดอยู่ในทรานแซกชันเดียว
บังคับกับ CSV สินค้าที่มีอยู่แล้วด้วย ตอนนี้มันไม่มีเพดานเลย

---

## แก้ฐานข้อมูล

### `db/009_edits.sql` — ประวัติการแก้ไข

```sql
-- ห้ามค้างคิวจนทั้งอู่ใช้งานไม่ได้ ถ้าจับล็อกไม่ได้ให้ล้มไปเลย
set local lock_timeout = '5s';

create table if not exists doc_edits (
  id         bigint generated always as identity primary key,
  tenant_id  uuid        not null references tenants(id) on delete cascade,
  document_id uuid       not null references documents(id) on delete cascade,
  at         timestamptz not null default now(),
  user_id    uuid        references users(id) on delete set null,
  user_name  text        not null default '',   -- ชื่อ ณ ตอนนั้น เผื่อบัญชีถูกลบ
  action     text        not null               -- 'create' | 'update' | 'void'
);
create index if not exists doc_edits_doc_idx on doc_edits (document_id, at desc);
```

พร้อม RLS แบบเดียวกับทุกตาราง (`enable` + **`force`** + policy `tenant_id = current_tenant_id()`)
และ trigger `after insert or update on documents`

จุดที่ต้องระวังในตัว trigger

- อ่านผู้ใช้ด้วย `current_setting('app.user_id', true)` — **พารามิเตอร์ตัวที่สองคือ true**
  แปลว่าคืน `null` ถ้าไม่ได้ตั้ง แทนที่จะโยน error
- แปลง `''` เป็น `null` ก่อน cast เป็น uuid ไม่งั้น `''::uuid` โยน error
- `update` ที่ไม่ได้เปลี่ยนอะไรเลย (`old.* is not distinct from new.*`) ไม่ต้องบันทึก
  ไม่งั้นการกดบันทึกโดยไม่แก้อะไรจะรกประวัติ
- ตัดแถวเก่าเมื่อเกิน 100 ต่อเอกสาร

### `db/010_pics.sql` — รูปสินค้า

```sql
set local lock_timeout = '5s';

create table if not exists product_pics (
  product_id uuid        primary key references products(id) on delete cascade,
  tenant_id  uuid        not null references tenants(id) on delete cascade,
  sha        text        not null,          -- SHA-256 ของรูปเต็ม ใช้เป็นส่วนหนึ่งของ URL
  mime       text        not null check (mime in ('image/jpeg','image/png','image/webp')),
  width      integer     not null check (width  between 1 and 2000),
  height     integer     not null check (height between 1 and 2000),
  full_bytes bytea       not null,
  thumb_bytes bytea      not null,
  bytes      integer     not null,          -- ขนาดรวมสองรูป ใช้คิดโควตา
  created_at timestamptz not null default now()
);
```

`primary key (product_id)` บังคับ **หนึ่งรูปต่อสินค้าหนึ่งรายการ** ตรงกับ `MAX_PICS = 1`
ของต้นฉบับ โดยไม่ต้องมีโค้ดคอยนับ

โควตาบังคับด้วย trigger `before insert or update` ที่รวม `bytes` ของอู่นั้นแล้วปฏิเสธถ้าเกิน 200 MB

**ไม่แตะตาราง `products` เลย** — ไม่มี `alter table` จึงไม่มีล็อกบนตารางที่แอปกำลังใช้อยู่

### ไฟล์สำรองข้อมูล

`backup.ts` ต้องรวมรูปด้วย ตามที่ 6.4 เขียนไว้ว่า "รวมรูปสินค้า"
เข้ารหัส base64 ในฟิลด์แยกของ JSON

**ผลข้างเคียงที่ต้องบอกผู้ใช้** — ไฟล์สำรองของอู่ที่ใส่รูปครบ 400 ตัวจะโตขึ้นราว 30 MB
เพดานอัปโหลดของเราอยู่ที่ 40 MB และ Server Action ตั้งไว้ที่ 48 MB จึงยังพอ
แต่ต้องมีตัวเลขบอกบนหน้าจอว่าไฟล์จะใหญ่แค่ไหน และต้องมีเทสต์ที่ยืนยันว่า
**สำรองแล้วกู้คืนได้รูปกลับมาครบไบต์ต่อไบต์**

---

## ไฟล์ที่จะแตะ

### งานที่ 0 — ทรานแซกชันของไมเกรชัน
- `tools/migrate.impl.mjs` — ครอบ `begin`/`commit` รอบไฟล์ + การจด `ops.migrations`
- `apps/web/test/migrate-runner.test.ts` — เพิ่มข้อที่พิสูจน์การย้อนกลับ

### งานที่ 1 — CSV ผู้ติดต่อ
- `apps/web/src/lib/contacts-csv.ts` **ใหม่** — ส่งออก · แบบฟอร์ม · วางแผน · นำเข้า
- `apps/web/src/lib/csv.ts` — เพิ่ม `CUST_CSV_HEADERS` · `VEND_CSV_HEADERS` (ยกจาก 6.4 ทั้งชุด)
- `apps/web/src/app/settings/import/` **ใหม่** — หน้า 07.3 ที่จัดใหม่ตามข้อ 7
- `apps/web/src/app/contacts/csv/route.ts` **ใหม่** — ดาวน์โหลดข้อมูลและแบบฟอร์ม
- `apps/web/src/components/menu-map.ts` — ชี้ 07.3 ไปหน้าใหม่
- `apps/web/src/app/settings/backup/` — เหลือเฉพาะสำรอง/กู้คืน หรือย้ายทั้งก้อนเข้าหน้าใหม่

### งานที่ 2 — ประวัติการแก้ไข
- `db/009_edits.sql` **ใหม่**
- `apps/web/src/lib/db.ts` — `withTenant` รับ `userId` เพิ่ม แล้วตั้ง `app.user_id`
- `apps/web/src/lib/mutate.ts` — ส่ง `session.userId` ต่อเข้าไป
- `apps/web/src/lib/doc-edits.ts` **ใหม่** — อ่านประวัติ
- หน้าดูเอกสารทั้งหก — แถบ "บันทึกล่าสุด … โดย …" + ปุ่มดูประวัติ

### งานที่ 3 — ฉลากบาร์โค้ดหลายรายการ
- `apps/web/src/app/stock/barcodes/page.tsx` **ใหม่** — รับพารามิเตอร์ตัวกรองชุดเดียวกับ
  `/stock/print` ที่มีอยู่แล้ว บวก `?n=` จำนวนดวงต่อสินค้า
- `apps/web/src/app/stock/page.tsx` — ปุ่มเข้าหน้านั้น

### งานที่ 4 — พิมพ์รายงานการเงิน
- `apps/web/src/app/finance/print/page.tsx` **ใหม่** — ใช้ `ListPaper` ที่มีอยู่
  และตัวกรองชุดเดียวกับ `/finance/csv`
- ปุ่มบนหน้า `/finance/*` ทั้งสี่แท็บ

### งานที่ 5 — รูปสินค้า
- `db/010_pics.sql` **ใหม่**
- `apps/web/src/lib/pics.ts` **ใหม่** — ตรวจ magic bytes · อ่านขนาดจากหัวไฟล์ · อ่าน/เขียน
- `apps/web/src/app/pics/[sha]/route.ts` **ใหม่** — เสิร์ฟรูป
- `apps/web/src/app/stock/[id]/pic-upload.tsx` **ใหม่** — ย่อด้วย canvas ฝั่งผู้ใช้
- `apps/web/src/lib/backup.ts` · `restore-core.ts` — รวมรูปเข้าไฟล์สำรอง
- `packages/importer/src/import.ts` — รับรูปจากไฟล์ของ 6.4

---

## ชุดทดสอบ

### ที่บังคับกติกาไมเกรชัน (ข้อ 1) — เขียนก่อนอย่างอื่น

- **ไฟล์ที่พังกลางคันต้องไม่ทิ้งอะไรไว้เลย** — ป้อนไฟล์ที่คำสั่งแรกสร้างตาราง
  คำสั่งที่สองผิดไวยากรณ์ แล้วยืนยันว่าตารางนั้น**ไม่เกิดขึ้น** และ `ops.migrations`
  ไม่มีแถวของมัน นี่คือเทสต์ที่สำคัญที่สุดของงานที่ 0
- **`ops.migrations` ตรงกับความจริงเสมอ** — หลังไฟล์พัง รันใหม่ได้ทันทีโดยไม่ต้องแก้ด้วยมือ
- `db/*.sql` ไม่มีคำสั่งที่รันในทรานแซกชันไม่ได้ (`concurrently` · `vacuum` · `alter system`)
  — อ่านทุกไฟล์ในโฟลเดอร์จริง ไฟล์ใหม่ที่ละเมิดจะแดงเอง
- ไฟล์ `009` และ `010` **ไม่มีการเปลี่ยนชนิดคอลัมน์** และมี `lock_timeout`
- **รันไฟล์ใหม่สองรอบติดกันต้องได้ผลเท่ากัน** — สร้างฐานจาก baseline แล้วรัน `009`, `010`
  สองรอบ เทียบสคีมาที่ได้
- **อัปเกรดจากฐานที่มีข้อมูลอยู่แล้ว** — ไม่ใช่ฐานเปล่า นำเข้าไฟล์ตัวอย่างก่อน
  แล้วค่อยรันไมเกรชัน แล้วยืนยันว่าข้อมูลเดิมยังครบทุกแถว

`migrations.test.ts` ที่มีอยู่เทียบสคีมาสองทาง (ติดตั้งใหม่ vs อัปเกรด) อยู่แล้ว
ต่อยอดจากตัวนั้น

### CSV ผู้ติดต่อ
- หัวคอลัมน์ตรงกับ `CUST_CSV_HEADERS` / `VEND_CSV_HEADERS` ของ 6.4 **ทีละช่อง**
  โดยอ่านจากไฟล์ต้นฉบับ ไม่ลอกมาไว้ในเทสต์ (วิธีเดียวกับ `menu-map.test.ts`)
- ไฟล์เดียวกันนำเข้าสองรอบ → รอบสองต้องไม่สร้างใครเพิ่ม (ทดสอบการจับคู่)
- จับคู่ครบสามชั้น — รหัส · เลขผู้เสียภาษี · ชื่อ — และ**ชั้นที่ถูกใช้ต้องตามลำดับ**
  (ทดสอบด้วยแถวที่รหัสตรงคนหนึ่ง แต่ชื่อตรงอีกคนหนึ่ง ต้องเข้าคนที่รหัสตรง)
- **ชื่อซ้ำสองรายต้องข้ามและรายงาน ไม่ใช่รวมเข้าคนแรก** (ข้อที่เราตั้งใจต่างจากต้นฉบับ)
- ลูกค้าคนเดียวสามแถวสามคัน → ได้ลูกค้าหนึ่งราย รถสามคัน
- นำเข้าซ้ำด้วยเลขตัวถังเดิม → ไม่เพิ่มคันใหม่ แต่ปรับปรุงคันเดิม
- **ช่องว่างในไฟล์ไม่ทับค่าเดิม**
- แถวไม่มีชื่อ → ข้ามพร้อมบอกเลขบรรทัด
- เกิน 3,000 แถว → ปฏิเสธทั้งไฟล์ ไม่ใช่นำเข้าครึ่งเดียว
- **นำเข้าล้มกลางคัน แล้วไม่มีอะไรถูกเขียนเลย** (ทรานแซกชันเดียว)

### ประวัติการแก้ไข
- สร้างเอกสาร → ได้แถว `create` หนึ่งแถว
- แก้แล้วบันทึก → ได้แถว `update`
- **บันทึกโดยไม่เปลี่ยนอะไรเลย → ไม่มีแถวเพิ่ม**
- ยกเลิกเอกสาร → ได้แถว `void`
- เขียนเอกสารผ่าน SQL ตรง ๆ โดยไม่ผ่านแอป → **ยังได้แถวประวัติ** (พิสูจน์ว่า trigger
  ไม่มีทางถูกข้าม — นี่คือเทสต์ที่สำคัญที่สุดของงานนี้)
- ไม่ได้ตั้ง `app.user_id` → บันทึกได้ปกติ `user_id` เป็น `null`
- แก้ 105 ครั้ง → เหลือ 100 แถว และเป็น 100 แถวล่าสุด
- ลบบัญชีพนักงาน → ประวัติยังอยู่ ชื่อยังอ่านได้
- **`doc_edits` อยู่ใต้ RLS แบบ force** — `tenant-isolation.test.ts` ไล่ตารางจาก
  `information_schema` อยู่แล้ว ตารางใหม่จะถูกจับเองถ้าลืมเปิด

### รูปสินค้า
- ไฟล์ที่ไม่ใช่รูป (เช่น HTML ที่ตั้งชื่อ `.jpg`) → ปฏิเสธ
- ไฟล์เกิน 1 MB → ปฏิเสธ
- รูปกว้างเกิน 2000px → ปฏิเสธ
- **content-type ที่เสิร์ฟออกมาเป็นค่าที่เราสรุปเอง ไม่ใช่ค่าที่ผู้ใช้ส่ง**
- อัปรูปจนเกินโควตา 200 MB → ปฏิเสธ และเป็นการปฏิเสธ**จากฐานข้อมูล** ไม่ใช่จากโค้ดแอป
- อัปรูปใหม่ทับ → รูปเดิมหายไปจริง ไม่ค้างเป็นขยะ
- ลบสินค้า → รูปหายตาม (`on delete cascade`)
- **สำรองแล้วกู้คืน → ได้ไบต์เดิมเป๊ะ**
- อู่ ก. ขอ URL รูปของอู่ ข. → ไม่ได้

### พิมพ์
- ฉลากบาร์โค้ด: สินค้าที่ไม่มีบาร์โค้ดถูกข้าม พร้อมบอกจำนวนที่ข้าม
- ฉลากบาร์โค้ด: จำนวนดวงถูกปัดกลับเป็นค่าตั้งต้นถ้าใส่ค่านอกช่วง (เหมือน `/forms`)
- รายงานการเงิน: ตัวเลขบนกระดาษตรงกับที่ CSV ส่งออกทุกแถว **โดยเรียกฟังก์ชันเดียวกัน**
- ทั้งสองหน้าต้องมีสิทธิ์ส่งออกจึงเข้าได้ (`requireExport`)

### การกลายพันธุ์
ทุกข้อข้างบนต้องแดงเมื่อย้อนโค้ดที่มันคุ้มอยู่กลับ — โดยเฉพาะสามข้อนี้ที่ผมจะทดสอบให้ดู
0. ถอด `begin`/`commit` ออกจากตัวรันไมเกรชัน → เทสต์ "พังกลางคัน" ต้องแดง
1. ถอด trigger ประวัติออก → เทสต์ "เขียนผ่าน SQL ตรง ๆ" ต้องแดง
2. เปลี่ยนลำดับการจับคู่ผู้ติดต่อ → เทสต์ลำดับสามชั้นต้องแดง
3. ถอด check โควตารูป → เทสต์โควตาต้องแดง

---

## วิธีตรวจว่าใช้ได้จริง

เทสต์เขียวไม่พอ ช่วงที่ 6 เจอสองจุดที่เทสต์เขียวหมดแต่ของจริงพัง

1. ส่งออกลูกค้าจากอู่จริง → เปิดด้วย Excel บนเครื่องจริง → **ภาษาไทยต้องไม่เพี้ยน** →
   แก้เบอร์โทรสองราย → บันทึกเป็น CSV UTF-8 → นำเข้ากลับ → ต้องได้ "ปรับปรุง 2 ราย" ไม่ใช่ "เพิ่มใหม่"
2. เอาไฟล์ CSV ที่ 6.4 ส่งออกมาจริง ๆ นำเข้าระบบเรา แล้วเทียบจำนวนราย
3. อัปรูปจากมือถือจริง (ไฟล์ 4–6 MB จากกล้อง) → ต้องย่อแล้วขึ้นได้ ไม่ใช่ค้างหรือ error
4. เปิดหน้ารายการสินค้า 50 แถวที่มีรูป → กด reload ครั้งที่สอง → **ต้องไม่มี request รูปเลย**
   (พิสูจน์ว่าแคชทำงาน) ดูจาก network ของเบราว์เซอร์
5. แก้เอกสารใบเดียวกันจากสองบัญชี → ประวัติต้องแสดงสองชื่อถูกคน
6. รัน `009`, `010` บนสำเนาของฐานจริง แล้วจับเวลาว่าล็อกนานแค่ไหน

---

## ที่ไม่ทำในช่วงนี้

- **รูปแนบเอกสาร** — 6.4 ไม่มี และยังไม่มีใครขอ
- **หลายรูปต่อสินค้า** — ต้นฉบับตั้ง `MAX_PICS = 1` ไว้ ตามนั้นก่อน โครงสร้างเผื่อไว้แล้ว
  (เปลี่ยน primary key เป็น composite ทีหลังได้)
- **ประวัติแบบเห็นว่าเปลี่ยนค่าอะไรจากอะไรเป็นอะไร** — 6.4 เก็บแค่ใครกับเมื่อไหร่
  การเก็บ diff รายฟิลด์ทำให้ตารางโตหลายเท่าและยังไม่มีใครขอ
  ทำเพิ่มทีหลังได้โดยไม่ต้องรื้อ เพราะ trigger อยู่ที่เดียว
- **นำเข้าเอกสาร (ใบเสร็จ ใบซื้อ) จาก CSV** — 6.4 ก็ไม่มี · ใครมีของเดิมให้ใช้ไฟล์สำรอง JSON
- **ส่งออกเป็น .xlsx จริง ๆ** — 6.4 เรียก CSV ว่า Excel ทั้งที่เป็น CSV
  ถ้าจะทำของจริงต้องลงไลบรารี ซึ่งยังไม่คุ้ม

---

## หลังช่วงนี้เหลืออะไร

ครบ 6.4 แล้ว ที่เหลือคืองานขึ้นใช้งานจริงที่ไม่เกี่ยวกับต้นฉบับ

| งาน | ทำไมต้องมี |
|---|---|
| ส่งอีเมล | ตอนนี้ลิงก์ตั้งรหัสผ่านต้องส่งด้วยมือ |
| ลืมรหัสผ่าน | ต้องมีอีเมลก่อน |
| คอนโซลผู้ให้บริการ | เปิดอู่ใหม่และดูวันหมดอายุ ตอนนี้ทำผ่าน CLI |
