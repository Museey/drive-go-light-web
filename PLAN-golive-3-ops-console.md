# คอนโซลผู้ให้บริการ

## Context

ตอนนี้ทุกอย่างที่ผู้ให้บริการต้องทำ ทำผ่านบรรทัดคำสั่งจากเครื่องที่ต่อฐานข้อมูลได้

| งาน | ตอนนี้ |
|---|---|
| เปิดอู่ใหม่ + บัญชีเจ้าของ | `node packages/importer/dist/cli.js …` |
| ออกลิงก์ตั้งรหัสผ่านใหม่ให้คนที่ลืม | คำสั่งเดียวกัน ใส่ `--tenant=` |
| ดูว่าอู่ไหนหมดอายุเมื่อไหร่ | เปิด psql เอง |
| ต่ออายุ / ปรับจำนวนที่นั่งพนักงาน | เปิด psql เอง |
| ดูข้อผิดพลาดที่เกิดขึ้น | เปิด psql เอง |

ที่ 20–30 อู่ยังพอไหว แต่มันแปลว่า**ต้องมีเครื่องที่มี connection string ของฐานข้อมูลจริง
อยู่ในมือตลอดเวลา** ซึ่งเป็นความเสี่ยงที่ใหญ่กว่าความไม่สะดวก — เครื่องหายหรือ
ประวัติคำสั่งหลุด เท่ากับฐานข้อมูลของทุกอู่หลุดไปด้วย

คอนโซลทำให้งานประจำวันไม่ต้องแตะ connection string อีกเลย

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. อยู่ในแอปเดิม ที่เส้นทาง `/ops` ไม่แยกเป็นบริการที่สอง

แยกบริการได้ความปลอดภัยเพิ่มขึ้นจริง แต่แลกมากับค่าเครื่องอีกตัว การ deploy อีกชุด
และไมเกรชันอีกที่ที่ต้องคอยให้ตรงกัน

**และที่สำคัญกว่า — เส้นแบ่งที่มีความหมายจริงอยู่ที่ฐานข้อมูล ไม่ใช่ที่โพรเซส**
ถ้าฟังก์ชันในฐานข้อมูลออกแบบถูก การอยู่คนละโพรเซสก็ไม่ได้เพิ่มอะไรมาก
ถ้าออกแบบผิด การแยกโพรเซสก็ไม่ช่วยอะไรเลย ดูข้อ 3

### 2. ผู้ให้บริการเป็นคนละตารางกับผู้ใช้ของอู่

`ops.operators` ไม่ใช่แถวใน `users`

`users` ทุกแถวมี `tenant_id` และอยู่ใต้ RLS — ผู้ให้บริการไม่ได้สังกัดอู่ไหน
ถ้ายัดเข้าไปต้องแต่ง tenant ปลอมให้ แล้วทุกด่านตรวจสิทธิ์ในระบบ (`canTab` `canEdit`
`canExport` `canCost` …) ต้องจำให้ได้ว่า **ต้องกัน role นี้ออกด้วย**
ลืมด่านเดียวคือเจ้าของอู่ยกระดับตัวเองเป็นผู้ให้บริการได้

คนละตารางแปลว่าไม่มีทางสับสน และไม่มีด่านไหนต้องจำอะไรเพิ่ม

ตามมาด้วย **คุกกี้คนละชื่อ (`dgl_ops`) และตาราง session คนละใบ (`ops.sessions`)**
session ของอู่ใช้เป็น session ผู้ให้บริการไม่ได้ และกลับกันก็ไม่ได้

### 3. ด่านตรวจสิทธิ์อยู่ในฐานข้อมูล ไม่ใช่ในโค้ดแอป

นี่คือข้อที่สำคัญที่สุดของช่วงนี้

ทางที่ง่ายคือเขียนฟังก์ชัน `ops.list_shops()` แล้วให้โค้ดแอปเรียก `requireOperator()`
ก่อนเสมอ — ซึ่งแปลว่า **ความปลอดภัยทั้งหมดขึ้นอยู่กับว่าไม่มีใครลืมเรียกด่านนั้น**
เป็นข้อผิดพลาดแบบเดียวกับที่รุ่น 6.4 เรียก `stampEdit()` ด้วยมือทุกจุด

ทำแบบเดียวกับที่ระบบล็อกอินของอู่ทำอยู่แล้วแทน — **ทุกฟังก์ชันรับ session hash
เข้าไปแล้วตรวจเองข้างใน** ถ้าโทเคนไม่ถูกต้องหรือหมดอายุ ฟังก์ชันคืนค่าว่าง
หรือโยน error ไม่ว่าใครจะเป็นคนเรียก

```sql
ops.list_shops(p_session bytea)                          -- ไม่ผ่าน = ไม่มีแถว
ops.open_shop(p_session bytea, p_name text, …)           -- ไม่ผ่าน = โยน error
```

ผลคือ **โค้ดแอปที่เขียนผิดหรือถูกหลอกให้เรียก ก็ยังทำอะไรไม่ได้** ถ้าไม่มีโทเคนจริง
`requireOperator()` ฝั่งแอปยังมีอยู่ แต่มีไว้เพื่อพาไปหน้าล็อกอินให้ถูก ไม่ใช่เพื่อกันของ

### 4. คอนโซลอ่านข้อมูลธุรกิจของอู่ไม่ได้เลย

`ops.list_shops()` คืนเฉพาะ — ชื่ออู่ · วันที่เปิด · สถานะลิขสิทธิ์ · วันหมดอายุ ·
จำนวนบัญชีผู้ใช้ · จำนวนที่นั่งที่เหลือ

**ไม่มี** ลูกค้า · เอกสาร · ยอดขาย · ต้นทุน · ราคา

ผู้ให้บริการต้องรู้ว่าใครกำลังจะหมดอายุ ไม่ใช่รู้ว่าเขาขายอะไรได้เท่าไหร่
และหน้า `/privacy` บอกอู่ไว้ว่าเราแยกข้อมูลไว้ที่ตัวฐานข้อมูล — คอนโซลที่อ่านได้ทุกอย่าง
จะทำให้ประโยคนั้นไม่จริงทันที

มีเทสต์ที่ไล่ชื่อคอลัมน์ที่ฟังก์ชันคืนออกมาแล้วยืนยันว่าไม่มีคอลัมน์ต้องห้าม

### 5. ไม่มีการสวมสิทธิ์เข้าไปเป็นเจ้าของอู่

เป็นสิ่งที่คอนโซลแบบนี้มักมี เพราะช่วยเรื่อง support ได้มาก
แต่มันคือประตูที่เปิดให้อ่านข้อมูลของอู่ได้ทั้งหมดโดยที่อู่ไม่รู้ตัว ซึ่งขัดกับข้อ 4
และทำให้ประวัติการแก้เอกสารเชื่อถือไม่ได้อีกต่อไป — แถวที่บอกว่า "สมชายแก้"
อาจไม่ใช่สมชายก็ได้

วิธีช่วย support ที่ไม่ต้องแลกกับสิ่งนี้คือ **ออกลิงก์ตั้งรหัสผ่านให้เจ้าของตัวจริง**
ซึ่งคอนโซลทำได้อยู่แล้ว

### 6. ทุกการกระทำถูกจดไว้

`ops.audit` — ใคร ทำอะไร กับอู่ไหน เมื่อไหร่ จาก IP ไหน

บัญชีนี้เปิดอู่ใหม่ได้และออกลิงก์ตั้งรหัสผ่านของคนอื่นได้ ถ้าวันหนึ่งถูกใช้ในทางที่ผิด
หรือเครื่องของผู้ให้บริการหาย ต้องมีบันทึกว่าเกิดอะไรขึ้นบ้าง

**ย้อนหลังไม่ได้** เหมือนประวัติการแก้เอกสาร — ทำตอนนี้ถูกกว่าทำทีหลังมาก
และเขียนโดยตัวฟังก์ชันเอง ไม่ใช่โดยโค้ดแอป จึงไม่มีทางลืม

### 7. บัญชีผู้ให้บริการคนแรกยังต้องสร้างด้วยคำสั่ง — ครั้งเดียว

ไก่กับไข่ ไม่มีใครในระบบที่มีสิทธิ์สร้างบัญชีนั้นให้

`tools/ops-admin.mjs` สร้างบัญชีแล้วพิมพ์**ลิงก์ตั้งรหัสผ่าน** ออกมา ไม่ใช่รับรหัสผ่าน
เข้าไปทางบรรทัดคำสั่ง — รหัสผ่านที่พิมพ์ในเทอร์มินัลไปค้างอยู่ใน history เสมอ
ใช้กลไกโทเคนชุดเดียวกับที่อู่ใช้ (`ops.setup_tokens`)

หลังจากนั้นเพิ่มบัญชีผู้ให้บริการคนอื่นได้จากในคอนโซล

### 8. ล็อกอินของคอนโซลเข้มกว่าของอู่

เป้าหมายมีค่ามากกว่ากันมาก — บัญชีอู่ที่หลุดคืออู่เดียว บัญชีผู้ให้บริการที่หลุด
คือทุกอู่

- รหัสผ่านขั้นต่ำ **14 ตัวอักษร** (ของอู่คือ 10)
- ผิด **5 ครั้งล็อก 15 นาที** เหมือนของอู่ แต่ล็อกที่ระดับบัญชี **และ** จดลง `ops.audit`
- session อายุ **1 วัน** ไม่ใช่ 14 วัน
- คุกกี้ `path=/ops` — ไม่ถูกส่งไปกับ request ของหน้าอู่เลย

**ไม่ทำ 2FA ในช่วงนี้** ควรมี แต่ต้องมีที่เก็บ recovery code และหน้าจอกู้คืน
ซึ่งเป็นงานอีกก้อน — เขียนไว้ในหัวข้อที่ยังไม่ทำ

### 9. "ปิดอู่" ใช้กลไกลิขสิทธิ์ที่มีอยู่แล้ว ไม่เพิ่มสวิตช์ใหม่

อู่ที่หมดอายุบันทึกรายการใหม่ไม่ได้อยู่แล้ว (`mutate()` ปฏิเสธ) แต่ยังเปิดดู พิมพ์
และดาวน์โหลดไฟล์สำรองได้ ซึ่งตรงกับที่ `/privacy` ข้อ 7 สัญญาไว้พอดี

การเพิ่มสวิตช์ "ระงับการใช้งาน" อีกอันจะสร้างสถานะที่สองที่ต้องคอยดูแลให้ตรงกัน
โดยไม่ได้ทำอะไรที่กลไกเดิมทำไม่ได้

---

## แก้ฐานข้อมูล — `db/011_ops_console.sql`

```sql
set local lock_timeout = '5s';

create table if not exists ops.operators (
  id            uuid        primary key default gen_random_uuid(),
  email         citext      not null unique,
  name          text        not null default '',
  password_hash text,                       -- null = ยังไม่ได้ตั้งรหัสผ่าน
  active        boolean     not null default true,
  failed_count  integer     not null default 0,
  locked_until  timestamptz,
  last_login_at timestamptz,
  created_at    timestamptz not null default now()
);

create table if not exists ops.sessions (
  token_hash   bytea       primary key,
  operator_id  uuid        not null references ops.operators(id) on delete cascade,
  created_at   timestamptz not null default now(),
  last_seen_at timestamptz not null default now(),
  expires_at   timestamptz not null,
  user_agent   text
);

create table if not exists ops.setup_tokens (
  token_hash  bytea       primary key,
  operator_id uuid        not null references ops.operators(id) on delete cascade,
  expires_at  timestamptz not null,
  used_at     timestamptz
);

create table if not exists ops.audit (
  id          bigint      generated always as identity primary key,
  at          timestamptz not null default now(),
  operator_id uuid        references ops.operators(id) on delete set null,
  operator_email text     not null default '',   -- ชื่อ ณ ตอนนั้น เผื่อบัญชีถูกลบ
  action      text        not null,
  tenant_id   uuid,                              -- ไม่มี FK จงใจ — ลบอู่แล้วบันทึกต้องอยู่
  detail      jsonb       not null default '{}'::jsonb
);
```

`ops.audit.tenant_id` **ไม่มี foreign key โดยตั้งใจ** — บันทึกว่าเคยทำอะไรกับอู่หนึ่ง
ต้องอยู่ต่อแม้อู่นั้นจะถูกลบไปแล้ว ซึ่งเป็นกรณีที่บันทึกมีค่าที่สุดพอดี

ฟังก์ชันทุกตัวเป็น `security definer` · `set search_path = ops, public, pg_temp` ·
รับ `p_session bytea` เป็นพารามิเตอร์แรกและตรวจเองข้างใน

| ฟังก์ชัน | ทำอะไร |
|---|---|
| `ops.operator_login(p_email)` | คืน hash ให้แอปตรวจเอง แบบเดียวกับ `auth.find_user_for_signin` |
| `ops.create_session(p_operator, p_hash, p_expires, p_ua)` | |
| `ops.load_session(p_hash)` | คืนตัวตนผู้ให้บริการ หรือไม่มีแถวถ้าหมดอายุ |
| `ops.list_shops(p_session)` | รายชื่ออู่ — **เฉพาะคอลัมน์ในข้อ 4** |
| `ops.open_shop(p_session, p_name, p_tel, p_email, p_owner)` | สร้างอู่ + เจ้าของ + โทเคน คืนโทเคน |
| `ops.issue_owner_reset(p_session, p_tenant)` | ออกลิงก์ตั้งรหัสผ่านใหม่ให้เจ้าของอู่ |
| `ops.record_renewal(p_session, p_tenant, p_plan, p_from, p_to, p_amount, p_note)` | |
| `ops.set_max_users(p_session, p_tenant, p_n)` | |
| `ops.add_operator(p_session, p_email, p_name)` | คืนโทเคนตั้งรหัสผ่าน |
| `ops.list_errors(p_session, p_limit)` | อ่าน `ops.errors` |

ทุกตัวที่เปลี่ยนแปลงข้อมูลเขียน `ops.audit` **ในฟังก์ชันเอง** ไม่ใช่ให้แอปเขียน

grant ให้ `dgl_app` เรียกฟังก์ชันได้ แต่ **ห้ามแตะตารางใน `ops` โดยตรง**
(ยกเว้น `ops.errors` ที่ให้อยู่แล้ว) — แบบเดียวกับที่ทำกับสคีมา `auth`

---

## ไฟล์ที่จะแตะ

- `db/011_ops_console.sql` **ใหม่** + เพิ่มเข้า `001_init.sql`
- `apps/web/src/lib/ops-auth.ts` **ใหม่** — ล็อกอิน · session · `requireOperator()`
- `apps/web/src/lib/ops-console.ts` **ใหม่** — เรียกฟังก์ชันในสคีมา `ops`
- `apps/web/src/app/ops/` **ใหม่** — `login` · หน้ารายชื่ออู่ · เปิดอู่ใหม่ ·
  รายละเอียดอู่ (ต่ออายุ · ที่นั่ง · ออกลิงก์) · ผู้ให้บริการ · ข้อผิดพลาด · บันทึกการใช้งาน
- `tools/ops-admin.mjs` + `.impl.mjs` **ใหม่** — สร้างบัญชีแรก (แยก loader ตามแบบเดิม
  เพราะ Node ปริยายของเครื่องพัฒนายังเป็นรุ่นเก่า)
- `apps/web/src/app/privacy/page.tsx` — เพิ่มหัวข้อว่าผู้ให้บริการเห็นอะไรได้บ้าง
- `ONBOARDING.md` — เปลี่ยนขั้นตอนเปิดอู่จากคำสั่งเป็นคอนโซล เก็บคำสั่งไว้เป็นทางสำรอง

---

## ชุดทดสอบ

### ด่านตรวจสิทธิ์ — หัวใจของช่วงนี้

- **เรียกฟังก์ชัน `ops.*` โดยไม่มีโทเคน → ไม่ได้อะไรเลย** ทดสอบทุกฟังก์ชัน
  ไล่ชื่อฟังก์ชันจาก `pg_proc` ไม่ใช่รายการที่พิมพ์ไว้ ฟังก์ชันใหม่ที่ลืมตรวจจะแดงเอง
- โทเคนที่หมดอายุแล้ว → ไม่ได้อะไรเลย
- โทเคนของบัญชีที่ถูกปิด (`active = false`) → ไม่ได้อะไรเลย
- **คุกกี้ session ของอู่ ใช้เป็น session ผู้ให้บริการไม่ได้** และกลับกัน
- ทุกหน้าใต้ `/ops` เรียก `requireOperator()` — ไล่จากไฟล์จริงในโฟลเดอร์
  แบบเดียวกับ `perms-db.test.ts`
- `dgl_app` **select จากตาราง `ops.operators` / `ops.sessions` ตรง ๆ ไม่ได้**

### ขอบเขตข้อมูล

- `ops.list_shops()` คืนคอลัมน์ที่อนุญาตเท่านั้น — เทียบรายชื่อคอลัมน์จริงกับรายการที่อนุญาต
  แล้วยืนยันว่าไม่มีคำว่า `party` `doc` `amount` `price` `cost` โผล่มา
- ผู้ให้บริการเรียกฟังก์ชันของอู่ (`current_tenant_id()` ที่ยังไม่ได้ตั้ง) แล้วไม่เห็นข้อมูล

### พฤติกรรม

- เปิดอู่ใหม่แล้วล้มกลางคัน → **ไม่มีอู่ค้าง** (ทรานแซกชันเดียว)
- เปิดอู่ด้วยอีเมลที่ซ้ำกับเจ้าของอู่อื่น → ปฏิเสธพร้อมบอกเหตุผล
- ออกลิงก์ตั้งรหัสผ่านใหม่ → ลิงก์เก่าใช้ไม่ได้อีก
- ต่ออายุ → `expires_on` ต่อจากวันหมดอายุเดิม ไม่ใช่จากวันนี้ (ตาม `renewalWindow()` ที่มีอยู่)
- ลดที่นั่งพนักงานให้น้อยกว่าที่ใช้อยู่ → ทำได้ แต่**ไม่ล็อกใครออก** ตามคอมเมนต์ในสคีมา
- ล็อกอินผิด 5 ครั้ง → ล็อก 15 นาที และมีแถวใน `ops.audit`

### บันทึกการใช้งาน

- ทุกฟังก์ชันที่เปลี่ยนแปลงข้อมูลเขียน `ops.audit` — ไล่จาก `pg_proc` เช่นกัน
- ลบบัญชีผู้ให้บริการ → บันทึกยังอยู่ อ่านอีเมลได้
- ลบอู่ → บันทึกที่อ้างถึงอู่นั้นยังอยู่

### การกลายพันธุ์
1. ถอดการตรวจโทเคนออกจากฟังก์ชันหนึ่งตัว → เทสต์ "เรียกโดยไม่มีโทเคน" ต้องแดง
2. เพิ่มคอลัมน์ยอดขายเข้าไปใน `ops.list_shops` → เทสต์ขอบเขตข้อมูลต้องแดง
3. ถอดการเขียน `ops.audit` ออกจากฟังก์ชันหนึ่งตัว → เทสต์บันทึกต้องแดง

---

## วิธีตรวจว่าใช้ได้จริง

1. สร้างบัญชีแรกด้วย `tools/ops-admin.mjs` → เปิดลิงก์ → ตั้งรหัสผ่าน → ล็อกอิน
2. เปิดอู่ใหม่จากคอนโซล → ก๊อปลิงก์ → เปิดในหน้าต่างส่วนตัว → ตั้งรหัสผ่าน → เข้าใช้งานได้
3. **เปิดคอนโซลกับหน้าอู่พร้อมกันในเบราว์เซอร์เดียว** → ทั้งสองต้องทำงานแยกกัน
   ไม่เตะกัน และออกจากระบบฝั่งหนึ่งต้องไม่ทำให้อีกฝั่งหลุด
4. ล็อกอินคอนโซลแล้วลองเปิด URL ของหน้าอู่ตรง ๆ → ต้องถูกพาไปหน้าล็อกอินของอู่
   ไม่ใช่เข้าได้
5. ต่ออายุอู่ที่ยังไม่หมด → วันหมดอายุใหม่ต้องต่อท้ายของเดิม ไม่ใช่เริ่มจากวันนี้
6. ดู `ops.audit` ว่าครบทุกอย่างที่เพิ่งกดไป

---

## ที่ไม่ทำในช่วงนี้

- **2FA ของบัญชีผู้ให้บริการ** — ควรมี แต่ต้องมีที่เก็บ recovery code และหน้ากู้คืน
  ซึ่งเป็นงานอีกก้อน · ระหว่างนี้ใช้รหัสผ่านยาว 14 ตัวกับ session 1 วันไปก่อน
- **สวมสิทธิ์เข้าเป็นเจ้าของอู่** — ดูข้อ 5
- **ลบอู่จากคอนโซล** — ทำลายเกินกว่าจะเป็นปุ่มเดียว ยังทำผ่านคำสั่งพร้อมยืนยันสองชั้น
- **นำเข้าไฟล์สำรองของอู่ที่ย้ายมาผ่านคอนโซล** — ทำได้ แต่หน้ากู้คืนของอู่เองมีอยู่แล้ว
  ให้เจ้าของอู่ทำเองดีกว่า ผู้ให้บริการไม่ต้องถือไฟล์ข้อมูลลูกค้าไว้ในมือ
- **แจ้งเตือนอัตโนมัติเมื่อใกล้หมดอายุ** — รอระบบส่งอีเมลก่อน

---

## หลังช่วงนี้

| งาน | ประเมิน | รอใคร |
|---|---|---|
| ส่งอีเมล | 1 วัน | รอโดเมนยืนยันเสร็จ (เริ่มสมัครไว้ได้เลยตั้งแต่วันนี้) |
| ลืมรหัสผ่านแบบผู้ใช้กดเอง | 1 วัน | รอระบบส่งอีเมล |
| แจ้งเตือนใกล้หมดอายุ | ครึ่งวัน | รอระบบส่งอีเมล |

**ประเมินช่วงนี้ 2–3 วัน**
