# โครงการนำทางและการวางเลเยอร์ — เมนูไม่บังกัน ทุกขนาดจอ

**วัดของจริงก่อนเขียนแผน** เปิดหน้าจอที่ 375 · 768 · 1280 · 1920 แล้ววัดด้วยเครื่อง
ไม่ได้เดาจากโค้ด ตัวเลขข้างล่างคือของที่วัดได้จากเครื่องพัฒนา

---

## สิ่งที่เจอ — และทำไมมันสำคัญ

### 1. แถบเมนูต้องการจอกว้าง **1,784px** ถึงจะเห็นครบ

| ความกว้างจอ | เมนูที่หลุดออกนอกจอ |
|---|---|
| 375 (มือถือ) | **8 จาก 10** |
| 768 (แท็บเล็ต) | **5 จาก 10** |
| 1280 (โน้ตบุ๊กทั่วไป) | **3 จาก 10** |
| 1920 | 0 |

โน้ตบุ๊กที่คนใช้จริงคือ 1280–1512 **ทุกเครื่องซ่อนเมนูบางตัวอยู่** และไม่มีอะไรบอกว่ามีต่อ
ไม่มีลูกศร ไม่มีเงา ไม่มีจุด — แถบเลื่อนก็ถูกซ่อนด้วย `::-webkit-scrollbar { display: none }`
คนที่ไม่รู้ว่าปัดได้ จะไม่มีวันเจอเมนู 06 07 08

### 2. แผงเมนูหล่นล้นขอบขวา **ทุกเมนู** บนมือถือ

วัดได้ว่าแผงกว้าง 324px แต่โค้ดที่ดันกลับเข้าจอใช้ค่า `width = 290` ที่พิมพ์ไว้ตายตัว
เลยดันไม่พอ ล้นออกไป **26px** ทุกเมนู

### 3. แผงเมนูหล่นทับปุ่มหลักของหน้า

บนมือถือ เปิดเมนูใดก็ตามแล้วแผงทับปุ่ม **"+ ใบเสร็จ"** ที่หัวหน้าพอดี
(วัดด้วยการเทียบกรอบสองอันแล้วซ้อนกันจริง) — ตรงกับข้อที่คุณยกมาเป๊ะ

### 4. ปุ่มย้อนกลับลอยทับของที่กดได้

ปุ่มลอยอยู่มุมซ้ายล่างตลอดเวลา `z-index: 60` สูงกว่าทุกอย่างยกเว้นแผงเมนู
สิ่งที่อยู่ใต้มันตอนวัด — **มือถือ:** แถบตัวกรองช่วงวันที่ · **แท็บเล็ต:** ช่องเลขที่เอกสารในตาราง
(ซึ่งเป็นลิงก์เปิดเอกสาร) มันไม่ได้ทับที่ว่าง มันทับปุ่มกับลิงก์

### 5. หัวหน้าไม่ติดขอบบน — ปุ่มหลักหายไปตอนเลื่อน

`.topbar` เป็น `static` เลื่อนลง 600px แล้วทั้งชื่อหน้าและปุ่มหลักหายหมด
เหลือแค่แถบเมนูที่ติดอยู่ บนมือถือที่จอสั้น รายการยาว ๆ ต้องเลื่อนกลับขึ้นไปสุดทุกครั้งที่จะกดปุ่ม

### 6. เป้ากดเล็กกว่ามาตรฐาน 96 จุดบนมือถือ

ปุ่มเมนูสูง 32–35px ปุ่มในแถวตาราง (`.btn.sm`) 60 ปุ่ม เล็กกว่า 44px ทั้งหมด
กติกาที่มีอยู่ (`.btn { min-height: 40px }` ที่ ≤560px) ไม่ครอบคลุม `.navbtn` และถูก `.btn.sm` ทับ

### 7. ปุ่มออกจากระบบหน้าตาเหมือนเมนู และอยู่ในแถวเดียวกัน

`ออกจากระบบ` ใช้คลาส `.navbtn` เหมือนปุ่มเมนูทุกประการ อยู่ท้ายแถบที่เลื่อนได้
บนมือถือที่ปัดแถบไปมา การกดโดนคือการถูกเด้งออกจากระบบทันทีโดยไม่มีการถาม

> ข้อนี้ผมเจอโดยบังเอิญ — สคริปต์ที่ผมเขียนไว้วัดเมนูไล่กดปุ่ม `.navbtn` ทุกปุ่ม
> แล้วมันกดปุ่มออกจากระบบเข้า เซสชันหลุดกลางการวัด ถ้าเครื่องยังแยกไม่ออก คนก็แยกไม่ออก

### 8. จุดตัดขนาดจอมีสี่ค่า และซ้อนกันเอง

`560` · `820` · `980` · `1000` — สองค่ากลางต่างกัน 20px โดยไม่มีเหตุผล
และมีกฎที่ตายแล้วหนึ่งข้อ (`.app { flex-direction: column }` ทั้งที่ `.app` เป็น `display: block`)

---

## สิ่งที่จะทำ

### ก. จุดตัดขนาดจอ — เหลือสามค่า มีชื่อ และอยู่ที่เดียว

| ชื่อ | ช่วง | อุปกรณ์จริง |
|---|---|---|
| **mobile** | `< 768px` | มือถือ ทั้งแนวตั้งและแนวนอน |
| **tablet** | `768px – 1279px` | ไอแพด แท็บเล็ตหน้าเคาน์เตอร์ โน้ตบุ๊กเล็ก |
| **desktop** | `≥ 1280px` | คอมพิวเตอร์ในห้องบัญชี |

```css
:root {
  --bp-tablet: 768px;    /* ตั้งแต่นี้ขึ้นไป = tablet */
  --bp-desktop: 1280px;  /* ตั้งแต่นี้ขึ้นไป = desktop */
}
```

เลือก 768 กับ 1280 เพราะเป็นค่าที่ตรงกับอุปกรณ์จริง (iPad = 768 พอดี · โน้ตบุ๊ก 13" = 1280)
ไม่ใช่เลขที่คิดเอง ส่วน 560/820/980/1000 เดิมจะถูกแทนที่ทั้งหมด
**มีเทสต์บังคับว่าในไฟล์ CSS ห้ามมี `@media` ที่ใช้ค่าอื่นนอกจากสองค่านี้**
(ยกเว้น `print`) — ไม่งั้นอีกหกเดือนจะมีค่าที่ห้ากลับมา

### ข. รูปแบบการนำทางต่ออุปกรณ์

| | mobile `<768` | tablet `768–1279` | desktop `≥1280` |
|---|---|---|---|
| เมนูหลัก | **แถบล่าง 5 ช่อง** + ปุ่ม "เพิ่มเติม" | แถบบน + **ลิ้นชักซ้าย** กดเรียก | **แถบซ้ายเต็ม** เห็นทุกเมนูพร้อมกัน |
| เมนูย่อย | ในลิ้นชักเดียวกัน (ชั้นที่สอง) | การ์ดใต้หัวหน้า | คอลัมน์ซ้ายในหน้า (แบบที่มีอยู่) |
| ปุ่มหลักของหน้า | ติดขอบล่างเหนือแถบล่าง | ในหัวหน้าที่ติดขอบบน | ในหัวหน้าที่ติดขอบบน |

**มือถือ — แถบล่าง 5 ช่องในระยะนิ้วโป้ง**

ห้าช่องคือเมนูที่อู่ใช้บ่อยที่สุด — หน้าแรก · รายรับ · รายจ่าย · สินค้า · เพิ่มเติม
ช่อง "เพิ่มเติม" เปิดลิ้นชักที่มีเมนูที่เหลือทั้งหมด (ลูกค้า · บัญชี · ตั้งค่า · ลิขสิทธิ์ · แบบฟอร์ม · ออกจากระบบ)

ทำไมแถบล่างไม่ใช่แฮมเบอร์เกอร์บนสุด — อู่ถือโทรศัพท์ยืนอยู่หน้ารถ ใช้มือเดียว
มุมบนซ้ายของจอ 6 นิ้วคือจุดที่นิ้วโป้งเอื้อมไม่ถึงโดยไม่ขยับมือ

**เดสก์ท็อป — กลับไปเป็นแถบซ้าย**

เมนูสิบตัวไม่มีวันพอดีแถวเดียวในแนวนอน (ต้องการ 1,784px) แต่ในแนวตั้งพอสบาย
รุ่น 3.6 เคยเป็นแถบซ้าย รุ่น 6.4 ย้ายขึ้นบน — **เราจะกลับไปเป็นซ้ายเฉพาะจอกว้าง**
เพราะบนจอกว้างแนวตั้งคือทิศที่มีที่เหลือ ส่วนบนแท็บเล็ตและมือถือยังเป็นแนวนอนตามเดิม

> ผู้ใช้เดิมจะเจอเมนูย้ายที่ **เลขกำกับ (01–08) ยังอยู่ครบทุกปุ่ม** และเรียงลำดับเดิม
> คนที่จำว่า "03 คือรายรับ" ยังใช้ความจำเดิมได้ แค่มองคนละทิศ

### ค. กติกาการวางเลเยอร์ — เขียนไว้ที่เดียว ห้ามใส่ตัวเลขมั่ว

```css
--z-content:  0;    /* เนื้อหาปกติ */
--z-sticky:  10;    /* หัวตาราง แถบเครื่องมือที่ติดขอบ */
--z-nav:     20;    /* แถบเมนูหลัก แถบล่าง */
--z-header:  30;    /* หัวหน้าที่ติดขอบบน */
--z-overlay: 40;    /* ฉากหลังทึบของลิ้นชัก/กล่องโต้ตอบ */
--z-drawer:  50;    /* ลิ้นชัก แผงเมนูหล่น กล่องโต้ตอบ */
--z-toast:   60;    /* ข้อความแจ้งผล */
```

**กติกาสามข้อที่จะบังคับด้วยเทสต์**

1. **ห้ามมี `z-index` เป็นตัวเลขดิบในไฟล์ CSS** ต้องใช้ตัวแปรพวกนี้เท่านั้น
2. **ของที่ลอยทับต้องมีฉากหลัง** — ลิ้นชักและแผงเมนูต้องมี backdrop ที่กดแล้วปิด
   ตอนนี้แผงเมนูหล่นลอยอยู่เฉย ๆ ไม่มีฉากหลัง จึงดูเหมือนส่วนหนึ่งของหน้า
3. **ห้ามลอยทับปุ่มหลัก** — เปิดของที่ลอยแล้วปุ่มหลักของหน้าต้องยังกดได้ หรือถูกปิดไปพร้อมกัน
   ไม่ใช่ถูกบังครึ่งเดียวแบบตอนนี้

### ง. แก้ของที่ทับกันอยู่ตอนนี้

| ปัญหา | วิธีแก้ |
|---|---|
| แผงเมนูล้นขอบขวา 26px | วัดความกว้างจริงจาก DOM แทนเลข 290 ที่พิมพ์ไว้ · มือถือให้เต็มความกว้างจอลบขอบ |
| แผงทับปุ่มหลัก | มือถือเปลี่ยนเป็นลิ้นชักเต็มจอ ไม่ใช่แผงลอย · แท็บเล็ต/เดสก์ท็อปวางใต้ปุ่มโดยไม่ทับแถบหัวหน้า |
| ปุ่มย้อนกลับทับเนื้อหา | **มือถือ/แท็บเล็ต — เอาออก** ใช้ปุ่มย้อนกลับของเครื่องและปุ่ม "← กลับ" ที่หัวหน้าแทน<br>เดสก์ท็อป — ย้ายไปอยู่ในหัวหน้า ไม่ลอย |
| หัวหน้าเลื่อนหาย | ทำให้ติดขอบบนใต้แถบเมนู ปุ่มหลักอยู่กับที่เสมอ |
| เป้ากดเล็ก | ทุกอย่างที่กดได้ขั้นต่ำ **44×44px** บนมือถือและแท็บเล็ต รวม `.navbtn` และ `.btn.sm` |
| ออกจากระบบปนกับเมนู | ย้ายไปอยู่ในลิ้นชัก/ท้ายแถบซ้าย หน้าตาต่างจากเมนู และถามยืนยันก่อน |

---

## เทสต์ที่ต้องมี

เทสต์ CSS/เลย์เอาต์ต้องเป็นเทสต์ที่**ตรวจของจริง** ไม่ใช่ตรวจว่ามีคลาสอยู่ในไฟล์

| เรื่อง | พิสูจน์อะไร |
|---|---|
| จุดตัดขนาดจอ | ไฟล์ CSS ไม่มี `@media` ที่ใช้ค่านอกจาก 768/1280 (ยกเว้น print) |
| ตัวแปรเลเยอร์ | ไม่มี `z-index` เป็นตัวเลขดิบในไฟล์ CSS |
| ลำดับเลเยอร์ | ค่าที่ตั้งไว้เรียงจากน้อยไปมากตามที่ประกาศ ไม่มีตัวไหนแซงตัวที่ควรอยู่บนกว่า |
| เมนูครบทุกขนาดจอ | ที่ 375/768/1280 ไม่มีปุ่มเมนูตัวไหนอยู่นอกกรอบที่มองเห็น |
| ไม่ทับปุ่มหลัก | เปิดเมนูทุกตัวที่ทุกขนาดจอ แล้วกรอบแผงต้องไม่ซ้อนกับปุ่มหลักของหน้า |
| ไม่ล้นขอบจอ | กรอบแผง/ลิ้นชักอยู่ในจอครบทั้งสี่ด้าน |
| เป้ากด | ทุกอย่างที่กดได้บนมือถือสูงและกว้างอย่างน้อย 44px |
| ปิดได้ | กดฉากหลังหรือ Esc แล้วของที่ลอยปิด และโฟกัสกลับไปที่ปุ่มที่เปิดมัน |

**เทสต์หกข้อล่างต้องรันบนเบราว์เซอร์จริง** ไม่ใช่อ่านไฟล์ CSS — ตอนนี้ชุดทดสอบไม่มีเครื่องมือนี้
จึงต้องเพิ่มตัวรันเบราว์เซอร์เข้ามาในโครงการ (Playwright) ซึ่งเป็นของใหม่ก้อนหนึ่ง
**ถ้าไม่อยากเพิ่ม** ผมจะเขียนเป็นสคริปต์ตรวจที่รันด้วยมือตอน deploy แทน แล้วเทสต์อัตโนมัติ
จะคุมได้แค่สามข้อแรก (ซึ่งเป็นการอ่านไฟล์ CSS) — บอกได้ว่าจะเอาทางไหน

---

## ที่ไม่ทำในก้อนนี้

- **เปลี่ยนสีหรือฟอนต์** — เรื่องนี้เป็นโครงสร้างการนำทางล้วน ไม่แตะหน้าตา
- **เปลี่ยนผังเมนูหรือเลขกำกับ** — ผู้ใช้เดิมจำเลขได้ ห้ามขยับ
- **ทำให้ตารางเลื่อนแนวนอนสวยขึ้นบนมือถือ** — เป็นคนละเรื่อง แยกก้อนได้
- **โหมดมืด** — ไม่เคยมีและยังไม่มีใครขอ

## เสร็จเมื่อ

- [ ] จุดตัดเหลือสองค่า มีชื่อ อยู่ที่เดียว และมีเทสต์กัน
- [ ] มือถือมีแถบล่าง 5 ช่องในระยะนิ้วโป้ง เมนูที่เหลืออยู่ในลิ้นชัก
- [ ] แท็บเล็ตมีแถบบน + ลิ้นชักซ้าย
- [ ] เดสก์ท็อปมีแถบซ้ายเต็มที่เห็นเมนูครบโดยไม่ต้องเลื่อน
- [ ] เปิดเมนูทุกตัวที่ทุกขนาดจอแล้วไม่ทับปุ่มหลักและไม่ล้นขอบจอ
- [ ] ปุ่มย้อนกลับลอยไม่ทับของที่กดได้อีก
- [ ] หัวหน้าติดขอบบน ปุ่มหลักอยู่กับที่ตอนเลื่อน
- [ ] เป้ากดบนมือถือ ≥ 44px ทุกจุด
- [ ] `npm test` เขียว · เปิดหน้าจอดูจริงที่ 375 · 768 · 1280 · 1920

---

## สรุปสิ่งที่ต้องการคำตอบ

1. **เดสก์ท็อปกลับไปเป็นแถบเมนูซ้าย** (จอ ≥1280) — โอเคไหม
   เป็นการเปลี่ยนที่ผู้ใช้เดิมเห็นทันที แต่เป็นทางเดียวที่เมนูสิบตัวจะพอโดยไม่ต้องเลื่อน
   ถ้าอยากให้อยู่ด้านบนเหมือนเดิม ทางออกคือยุบเมนูที่ใช้น้อยไปไว้ใต้ปุ่ม "เพิ่มเติม"
2. **มือถือใช้แถบล่าง 5 ช่อง** — ห้าช่องที่ผมเลือกคือ หน้าแรก · รายรับ · รายจ่าย · สินค้า · เพิ่มเติม
   ตรงกับที่อู่ใช้บ่อยที่สุดไหม หรืออยากสลับตัวไหน
3. **เพิ่ม Playwright เข้าโครงการ** เพื่อให้เทสต์เรื่องการทับกันเป็นอัตโนมัติ — เอาไหม
   หรือให้เป็นสคริปต์ตรวจด้วยมือตอน deploy
