# ช่วงที่ 3 — ใบวางบิล (03.4)

แผนละเอียดสำหรับช่วงที่ 3 ของ [PLAN-6.4.md](PLAN-6.4.md) · ประเมิน 3–4 วัน

---

## Context

ลูกค้าองค์กรไม่จ่ายทีละใบ — อู่ส่งงานให้ตลอดเดือน แล้วสิ้นเดือนรวมใบที่ค้างทั้งหมด
ทำเป็น "ใบวางบิล" ใบเดียวส่งไปที่แผนกการเงิน นัดวันรับเช็ค แล้วรับเงินก้อนเดียว
เป็นขั้นตอนมาตรฐานของงาน B2B ในไทย ซึ่งรุ่น 6.4 เพิ่มเข้ามาเป็นแท็บ 03.4

เว็บเรายังไม่มีเลย อู่ที่มีลูกค้าเป็นบริษัทจึงยังใช้แทนของเดิมไม่ได้

เป็นโมดูลใหม่ตัวแรกที่ทำหลังเปลี่ยนเปลือก UI เสร็จ — หน้าจอที่เขียนในช่วงนี้
จึงอยู่บนหน้าตาชุดสุดท้ายแล้ว ไม่ต้องรื้อ

---

## สิ่งที่ต้องเข้าใจให้ตรงก่อน

**ใบวางบิลไม่ใช่เอกสารทางบัญชี** — คำพูดของ 6.4 เองบนหน้าจอ:

> เป็นเอกสารแจ้งเก็บเท่านั้น **ไม่ตั้งลูกหนี้ซ้ำและไม่นับรายได้** —
> เมื่อได้รับเงินแล้วให้ออกใบเสร็จรับเงินตามปกติ

ตรวจแล้วว่าจริง: ค้นทั้งไฟล์ 6.4 ไม่มีจุดไหนที่ `billnotes` เข้าไปในรายงานยอดขาย
ลูกหนี้ ภาษี หรือหน้าแรกเลย มันเป็นแค่ใบปะหน้าที่รวมใบอื่นไว้ด้วยกัน

**ผลต่อการออกแบบ** ตาราง `documents` ของเราผูกกับยอดเงิน ภาษี และการนับรายได้ทั้งหมด
ถ้าเอาใบวางบิลใส่เข้าไปเป็น `doc_kind` ใหม่ ต้องไล่ยกเว้นมันทุกจุดที่นับเงิน
ซึ่งเป็นการเพิ่มโอกาสพลาดในที่ที่พลาดแล้วเจ็บที่สุด

**จึงแยกเป็นตารางของตัวเอง** ดูเหตุผลข้อ 1 ข้างล่าง

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

**1. ตารางแยก ไม่ใช่ `doc_kind` ใหม่**

`documents` มีคอลัมน์เงิน 10 ช่อง ภาษี ลูกหนี้ และถูกอ่านโดย `SALES_DOCS`
งบกำไรขาดทุน ลูกโซ่ภาษี และหน้าแรก ใบวางบิลไม่มียอดของตัวเอง —
ยอดของมันคือผลรวมยอดค้างของใบอื่น ณ เวลาที่ถาม

ใส่เข้า `documents` แล้วต้องเขียน `and kind <> 'BN'` เพิ่มในทุกคิวรีที่นับเงิน
ลืมที่เดียวคือรายได้ซ้ำสองเท่า ตารางแยกทำให้ลืมไม่ได้เลยโดยโครงสร้าง

```sql
create table billnotes (
  id, tenant_id, no,                    -- BN-YYYYMM-NNN
  bill_date, due_date,                  -- วันวางบิล · วันนัดรับเงิน
  party_id, party_name, party_tax_id, party_addr_text,   -- snapshot เหมือนเอกสารอื่น
  by_whom, note,
  total_snapshot numeric(14,2),         -- ยอด ณ วันที่บันทึก ไว้เทียบย้อนหลัง
  status doc_status, voided_at, voided_reason,
  created_by, created_at, updated_at
);

create table billnote_docs (
  billnote_id, doc_id, tenant_id,
  primary key (billnote_id, doc_id)
);
```

**2. ใบแจ้งหนี้หนึ่งใบอยู่ในใบวางบิลที่ยังไม่ยกเลิกได้ใบเดียว**

6.4 บังคับข้อนี้ตอนจะยกเลิกเอกสาร (`voidBlockReason`) — ถ้าใบอยู่ในใบวางบิลอยู่
ต้องเอาออกก่อน เราบังคับที่ฐานข้อมูลด้วย partial unique index
เพราะการวางบิลซ้ำสองใบคือการทวงเงินก้อนเดียวสองรอบ ซึ่งเสียความน่าเชื่อถือกับลูกค้าองค์กร

```sql
create unique index billnote_doc_once on billnote_docs (tenant_id, doc_id)
  where <ใบวางบิลนั้นยังไม่ถูกยกเลิก>;
```
เงื่อนไข "ยังไม่ยกเลิก" อ้างตารางอื่นไม่ได้ใน partial index จึงเก็บ `voided boolean`
ซ้ำไว้ใน `billnote_docs` แล้วอัปเดตพร้อมกันตอนยกเลิก — ยอมเก็บซ้ำเพื่อให้ฐานข้อมูลบังคับได้เอง

**3. ยอดคิดสด ไม่ใช่ยอดที่แช่ไว้**

`billTotal` ของ 6.4 คำนวณใหม่ทุกครั้งจากยอดค้างปัจจุบัน ลูกค้าจ่ายบางใบไปแล้ว
ยอดบนหน้าจอลดตาม เก็บ `total_snapshot` ไว้ด้วยเพื่อให้รู้ว่าตอนวางบิลแจ้งไปเท่าไร
แต่หน้าจอและใบที่พิมพ์ใหม่ใช้ยอดสด

**4. รับชำระหลายใบพร้อมกันอยู่ในช่วงนี้ด้วย**

ตรวจแล้วว่า `bulkPayModal` ของ 6.4 อยู่ที่หน้าลูกหนี้/เจ้าหนี้ (06.2/06.3) ไม่ใช่หน้าวางบิล
แต่มันคือครึ่งหลังของงานเดียวกัน — วางบิลแปดใบแล้วลูกค้าจ่ายเช็คใบเดียว
ถ้าไม่มี ต้องเปิดตัดชำระทีละใบแปดรอบ ทำให้ใบวางบิลเสียประโยชน์ไปครึ่งหนึ่ง

กติกาที่ยกมา: จำนวนตั้งต้น = ยอดค้างของใบนั้น · แก้รายใบได้ ·
**ยอดที่เกินยอดค้างถูกปรับลงให้พอดี** · ใบที่กรอก 0 ถูกข้าม · วันที่/ช่องทาง/เลขอ้างอิงใช้ร่วมกัน

---

## แก้ฐานข้อมูล — `db/004_billnotes.sql`

- สองตารางข้างบน พร้อม RLS แบบ force ทั้งคู่ (เทสต์การแยกอู่จะจับเองถ้าลืม)
- index `billnotes (tenant_id, bill_date desc)` และ `billnote_docs (tenant_id, doc_id)`
- เพิ่ม `'BN'` เข้า `doc_sequences.kind`? **ไม่** — `doc_sequences.kind` เป็น `doc_kind`
  ซึ่งเราตั้งใจไม่เพิ่ม BN เข้าไป ใช้ตารางเลขที่ของตัวเองหรือเก็บใน `doc_sequences`
  ด้วย kind แยก — **เลือกทางแรก** เพราะการแก้ enum `doc_kind` เพื่อเลขที่อย่างเดียว
  จะทำให้ทุกคิวรีที่ `switch` บน kind ต้องคิดถึง BN ทั้งที่ไม่เกี่ยว

  ```sql
  create table billnote_sequences (tenant_id, period text, last_no int);
  ```

เหมือน migration 003 — ใส่ในไฟล์ `001_init.sql` ด้วยสำหรับการติดตั้งใหม่
และมี `004` แยกไว้สำหรับฐานที่มีอยู่แล้ว

---

## ไฟล์ที่จะแตะ

### สร้างใหม่

| ไฟล์ | หน้าที่ |
|---|---|
| `db/004_billnotes.sql` | migration สำหรับฐานเดิม |
| `lib/billnotes.ts` | อ่าน/เขียนใบวางบิล · `unpaidInvoicesOf()` · `billTotal()` · ยกเลิก |
| `app/income/billing/page.tsx` | รายการใบวางบิล (03.4) |
| `app/income/billing/[id]/page.tsx` | ฟอร์มออก/แก้ใบวางบิล |
| `app/income/billing/[id]/print/page.tsx` | ใบวางบิลที่พิมพ์ |
| `app/income/billing/bill-form.tsx` | ฟอร์มฝั่ง client — เลือกลูกค้า ติ๊กใบ |
| `app/income/billing/actions.ts` | server action บันทึก/ยกเลิก |
| `app/finance/bulk-pay.tsx` | รับชำระหลายใบพร้อมกัน |

### แก้

| ไฟล์ | ทำอะไร |
|---|---|
| `components/menu-map.ts` | เอา `todo: true` ออกจาก 03.4 |
| `lib/sales.ts` | `voidSalesDoc` ปฏิเสธถ้าใบอยู่ในใบวางบิลที่ยังไม่ยกเลิก |
| `app/income/[id]/page.tsx` | แสดงว่าใบนี้อยู่ในใบวางบิลใบไหน พร้อมลิงก์ |
| `lib/receivables.ts` | `recordBulkPayments()` — หลายใบในทรานแซกชันเดียว |
| `app/finance/ar/page.tsx` · `ap/page.tsx` | ติ๊กเลือกหลายใบ + ปุ่มรับ/จ่ายชำระรวม |
| `lib/backup.ts` · `packages/importer` | ส่งออกและนำกลับเข้าได้ · เอา `billnotes` ออกจากรายการที่เตือนว่ารองรับไม่ได้ |

---

## ลำดับการทำ

1. **ฐานข้อมูล + `lib/billnotes.ts` + เทสต์ (1 วัน)** — ตรรกะทั้งหมดพร้อมเทสต์ ก่อนมีหน้าจอ
2. **หน้ารายการและฟอร์ม (1–1.5 วัน)**
3. **ใบวางบิลที่พิมพ์ (0.5 วัน)** — ตามหน้ากระดาษของ 6.4 รวมแถวว่างเติมให้ครบแปดบรรทัด
4. **รับชำระหลายใบพร้อมกัน (0.5–1 วัน)**
5. **สำรอง/กู้คืน + เอาออกจากรายการเตือน (0.5 วัน)**

---

## ชุดทดสอบ

**`billnotes.test.ts` (ฐานข้อมูลจริง)**
- ยอดใบวางบิล = ผลรวมยอดค้างของใบข้างในเสมอ — รับชำระบางใบแล้วยอดลดตาม
- ใบที่จ่ายครบแล้วยังอยู่ในใบวางบิล แต่ไม่นับยอด
- **ใบแจ้งหนี้ใบเดียวกันใส่ใบวางบิลที่สองไม่ได้** — ฐานข้อมูลปฏิเสธ
- ยกเลิกใบวางบิลแล้วใบข้างในกลับไปวางบิลใหม่ได้
- ยกเลิกใบแจ้งหนี้ที่อยู่ในใบวางบิลไม่ได้ ต้องเอาออกก่อน
- **ใบวางบิลไม่โผล่ในยอดขาย ภาษี ลูกหนี้ หรือกำไรขาดทุน** — ข้อนี้สำคัญที่สุด
  ออกใบวางบิลรวม 5 ใบแล้วยอดขายกับลูกหนี้ต้องเท่าเดิมเป๊ะ

**`bulk-pay.test.ts`**
- จ่ายสามใบพร้อมกัน ยอดค้างแต่ละใบลดถูกต้อง
- ยอดที่กรอกเกินยอดค้างถูกปรับลงให้พอดี ไม่เกิดยอดจ่ายเกิน
- กรอก 0 แล้วใบนั้นไม่มีรายการชำระเพิ่ม
- ทั้งชุดอยู่ในทรานแซกชันเดียว — ใบสุดท้ายพังแล้วใบแรกต้องไม่ถูกบันทึก

**เดิมต้องไม่พัง** — 284 ข้อผ่านครบ

---

## เสร็จเมื่อ

- ออกใบวางบิลจากใบค้างของลูกค้าหนึ่งราย พิมพ์ได้ และเปิดกลับมาแก้ได้
- ใบแจ้งหนี้ใบหนึ่งอยู่ในใบวางบิลที่ยังไม่ยกเลิกได้ใบเดียว บังคับที่ฐานข้อมูล
- รับชำระหลายใบพร้อมกันจากหน้าลูกหนี้ได้
- **ยอดขาย ภาษี ลูกหนี้ และกำไรขาดทุนไม่เปลี่ยนเลยหลังออกใบวางบิล** มีเทสต์ยืนยัน
- ไฟล์สำรองพาใบวางบิลไปด้วย และนำกลับเข้ามาได้ครบ
- แท็บ 03.4 ไม่จางแล้ว

---

## ที่ไม่ทำในช่วงนี้

- **ใบวางบิลข้ามลูกค้า** — 6.4 บังคับลูกค้าเดียวต่อใบ เราทำตาม
- **ใบลดหนี้ / ใบเพิ่มหนี้** — ไม่มีในต้นแบบทั้งสองรุ่น
- **ติดตามสถานะการวางบิล** (ส่งแล้ว/รับเช็คแล้ว) — 6.4 ไม่มี ใช้ช่องหมายเหตุแทน

---

# ผลที่ทำจริง

**ครบทุกข้อใน "เสร็จเมื่อ"** · เทสต์ 312 ข้อผ่าน (เดิม 284) · build ผ่าน

## ใบวางบิล

- `db/001_init.sql` + `db/004_billnotes.sql` — `billnotes` · `billnote_docs` ·
  `billnote_sequences` · `next_billnote_no()`
  กฎ "ใบเดียวอยู่ในใบวางบิลที่ยังไม่ยกเลิกได้ใบเดียว" บังคับที่ฐานด้วย
  `create unique index billnote_doc_once on billnote_docs (tenant_id, doc_id) where not voided;`
  ต้องคัดลอก `voided` มาไว้ที่ `billnote_docs` ด้วย เพราะ partial index อ้างตารางอื่นไม่ได้
- `lib/billnotes.ts` · หน้ารายการ / หน้าใบ / หน้าพิมพ์ · แท็บ 03.4 ไม่จางแล้ว
- `voidSalesDoc` ปฏิเสธการยกเลิกใบที่อยู่ในใบวางบิลที่ยังไม่ยกเลิก
- ยอดบนหน้าจอคิดสดจากยอดค้างปัจจุบัน ส่วน `total_snapshot` คือยอดที่แจ้งไปวันวางบิล

## รับชำระหลายใบพร้อมกัน

- `lib/bulk-pay.ts` (ตรรกะล้วน รับ client) + ตัวห่อ `recordBulkPayments` ใน `receivables.ts`
- แผง `finance/bulk-pay.tsx` อยู่บนหน้าลูกหนี้และเจ้าหนี้ ใช้แถวที่หน้ากรองมาให้แล้ว
  กรองต่อรายลูกค้า/ผู้ขายได้ · วันที่ ช่องทาง เลขอ้างอิงใช้ร่วมกันทุกใบ
- ปุ่ม **รับชำระตามใบวางบิลนี้** บนหน้าใบวางบิล พาไป `/finance/ar?bulk=1&q=<ชื่อลูกค้า>`
- ต่างจากการตัดทีละใบตรงที่**ยอดเกินถูกปรับลงให้พอดี**แทนที่จะฟ้องกลับ
  เพราะยอดตั้งต้นมาจากยอดค้าง ณ ตอนเปิดหน้า ระหว่างนั้นอาจมีคนอื่นตัดไปแล้ว
  แต่คืนรายการที่ปรับกลับไปเสมอ ไม่ให้การปรับเงียบหาย

## ที่พบระหว่างทาง — สองจุดที่เป็นบั๊กจริง

**1. ตัวนำเข้าไม่ได้อ่านใบวางบิลเลย** แต่ถูกถอดออกจากรายการ "ยังรองรับไม่ได้" ไปแล้ว
แปลว่าอู่ที่ย้ายจาก 6.4 เข้ามาจะเสียใบวางบิลไปเงียบ ๆ ซึ่งเป็นสิ่งที่ไฟล์นั้นมีไว้กันโดยตรง
ทำให้เสร็จแล้ว — `import.ts` แปลง `DB.billnotes` เข้าสามตาราง พร้อมรับมือกับ
ไฟล์จริงที่รุ่นเดิมไม่เคยบังคับความสะอาด: เลขที่ซ้ำ · ใบเดียวถูกวางบิลสองใบ ·
อ้างเอกสารที่ถูกลบไปแล้ว · `id` เดิมซ้ำข้ามอาเรย์ — ทุกกรณีเก็บของไว้ให้มากที่สุดแล้วเตือน
`backup.ts` ส่งตัวนับ `seq.bn` ออกไปด้วย ไม่งั้นใบวางบิลใบถัดไปหลังกู้คืนจะทับเลขเดิม

**2. กู้คืนทับอู่ที่เคยวางบิลไว้จะล้มทั้งชุด** — `WIPE_ORDER` ไม่ได้ล้างใบวางบิลก่อนลบเอกสาร
และ `billnote_docs.doc_id` เป็น `on delete restrict` พังตอนผู้ใช้กดปุ่มจริงเท่านั้น
พิสูจน์แล้วด้วยการถอดสามตารางออกจาก `WIPE_ORDER` — เทสต์แดงทันทีด้วย foreign key

## ชุดทดสอบ

- `billnotes.test.ts` 11 ข้อ · `bulk-pay.test.ts` 10 ข้อ
- `hard-cases.test.ts` +6 ข้อ — ไฟล์ 6.4 ที่ไม่สะอาด
- `backup-roundtrip.test.ts` +2 · `restore.test.ts` +1 · `tenant-isolation.test.ts` ครอบสามตารางใหม่
- **กลายพันธุ์แล้วเทสต์ต้องแดง** ตรวจสามจุด: `commit` ทีละใบ · ไม่ปรับยอดลง ·
  ไม่ล้างใบวางบิลก่อนกู้คืน
  จุดแรกจับบั๊กในเทสต์เอง — เดิมให้ใบแรกพังไปพร้อมใบหลัง (วันที่ใช้ร่วมกัน)
  จึงพิสูจน์อะไรไม่ได้ แก้เป็นวางกับดักที่ใบที่สองใบเดียว
