# แผน — ตั้งรหัสผ่านตามลิงก์แล้วเข้าระบบให้เลย (ฝั่งอู่)

branch `fix/setup-autologin-2569-09-16` (แตกจาก `main` = `8d65976`) · **อนุมัติแล้ว 16 ก.ย. 2569** · ไม่ merge จนกว่าอนุมัติอีกรอบ

## อาการ (ผู้ใช้ทดสอบหลัง deploy)

ตั้งรหัสผ่านตามลิงก์เสร็จ ระบบไม่เข้าให้ — เด้งออกมาหน้าล็อกอินให้กรอกใหม่

รอบก่อนแก้ให้ "ออกจากบัญชีเดิมก่อนแล้วไปหน้าล็อกอิน" ซึ่งกันไม่ให้หลุดเข้าอู่อื่นได้แล้ว
แต่ผู้ใช้ต้องพิมพ์อีเมลกับรหัสซ้ำอีกรอบทั้งที่เพิ่งตั้งเอง

## แก้ (ผู้ใช้เลือก: ฝั่งอู่เข้าให้เลย · ฝั่งคอนโซลคงเดิม)

| จุด | แก้เป็น |
|---|---|
| `/setup/[token]` (อู่) | ตั้งรหัสสำเร็จ → เพิกถอนเซสชันเดิมที่ค้าง → **เข้าระบบด้วยบัญชีของลิงก์** → หน้าแรกของอู่นั้น |
| `/ops/setup/[token]` (คอนโซล) | **คงเดิม** — ออกจากบัญชีเดิมแล้วไปหน้าล็อกอินของคอนโซล (ผู้ใช้เลือก) |
| `lib/auth.ts` | แยกส่วนสร้างเซสชัน (ออกโทเคน · บันทึกลงฐาน · ตั้งคุกกี้) เป็นฟังก์ชันใช้ร่วม — ใช้ทั้งตอนล็อกอินปกติและตอนตั้งรหัสเสร็จ ไม่เขียนซ้ำสองที่ |
| `consumeSetupToken` | คืน id ของผู้ใช้แทน true/false (ฟังก์ชันในฐานคืนมาอยู่แล้ว) เพื่อเอาไปเปิดเซสชันต่อ |

**ความปลอดภัย**: คนที่เปิดลิงก์ได้คือคนที่ถือโทเคนใช้ครั้งเดียว หมดอายุ 7 วัน และเพิ่งตั้งรหัสผ่านเอง
เซสชันเก่าของอู่อื่นในเบราว์เซอร์ยังถูกเพิกถอนในฐานข้อมูลเหมือนเดิม (ไม่ใช่แค่ลบคุกกี้)

## เทสต์
- e2e (ปรับของเดิมตามความต้องการใหม่ ไม่ใช่ผ่อนเทสต์): ล็อกอินอู่ A ค้างไว้ → เปิดลิงก์ของอู่ B → ตั้งรหัสเสร็จ
  ต้องเข้าหน้าแรกของ **อู่ B** ทันทีโดยไม่ต้องกรอกอะไร · แถบบนเป็นชื่ออู่ B
- เซสชันเดิมของอู่ A ต้องถูกเพิกถอนในฐานข้อมูลจริง (ไม่ใช่แค่คุกกี้หาย)
- ฝั่งคอนโซลยังพาไปหน้าล็อกอินเหมือนเดิม (ด่านไฟล์เดิมยังอยู่)
- ย้อนการแก้แล้วต้องแดง

## ตรวจรวม
typecheck · `npm test` · e2e ครบบน production build · ไม่มีไมเกรชันใหม่ · §13.7 ปรับตาม
