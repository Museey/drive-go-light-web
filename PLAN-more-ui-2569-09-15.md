# แผน — ปรับแก้อีก 7 ข้อ (ก่อนแพตช์มือถือของทีม)

branch `fix/more-ui-2569-09-15` (แตกจาก `main` = `367e3a2` ที่ push แล้ว) · **อนุมัติแล้ว 15 ก.ย. 2569 (8 ข้อ) · ยังไม่ merge จนกว่าอนุมัติอีกรอบ**

กติกาเดิม: แก้ที่ต้นเหตุ · จุดตัดจอ 768/1280 เท่านั้น · เทสต์ใหม่ต้องแดงก่อนแก้ ย้อนการแก้แล้วแดง · ห้ามผ่อนเทสต์
หลักฐาน: ภาพข้อละ 1–2 รูป · e2e บน production build · ไม่ merge จนกว่าอนุมัติ

---

## 1 · ช่องติ๊ก "เปิดใช้งาน" ท้ายฟอร์มสินค้า

**ตรวจแล้ว — ไม่ใช่พิมพ์ผิด และไม่ติ๊ก = ปิดใช้งานจริง**
- ไม่ติ๊ก → เบราว์เซอร์ไม่ส่งช่องนี้ → `flag(fd, 'active')` = false → บันทึก `active = false` ทั้งตอนเพิ่มและแก้ไข
- สินค้าที่ปิดแล้วถูกกรองออกจาก: ช่องค้นหาสินค้าในใบขาย · ใบซื้อ · ใบเคลม · ใบตรวจนับ (ใช้ `searchProducts` ตัวเดียวกัน กรอง `p.active`) ·
  ชุดอะไหล่ · ทะเบียนสินค้า (ค่าตั้งต้น) · เตือนของใกล้หมดหน้าแรก · ยิงบาร์โค้ดขึ้นว่า "ปิดใช้งานอยู่" (มีเทสต์เดิม `scan-db.test.ts`)
- คงความหมายเดิม (ติ๊ก = เปิดใช้) ตามที่ผู้ใช้ให้ตรวจ ไม่เปลี่ยนเป็น "ปิดใช้งาน"

**แก้**: กดติดเฉพาะที่ช่องสี่เหลี่ยม — ตอนนี้ทั้งบรรทัดเป็น `<label>` ครอบช่องติ๊กกับข้อความ คลิกข้อความก็สลับ
→ ข้อความไม่เป็น label ของช่อง (ช่องติ๊กมี `aria-label` แทน เพื่อโปรแกรมอ่านหน้าจอ) · ช่องติ๊กใหญ่พอกด 22px เท่าเดิม
**เทสต์**: e2e — คลิกข้อความแล้วช่องไม่เปลี่ยน · คลิกช่องแล้วเปลี่ยน · เพิ่มสินค้าแบบไม่ติ๊กแล้วบันทึก → ฐานข้อมูลได้ `active = false` และค้นในใบขายไม่เจอ (ลบสินค้าทดสอบทิ้งหลังจบ)

## 2 · รูปสินค้า 1 รูป ในฟอร์มเพิ่มสินค้าใหม่

- ตอนนี้: มีระบบรูปอยู่แล้ว (`stock/[id]/pic-panel.tsx` + `savePicAction` ย่อรูปในเบราว์เซอร์ ≤ 1MB ขอบยาว 800px + รูปย่อ 160px)
  แต่แสดงเฉพาะหน้าสินค้าที่บันทึกแล้ว — หน้าเพิ่มสินค้าใหม่ไม่มี เพราะยังไม่มีรหัสสินค้าให้ผูกรูป
- แก้ (เลือกแล้ว): ฟอร์มเพิ่มสินค้าใหม่มีช่องเลือกรูป · ย่อรูปด้วยโค้ดชุดเดียวกับ PicPanel (แยกเป็นฟังก์ชันใช้ร่วม ไม่คัดลอก) ·
  กดบันทึกครั้งเดียว → สร้างสินค้าแล้วบันทึกรูปในทรานแซกชันเดียวกัน · รูปไม่ผ่าน (ชนิดไฟล์/ขนาด) → แจ้งผิดและ**ไม่สร้างสินค้าครึ่ง ๆ**
- หน้าสินค้าเดิมยังเปลี่ยน/ลบรูปได้เหมือนเดิม · ไม่มี migration (ตาราง `product_pics` มีอยู่แล้ว)
- **เทสต์**: e2e เลือกรูปในฟอร์มใหม่ → บันทึก → หน้าสินค้ามีรูป · ไฟล์ไม่ใช่รูป → ไม่มีสินค้าใหม่เกิดขึ้น

## 3 · ทะเบียนลูกค้า/ผู้ขาย: คอลัมน์ชื่อกว้างไป

- วัดแล้ว: คอลัมน์ชื่อตั้งไว้ 24% ของตาราง = 296px ที่ 1280 · 335px ที่ 1440 · **450px ที่ 1920** แต่ชื่อยาวสุดในข้อมูล 220px
- แก้: ชื่อกว้างคงที่ตามข้อมูล (ราว 240px รวมระยะในช่อง) · ที่เหลือให้คอลัมน์ที่อยู่ · ชื่อยาวกว่านั้นตัดบรรทัดในช่องเหมือนเดิม
- **เทสต์**: e2e ที่ 1280/1920 — ชื่อยาวสุดไม่ถูกตัด · คอลัมน์ชื่อไม่เกิน 260px · แถวไม่สูงผิดปกติ (ตรวจเดิม)

## 4 · เมนูหลัก 06 การเงิน ไม่มีขีดเหลืองใต้เมนู

- ต้นเหตุ: เมนู 06 ลิงก์ไป `/finance/ar` · `isCurrent` เทียบว่า URL ขึ้นต้นด้วย `/finance/ar`
  → หน้า `/finance/sales` `/finance/ap` `/finance/pl` ไม่ตรง ขีดเหลืองไม่ขึ้น (เมนูอื่นลิงก์ตรงกับ path ของหมวดจึงไม่เป็น)
- แก้: เทียบด้วยส่วนแรกของ path (`/finance`) — ทุกเมนูใช้กติกาเดียวกัน
- **เทสต์**: `test/nav-current.test.ts` เพิ่ม `/finance/sales|ar|ap|pl` ต้องเป็นเมนู 06 · `/finance-x` ไม่นับ · หน้าแรกยังตรงเฉพาะ `/`

## 5 · หน้าแรก: แผนภูมิสรุปยอดขายสีเข้ม-อ่อนต่างกันแต่ละเดือน

- ตอนนี้: เดือนปัจจุบันเขียวเข้ม เดือนอื่นเทาเท่ากันหมด
- แก้: 6 เฉดเขียวไล่จากอ่อน (เดือนเก่าสุด) → เข้มสุด (เดือนปัจจุบัน) · ตัวเลขยอดใน tooltip เท่าเดิม · พิมพ์ออกกระดาษยังเห็นต่าง
- **เทสต์**: e2e — แท่ง 6 แท่งสีไม่ซ้ำกัน และความเข้มเพิ่มขึ้นตามลำดับเดือน

## 6 · ระบบรันเลขที่เอกสาร (ตรวจ ไม่แก้รูปแบบ)

**ตรวจแล้ว — อัตโนมัติ และเริ่มนับใหม่ทุกเดือน** (ผู้ใช้เลือกคง 4 หลัก)
- รูปแบบ: คำนำหน้า + ปี พ.ศ. 2 หลัก + เดือน + วัน + ลำดับ 4 หลัก เช่น `QT6909130001`
- ลำดับนับแยกตาม "ชนิดเอกสาร + ปี/เดือน ของวันที่เอกสาร" ในฐานข้อมูล (`next_doc_no` · `next_billnote_no` · ใบเคลม · ใบตรวจนับ)
  → เดือนใหม่เริ่มที่ 0001 ทุกชนิด · ในเดือนเดียวกันวันที่เปลี่ยนแต่ลำดับนับต่อ (ไม่รีเซ็ตรายวัน)
- เลขที่เอกสารจองในทรานแซกชันเดียวกับการบันทึก → สองคนกดพร้อมกันไม่ได้เลขซ้ำ · ตัวอย่างเลขในฟอร์มใช้คีย์เดียวกัน
- ข้อสังเกต: ออกเอกสารย้อนวันที่ของเดือนก่อน จะนับต่อจากเลขของเดือนนั้น (ถูกต้องตามกติกา)
- **ช่องโหว่ของเทสต์**: ตอนนี้มีแค่เทสต์รูปแบบตัวอักษร ไม่มีเทสต์ฐานข้อมูลยืนยันการรีเซ็ต
  → เพิ่มเทสต์ฐานข้อมูล: ใบที่ 1–3 เดือน ก.ย. → ใบแรกเดือน ต.ค. ได้ 0001 · กลับไปออกย้อน ก.ย. ได้ 0004 · ชนิดเอกสารต่างกันนับแยก · ใบวางบิล/ใบเคลม/ใบตรวจนับด้วย

## 7 · หน้ารายการเอกสารในเมนู 03 รายรับ: ค้นหาซ้ำซ้อน

ตอนนี้มีค้นหา 3 แถว — (1) ช่องค้นหา + รวมใบที่ยกเลิก + **ปุ่มค้นหา** + พิมพ์รายงาน · (2) ช่วงวันที่ลัด + **เดือน/ปี + ดู** · (3) **ตั้งแต่–ถึง (ปฏิทิน) + ดู**

- แก้ (เลือกแล้ว):
  - แถวบน: ตัดปุ่ม "ค้นหา" — พิมพ์แล้ว Enter ค้นหาได้ (ตัวเลือก "รวมใบที่ยกเลิก" ยังมีผลตอนกด Enter)
  - ช่วงวันที่: ตัด dropdown เดือน/ปี และปุ่มดูของมัน · คงปุ่มลัด ทั้งหมด/วันนี้/เดือนนี้/… · คงตั้งแต่–ถึง จากปฏิทิน + ดู
- ขอบเขต: **เฉพาะหน้ารายการเอกสารในเมนู 03** — ประวัติรายรับทุกชนิด · ประวัติใบวางบิล · ประวัติขายหน้าร้าน
  เมนูอื่นที่ใช้ตัวกรองเดียวกัน (รายจ่าย ใบเคลม ตรวจนับ) คงเดิม
- **ไม่แตะ**: ฟอร์มเปิดเอกสารใหม่ (ช่องค้นหาลูกค้า/อ้างอิงใบเสนอราคาที่แสดงผลแบบ dropdown) · ตัวแปร `?month=&year=` ฝั่งเซิร์ฟเวอร์ยังอ่านได้ (ลิงก์เก่าไม่พัง)
- **เทสต์**: e2e หน้า 03 ทุกหน้าในขอบเขต — ไม่มีปุ่มค้นหาแถวบน · พิมพ์ + Enter แล้วผลกรองจริง · ไม่มี dropdown เดือน/ปี · ตั้งแต่–ถึงยังกรองได้ ·
  หน้ารายจ่ายยังมี dropdown เดือน (ขอบเขตไม่ลาม) · ฟอร์มใบเสนอราคาใหม่ยังค้นลูกค้าแบบ dropdown ได้

## 8 · กดย้ายจากใบเสนอราคา → ใบส่งมอบ → ใบเสร็จ แต่ฟอร์มเอกสารไม่เปลี่ยนตาม

**ทำให้เกิดซ้ำได้แล้ว** (production build)
- `/income?kind=QT` กดแท็บ 03.2 แล้ว 03.4 → URL และแท็บที่เลือกเปลี่ยน แต่ฟอร์มยังเป็น **"สร้างใบเสนอราคา" เลขที่ `QT6909150001`** — กดรีเฟรชถึงเป็นใบเสร็จ
- `/expense?kind=PO` กดไปค่าใช้จ่าย → ช่องซ่อนส่ง `kind=EX` แล้ว **แต่ฟอร์มยังเป็น "สร้างใบซื้อสินค้า" เลขที่ PO** — ข้อมูลที่เห็นกับชนิดที่จะบันทึกไม่ตรงกัน
- `/income/billing?vat=yes` → `vat=no` หัวการ์ดเปลี่ยน แต่ลูกค้า/ใบที่ติ๊กไว้ในฟอร์มค้างจากหน้าเดิม

**ต้นเหตุ**: `DocEditor` (`doc-editor.tsx:204`) · `BuyEditor` (`buy-editor.tsx:163`) · `BillForm` ตั้งค่าฟอร์มจาก props ด้วย `useState` ครั้งแรกเท่านั้น
ไปหน้าเดียวกันที่เปลี่ยนแค่ `?kind=` / `?vat=` React ใช้คอมโพเนนต์ตัวเดิมต่อ ค่าตั้งต้นใหม่จึงไม่ถูกใช้

**แก้ที่ต้นเหตุ**: ใส่ `key` ให้ฟอร์มตามสิ่งที่ทำให้เป็น "เอกสารใบใหม่" — เปลี่ยนแล้วฟอร์มเริ่มใหม่ทั้งชุด
- รายรับ (`FormBlock`): ชนิดเอกสาร · ใบใหม่ `/income/new`: ชนิด + ใบต้นทาง (`from`) + คัดลอก (`copy`)
- รายจ่าย: ชนิด (PO/EX) ทั้งหน้ารายจ่ายและ `/expense/new` · ใบวางบิล: vat
- หน้าแก้ไข `[id]/edit` ใช้รหัสเอกสารเป็น key (กันแบบเดียวกันเมื่อกดจากใบหนึ่งไปอีกใบ)
- ไม่แตะวิธีบันทึก/ตรวจค่า — แก้แค่ให้ฟอร์มเริ่มใหม่ตรงกับ URL

**เทสต์**: e2e ครบ 3 ขนาดจอ — กดแท็บ 03.1 → 03.2 → 03.4 ไม่รีเฟรช หัวฟอร์มและคำนำหน้าเลขที่ต้องเปลี่ยนตามทุกครั้ง ·
รายจ่าย PO → EX หัวฟอร์ม/เลขที่เปลี่ยน · ใบวางบิล IVT → IV ลูกค้าที่เลือกไว้ถูกล้าง · ย้อนการแก้ (ถอด key) ต้องแดง

---

## ลำดับทำ
8 → 4 → 3 → 5 → 1 → 6 (เทสต์) → 7 → 2 (ข้อ 8 ก่อน — ฟอร์มกับชนิดที่บันทึกไม่ตรงกันเป็นเรื่องข้อมูล ไม่ใช่แค่หน้าตา)

## ตรวจรวม
typecheck · `npm test` (ล้างฐานในเครื่อง → reseed + ลิงก์ตั้งรหัสผ่าน) · e2e ครบบน production build · ย้อนการแก้ทีละจุด ·
ภาพข้อละ 1–2 รูป · §13 บันทึกจุดที่ต่างจากสเปก
