# แผน — ปรับแก้เล็ก ๆ 10 ข้อ (ก่อนแพตช์มือถือของทีม)

branch `fix/small-ui-2569-09-15` (แตกจาก `main` = `44c70e2` ที่ push แล้ว) · **รออนุมัติ ยังไม่แก้โค้ด**

กติกาเดิม: แก้ที่ต้นเหตุ · จุดตัดจอ 768/1280 เท่านั้น · เทสต์ใหม่ต้องแดงก่อนแก้ ย้อนการแก้แล้วแดง · ห้ามผ่อนเทสต์
หลักฐาน: ภาพข้อละ 1–2 รูป (ก่อน/หลัง) · ไม่ merge จนกว่าอนุมัติ

---

## 1–2 · ปุ่ม "รับชำระ" (06.2 ลูกหนี้) · "จ่ายชำระ" (06.3 เจ้าหนี้) เป็นสีเขียว
- ต้นเหตุ: `finance/ar/ar-row.tsx:33` และ `finance/ap/ap-row.tsx:36` เป็น `btn` เปล่า
- แก้: ใช้ปุ่มเขียวที่มีอยู่แล้ว `btn ok` (เขียว ตัวหนา — ชุดเดียวกับ "บันทึกรับชำระ")
- ปุ่ม "จ่ายชำระหลายใบ" / "รับชำระหลายใบ" ด้านบนของหน้าให้เขียวด้วย (ปุ่มประเภทเดียวกัน)

## 3 · ผู้ติดต่อ: เลือก "ผู้ขาย" แล้วไม่มีส่วน "รถที่ดูแล"
- ต้นเหตุ: `customers/contact-form.tsx` แสดงส่วนรถทุกชนิด
- แก้: ซ่อนส่วนรถ + ปุ่ม "+ เพิ่มรถ" เมื่อเลือกผู้ขาย (สลับกลับเป็นลูกค้าแล้วรถที่พิมพ์ไว้ยังอยู่)
- **ต้องแก้ฝั่งเซิร์ฟเวอร์ด้วย** — `lib/contacts.ts:327` ลบรถที่ไม่ได้ส่งมา ถ้าซ่อนอย่างเดียว
  ผู้ขายที่เคยมีรถ (เช่นเคยบันทึกผิดชนิด) กดบันทึกแล้วรถหายเงียบ ๆ → เป็นผู้ขายให้ข้ามการแตะตารางรถทั้งหมด
- เทสต์ฐานข้อมูล: บันทึกผู้ขายที่มีรถอยู่แล้ว → รถยังอยู่

## 4 · ตารางรายการสินค้า: คอลัมน์ตัวเลขแคบลง ชื่อสินค้ากว้างที่สุด
- ปัจจุบัน (`income/doc-editor.tsx` LineRow): รหัส 150 · จำนวน 84 · หน่วย 80 · ราคา/หน่วย 120 · ส่วนลด 84 · ชื่อได้ที่เหลือ
- แก้ (เลือกแล้ว): กว้างตามจำนวนตัวอักษร (mono 14px ≈ 8.4px/หลัก + ขอบ/ระยะในช่อง) —
  **จำนวน 6 หลัก · ชื่อหน่วย 8 ตัวอักษร · ส่วนลด 4 หลัก** · ราคา/หน่วย และรหัสคงเดิม · ชื่อสินค้าได้ที่เหลือทั้งหมด
- ทุกหน้าที่เป็นตารางแบบนี้: ออกเอกสารขาย (QT/IVT/IV/RC · ขายหน้าร้าน · แก้ไข) · ใบซื้อ/ค่าใช้จ่าย (`buy-editor`) ·
  ใบเคลม (`claim-editor`) · ชุดอะไหล่ (`kit-editor`) · หน้าดูรายใบ `income/[id]` `expense/[id]` `stock/claim/[id]`
- ทำเป็นคลาสเดียวใน CSS (ไม่ใช่ `style={{ width }}` กระจายทุกไฟล์) — `table.lines col.qty/.unit/.disc` ฯลฯ
- เทสต์: ชื่อสินค้ากว้างสุดในแถว · ช่องจำนวนแสดง 6 หลักไม่ถูกตัด · ทั้ง 3 ขนาดจอ (มือถือยังเลื่อนในกรอบตาราง)

## 5 · รายการเอกสารเรียงล่าสุดบนสุด
- ขอบเขต (เลือกแล้ว): **ทุกหน้าในเมนู 03 รายรับ**
- ตรวจครบทุกรายการในเมนู 03:
  - ประวัติรายรับ (QT/IVT/IV/RC) และขายหน้าร้าน — `listIncomeDocs` เรียง `วันที่ desc, เลขที่ desc` อยู่แล้ว ✔
  - ประวัติใบวางบิล — `listBillnotes` เรียง `วันที่ desc, เลขที่ desc` อยู่แล้ว ✔
  - **รายการใบในหน้าสร้าง/เปิดใบวางบิล — `openInvoices` (`billnotes.ts:84`) เรียง `วันที่, เลขที่` เก่า→ใหม่** ← ต้นเหตุที่เห็นในเมนูใบวางบิล
    (`getBillnote` อ่านแค่รหัสใบ ไม่เรียง — รายการที่เห็นมาจาก `openInvoices` ทั้งหน้าสร้างและหน้าเปิดใบเดิม)
- แก้: `openInvoices` → `วันที่ desc, เลขที่ desc`
- ไม่แตะ (นอกขอบเขตที่เลือก): ลูกหนี้/เจ้าหนี้ (ตามวันครบกำหนด) · รายงานการเงิน · ของใกล้หมดอายุ · ลำดับบรรทัดในเอกสาร ·
  ประวัติรับชำระของใบ · สายเอกสาร QT→IVT→RC · ไฟล์สำรอง · **ใบวางบิลที่พิมพ์ (กระดาษ)** คงลำดับเดิม
- ข้อมูลในเครื่องไม่มีใบวางบิล — เทสต์ฐานข้อมูลสร้างใบหลายวันแล้วยืนยันลำดับ (ย้อนการแก้ต้องแดง)

## 6 · หน้าแรก: การ์ดช่วงเวลา ทั้งหมด/วันนี้/เดือนนี้/… สวยขึ้น · ไอคอนค้นหาเข้มขึ้น
- ต้นเหตุ: `components/date-range.tsx` เป็นชิปใหญ่ 52px เรียงกัน 5 ปุ่ม + ช่องวันที่ลอยขวา
- แบบ (เลือกแล้ว): **แถบปุ่มต่อกัน** — ทั้งหมด · วันนี้ · เดือนนี้ · เดือนที่แล้ว · ปีนี้ · ปีที่แล้ว เป็นแถบเดียวขอบมน
  อันที่เลือกพื้นเขียวเข้มตัวขาว · ช่อง ตั้งแต่–ถึง + ดู ชิดขวาแถวเดียวกัน (จอแคบลงแถวถัดไป)
  มือถือ: แถบเป็น 3+3 ช่องเท่ากัน · เป้ากด 44px บนจอสัมผัส · เพิ่ม "วันนี้" ให้ตรงกับตัวกรองหน้าประวัติ
- ไอคอนแว่นใน `.in.search` สีเทาอ่อน `#7C8B96` → **เขียวเข้ม `--steel-dk`** + **กรอบช่องค้นหาเขียวเข้ม** — ทุกช่องค้นหาทั้งระบบ (คลาสเดียว)

## 7 · dropdown "เลือกเดือน" → "เดือน"
- `components/doc-date-filter.tsx:80` จุดเดียว ใช้ทุกหน้าประวัติ

## 8 · ปิดประวัติที่เบราว์เซอร์จำไว้ (autocomplete)
- ต้นเหตุ: ไม่มี `autoComplete="off"` — มี 69 ฟอร์ม 302 ช่องกรอก
- แก้: `autoComplete="off"` ที่ทุก `<form>` + ช่องค้นหา/ช่องกรอกที่อยู่นอกฟอร์ม
- เทสต์ถาวร (อ่านไฟล์): ทุก `<form>` ต้องมี `autoComplete` · ช่องที่ไม่อยู่ในฟอร์มต้องมีเอง
- **ยกเว้น** หน้าเข้าสู่ระบบ/ตั้งรหัสผ่าน — คง `username` / `current-password` / `new-password` ไว้ให้ตัวจัดการรหัสผ่านทำงาน
- ข้อจำกัดที่ต้องบอก: Chrome/Edge บางรุ่นไม่ฟัง `off` กับช่องที่เดาว่าเป็นชื่อ/ที่อยู่/เบอร์โทร
  ถ้าเปิดเครื่องจริงแล้วยังเด้ง จะใช้ค่า `autocomplete` ที่เบราว์เซอร์ไม่รู้จัก (เช่น `off-dgl`) กับช่องนั้น — ตรวจด้วยเบราว์เซอร์จริง

## 9 · ปฏิทิน/ช่องวันที่เป็นภาษาไทยทั้งหมด
- ต้นเหตุ: มีปฏิทินไทย พ.ศ. อยู่แล้ว (`ThaiDateInput` / `ThaiDateField`) แต่ยังมี `<input type="date">` ของเบราว์เซอร์ **12 จุด**
  (ปฏิทินของเบราว์เซอร์ใช้ภาษา/ค.ศ. ตามเครื่อง): รับชำระในแถว · ฟอร์มรับ/จ่ายชำระ · ชำระหลายใบ · ใบวางบิล (วันที่/ครบกำหนด) ·
  รับเข้า/ปรับยอดสต๊อก (2) · ยอดยกมาสินค้าใหม่ · เปิดใบตรวจนับ · หัวใบตรวจนับ · ใบเคลม · หน้าผู้ดูแลระบบ (2)
- แก้: เปลี่ยนเป็นช่องไทยทั้งหมด (ชื่อช่องที่ส่งเข้าฟอร์มเท่าเดิม เซิร์ฟเวอร์ไม่ต้องแก้) · เพิ่ม `required`/`disabled` ให้คอมโพเนนต์
- เทสต์ถาวร (อ่านไฟล์): ห้ามมี `type="date"` ในแอป

## 10 · เพิ่มสินค้าใหม่: ผู้ขาย + วันหมดอายุ
- ปุ่ม "+ เพิ่มรายชื่อเอง" → สีเขียว
- "มีได้หลายราย" ย้ายไปอยู่ในวงเล็บต่อหัวข้อ: **ผู้ขายของสินค้านี้ (มีได้หลายราย)**
- วันหมดอายุ — ต้นเหตุที่งง: ช่องชื่อ "วันหมดอายุ / อายุการเก็บ (เดือน)" จริง ๆ รับ **จำนวนเดือน**
  ส่วน **วันที่** หมดอายุอยู่อีกช่องติดกับยอดยกมา (ปฏิทินเบราว์เซอร์ ไม่มีป้ายชื่อ)
- แบบใหม่ (เลือกแล้ว): **ถามทีละขั้น**
  - วันหมดอายุ: (•) ไม่มีวันหมดอายุ · ( ) มีวันหมดอายุ
  - เลือก "มี" แล้วโผล่: **อายุการเก็บ [__] เดือน** — "ระบบเติมวันหมดอายุให้เองตอนรับของเข้า (วันที่รับ + N เดือน)"
    และ (เฉพาะสินค้าใหม่) **วันหมดอายุของยอดยกมา [ปฏิทินไทย]** — "ของที่มีอยู่ตอนนี้หมดอายุวันไหน · เว้นว่างได้"
  - ยอดยกมา (จำนวน) เป็นช่องของตัวเอง ไม่ติดกับวันที่
  - ชื่อช่องที่ส่งเข้าเซิร์ฟเวอร์เท่าเดิม (`shelfLifeMonths` · `openingExpiresOn`) — เลือก "ไม่มี" ส่งค่าว่างทั้งสองช่อง
  - สินค้าเดิมที่มีอายุการเก็บอยู่แล้ว เปิดแก้ไขมาต้องติ๊ก "มี" ไว้ให้เอง
  - เทสต์: เลือก "ไม่มี" แล้วบันทึก → อายุการเก็บว่างจริง (ไม่ใช่ค่าที่เคยพิมพ์ค้างไว้)

---

## ผลหลังแก้ (15 ก.ย. 2569)

**ทำครบ 10 ข้อ** ตามแบบที่เลือก — ภาพก่อน/หลังข้อละ 1–2 รูป · จุดที่ต่างจากสเปกบันทึก `docs/ui-layout-spec.md` §13.2

**เจอระหว่างทดสอบ แก้ที่ต้นเหตุ**
- ข้อ 4: `style={{ width }}` ของช่องเดิมถูกเมินทั้งหมด — `<input>` กว้างเอง 172px ทุกคอลัมน์ 184px ตาราง 1,419px ในกรอบ 1,234px
  (ความกว้างที่ตั้งไว้ถูกอยู่แล้ว ปัญหาคือช่องกรอกไม่ฟังคอลัมน์) → ช่องกรอกเต็มคอลัมน์ + width/min-width ที่ CSS
  · รอบแรกลืม min-width จอแคบบีบช่องจำนวนเหลือ 46px "999999" ถูกตัด · หัว "ส่วนลด %" ดันคอลัมน์เป็น 79px → ให้ตัดสองบรรทัด
- ข้อ 3: ถ้าซ่อนส่วนรถอย่างเดียว ผู้ขายที่เคยมีรถกดบันทึกแล้วรถถูกลบ (`contacts.ts` ลบรถที่ไม่ได้ส่งมา) → เซิร์ฟเวอร์ข้ามตารางรถของผู้ขาย
- ข้อ 9: `ThaiDateField` เดิมลบช่องแล้วไม่ส่งค่า — ใช้กับวันที่เอกสาร เปลี่ยนเป็นส่งค่าว่างทุกที่จะทำให้เอกสารไม่มีวันที่ → `allowEmpty` เฉพาะช่องที่เว้นว่างได้ ·
  ใบตรวจนับบันทึกตอนออกจากช่อง เลือกจากปฏิทินไม่ออกจากช่อง → `onCommit`
- เทสต์ยิงบาร์โค้ด (มีอยู่เดิม) พิมพ์ก่อนช่องยิงได้โฟกัสบน production build ตัว "4" ของ "40*" หาย —
  ไม่ใช่ผลของรอบนี้ (โค้ดช่องยิง/โฟกัสไม่ถูกแก้ · dev ผ่าน 15/15) → เทสต์รอโฟกัสก่อนยิงแบบเดียวกับข้อแรกในไฟล์ (ไม่ผ่อนสิ่งที่ตรวจ)

**หลักฐาน**
- e2e ทุกไฟล์บน production build 3 ขนาดจอ: 197 ผ่าน · 10 ข้ามตามที่ออกแบบ · 0 ไม่ผ่าน
- ย้อนการแก้ทีละจุดแล้วเทสต์แดงตามอาการ 17 จุด (e2e 12 · อ่านไฟล์ 3 · ฐานข้อมูล 2) — ปุ่มเขียว 2 · ส่วนรถผู้ขาย (หน้าจอ + เซิร์ฟเวอร์) ·
  ความกว้างตาราง 2 · ลำดับใบวางบิล · แถบช่วงเวลา · กรอบค้นหา · ป้ายวันที่ · "เดือน" · ปฏิทินไทย 2 · วันหมดอายุ 2 · autocomplete 2
- `npm test` พร้อมฐานข้อมูล: 1,030 ผ่าน (web 789 · importer 151 · core 90) · 8 ข้ามตามที่ออกแบบ · 0 ไม่ผ่าน
- typecheck ผ่าน · build production ผ่าน

## ลำดับทำ
7 → 1–2 → 3 → 5 → 9 → 8 → 4 → 10 → 6 (จากเล็ก/เสี่ยงน้อย ไปหน้าตาที่ต้องดูด้วยตา)

## ตรวจรวม
typecheck · `npm test` (ล้างฐานในเครื่อง → reseed + ลิงก์ตั้งรหัสผ่าน) · e2e ครบ · build production ·
ภาพข้อละ 1–2 รูป · §13 บันทึกจุดที่ต่างจากสเปก
