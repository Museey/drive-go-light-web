# แผน — โลโก้เว็บ: ไอคอนแท็บเบราว์เซอร์ และไอคอนตอน add to home screen (18 ก.ย. 2569)

branch `feat/app-icon-2569-09-18` (แตกจาก `main` = `ea4a7ac`) · ผู้ใช้ส่งภาพโลโก้วงกลมเหลือง 1280×1280

## ตัดสินแล้ว (ผู้ใช้เลือก)
- **ตัดขอบขาวออกครึ่งเดียว** เหลือขอบไว้ครึ่งหนึ่ง
- ชื่อใต้ไอคอนตอน add to home screen: **DriveGoLight!**

## ตอนนี้เป็นอย่างไร
- ไม่มีไอคอนเลย — แท็บเบราว์เซอร์ขึ้นไอคอนว่างของ Next.js · เพิ่มลงหน้าจอมือถือได้ภาพจับหน้าจอ ไม่ใช่โลโก้
- ไม่มีไฟล์ manifest (ข้อมูลชื่อ/ไอคอน/สีของแอปตอนติดตั้งลงหน้าจอ)

## จะทำ
### 1. ไฟล์ไอคอนจากภาพที่ส่งมา
วัดจากภาพจริงแล้ว: โลโก้อยู่ห่างขอบซ้าย/บน 192px มีเงาทางขวา/ล่าง — ตัดรอบโลโก้โดยเหลือขอบขาวครึ่งเดียว (ครอปสี่เหลี่ยมจัตุรัสกลางโลโก้ 1,152px จาก 1,280px)
| ไฟล์ | ขนาด | ใช้ที่ไหน |
|---|---|---|
| `icon.png` | 512 | ไอคอนแท็บเบราว์เซอร์ (เบราว์เซอร์ย่อเอง) |
| `apple-icon.png` | 180 | iPhone/iPad ตอน "เพิ่มไปยังหน้าจอโฮม" |
| `icon-192.png` · `icon-512.png` | 192 · 512 | Android ตอนติดตั้งลงหน้าจอ |
- แปลงเป็น PNG พื้นขาวทึบ (ภาพต้นฉบับพื้นขาวอยู่แล้ว) เก็บไฟล์ต้นฉบับไว้ในโปรเจกต์ด้วย เผื่อทำไอคอนใหม่ทีหลัง

### 2. ข้อมูลแอปตอนติดตั้งลงหน้าจอ (manifest)
- ชื่อ **DriveGoLight!** · คำอธิบายสั้น "ระบบบริหารงานอู่ซ่อมรถ"
- เปิดแบบเต็มหน้าจอไม่มีแถบที่อยู่ (standalone) · เริ่มที่หน้าแรก · สีพื้นหลังขาว · สีแถบบนเขียวเข้มของระบบ
- ภาษาไทย · ไอคอน 192/512

## ไม่แตะ
- โลโก้ในแอป (แถบเมนู หัวกระดาษ) ยังเป็นตัวหนังสือ DriveGoLight! เหมือนเดิม · โลโก้ของอู่บนเอกสารเป็นคนละเรื่อง (ตั้งที่ 07 ตั้งค่าร้าน)

## เทสต์
- หน่วย: ไฟล์ไอคอนมีจริงครบทุกขนาด · เป็น PNG สี่เหลี่ยมจัตุรัสขนาดตรงตามชื่อ
- e2e: หน้าเว็บมีลิงก์ไอคอนและ manifest · โหลดไฟล์ไอคอนและ manifest ได้จริง (200 · ชนิดไฟล์ถูก) · manifest มีชื่อ DriveGoLight! และไอคอน 192/512
- ทั้งชุดเดิมต้องเขียว · กลายพันธุ์: ลบไอคอนออกจากหน้า · manifest ไม่มีไอคอน
