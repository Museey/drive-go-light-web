# แผน — นำชุดแก้ไขรอบ 14 ก.ย. 2569 22:37 เข้าระบบ

branch `integrate/final-2569-09-14` · แตกจาก `main` = `3223367` (ตัวที่ขึ้นเครื่องจริงแล้ว) · **ไม่แตะ `main` จนกว่าคุณอนุมัติ merge**

ต้นทาง: `~/Downloads/dgl final files 14092026 22-37/dgl-changes-2569-09-13.zip`

---

## คำตอบที่ได้แล้ว

| เรื่อง | เลือก |
|---|---|
| ฐานของ branch | `main` ปัจจุบัน — แทนเฉพาะไฟล์ที่ต่างจากชุดก่อน ทีละไฟล์ผ่าน Git |
| ถังขยะ | **ตาม zip ทั้งหมด** — กู้คืนได้ทุกชนิด ใบเสร็จตัดสต๊อกใหม่ · ลบถาวรเฉพาะเจ้าของ + รหัสผ่าน (ยกเลิกกติกา "กู้ได้เฉพาะ QT/BN/PO/EX" ของ 14 ก.ย. ช่วงเช้า) |

## สิ่งที่ตรวจแล้ว

- zip ใหม่เทียบชุดก่อน: เปลี่ยน 28 ไฟล์ · เพิ่ม 11 ไฟล์ (รวม `029_kits.sql` `030_kit_prices.sql`) · ไมเกรชัน 020–028 **ไม่เปลี่ยน**
- วางบน `main`: แทนตรง ๆ 21 · รวมอัตโนมัติ 3 (`globals.css` `sales.ts` `001_init.sql`) · **ขัดกัน 2** (`income/page.tsx` `lib/trash.ts`) · ไฟล์ที่ชุดก่อนไม่ได้แตะ 6 (ต่างไม่กี่บรรทัด — เลขเมนูใหม่)
- zip ยังทำบนฐานเก่า ไม่มีงานแก้ 25 ไฟล์ของเรา → **ห้ามแทนไฟล์เหล่านี้ด้วยของ zip**
- `025_product_suppliers.sql` ของ zip ไม่มี force RLS — ถ้าแทน ตัวรันไมเกรชันจะปฏิเสธ (ไฟล์ที่ขึ้นเครื่องจริงแล้วห้ามแก้) และ **deploy ล้ม** → คงของ `main`

---

## ขั้น 0 · นำเข้าตามต้นฉบับ ทีละไฟล์

- 1 commit ต่อ 1 ไฟล์ (หรือกลุ่มไฟล์ที่ต้องมาด้วยกันจึง compile ผ่าน) ข้อความ commit บอกชนิด: แทนตรง / รวมสามทาง / แก้ขัดกัน
- **ไม่นำเข้า**: ไฟล์เอกสาร 5 ไฟล์ใน zip (เก็บ `DELIVERY.md` ไว้ที่ `docs/` แทน SUMMARY เดิม) · `db/020–028` (เหมือนเดิมอยู่แล้ว)
- ไฟล์ที่ขัดกัน:
  - `income/page.tsx` — คงตาราง `IncomeHistoryTable` ของเรา แล้วย้ายของใหม่เข้าไป: `RowPay` รับชำระตรงแถว · กติกาแก้ไข (ใบเสร็จ/รับเงินแล้ว ปุ่มจาง + เหตุผล) · ขนาดหน้า 10/20/30
  - `lib/trash.ts` — ดูขั้น 2

## ขั้น 1 · ฐานข้อมูล

**1.1 `029_kits.sql` ขาดสิทธิ์ของ role แอป** — ทุกตารางข้อมูลอู่ (004 005 006 009 010 025) grant ให้ `dgl_app` แบบมีเงื่อนไข
029 ไม่มี → เครื่องที่มี role นี้ (รวมฐานทดสอบ) แอปอ่านชุดอะไหล่ไม่ได้ · เพิ่ม grant แบบเดียวกัน
แก้ได้เพราะ 029 ยังไม่เคยขึ้นเครื่องจริง

**1.2 `001_init.sql`** รวมอัตโนมัติแล้วมี kits/kit_items ใน loop force RLS — ตรวจด้วย `migrations.test` (ติดตั้งใหม่ ↔ อัปเกรด สคีมาต้องตรงกันทุกคอลัมน์) + `tenant-isolation` เพิ่มสองตารางใหม่

**1.3 อัปเกรดจากสำเนาเครื่องจริง** — ฐานที่อยู่ 028 พร้อมข้อมูล → รัน 029–030 → ยอด/สต๊อกเท่าเดิม · role แอปอ่านเขียนชุดได้

## ขั้น 2 · ถังขยะตาม zip (คงการแก้บั๊กของเรา)

ใช้กติกาของ zip แต่ย้ายเข้า `trash-core.ts` ที่ทดสอบได้ — ของ zip มีบั๊กที่เราเคยเจอและแก้ไปแล้ว:

| # | ของ zip | ต้องเป็น |
|---|---|---|
| 2.1 | กู้ใบวางบิล: ล้าง `voided_at` อย่างเดียว | ตั้ง `status='issued'` + ผูก `billnote_docs` กลับ + ปฏิเสธถ้าใบในนั้นไปอยู่ใบวางบิลอื่น (ไม่งั้นกู้ไม่ได้เลยสักใบ — บั๊กเดิม) |
| 2.2 | กู้ใบเสร็จ: ตัดสต๊อกเฉพาะบรรทัดสินค้า | **ตัดชิ้นส่วนของชุดอะไหล่ด้วย** — ตอนยกเลิก `returnDocStock` คืนทั้งสองแบบ ถ้ากู้ไม่ตัดชิ้นส่วน สต๊อกเกินจริง (DELIVERY ข้อ 12 ก็เขียนว่าต้องตัด) |
| 2.3 | ไม่พบเอกสาร/ลบซ้ำ เงียบ | แจ้ง "ไม่พบเอกสารในถังขยะ" |
| 2.4 | ใบกำกับภาษี/ใบส่งมอบ กู้คืนได้ | ตาม zip — แต่ถ้ามีใบต่อที่ยังไม่ยกเลิก หรือชนเลขที่ ต้องปฏิเสธ |

- ลบถาวร: เจ้าของเท่านั้น + รหัสผ่าน (ตรวจกับ `verifyPassword` ของบัญชีที่ล็อกอิน) — พนักงานเห็นปุ่มจาง
- หน้า 07.4: เอาช่องติ๊ก "เข้าใจแล้ว" ออก ใส่ช่องรหัสผ่านแทน · เอาปุ่ม "คัดลอกเป็นใบใหม่" ออก · ลบ `trash-rules.ts`
- เทสต์ฐานข้อมูล: กู้ใบเสร็จที่มีชุดอะไหล่ → สต๊อกสินค้าและชิ้นส่วนตัดกลับเท่าก่อนยกเลิก · รหัสผ่านผิด/พนักงาน → ไม่ลบ · อู่อื่นกู้/ลบของเราไม่ได้

## ขั้น 3 · หน้าที่ขาด — 05.7 ชุดอะไหล่ซ่อมบำรุง

zip มี `lib/kits.ts` + ผังเมนูชี้ `/stock/kits` แต่ **ไม่มีไฟล์หน้า** (เหมือนรอบก่อนที่ขาดขายหน้าร้าน/ถังขยะ)

- `/stock/kits` รายการชุด (รหัส ชื่อ ราคา A · B/C ต้นทุน กำไร) + การ์ด "+ สร้างชุด"
- ฟอร์มชุด: รหัสถัดไป · ชื่อ · ราคา A/B/C · หมายเหตุ · วัสดุจากทะเบียน (ค้นสด) หรือพิมพ์เอง · จำนวน · ทุน · Σ รวมต้นทุน · ปิดใช้งาน
- สิทธิ์ `stock.product` ตาม DELIVERY ข้อ 6 · ตรวจ `saveKit` ว่าเขียนผ่าน `mutate` + tenant
- เทสต์: สร้างชุด → ขายในใบเสร็จ → ชิ้นส่วนลดตามจำนวนชุด → ยกเลิก → คืน · ระดับราคา B เปลี่ยนราคาชุด

## ขั้น 4 · งานแก้ของเราที่ต้องรอดหลังแทนไฟล์

ตรวจทีละข้อหลังนำเข้า (ลองย้อนการแก้แล้วเทสต์ต้องแดง):

- เมนูเดสก์ท็อปชื่อย่อ + ซ่อนชื่อร้าน ที่ 1280px ปุ่มไม่หลุดขอบ (`menu-map.ts` ถูกแทน — ชื่อ/เลขเมนูใหม่ต้องเข้ากับ `RAIL_LABEL`)
- แถบล่างมือถือ หน้าแรก · รายรับ · สินค้า · ลูกค้า
- ยิงบาร์โค้ดรัว ไม่ตกหล่น (`doc-editor.tsx` ถูกแทน)
- หน้า `[id]` รหัสผิดรูปแบบ → 404
- ขายหน้าร้าน `/income/walkin` ใช้เลขเมนู 03.6
- สำรอง/กู้ข้อมูล: เพิ่ม kits, kit_items, `doc_items.kit_id` ใน `backup.ts` + ตัวนำเข้า + `WIPE_ORDER` · ขยาย `backup-roundtrip`
- ตัวหนังสือกลางกรอบทั้งระบบ × 3 จอ

## ขั้น 5 · ตรวจรวม

- typecheck 3 แพ็กเกจ · `npm test` พร้อมฐานข้อมูล · e2e 3 จอ · `next build`
- เดินตามเช็กลิสต์ 17 ข้อใน DELIVERY.md ของจริงบน dev server (เลขเมนูตามผังใหม่)
- crawl ทุกหน้า × 3 จอ ไม่มี error
- เอกสาร: `docs/ui-layout-spec.md` §13 เพิ่มแถว (ถังขยะตาม zip, grant 029, หน้า kits) · `DEPLOY.md` ไมเกรชัน 029–030

---

## ขึ้นเครื่องจริง (หลัง merge · คุณ push เอง)

- Render รัน 029–030 ให้เองก่อนสลับเวอร์ชัน (สร้างตารางใหม่ + เพิ่มคอลัมน์ ไม่เขียนข้อมูลเดิมใหม่ — เร็ว ไม่ล็อกนาน)
- สำรองก่อน push ตาม DEPLOY.md หัวข้อ 0ค. ข้อ จ. (จุดกู้คืนของ Render + census)
  — ~~`tools/backup-db.sh` ผ่าน Docker~~ ใช้กับ Render ไม่ได้ (force row level security มีผลกับ role เจ้าของตาราง) เจอจริง 15 ก.ย. 2569
- หลัง deploy: `/healthz` ต้อง `ok:true` (โค้ดใหม่คาดหวัง 30 ไฟล์)

## ไม่ทำในรอบนี้

สิทธิ์แยก "ลบถาวร" ใน `perms.ts` (DELIVERY ข้อ 6) · e2e ใหม่ของใบเคลม/ฟอร์มเปล่า (ข้อ 5 — ทำเฉพาะถังขยะ ชุดอะไหล่ รับชำระตรงแถว) · i18n · ย้าย `<col style>` เข้า CSS
