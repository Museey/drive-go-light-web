# แผน — จำกัดสินค้าไม่เกิน 3,000 รายการต่ออู่ (16 ก.ย. 2569)

branch `feat/product-limit-2569-09-16` (แตกจาก `main` = `1bab29a`) · **งานตรรกะระบบ**

## ตัดสินแล้ว (ผู้ใช้เลือก)
- **นับเฉพาะสินค้าที่เปิดใช้งาน** — ระบบไม่มีทางลบสินค้า มีแค่ปิดใช้งาน ปิดใช้งานสินค้าที่เลิกขาย = คืนที่ให้เพิ่มใหม่
  ประวัติเอกสารที่อ้างสินค้าเก่ายังอยู่ครบ
- **นำเข้า CSV / กู้คืนไฟล์สำรองที่ทำให้เกิน → ปฏิเสธทั้งไฟล์** บอกว่าเกินกี่รายการ ข้อมูลเดิมไม่ถูกแตะ
- **แสดงตัวนับ "ใช้งานอยู่ 200 / 3,000 รายการ"** ที่หัวหน้าทะเบียนสินค้า

## ทางที่สินค้าเกิดขึ้นได้ (ค้นทั้งโค้ดแล้ว)
| ทาง | ไฟล์ |
|---|---|
| เพิ่มสินค้าใหม่ (ฟอร์ม) | `lib/products.ts` |
| เปิดใช้งานสินค้าที่ปิดไว้กลับมา (ฟอร์มแก้ไข) | `lib/products.ts` |
| นำเข้า CSV (เพิ่มรหัสใหม่ · แก้สถานะใช้งาน) | `lib/products-csv.ts` |
| ลงทะเบียนรายการค้างทำเป็นสินค้า | `lib/pending.ts` |
| กู้คืนไฟล์สำรองทับอู่ · นำเข้าไฟล์สร้างอู่ (CLI ย้ายข้อมูล) | `lib/restore-core.ts` → `packages/importer` |

## จะทำ

### 1. บังคับที่ฐานข้อมูล (กันทุกทาง รวมทางที่ยังไม่มีในวันนี้)
แบบเดียวกับโควตารูปสินค้าที่มีอยู่แล้ว (`check_pic_quota` — "บังคับที่ฐานข้อมูล ไม่ใช่ที่โค้ดแอป")
- ไมเกรชันใหม่ `db/032_product_limit.sql` + เติมชุดเดียวกันใน `001_init.sql` (ไฟล์ภาพรวม) — ไม่แก้ไฟล์ที่ปล่อยแล้ว
- ทริกเกอร์ระดับคำสั่ง (ไม่ใช่รายแถว) หลัง **เพิ่มสินค้า** และหลัง **แก้สินค้าจากปิดเป็นเปิดใช้งาน**
  นับสินค้าที่ใช้งานของอู่ที่ถูกแตะ ถ้าเกิน 3,000 ยกเลิกทั้งคำสั่ง พร้อมข้อความไทยและรหัสข้อผิดพลาดเฉพาะ
- ล็อกรายอู่ (`pg_advisory_xact_lock`) ก่อนนับ — สองคนกดเพิ่มพร้อมกันตอนมี 2,999 ต้องได้แค่คนเดียว
- **ระดับคำสั่ง** เพราะกู้คืนไฟล์ใส่สินค้าทีละหลายพันแถวในคำสั่งเดียว ทริกเกอร์รายแถวจะนับซ้ำหลายพันรอบ
- แก้ราคา/ชื่อสินค้าเดิม ไม่นับ ไม่ช้าลง · **อู่ที่เกิน 3,000 อยู่แล้ว (ถ้ามี) ยังใช้และแก้ไขได้ปกติ** แค่เพิ่ม/เปิดใช้งานกลับไม่ได้จนกว่าจะต่ำกว่า

### 2. ข้อความที่อ่านรู้เรื่องในแอป (ก่อนถึงฐานข้อมูล)
ค่าคงที่ `PRODUCT_LIMIT = 3000` ใน `packages/core` — เทสต์ตรวจว่าเลขในไฟล์ SQL ตรงกัน
- **ฟอร์มเพิ่มสินค้า / เปิดใช้งานกลับ / ลงทะเบียนรายการค้างทำ**: ครบแล้วขึ้น
  "สินค้าที่ใช้งานครบ 3,000 รายการแล้ว — ปิดใช้งานสินค้าที่เลิกขายก่อนจึงจะเพิ่มได้" ค่าที่กรอกไว้ไม่หาย
- **นำเข้า CSV**: นับก่อนเริ่ม (ใช้งานอยู่ + รหัสใหม่ที่เปิดใช้งาน + รหัสเดิมที่ถูกเปิดกลับ − รหัสเดิมที่ถูกปิด)
  เกินแล้วปฏิเสธทั้งไฟล์ "ไฟล์นี้จะทำให้มีสินค้าใช้งาน 3,120 รายการ เกิน 120 รายการ — ไม่ได้นำเข้าเลย"
- **กู้คืนไฟล์สำรอง**: ตรวจตอนเปิดดูไฟล์ก่อนกดกู้คืน (หน้าพรีวิว) และตรวจซ้ำก่อนล้างข้อมูลเดิม
  "ไฟล์นี้มีสินค้าใช้งาน 3,050 รายการ เกินกำหนด 3,000 — ไม่ได้กู้คืน ข้อมูลเดิมยังอยู่ครบ"
- ถ้าฐานข้อมูลปฏิเสธด้วยรหัสของเรา (เช่นแข่งกันกดพร้อมกัน) แปลงเป็นข้อความเดียวกัน ไม่ใช่หน้า 500

### 3. ตัวนับที่หน้าทะเบียนสินค้า
หัวหน้าเปลี่ยนจาก "200 รายการ" เป็น **"ใช้งานอยู่ 200 / 3,000 รายการ"** (นับทั้งอู่ ไม่ขึ้นกับตัวกรองที่เลือก)
· มูลค่าสต๊อกยังต่อท้ายเหมือนเดิม

## ไม่แตะ
- ไม่มีปุ่มลบสินค้า (ยังเป็นแค่ปิดใช้งาน) · ไม่มีแถบเตือนใกล้เต็ม (ผู้ใช้เลือกแค่ตัวนับ)
- ตัวเลข 3,000 เท่ากันทุกอู่ ยังไม่ปรับรายอู่จากคอนโซล
- เรื่องไฟล์สำรองคำนวณหัก ณ ที่จ่ายใหม่ (คุยค้างไว้) ไม่รวมในรอบนี้

## เทสต์ (ต้องรันพร้อมฐานข้อมูล `DATABASE_URL=… npm test` — ไม่งั้นถูกข้ามเงียบ ๆ อย่างที่เคยเกิด)
`test/product-limit-db.test.ts` (ฐานข้อมูลจริง):
- ใส่สินค้าใช้งาน 3,000 ได้ · ตัวที่ 3,001 ถูกปฏิเสธด้วยรหัสของเรา
- สินค้าปิดใช้งานเพิ่มได้แม้ครบ 3,000 · ปิดใช้งานหนึ่งตัวแล้วเพิ่มใหม่ได้หนึ่งตัว
- เปิดใช้งานกลับตอนครบ 3,000 ถูกปฏิเสธ · แก้ราคาสินค้าเดิมตอนครบได้ปกติ
- คำสั่งเดียวใส่หลายแถวข้ามเส้น — ถูกยกเลิกทั้งคำสั่ง ไม่มีแถวไหนเข้า
- อู่อื่นไม่ถูกนับรวม
- สองทรานแซกชันพร้อมกันตอนมี 2,999 — ได้เข้าแค่หนึ่ง
- ฐานติดตั้งใหม่กับฐานอัปเกรดได้ทริกเกอร์เหมือนกัน (เทสต์ `migrations.test.ts` เดิม) · checksum ของไฟล์ใหม่ลงทะเบียน

`test/product-limit.test.ts` (แอป): ฟอร์มเพิ่ม/เปิดกลับ/ลงทะเบียนค้างทำตอนครบได้ข้อความไทย ·
CSV ที่เกินถูกปฏิเสธทั้งไฟล์และไม่มีแถวเปลี่ยน · CSV ที่แก้แค่ราคาสินค้าเดิมตอนครบผ่าน ·
กู้คืนไฟล์ที่เกินถูกปฏิเสธก่อนล้างข้อมูลเดิม · ค่าคงที่ใน core ตรงกับเลขในไฟล์ SQL

`e2e`: หัวหน้าทะเบียนสินค้าแสดง "ใช้งานอยู่ N / 3,000 รายการ" ทุกขนาดจอ · N ไม่เปลี่ยนตามตัวกรอง

**กลายพันธุ์**: ทริกเกอร์นับรวมสินค้าปิดใช้งาน · ไม่ล็อกรายอู่ · ไม่ดักการเปิดใช้งานกลับ ·
CSV ตรวจแค่แถวใหม่ไม่นับที่เปิดกลับ · กู้คืนตรวจหลังล้างข้อมูล · ค่าคงที่ core กับ SQL ไม่ตรงกัน
