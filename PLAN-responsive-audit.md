# แผน — ตรวจและแก้การแสดงผลทุกขนาดจอ

branch `fix/responsive-audit` (แตกจาก `main` = `d27eba2`) · **ยังไม่แก้โค้ด รออนุมัติ**

## ผลสำรวจ

สคริปต์ Playwright ไล่ **75 หน้า × 11 ความกว้าง** (360 · 390 · 411 · 430 · 767 · 768 · 1024 · 1279 · 1280 · 1440 · 1920)
วัดครบ **825/825 ครั้ง** (รอบแรกข้าม 309 ครั้ง — เซสชันถูกยกเลิกกลางทางตอนตั้งรหัสผ่านใหม่ และบางหน้าหมดเวลา · รันซ้ำจนครบ)
วัดจาก DOM จริง แล้วเปิดดูภาพจริงประกอบในกลุ่มที่ตัวเลขตีความได้หลายทาง

| อาการ | มือถือ | แท็บเล็ต | เดสก์ท็อป |
|---|---:|---:|---:|
| ตัวหนังสือในปุ่ม/ไทล์/เมนูย่อยตกบรรทัด | 1,069 | 160 | 156 |
| เป้ากดเตี้ยกว่า 44px (จอสัมผัส) | 596 | 333 | — |
| แถวตารางสูงผิดปกติ | 100 | 0 | 1 |
| ทั้งหน้าเลื่อนซ้ายขวา | 44 | 1 | 0 |

บวกสองเรื่องที่สคริปต์วัดไม่ได้ แต่เห็นจากภาพ — แถบเมนูบนเดสก์ท็อปทับกัน · ชิปตัวกรองบนมือถือเรียงลงเต็มจอ

---

## กลุ่มปัญหาตามต้นเหตุ (เรียงตามผลกระทบ)

### ก. ปุ่มในแถวตารางถูกบีบ/ล้นตาราง — ทุกขนาดจอ
หน้า: ลูกค้า/ผู้ขาย · ประวัติรายรับ · ประวัติรายจ่าย · ขายหน้าร้าน
- ที่ 1280: "ใบเสนอราคา" กว้าง **41px ตัวหนังสือ 3 บรรทัด** · ปุ่ม แก้ไข/พิมพ์/เปิด ล้นขอบการ์ดขวา
- มือถือ/แท็บเล็ต: ปุ่มสูง 32–40px
- **ต้นเหตุ** `table.hist .row-acts { display: inline-flex }` specificity สูงกว่า `.row-acts.grid2 { display: grid }`
  ปุ่มที่ออกแบบให้เป็นตาราง 2×2 จึงเรียงแถวเดียว (วัดได้ display = inline-flex) + กฎ nowrap ที่บรรทัด 1337 ขัดกับ white-space: normal ที่บรรทัด 1111
- **แก้** ให้ grid ชนะ · คอลัมน์ปุ่มกว้างพอ 2×2 · ปุ่มไม่ตัดบรรทัด · สูง ≥ 44px เมื่อจอ < 1280

### ข. แถวตารางสูงเกินจริง — ทุกตารางที่มีช่องชื่อตัดบรรทัด
หน้า: ทะเบียนสินค้า (แถวสูง 111px) · หน้าแรก · รายการค้างทำ · ลูกค้า · ชุดอะไหล่ · ถังขยะ · ประวัติ
- **ต้นเหตุ** `.main .wrap { padding-bottom: 80px }` (มือถือ 130px) ตั้งใจให้กรอบหน้า
  แต่ selector โดน **ช่องตาราง `td.wrap` ทั้ง 40 จุด** และ `span.wrap` ในกราฟยอดขายด้วย
  (วัดได้ช่องชื่อ padding-bottom 80px ชื่ออยู่ขอบบน ช่องอื่นอยู่กลาง)
- **แก้** `.main > .wrap` — กรอบหน้าเป็นลูกตรงของ .main (shell.tsx) ช่องตารางไม่โดนอีก

### ค. แถบเมนูบนเดสก์ท็อปทับกล่องชื่อผู้ใช้/ลิขสิทธิ์ — 1280 ถึงราว 1450px
- ที่ 1280: เมนูกว้าง 991px ในกล่อง 823px **ทับกล่องขวา 158px** ("09 ฟอร์มเปล่า" ซ้อน "ทดลองใช้ เหลือ 14 วัน")
- 1279 ลงไปเป็นไอคอนล้วน ไม่ทับ · 1440 ขึ้นไปไม่ทับ
- **ต้นเหตุ** กล่องขวา (ชื่อ · บทบาท + ข้อความลิขสิทธิ์ nowrap) กว้าง 188px ถูกเพิ่มทีหลังตอนย่อชื่อเมนูไปแล้ว
- **แก้ (เสนอ)** แถบบนแสดงลิขสิทธิ์แบบสั้น ("เหลือ 14 วัน" · หมดอายุยังแดงกะพริบ) ข้อความเต็มอยู่ใน title
  ชื่อผู้ใช้/บทบาทย้ายไปอยู่ในลิ้นชักและ title · บีบระยะปุ่มเมนูเล็กน้อย — วัดซ้ำที่ 1280/1366/1440 ต้องไม่ทับ ห้ามเพิ่มจุดตัด

### ง. ชิปตัวกรองบนมือถือเรียงลงทีละปุ่มเต็มจอ
หน้า: หน้าแรก (ช่วงเวลา) · ลูกค้า (ประเภท) · รายรับ · ใบวางบิล · ตรวจนับ · ใบเคลม · จ่ายชำระหลายใบ
- **ต้นเหตุ** ชิปเป็นลูกตรงของ `.toolbar` ซึ่งบนมือถือเป็น `flex-direction: column; align-items: stretch`
  ชิปแต่ละปุ่มจึงยืดเต็มความกว้าง สูง 52px กินครึ่งจอ
- **แก้** ห่อกลุ่มชิปด้วย `.tag-row` ที่มีอยู่แล้ว (เรียงแนวนอน ตัดบรรทัดได้) — 8 ไฟล์

### จ. เมนูย่อยบนมือถือ (ไทล์ 2 คอลัมน์ กว้าง 165px) ตัดคำกลางวลี — 360–430px, 40 หน้า
- "ทะเบียนผู้ / ขาย" · "+ เพิ่มผู้ / ติดต่อ" · "นำเข้า / ส่งออก / CSV" 3 บรรทัด
- ไทล์ชื่อ + เลขเมนูสองชั้นเป็นดีไซน์ (ไม่นับว่าพัง) — ที่พังคือชื่อถูกตัดกลางคำเพราะไทล์แคบ
- **แก้** บนจอฐานลดระยะขอบ/ไอคอน/ขนาดตัวอักษรของไทล์ ให้ชื่อทั่วไปอยู่บรรทัดเดียว ชื่อยาวไม่เกินสองบรรทัด

### ฉ. หน้าพิมพ์รายงานบนมือถือล้นจอ + ปุ่มแถบพิมพ์ตกบรรทัด
หน้า: ทะเบียนลูกค้า · รายการรายจ่าย · รายงานการเงิน (กว้างถึง 873px) · ทะเบียนสินค้า · ใบนับสต๊อก · ของใกล้หมดอายุ · รายการค้างทำ · ใบตรวจนับ · ฉลากบาร์โค้ด (624px)
- ปุ่ม "พิมพ์เอกสาร" 2 บรรทัด · "← กลับทะเบียนผู้ติดต่อ" 3 บรรทัด
- (หน้าพิมพ์เอกสาร/ฟอร์มเปล่าที่แก้รอบก่อนผ่านแล้ว)
- **แก้** แถบพิมพ์ตัดแถวได้ ปุ่มไม่ตัดบรรทัด · ตารางในกระดาษเลื่อนในกรอบของตัวเองบนจอแคบ
  · **ผลพิมพ์ A4 ต้องเท่าเดิม** (เทียบจำนวนหน้า PDF ก่อน/หลังทุกหน้า)

### ช. ปุ่มสั้นตกบรรทัดบนมือถือ
- ตัวแบ่งหน้า "ถัดไป" (55px) · "🖨 พิมพ์รายงาน" · "+ เปิดใบเคลม" ในหัวหน้า
- **แก้** ปุ่มคำสั้นไม่ตัดบรรทัด · กลุ่มปุ่มในหัวหน้าย้ายลงแถวใหม่ทั้งก้อนแทน

### ซ. เป้ากดเล็ก/ข้อความยาวในฟอร์ม
- ปุ่ม "×" ลบบรรทัดในใบเคลม สูง 21px → 44px บนจอสัมผัส
- ไทล์วิธีคิดต้นทุนในฟอร์มสินค้า ("FEFO — หมดอายุก่อนออกก่อน (ค่าเริ่มต้น)") 4 บรรทัดที่ 360px → เรียงเต็มแถวบนมือถือ

---

## ไม่นับว่าพัง (ตั้งใจ)
- ตารางกว้างบนมือถือ/แท็บเล็ตเลื่อนซ้ายขวา **ในกรอบตาราง** (หน้าไม่เลื่อน) — ตัดสินใจรอบก่อน
- ไทล์เมนูย่อยมือถือแสดงชื่อกับเลขเมนูสองชั้น
- ผลพิมพ์ A4
- ค่าที่ 767px บนหน้าเพิ่มลูกค้าที่วัดได้เพี้ยนหนึ่งครั้ง — วัดซ้ำปกติ (ช่องกรอก 44px) เกิดตอนหน้ากำลังโหลด

## กติกาการแก้
- จุดตัดมีแค่ 768 และ 1280 ใช้ min-width เท่านั้น (test/css-layers.test.ts) — ไม่เพิ่มจุดตัด
- แก้ที่ต้นเหตุร่วม ไม่ไล่แก้ทีละหน้า · จุดที่ต่างจากสเปกบันทึก docs/ui-layout-spec.md §13 พร้อมตัวเลขที่วัดได้
- ห้ามผ่อนเทสต์ · ลองย้อนการแก้แต่ละกลุ่มแล้วเทสต์ต้องแดง

## เทสต์ถาวรที่จะเพิ่ม (e2e ครบ 3 project + วนความกว้างในตัวเทสต์ที่จำเป็น)
1. ทั้งหน้าไม่เลื่อนซ้ายขวา — หน้ารายการหลักและหน้ารายงาน
2. ปุ่มในแถว/ตัวแบ่งหน้า/แถบพิมพ์/ปุ่มหัวหน้า/เมนูบน ไม่ตกบรรทัด
3. แถบเมนูบนไม่ทับกล่องขวาที่ 1280 · 1366 · 1440 · 1920
4. แถวตารางไม่มี padding ล้นจาก .wrap (ความสูงแถวสมเหตุผล)
5. เป้ากด ≥ 44px เมื่อจอ < 1280 บนหน้าที่มีปุ่มในแถว
6. ชิปตัวกรองบนมือถือเรียงแนวนอน

## ตรวจรวมก่อนส่ง
typecheck · npm test พร้อมฐานข้อมูล · e2e ครบ · รันสคริปต์สำรวจ 75 หน้า × 11 ความกว้างซ้ำ ต้องไม่เหลืออาการในกลุ่ม ก–ซ
· ภาพก่อน/หลังของทุกกลุ่ม · หน้าพิมพ์ยังได้ A4 หน้าเดียวเท่าเดิม · ไม่ merge จนกว่าอนุมัติ
