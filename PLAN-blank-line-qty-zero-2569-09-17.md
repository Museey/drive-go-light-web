# แผน — บรรทัดรายการที่ยังไม่กรอก จำนวนเป็น 0 (17 ก.ย. 2569)

branch `feat/blank-line-qty-zero-2569-09-17` (แตกจาก `main` = `fa627c4`) · ผู้ใช้ส่งภาพหน้าซื้อสินค้า: บรรทัด 3–5 ที่ยังว่างขึ้นจำนวน 1

## ตัดสินแล้ว (ผู้ใช้เลือก)
- ใช้กับ **ทุกฟอร์มที่มีตารางรายการ**
- บรรทัดว่างโชว์ **0** · พอบรรทัดเริ่มมีของ **จำนวนขึ้น 1 ให้เอง** (ทั้งเลือกสินค้า/ยิงบาร์โค้ด และพิมพ์ชื่อเอง) — กดบันทึกได้เหมือนเดิม

## ต้นเหตุ
บรรทัดเปล่าทุกฟอร์มสร้างด้วยจำนวน 1 (ต้นแบบก็ 1) · บรรทัดเปล่าไม่ถูกส่งไปบันทึก จึงไม่กระทบยอดในระบบ แค่หน้าจอ

## ฟอร์มที่แก้ (ค้นทั้งโค้ดแล้ว)
| ฟอร์ม | ไฟล์ | บรรทัดว่างคือ |
|---|---|---|
| ซื้อสินค้า · บันทึกค่าใช้จ่าย (ฟอร์มเดียวกัน) | `app/expense/buy-editor.tsx` | ไม่มีสินค้า ชื่อ รหัส |
| ใบเสนอราคา · ใบส่งมอบ · ใบเสร็จ · ขายหน้าร้าน | `app/income/doc-editor.tsx` | ไม่มีสินค้า ชื่อ รหัส |
| ใบเคลมสินค้า | `app/stock/claim/claim-editor.tsx` | ไม่มีสินค้าและชื่อ |
| ชุดอะไหล่ | `app/stock/kits/kit-editor.tsx` | ไม่มีสินค้าและชื่อ |

## จะทำ
กติกาเดียวไว้ที่ `lib/line-qty.ts` (ทดสอบได้ตรง ๆ) แล้วทั้ง 4 ฟอร์มเรียกตัวเดียวกัน:
- บรรทัดเปล่าใหม่ (เปิดฟอร์ม · + เพิ่มบรรทัด · Enter บรรทัดสุดท้าย · ลบแล้วเติมให้ครบ 5) → จำนวน **0**
- บรรทัดว่างที่เริ่มมีของ (พิมพ์ชื่อ/รหัส · เลือกจากผลค้นหา · ยิงบาร์โค้ด) และจำนวนยังเป็น 0 → **1**
  (ยิงแบบ `40*` ได้ 40 · พิมพ์จำนวนไว้ก่อนแล้วค่อยพิมพ์ชื่อ → คงจำนวนที่พิมพ์)
- ลบชื่อ/รหัสจนบรรทัดกลับเป็นว่าง → กลับเป็น **0**
- ปุ่ม "+ ค่าแรง" ในฟอร์มรายรับ (บรรทัดมีชื่อตั้งแต่สร้าง) → 1 เหมือนเดิม
- ใบที่เปิดแก้ไข: บรรทัดเดิมคงจำนวนเดิม · บรรทัดว่างที่เติมให้ครบ 5 → 0

## ไม่แตะ
ตรรกะบันทึก (บรรทัดว่างถูกตัดทิ้งก่อนส่งอยู่แล้ว) · ข้อความ "จำนวนทุกบรรทัดต้องมากกว่า 0" · ตรวจนับสต๊อก (ไม่มีบรรทัดเปล่า)

## เทสต์
- หน่วย `test/line-qty.test.ts`: ว่าง→มีของได้ 1 · พิมพ์จำนวนไว้ก่อนคงไว้ · แก้จำนวนตรง ๆ ไม่ถูกทับ · ลบจนว่างกลับ 0 · บรรทัดมีของแก้ช่องอื่นไม่แตะจำนวน
- e2e ทั้ง 4 ฟอร์ม 3 ขนาดจอ: บรรทัดว่างโชว์ 0 · พิมพ์ชื่อแล้วขึ้น 1 · บันทึกได้ · เลือกสินค้าจากค้นหาได้ 1 (ซื้อสินค้า/รายรับ)
- ทั้งชุดเดิมต้องเขียว (หลายเทสต์พิมพ์ชื่อแล้วบันทึกโดยไม่กรอกจำนวน — ต้องยังผ่าน)
- กลายพันธุ์: บรรทัดเปล่ายังเป็น 1 · ไม่ขึ้น 1 ตอนเริ่มมีของ · ฟอร์มใดฟอร์มหนึ่งไม่เรียกกติกา
