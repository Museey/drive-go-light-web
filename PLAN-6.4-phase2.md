# ช่วงที่ 2 — เปลือก UI ให้เป็นรุ่น 6.4 · **เสร็จแล้ว**

แผนละเอียดสำหรับช่วงที่ 2 ของ [PLAN-6.4.md](PLAN-6.4.md) · ประเมิน 5–7 วัน

---

## Context

เว็บที่ทำมาทั้งหมดสร้างตามหน้าตาของรุ่น 3.6 — แถบเมนูซ้าย สีน้ำเงินเทา ไม่มีไอคอน
ส่วนรุ่น 6.4 ที่จะใช้จริงเปลี่ยนหน้าตาไปทั้งหมด: CSS โตจาก 39 KB เป็น 90 KB
เมนูหลักย้ายขึ้นเป็นแถบบน สีเปลี่ยนเป็นเขียว และมีชุดไอคอนวาดมือ 34 ตัว

ทำก่อนโมดูลใหม่ทั้งสี่ (ใบวางบิล เคลม ตรวจนับ สิทธิ์ละเอียด) เพราะถ้าทำหลัง
ต้องเขียนหน้าจอของโมดูลเหล่านั้นสองรอบ — รอบแรกบนหน้าตาเก่า รอบสองตอนรื้อ

**ขอบเขต** เปลี่ยนเฉพาะเปลือก ไม่แตะตรรกะธุรกิจ เส้นทาง server action หรือหน้าพิมพ์เอกสาร
ชุดทดสอบ 277 ข้อต้องผ่านครบเหมือนเดิมตลอดทาง — ถ้าข้อไหนแดง แปลว่าเผลอแตะของที่ไม่ควรแตะ

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

**1. คงหน้าแยกไว้ ไม่ทำ docwin** — ตกลงกันแล้ว

6.4 เปิดเอกสารเป็นหน้าต่างทับหน้าจอ (`.docwin`) เพราะเป็นไฟล์ HTML ไฟล์เดียวที่ไม่มีระบบเส้นทาง
จึงไม่มีทางเลือกอื่น ส่วนเราเปิดเป็นหน้าที่มี URL ของตัวเอง ซึ่งส่งลิงก์ให้กันได้
กดย้อนกลับของเบราว์เซอร์ทำงาน พิมพ์ง่าย และเปิดหลายใบคนละแท็บได้

**เอาเฉพาะสีและหัวแถบเขียวของ docwin มาใช้กับหัวหน้าเอกสาร** เพื่อให้หน้าตากลมกลืน
แต่ยังเป็นหน้าจริง — ผู้ใช้เดิมจะเห็นสีเดียวกัน ต่างแค่เอกสารเต็มหน้าจอแทนที่จะลอยทับ

**2. ไม่ยกกองซ้อนหน้า (`NAV[]`) มา**

6.4 เก็บประวัติหน้าเองในอาเรย์ 40 ช่อง แล้วมีปุ่มย้อนกลับลอยอ่านจากอาเรย์นั้น
เพราะทุกหน้าคือ URL เดียวกัน เบราว์เซอร์จึงไม่รู้ว่าเปลี่ยนหน้า

ของเราทุกหน้ามี URL จริงอยู่แล้ว ปุ่มย้อนกลับลอยเรียก `router.back()` ได้ตรง ๆ
ได้พฤติกรรมเดียวกันโดยไม่ต้องดูแลกองซ้อนเอง และปุ่มย้อนกลับของเบราว์เซอร์กับของแอปตรงกันเสมอ

**3. ไอคอนเป็นคอมโพเนนต์ ไม่ใช่ไฟล์รูป**

ไอคอนทั้ง 34 ตัวเป็น SVG เส้น `viewBox="0 0 48 48"` รับสีเป็นพารามิเตอร์
ยกมาเป็นคอมโพเนนต์ React ตัวเดียวที่รับชื่อกับสี — ไม่ต้องโหลดไฟล์เพิ่ม ไม่ต้องมี sprite
และเปลี่ยนสีตามธีมมืดได้

**4. ไม่แตะหน้าพิมพ์เอกสาร**

6.4 ไม่ได้แก้หน้ากระดาษเลย (ตรวจแล้วตอนเทียบรุ่น) หน้าพิมพ์ทั้ง 1,231 บรรทัดจึงอยู่เหมือนเดิม
ยืนยันด้วยการที่เทสต์ `print.test.ts` ต้องผ่านโดยไม่แก้

---

## หน้าตาที่ต้องทำให้ตรง

### แถบเมนูหลัก — จากซ้ายเป็นบน

| | ตอนนี้ | 6.4 |
|---|---|---|
| ตำแหน่ง | แถบซ้ายกว้างคงที่ | แถบบน `position:sticky` เลื่อนซ้ายขวาได้ |
| สี | น้ำเงินเทา `#17607A` | เขียวไล่เฉด `#0F5C3E` → `#0B4A32` |
| ปุ่มที่เลือก | พื้นเข้ม | **พื้นขาว ตัวหนังสือเขียวเข้ม** |
| เมนูย่อย | ไม่มี | กดแล้วหล่นแผงการ์ด `.mmenu` |
| บนมือถือ | ซ่อน | เลื่อนได้ · ปุ่มหน้าแรกตรึงซ้ายสุดตลอด |

### แผงเมนูหล่น (`.mmenu` + `.mcard`)

กดเมนูที่มีแท็บย่อยแล้วหล่นแผงกว้าง 270px มีการ์ดสองคอลัมน์
แต่ละใบมีไอคอน 42px · เลขกำกับมุมซ้ายบน · ชื่อ · คำอธิบายบรรทัดเดียว

### เมนูย่อยแนวตั้ง (`.subwrap` + `.subnav`)

หกเมนูที่มีแท็บย่อยใช้โครง grid `206px 1fr` — คอลัมน์ซ้ายเป็นการ์ดปุ่มแนวตั้ง `position:sticky`
มีไอคอน 30px ขอบ navy 1.5px และขยับขวา 2px ตอนชี้เมาส์
ใต้รายการแท็บมี **การ์ดปุ่มทำงาน** (`.actcard`) เช่น "+ เพิ่มสินค้า" พร้อมคำอธิบายบรรทัดเล็ก

### หน้าแรก (`.hgrid` + `.hcard`)

การ์ดสองคอลัมน์ หัวการ์ดมีไอคอนกับปุ่ม "ดู →" ชิดขวา ลูกศรขยับตอนชี้เมาส์

### ชุดสี

```
--steel      #1D8A5F   เขียวหลัก        (เดิม #17607A)
--steel-dk   #0F5C3E   เขียวเข้ม
--steel-soft #E9F8F0   เขียวอ่อนพื้นหลัง
--navy       #1B2A4A   ขอบการ์ด
--surface    #F3F7F5   พื้นหลังหน้า      (เดิม #EFF2F4)
--grn-hi     #D8F5E6   สีตอนกด
```
สีสถานะ (`--ok --warn --danger`) และสีป้ายสต๊อกคงเดิม — 6.4 ไม่ได้เปลี่ยน

---

## ไฟล์ที่จะแตะ

### สร้างใหม่

| ไฟล์ | หน้าที่ |
|---|---|
| `components/icon.tsx` | ไอคอน 34 ตัว ยกจาก `IC` ของ 6.4 รับ `name` กับ `color` |
| `components/menu-bar.tsx` | แถบเมนูบน + แผงหล่น (client component เพราะต้องเปิดปิด) |
| `components/sub-nav.tsx` | เมนูย่อยแนวตั้ง + การ์ดปุ่มทำงาน |
| `components/back-fab.tsx` | ปุ่มย้อนกลับลอย เรียก `router.back()` |
| `components/menu-map.ts` | ผังเมนู เลข ไอคอน สี และคำอธิบาย — ที่เดียวที่ทั้งสามคอมโพเนนต์อ่าน |

### แก้

| ไฟล์ | ทำอะไร |
|---|---|
| `app/globals.css` | ชุดสีใหม่ · โครงแถบบน · `.mmenu .mcard .subwrap .subnav .actcard .hcard` |
| `components/shell.tsx` | จาก `rail + main` เป็น `menubar + main` รับ prop เมนูย่อย |
| `components/rail.tsx` | ลบ แทนที่ด้วย `menu-bar.tsx` |
| `app/page.tsx` | การ์ดหน้าแรกแบบใหม่ |
| 6 หน้ารายการหลัก | ใส่ `SubNav` แทนแถบชิปแท็บย่อย — income · expense · stock · finance · customers · settings |
| `components/finance-nav.tsx` | ยุบเข้า `sub-nav.tsx` |

หน้าอื่นอีก 27 หน้าไม่ต้องแก้เพราะใช้ `Shell` อยู่แล้ว — เปลี่ยนที่ `Shell` ที่เดียวได้ทั้งหมด

---

## ลำดับการทำ

1. **ชุดสีและไอคอน (1 วัน)** — เปลี่ยนโทเคนสี ยกไอคอน 34 ตัว ทำหน้าตัวอย่างไว้ดูทีละตัว
2. **แถบเมนูบน + แผงหล่น (1.5–2 วัน)** — รวมพฤติกรรมบนมือถือและปุ่มหน้าแรกที่ตรึงไว้
3. **เมนูย่อยแนวตั้ง (1.5–2 วัน)** — ทั้งหกเมนู พร้อมการ์ดปุ่มทำงาน
4. **หน้าแรกแบบการ์ด (1 วัน)**
5. **ปุ่มย้อนกลับลอย + เก็บรายละเอียด (0.5–1 วัน)** — หัวหน้าเอกสารสีเขียว · แผงเลือกเดือน · ป๊อปอัปยืนยัน/บันทึกแล้ว

หยุดหลังข้อไหนก็ได้ ระบบยังใช้งานได้ — แต่ละข้อเปลี่ยนเปลือกเป็นชั้น ๆ ไม่ทิ้งครึ่ง ๆ กลาง ๆ

---

## ชุดทดสอบ

เปลือกหน้าตาเขียนเทสต์อัตโนมัติได้จำกัด สิ่งที่ทำได้และคุ้ม:

**1. ของเดิมต้องไม่พัง** — เทสต์ 277 ข้อผ่านครบทุกขั้น โดยเฉพาะ `print.test.ts`
ที่พิสูจน์ว่าไม่ได้เผลอแตะหน้าพิมพ์

**2. ผังเมนูตรงกับ 6.4** — `menu-map.test.ts` ตัวใหม่ ดึง `MENU_ICON` `SUB_ICON`
และแท็บทั้งหกชุดออกจากไฟล์ 6.4 แล้วเทียบว่าเลขกำกับ ชื่อ และชื่อไอคอนตรงกันทุกช่อง
กันการพิมพ์เลขผิดหรือลืมแท็บ ซึ่งเป็นความผิดพลาดที่เกิดง่ายและหายาก

**3. ตรวจด้วยตาทีละเมนู** บนจอ 1440 และ 375 เทียบกับ 6.4 ที่เปิดคู่กัน

---

## เสร็จเมื่อ

- ทั้งแปดเมนูมีแถบบนสีเขียว แผงหล่นการ์ด และเมนูย่อยแนวตั้งครบ
- ไอคอนครบ 34 ตัว ตรงตำแหน่งกับที่ 6.4 กำหนดไว้ใน `MENU_ICON` และ `SUB_ICON`
- เทสต์ 277 ข้อเดิมผ่านครบ บวกเทสต์ผังเมนูใหม่
- บนจอ 375 แถบเมนูเลื่อนได้ ปุ่มหน้าแรกตรึงซ้าย และไม่มีอะไรล้นขอบจอ
- ปุ่มย้อนกลับลอยพาไปหน้าก่อนหน้าได้จริง และตรงกับปุ่มย้อนกลับของเบราว์เซอร์

---

## ผลที่ทำจริง

ครบทั้งห้าข้อตามลำดับที่วางไว้ · เทสต์ 277 ข้อเดิมผ่านครบทุกขั้น บวกเทสต์ผังเมนู 7 ข้อ

**เรื่องที่แผนไม่ได้เผื่อไว้และเจอตอนทำ**

1. **แผงหล่นถูกแถบตัดขอบ** — แถบเมนูมี `overflow-x` สำหรับเลื่อนบนมือถือ
   แผงที่วางแบบ `absolute` ข้างในจึงหายไป ต้องใช้ `fixed` แล้ววัดตำแหน่งจากปุ่มเอง
   (เป็นเหตุผลเดียวกับที่ 6.4 ใช้ `position:fixed`)

2. **แผงปิดทันทีที่เปิด** — กดปุ่มในแถบที่เลื่อนได้ เบราว์เซอร์เลื่อนปุ่มเข้ามาในจอ
   เกิด `scroll` แล้วตัวปิดแผงทำงานทันที แก้เป็นให้แผงตามปุ่มไปแทนการปิด

3. **เมนูย่อยบีบเนื้อหาบนมือถือ** — ยกกติกา `@media(max-width:980px)` ของ 6.4 มา
   เมนูย่อยกลายเป็นตารางสามคอลัมน์เหนือเนื้อหา และสองคอลัมน์ที่ต่ำกว่า 560px

4. **เพิ่ม `tools/dev-seed.sh`** — ชุดทดสอบที่แตะฐานข้อมูลล้าง schema ทุกครั้ง
   ข้อมูลตัวอย่างที่เปิดดูอยู่จึงหายไปด้วย สคริปต์นี้เอากลับมาในคำสั่งเดียว

**หมายเหตุการทำงาน** อย่ารัน `npm run build` ขณะ dev server เปิดอยู่ —
build เขียนทับ `.next` ที่ dev server ใช้อยู่ แล้วหน้าจะพังจนกว่าจะรีสตาร์ท

---

## ที่ไม่ทำในช่วงนี้

- **docwin** — ตกลงกันแล้วว่าคงหน้าแยกไว้ เอาเฉพาะสีและหัวแถบมาใช้
- **กองซ้อนหน้าเอง** — ใช้ประวัติของเบราว์เซอร์แทน
- **โมดูลใหม่** — ใบวางบิล เคลม ตรวจนับ อยู่ช่วงที่ 3–5 ช่วงนี้ทำแค่ที่ทางให้มันลง
- **ธีมมืด** — 6.4 ไม่มี ของเราก็ยังไม่มี ไม่เพิ่มตอนนี้
