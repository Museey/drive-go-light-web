# แผน — ยอดชำระบนใบเสร็จ · ปุ่มย้อนกลับหน้าพิมพ์ · สถานะ "เรียบร้อย" · ล็อกเอกสารที่บันทึกแล้ว (17 ก.ย. 2569)

branch `feat/doc-lock-receipt-pay-2569-09-17` (แตกจาก `main` = `c82a2c1`) · งานตรรกะระบบ + หน้าจอ · อ้างอิงต้นแบบ `dgl-prototype-2569-09-13-mobile.html`

## ตัดสินแล้ว (ผู้ใช้เลือก)
- popup ขึ้น **ตอนกดปุ่มบนใบที่บันทึกแล้ว** (ไม่ใช่ทันทีที่เปิดดู)
- กติกาแก้ไข/ยกเลิก **ตามต้นแบบ** (ดูข้อ 4)
- ใบส่งมอบ: **"เรียบร้อย" ทันทีที่บันทึก · เลยวันครบกำหนดแล้วยังไม่ได้เงิน ยังขึ้น "เกินกำหนด"**
- ใบเสร็จพิมพ์: **เพิ่ม "ชำระแล้ว" + "คงค้างชำระ" ต่อจากรวมทั้งสิ้น** (ต้นแบบไม่มี — ผู้ใช้ขอเพิ่ม)

## ต้นเหตุที่เจอ (อ่านโค้ดแล้ว)
| อาการที่แจ้ง | ต้นเหตุ |
|---|---|
| ใบเสร็จไม่แสดงยอดชำระ/คงค้าง | หน้าพิมพ์ทำตามต้นแบบ ซึ่งไม่มีสองบรรทัดนี้ — ยอดในระบบถูกต้อง แค่ไม่ได้พิมพ์ |
| หน้าพิมพ์ไม่มีปุ่มย้อนกลับ | หน้าพิมพ์ทุกหน้าไม่ได้อยู่ในโครงหน้าหลัก ปุ่มย้อนกลับลอยจึงไม่ถูกวาด |
| ประวัติขึ้น "ค้างส่งมอบ" ทั้งที่ออกใบส่งมอบแล้ว | **บั๊ก** — ระบบไปดูว่าใบเสนอราคาออกใบต่อหรือยัง **เฉพาะตอนอยู่แท็บใบเสนอราคา** แท็บ "ทั้งหมด" / การ์ดมือถือ / ผลค้นหา จึงขึ้นค้างส่งมอบเสมอ |
| ใบเสนอราคาที่ออกใบส่งมอบแล้ว ยังออกใบใหม่ได้อีก | **บั๊ก** — แถบขั้นตอน A→B→C ซ่อนทางไปต่อแล้ว แต่ปุ่ม "ออกใบส่งมอบ…" ใต้แถบยังโชว์ และฝั่งเซิร์ฟเวอร์ไม่กันใบต่อใบที่สอง |
| (เจอเพิ่ม) ยกเลิกใบส่งมอบแล้ว ใบเสนอราคายังนับว่าออกใบต่อแล้ว | ตอนยกเลิกไม่ได้คืนสถานะใบต้นทาง — หน้าแรก "งานค้างส่งมอบ" จึงนับขาด |

## จะทำ

### 1. ใบเสร็จพิมพ์: ชำระแล้ว + คงค้างชำระ (ข้อ 1, 2)
ตารางยอดรวมท้ายใบเสร็จ (เฉพาะใบเสร็จ) ต่อจาก "รวมทั้งสิ้น":
```
รวมทั้งสิ้น            1,500.00
หัก ณ ที่จ่าย 3%          −30.00   ← เฉพาะใบที่มีหัก ณ ที่จ่าย ไม่งั้นตัวเลขบวกลบไม่ลงกัน
ชำระแล้ว                 500.00
คงค้างชำระ               970.00   (ครบแล้วขึ้น 0.00)
```
- ยอดชำระ = ทุกครั้งที่รับไว้กับใบเสร็จใบนั้น ณ ตอนกดพิมพ์ (รับเพิ่มทีหลัง พิมพ์ใหม่ได้ยอดใหม่)
- ใบชนิดอื่นหน้าตาเดิมทุกอย่าง

### 2. ปุ่มย้อนกลับบนหน้าพิมพ์ (ข้อ 1)
- ปุ่ม "← ย้อนกลับ" ลอยมุมขวาล่างแบบเดียวกับหน้าอื่น ใส่ในหน้าพิมพ์ทุกหน้า (เอกสารขาย/ซื้อ ใบวางบิล ใบเคลม ใบตรวจนับ และรายงาน)
- ไม่ติดไปบนกระดาษ · มือถือไม่มีแถบล่างในหน้าพิมพ์ ปุ่มจึงชิดล่างไม่ลอยค้างกลางอากาศ
- เปิดหน้าพิมพ์ในแท็บใหม่ (ไม่มีหน้าก่อนหน้า) → กดแล้วไปหน้าเอกสารใบนั้นแทนการไม่ทำอะไร

### 3. สถานะ "เรียบร้อย" (ข้อ 3.2, 4)
ใช้ชิปเดียวกันทุกที่ — ตารางประวัติ · การ์ดมือถือ · ประวัติในหน้าลูกค้า · หน้าเอกสาร
| ชนิด | ก่อน | หลัง |
|---|---|---|
| ใบเสนอราคา ยังไม่ออกใบต่อ | ค้างส่งมอบ | ค้างส่งมอบ (เดิม) |
| ใบเสนอราคา ออกใบส่งมอบ/ใบเสร็จแล้ว | ออกใบต่อแล้ว (หรือค้างส่งมอบเพราะบั๊ก) | **เรียบร้อย** |
| ใบส่งมอบ/ใบกำกับภาษี | ค้างชำระ / ชำระครบ / เกินกำหนด | **เรียบร้อย** · เลยวันครบกำหนดแล้วยังค้าง → **เกินกำหนด** · ยอดคงค้างยังโชว์ตัวเลขเหมือนเดิม |
| ใบส่งมอบที่ออกใบเสร็จต่อแล้ว | ค้างชำระได้ ทั้งที่หนี้ย้ายไปใบเสร็จแล้ว | **เรียบร้อย** ไม่เตือนเกินกำหนด (ติดตามหนี้ที่ใบเสร็จ — ตรงกับหน้าลูกหนี้ที่ทำอยู่แล้ว) |
| ใบเสร็จ | ค้างชำระ / ชำระครบ / เกินกำหนด | เดิม |
| ใบที่ยกเลิก | ยกเลิก | เดิม |

- แก้บั๊กแท็บทั้งหมด: หาใบต่อของใบเสนอราคาทุกแถวที่อยู่ในหน้า ไม่ใช่เฉพาะแท็บใบเสนอราคา (คิวรีเดียวทั้งหน้าเหมือนเดิม)
- ตัวกรอง "เฉพาะงานค้างส่งมอบ" และการ์ดหน้าแรกนับตรงกับชิป

### 4. ล็อกเอกสารที่บันทึกแล้ว + popup (ข้อ 3.3)
**popup** เมื่อกด "แก้ไข" บนใบที่บันทึกแล้ว (หน้าเอกสาร และปุ่มแก้ไขในแถวประวัติ):
```
┌ เอกสารได้บันทึกเรียบร้อยแล้ว ─────────────┐
│ ใบเสนอราคา QT690917-0001 · ประเสริฐ ทองมาก │
│ ต้องการแก้ไขใช่หรือไม่                       │
│ [แก้ไขเอกสาร] [ยกเลิกเอกสาร] [ปิด]          │
└────────────────────────────────────────┘
```
- **แก้ไขเอกสาร** → เปิดฟอร์มแก้ **ใบเดิม เลขที่เดิม** (ไม่สร้างใบใหม่)
- **ยกเลิกเอกสาร** → แผงกรอกเหตุผลที่มีอยู่แล้ว → ยืนยัน → ใบขึ้น "ยกเลิก" ในประวัติ
  (ประวัติยังซ่อนใบที่ยกเลิกไว้เป็นค่าตั้งต้น ติ๊ก "รวมใบที่ยกเลิก" หรือค้นเลขที่แล้วเจอ — เหมือนที่ใช้อยู่ ·
  ต้นแบบโชว์ใบยกเลิกตลอด ถ้าอยากให้โชว์ตลอดบอกได้ครับ)
- ใบที่แก้ไม่ได้ → popup เดียวกัน แต่ปุ่มแก้ไขเป็นสีจาง พร้อมเหตุผล เหลือ [ยกเลิกเอกสาร] [ปิด]

**กติกาตามต้นแบบ**
| | แก้ในใบเดิม | ยกเลิก |
|---|---|---|
| ใบเสนอราคา / ใบส่งมอบ / ใบกำกับภาษี ที่ยังไม่รับเงิน | **ได้ แม้ออกใบต่อไปแล้ว** (ใบต่อไม่เปลี่ยนตาม) | ได้ แม้มีใบต่อ |
| ใบเสร็จ (ตัดสต๊อกแล้ว) | ไม่ได้ — "ยกเลิกใบนี้แล้วออกใบใหม่" | ได้ (คืนสต๊อกให้) |
| ใบที่รับเงินแล้ว | ไม่ได้ — "ยกเลิกใบนี้แล้วออกใบใหม่" | ได้ |
| ใบที่อยู่ในใบวางบิล | ตามข้างบน | ไม่ได้จนกว่าเอาออกจากใบวางบิล (กติกาเดิมที่ต้นแบบไม่มี — คงไว้เพราะลูกค้าถือใบวางบิลอยู่) |

เปลี่ยนจากตอนนี้: ใบที่ออกใบต่อแล้ว **แก้และยกเลิกได้** (ตอนนี้ล็อกทั้งคู่)

**กันออกใบต่อซ้ำ** (ต้นแบบให้ทำขั้นถัดไปได้เฉพาะใบที่ยังไม่มีใบต่อ)
- หน้าเอกสาร: ใบที่มีใบต่อแล้ว ซ่อนปุ่ม "ออกใบส่งมอบ / ออกใบเสร็จ" (แถบขั้นตอนพาไปใบต่อที่มีอยู่)
- ฝั่งเซิร์ฟเวอร์: บันทึกใบใหม่ที่อ้างใบต้นทางซึ่งมีใบต่ออยู่แล้ว → ปฏิเสธ "ใบ X ออก Y ต่อไปแล้ว" (ล็อกใบต้นทางก่อนตรวจ กันกดพร้อมกันสองเครื่อง)
- เปิดลิงก์ออกใบต่อค้างไว้/พิมพ์ URL เอง → หน้าฟอร์มบอกเลยว่าออกต่อไปแล้ว พร้อมลิงก์ไปใบนั้น ไม่ต้องกรอกทั้งใบแล้วค่อยพัง
- ยกเลิกใบต่อแล้ว → ใบต้นทางกลับมาออกใบต่อได้ และใบเสนอราคากลับเป็น "ค้างส่งมอบ" (ตามต้นแบบ)

## ไม่แตะ
- ฝั่งรายจ่าย (ใบซื้อ/ค่าใช้จ่าย) ใบวางบิล ใบเคลม — กติกาแก้/ยกเลิกเดิม
- หน้าตากระดาษของใบชนิดอื่น · เดสก์ท็อปของหน้าที่ไม่เกี่ยว
- เรื่องไฟล์สำรองคำนวณหัก ณ ที่จ่ายใหม่ (ยังพักไว้)

## ผลที่ควรรู้ก่อนอนุมัติ
- แก้ใบเสนอราคา/ใบส่งมอบหลังออกใบต่อแล้ว **ใบต่อไม่เปลี่ยนตาม** — เช่นแก้ราคาในใบส่งมอบหลังออกใบเสร็จ ยอดสองใบจะไม่เท่ากัน (ต้นแบบเป็นแบบนี้)
- ยกเลิกใบต้นทางที่มีใบต่อ ใบต่อยังอยู่และยังนับยอด (ต้นแบบเป็นแบบนี้)

## เทสต์
ฐานข้อมูลจริง (`DATABASE_URL=… npm test`):
- ออกใบส่งมอบจากใบเสนอราคาที่มีใบส่งมอบแล้ว → ปฏิเสธ · ยกเลิกใบแรกแล้วออกได้ · สองทรานแซกชันพร้อมกันได้ใบเดียว
- ออกใบเสร็จจากใบเสนอราคาที่ใบส่งมอบมีใบเสร็จแล้ว → ปฏิเสธ
- แก้ใบเสนอราคาที่มีใบต่อได้ · แก้ใบเสร็จ/ใบที่รับเงินไม่ได้ · ยกเลิกใบที่มีใบต่อได้ · ยกเลิกใบในใบวางบิลไม่ได้
- ยกเลิกใบส่งมอบ → ใบเสนอราคากลับเป็นค้างส่งมอบ (มีใบต่ออีกใบที่ยังไม่ยกเลิก → ไม่กลับ)
- รายการแท็บทั้งหมดได้ใบต่อของใบเสนอราคาครบ

หน่วย: ชิปสถานะทุกกรณีในตารางข้อ 3

e2e (production build · 3 ขนาดจอ):
- ขายหน้าร้านชำระบางส่วน → พิมพ์ → ชำระแล้ว/คงค้างถูก · มีปุ่มย้อนกลับ กดแล้วกลับหน้าก่อน · ปุ่มไม่ขึ้นตอนสั่งพิมพ์
- ใบเสนอราคา → ใบกำกับภาษี → ใบเสร็จชำระบางส่วน → พิมพ์ถูก · ประวัติแท็บทั้งหมด: ใบเสนอราคา/ใบส่งมอบ "เรียบร้อย"
- หน้าใบเสนอราคาที่ออกใบต่อแล้ว: ไม่มีปุ่มออกใบต่อ · URL ออกใบต่อตรง ๆ ขึ้นข้อความ
- กดแก้ไข → popup → แก้ไขเอกสาร → บันทึก → เลขที่เดิม · popup → ยกเลิกเอกสาร → ประวัติขึ้นยกเลิก · ใบเสร็จ popup ปุ่มแก้ไขจาง

กลายพันธุ์ (ย้อนการแก้ทีละจุดแล้วต้องแดง): หาใบต่อเฉพาะแท็บ QT · ไม่กันใบต่อซ้ำ · ไม่ล็อกก่อนตรวจ · ไม่คืนสถานะตอนยกเลิก ·
ยอมแก้ใบเสร็จ · ยังล็อกใบที่มีใบต่อ · ไม่พิมพ์คงค้าง · คงค้างไม่หักภาษี ณ ที่จ่าย · ใบส่งมอบที่มีใบเสร็จยังเตือนเกินกำหนด · ไม่มีปุ่มย้อนกลับ
