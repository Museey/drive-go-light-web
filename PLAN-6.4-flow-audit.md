# ตรวจ flow ของรุ่น 6.4 ให้ครบ — ฉบับสมบูรณ์

10 กันยายน 2569 · แทนที่การตรวจแบบดูเป็นจุด ๆ ใน [`PLAN-6.4-flow-gap.md`](PLAN-6.4-flow-gap.md)
ซึ่งพลาดไปสองเรื่องเพราะเลือกดูเฉพาะที่นึกออก

## วิธีตรวจ — ให้ตรวจซ้ำได้

ไล่จากไฟล์ต้นฉบับโดยตรง ไม่ได้ดูจากความจำ และไล่**ทั้งชุด** ไม่ใช่สุ่มดู

| ชั้นที่ไล่ | เจอเท่าไร |
|---|---|
| กล่องโต้ตอบ `openModal(` | 25 จุด |
| ปุ่มที่ผูกด้วย id | 113 ปุ่ม |
| ปุ่มในแถวที่ผูกด้วย `data-*` | 94 ชนิด |
| ปุ่มในแถวของทุกหน้ารายการ (`btn sm` + data-*) | ครบทุกหน้า |
| ข้อความที่ระบบปฏิเสธผู้ใช้ (`toast`) | 23 แบบ — เป็นที่ที่กฎธุรกิจซ่อนอยู่ |
| หัวตารางของทุกหน้าจอ | 13 หน้า |
| สูตรคำนวณ | 6 ฟังก์ชัน |

---

## แก้สิ่งที่ผมเคยสรุปผิด

**1. "ขอบเขตก้อนที่ 1 ครบแล้ว ไม่มีทางเดินซ่อนอยู่อีก" — ผิด**
ตอนนั้นผมค้นแค่ `data-mk*` กับ `data-go*` จึงไม่เห็นว่า
**หน้ารายการใบส่งมอบมีปุ่ม `ออกใบเสร็จ` ในแถว** (`data-ivrc`) ซึ่งขึ้นเฉพาะใบที่
ยังไม่มีใบเสร็จและยังไม่ถูกยกเลิก

**2. `pmFull` / `pmHalf` ไม่ใช่โหมดพิมพ์** แต่เป็นปุ่มลัดในกล่องรับชำระ
(เต็มจำนวน / ครึ่งหนึ่ง) — ของเราไม่มีปุ่มลัดนี้ แต่กรอกเองได้ ไม่ถือเป็นช่องว่าง

(ข้อที่แก้ไปแล้วก่อนหน้านี้: `openQ` ไม่ได้อยู่บนหน้าแรก)

---

## ก. ช่องว่างที่เป็นระบบ — ปุ่มในแถว

**นี่คือความต่างที่ใหญ่ที่สุด และอธิบายเรื่องที่ผู้ใช้เจอตั้งแต่แรกได้ทั้งหมด**

6.4 ทำงานจบได้จากหน้ารายการ ของเราต้องเปิดเอกสารเข้าไปก่อนแทบทุกอย่าง

| หน้า | 6.4 มีในแถว | ของเรามี |
|---|---|---|
| ใบเสนอราคา | เปิด · พิมพ์ · ยกเลิก · **คัดลอกใบใหม่** · รอจัดทำ ×2 | พิมพ์ · รอจัดทำ ×2 |
| ใบส่งมอบ | เปิด · พิมพ์ · ยกเลิก · **ออกใบเสร็จ** | พิมพ์ |
| ใบเสร็จ | เปิด · พิมพ์ · ยกเลิก | พิมพ์ |
| ใบวางบิล | เปิด · พิมพ์ · ยกเลิก · **คัดลอกใบใหม่** | พิมพ์ |
| ใบซื้อ | เปิด · พิมพ์ · ยกเลิก · **กู้คืน** | — |
| ค่าใช้จ่าย | เปิด · พิมพ์ · ยกเลิก · **กู้คืน** | — |
| ใบเคลม | เปิด · พิมพ์ · ยกเลิก | — |
| ทะเบียนลูกค้า | แก้ไข · **เปิดใบซ่อม** · **เปิดใบซื้อ** | — |
| รายการค้างทำ | เพิ่มเข้าทะเบียน · ข้าม | มี (ในแผงที่กางออก) |
| สินค้า | แก้ไข | — |
| ลูกหนี้ / เจ้าหนี้ | ชำระเงิน · ดูการชำระ | **ใช้แผงรับชำระหลายใบแทน — ต่างแต่ไม่ด้อยกว่า** |

**"เปิดใบซ่อม / เปิดใบซื้อ" จากแถวลูกค้า** เป็นทางเข้างานที่ของเราไม่มีเลย —
ลูกค้าโทรมา เปิดทะเบียนลูกค้า กดเปิดใบซ่อมได้ทันทีโดยไม่ต้องไปเมนูรายรับ

## ข. เอกสารที่ยกเลิกแล้วหายไปจากหน้ารายรับ

`listIncomeDocs()` มี `d.status <> 'void'` อยู่ในเงื่อนไข
**ใบที่ยกเลิกแล้วจึงไม่ปรากฏในรายการเลย และค้นหาก็ไม่เจอ**

6.4 แสดงไว้พร้อมขีดฆ่าและป้าย "ยกเลิกแล้ว" พร้อมปุ่มกู้คืนหรือคัดลอกใบใหม่

**ผลที่ตามมาซึ่งเป็นความผิดของผมเอง** — ปุ่ม "คัดลอกใบใหม่" ที่เพิ่งทำในช่วงที่ 8
อยู่บนหน้าเอกสารที่ยกเลิกแล้ว ซึ่ง**เดินไปถึงไม่ได้** ต้องรู้ id ของเอกสารเท่านั้น
ปุ่มที่ไปไม่ถึงก็เท่ากับไม่มี

หน้าอื่นของเราไม่มีปัญหานี้ — รายจ่าย ใบซื้อ ใบวางบิล ใบเคลม แสดงใบที่ยกเลิกอยู่แล้ว
**หน้ารายรับเป็นหน้าเดียวที่ซ่อน** จึงไม่ตรงกับทั้ง 6.4 และกับหน้าอื่นของเราเอง

## ค. ไม่มีการกู้คืนเอกสารที่ยกเลิก

`unvoidDoc()` ของ 6.4 ใช้ได้กับทุกชนิดเอกสาร คืนสต๊อกที่เคยตัด และผูกใบเสร็จ
กลับเข้าใบส่งมอบให้ พร้อมถามยืนยันว่า *"ยอดของใบนี้จะกลับไปนับในยอดขาย
ลูกหนี้ เจ้าหนี้ ภาษี และสต๊อกตามเดิม"*

ของเราไม่มีเลย ยกเลิกแล้วจบ

**เรื่องนี้ต้องตัดสินใจ ไม่ใช่แค่ทำตาม** — เอกสารภาษีที่ออกแล้วยกเลิกแล้วเอากลับมา
เป็นเรื่องที่กรมสรรพากรมองว่าไม่ควรทำ แต่ในทางปฏิบัติอู่กดยกเลิกผิดใบได้
ทางออกกลาง ๆ คือ**ให้กู้คืนได้เฉพาะใบที่ยังไม่เคยถูกวางบิลและยังไม่มีใบต่อ**
และบันทึกการกู้คืนลงประวัติเอกสารเสมอ

## ง. ไม่มีกล่องเลือกใบตอนออกเอกสารจากศูนย์

`invNewModal` และ `rcNewModal` — กด "เปิดใบส่งมอบใหม่" แล้ว 6.4 เสนอรายการ
ใบเสนอราคาที่ยังค้างให้เลือกก่อน พร้อมช่องค้นหาและแบ่งหน้า
ในกล่องนั้นแต่ละแถวมีปุ่ม **มีภาษี / ไม่มีภาษี** ให้เลือกชนิดไปในตัว

ของเราให้ฟอร์มเปล่าทันที (ข้อ 6 ในแผนช่วงที่ 9 — ยังไม่ได้ทำ)

## จ. สิ่งที่ตรวจแล้ว **ตรงกัน** — ไม่ต้องทำอะไร

เพื่อให้รายงานนี้เชื่อถือได้ ต้องบอกด้วยว่าอะไรตรวจแล้วผ่าน

| เรื่อง | ผล |
|---|---|
| สูตรคำนวณทั้งหมด | ตรงทุกบรรทัด รวมลำดับการปัดเศษที่แปลก |
| ภาษีมูลค่าเพิ่มและเครดิตยกยอด | ตรง |
| งบกำไรขาดทุน | ของเราละเอียดกว่า (ต้นทุนที่บันทึกจริง + แยกของหายจากคลัง) |
| ช่องบนกระดาษที่พิมพ์ | ครบ |
| ปฏิเสธรับชำระเกินยอดคงค้าง | มี |
| เพดานนำเข้าผู้ติดต่อ 3,000 ราย | มี ตรงกับ `IMPORT_LIMIT` ของ 6.4 |
| จำกัดขนาดไฟล์รูป | มี |
| ตัด/รับของเข้าสต๊อกด้วยมือ | มี (`move-form` · `adjust-form`) |
| โลโก้ร้าน | มี |
| รับชำระหลายใบพร้อมกัน | มี — ของเราทำได้ดีกว่าแบบทีละใบของ 6.4 |
| ประวัติการชำระ · ประวัติการบันทึกเอกสาร | มี |
| นำเข้าผู้ติดต่อจาก CSV · ฉลากบาร์โค้ด · รูปสินค้า | มี |
| พิมพ์ใบตรวจนับเปล่า · แบบฟอร์มเปล่า | มี |

## ฉ. ที่ตั้งใจต่างและจะไม่ทำตาม

- **เข้าใช้งานในนามผู้ใช้อื่น** (`switchUserModal`) — 6.4 เป็นไฟล์เดียวไม่มีการเข้าระบบจริง
  ปุ่มนี้คือการสลับ "ใครกำลังใช้" ของเรามีการเข้าระบบจริงแล้ว และตัดสินใจไว้ว่า
  **จะไม่ทำการสวมสิทธิ์** เพราะทำให้ประวัติการแก้เอกสารเชื่อไม่ได้
- **รหัสผ่านรายพนักงานห้ามซ้ำกัน** — 6.4 ใช้รหัสผ่านเป็นตัวระบุตัวคน
  ของเราใช้อีเมล+รหัสผ่าน จึงไม่มีข้อจำกัดนี้และไม่ควรมี
- **ปุ่มลัดเต็มจำนวน / ครึ่งหนึ่ง** ในกล่องรับชำระ — เล็กน้อย กรอกเองได้

---

## สรุปงานที่เพิ่มจากการตรวจรอบนี้

เรียงตามที่ผมคิดว่าควรทำก่อน

1. **ให้ใบที่ยกเลิกแล้วกลับมาเห็นได้ในหน้ารายรับ** (ข้อ ข) — แก้เงื่อนไขเดียว
   บวกตัวกรอง "รวมใบที่ยกเลิก" และทำให้ปุ่มคัดลอกใบใหม่เดินไปถึงได้จริง
2. **ปุ่มในแถวให้ครบทุกหน้ารายการ** (ข้อ ก) — เปิด · ยกเลิก · คัดลอก · ออกใบเสร็จ
3. **เปิดใบซ่อม / ใบซื้อ จากแถวทะเบียนลูกค้า** (ข้อ ก)
4. **กล่องเลือกใบตอนออกเอกสารจากศูนย์** (ข้อ ง — เดิมคือข้อ 6 ของช่วงที่ 9)
5. **กู้คืนเอกสารที่ยกเลิก** (ข้อ ค) — ต้องตัดสินใจเรื่องขอบเขตก่อนลงมือ

ข้อ 1 กับ 2 ควรทำก่อนข้อ 5 และ 7 ของแผนช่วงที่ 9 เพราะกระทบการใช้งานทุกวันมากกว่า

---

# รอบที่สอง — ไล่ช่องกรอกในฟอร์มและหน้าพิมพ์ทุกตัว

10 กันยายน 2569 · รอบแรกไล่ปุ่ม กล่องโต้ตอบ และกฎที่ระบบปฏิเสธ
รอบนี้ไล่ **ช่องกรอกของทุกฟอร์ม** และ **สิ่งที่พิมพ์ลงกระดาษ** ซึ่งยังไม่เคยไล่เป็นระบบ
เทียบจากป้ายกำกับของทั้งสองฝั่งตัวต่อตัว ไม่ได้ดูจากความจำ

## แก้ที่ผมรายงานผิดในรอบแรก

**"คิวอาร์รับเงินบนเอกสาร — มี" ผิด** เราไม่มีเลย ตอนนั้นผม grep คำว่า `qr`
แล้วไปเจอคำอื่นที่มีตัวอักษรนี้อยู่ข้างใน แล้วสรุปว่ามี — เป็นการตรวจที่หลวมเกินไป
รอบนี้ตรวจซ้ำโดยดูผลลัพธ์ของ grep จริง ๆ ไม่ใช่แค่ว่ามีหรือไม่มีบรรทัด

## ก. ข้อมูลบัญชีธนาคารของอู่ — ขาดทั้งสามชั้น

6.4 เก็บ `bankName` `bankAcc` `bankHolder` และ `bankQR` ไว้ในข้อมูลร้าน
มีช่องกรอกที่หน้าตั้งค่า และมีฟังก์ชัน `bankLine()` ที่**พิมพ์ลงบนกระดาษ**

| พิมพ์บน | 6.4 | ของเรา |
|---|---|---|
| ใบเสร็จ | ธนาคาร · เลขที่บัญชี · ชื่อบัญชี · คิวอาร์ | ไม่มี |
| ใบวางบิล | เหมือนกัน | ไม่มี |
| แบบฟอร์มเปล่า | เว้นเส้นให้เขียนมือ | ไม่มี |

ที่ 6.4 คิดมาดีคือ **ถ้ายังไม่ได้ตั้งค่า จะพิมพ์เส้นว่างให้เขียนด้วยมือ**
แทนที่จะไม่พิมพ์อะไรเลย

ของเราขาดครบทั้งสามชั้น — ไม่มีคอลัมน์ในตาราง `tenants` ไม่มีช่องกรอกในหน้าตั้งค่า
และไม่มีบรรทัดบนหน้าพิมพ์ **อู่ออกใบเสร็จแล้วลูกค้าไม่รู้ว่าจะโอนเงินไปที่ไหน**

เป็นข้อเดียวในรอบนี้ที่ต้องแตะสคีมา

## ข. ใบเคลม — ขาดช่องรถแบบเดียวกับที่เพิ่งแก้ไปในเอกสารขาย

6.4 (`blankClaim`) เก็บ `mileage` `engineNo` `chassisNo` และให้กรอกได้ที่ฟอร์ม
ของเราช่องรถเป็น **dropdown ของรถที่ลงทะเบียนไว้แล้วเท่านั้น** และ**ไม่มีช่องเลขไมล์เลย**

เป็นปัญหาชนิดเดียวกับที่แก้ไปแล้วในช่วงที่ 8 — **แปลว่าตอนนั้นผมแก้ไม่ครบ**
ผมแก้เฉพาะฟอร์มเอกสารขาย แล้วไม่ได้ไล่ดูว่าฟอร์มอื่นมีอาการเดียวกันไหม

**ไม่ต้องแตะสคีมา** — `claims.vehicle` เป็น jsonb เก็บได้ครบอยู่แล้ว เหมือน `documents.vehicle`

## ค. ใบเคลมฝั่งผู้ขาย — ป้ายช่องไม่เปลี่ยนตามฝั่ง

6.4 ใช้ช่องเดียวกันแต่เปลี่ยนป้ายตามประเภทการเคลม

| ฝั่ง | 6.4 เรียกช่องนั้นว่า |
|---|---|
| เคลมลูกค้า | รถยนต์ |
| เคลมผู้ขาย | **เลขที่ใบซื้ออ้างอิง** |

เพราะเคลมผู้ขายคือการส่งของชำรุดคืนร้านอะไหล่ ไม่เกี่ยวกับรถของใคร
ของเราเขียนว่า "รถของลูกค้า" ทั้งสองฝั่ง ซึ่งอ่านแล้วสับสนบนใบเคลมผู้ขาย

## ง. ที่ไล่แล้วครบดี ไม่ต้องทำอะไร

| ฟอร์ม | ผล |
|---|---|
| ใบซื้อ / ค่าใช้จ่าย | ครบ และมีมากกว่า 6.4 (ส่วนลด · รับของเข้าสต๊อก · วิธีจ่าย) |
| ใบตรวจนับ | ครบ — วันที่กับหมายเหตุอยู่ที่หัวใบ ไม่ได้อยู่ในตัวแก้ไข |
| ใบวางบิล | ครบ และมีมากกว่า (วันที่วางบิล · นัดรับเงิน · หมายเหตุ) |
| ทะเบียนสินค้า | ครบ และมีมากกว่า (บาร์โค้ด · จุดสั่งซื้อ · ยอดยกมา · เปิด/ปิดใช้งาน) |
| ทะเบียนผู้ติดต่อ | ครบ 34 ช่อง มากกว่า 6.4 ที่มี 31 |
| ตั้งค่าร้าน | ครบ **ยกเว้นบัญชีธนาคาร** และมีมากกว่าตรงข้อความรับประกันตั้งต้น |
| หน้าพิมพ์ใบเสร็จ | ครบ — จำนวนเงินตัวอักษร · เงื่อนไขรับประกัน · ลายเซ็น **ยกเว้นบรรทัดบัญชี** |
| หน้าพิมพ์ใบวางบิล | ครบ **ยกเว้นบรรทัดบัญชี** |

---

## งานที่เพิ่มจากรอบนี้

1. **บัญชีธนาคารของอู่** — คอลัมน์ใน `tenants` · ช่องกรอกในหน้าตั้งค่า ·
   บรรทัดบนใบเสร็จ ใบวางบิล และแบบฟอร์มเปล่า พร้อมเว้นเส้นให้เขียนมือถ้ายังไม่ได้ตั้ง
   · คิวอาร์รับเงิน (ทำทีหลังได้ แยกจากตัวเลขบัญชี)
2. **ใบเคลม** — ช่องรถกรอกเองได้ พร้อมเลขไมล์ เลขเครื่องยนต์ เลขตัวถัง
3. **ป้ายช่องของใบเคลมผู้ขาย** — เปลี่ยนเป็น "เลขที่ใบซื้ออ้างอิง"

ข้อ 1 เป็นข้อเดียวที่ต้องไมเกรชัน และเป็นข้อที่กระทบลูกค้าของอู่โดยตรง
