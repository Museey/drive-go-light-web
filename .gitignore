node_modules/
# สร้างใหม่ได้ด้วย npm run fixture (เหมือนเดิมทุกไบต์) ไม่ต้อง commit ไฟล์ 1.5 MB
fixtures/demo-backup.json
.next/
.env.local
*.tsbuildinfo

# ไฟล์สำรองฐานข้อมูล ห้ามขึ้น git เด็ดขาด
backups/

# ผลการรันเทสต์เบราว์เซอร์ — เป็นของชั่วคราวของแต่ละเครื่อง
apps/web/test-results/
apps/web/playwright-report/
