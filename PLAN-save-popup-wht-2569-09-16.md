# แผน — popup บันทึก · หัก ณ ที่จ่าย 1,000 บาท · หน้าเอกสารไม่มี VAT · ตารางลูกค้า · หน้ารายรับเปิดมาเห็นหัว

branch `fix/save-popup-wht-2569-09-16` (แตกจาก `main` = `d34bfa8`) · **รออนุมัติ** · ไม่ merge จนกว่าอนุมัติอีกรอบ

กติกาเดิม: แก้ที่ต้นเหตุ · จุดตัดจอ 768/1280 · เทสต์ใหม่ต้องแดงก่อนแก้ ย้อนการแก้แล้วแดง · ห้ามผ่อนเทสต์ · e2e บน production build · ภาพข้อละ 1–2 รูป

ลำดับทำ: 3 → 4 → 6 → 5 → 1/2 (ข้อ 3 เป็นเรื่องตัวเงินในเอกสาร ทำก่อน · ข้อ 1/2 ใหญ่สุดทำท้าย)

---

## 3 · ภาษีหัก ณ ที่จ่าย: ค่าบริการ 1,000 บาทขึ้นไปเท่านั้น (สำคัญ)

**ต้นเหตุ**: `recTotals` (`packages/core/src/totals.ts`) หัก ณ ที่จ่ายทุกยอด ไม่มีเงื่อนไขขั้นต่ำ — ค่าแรง 500 บาทก็หัก 3% (15 บาท)
ทั้งฟอร์มและเซิร์ฟเวอร์ใช้ฟังก์ชันนี้ตัวเดียวกัน (`saveSalesDoc` คิดยอดใหม่ก่อนบันทึก)

**แก้ (ผู้ใช้เลือก: เฉพาะเอกสารรายรับ)**
- ฐานหัก ณ ที่จ่าย (ค่าแรง/ค่าบริการ ก่อน VAT หลังเฉลี่ยส่วนลด — ฐานเดิม) **ต่ำกว่า 1,000 บาท → ไม่หัก** (หัก = 0 ยอดชำระ = รวมทั้งสิ้น)
  **1,000.00 บาทพอดีขึ้นไป → หักตามอัตรา** (ตามกติกา "จ่ายครั้งหนึ่ง 1,000 บาทขึ้นไป")
- ค่าคงที่ตัวเดียว `WHT_MIN_BASE = 1000` ใน core พร้อมคำอธิบายที่มา
- ฟอร์มรายรับ ใต้ช่อง "หัก ณ ที่จ่าย (%)": ข้อความสีแดง **(ค่าบริการ 1,000 บาทขึ้นไป)**
- สรุปยอด: ตั้งอัตราไว้แต่ค่าแรงไม่ถึง → บรรทัดหมายเหตุ "ค่าแรง xxx บาท ไม่ถึง 1,000 บาท — ไม่หัก ณ ที่จ่าย"
- ค่าใช้จ่าย (ฝั่งอู่เป็นผู้จ่าย `exTotals`) **คงเดิม** ตามที่ผู้ใช้เลือก
- เอกสารที่บันทึกไปแล้วไม่ถูกแก้ย้อนหลัง (ยอดเก็บเป็นตัวเลขในฐาน) · เปิดแก้ไขแล้วบันทึกใหม่จะคิดตามกติกาใหม่
- ไม่แตะลำดับการปัดเศษเดิม (ห้ามเปลี่ยนตามคอมเมนต์ในโค้ด)

**เทสต์**: core — ฐาน 999.99 หัก 0 · 1,000.00 หัก 30.00 · 1,500 หัก 45 · ค่าแรง 1,200 ลดท้ายบิลจนฐานเหลือ 960 → 0 · ราคารวม VAT ฐานหลังถอด VAT 990 → 0 ·
ค่าใช้จ่าย 500 ยังหักเท่าเดิม · e2e ใบเสร็จค่าแรง 800 → ไม่มีบรรทัดหัก ยอดบันทึกในฐาน `wht_amount = 0` · ข้อความแดงแสดง

## 4 · หน้าเปิดดูเอกสารที่ไม่คิดภาษี ยังแสดงมูลค่าก่อนภาษี / VAT 7%

**ต้นเหตุ**: `/income/[id]/page.tsx` แสดง "มูลค่าก่อนภาษี" และ "ภาษีมูลค่าเพิ่ม 7%" ทุกใบ ไม่ดู `vatMode` (ฟอร์ม · หน้าพิมพ์ · หน้าค่าใช้จ่ายซ่อนถูกแล้ว)
**แก้**: เอกสารไม่คิดภาษี แสดงแค่ รวมเป็นเงิน · ส่วนลด · รวมทั้งสิ้น (· หัก ณ ที่จ่ายถ้ามี)
**เทสต์**: e2e เปิดใบ IV (ไม่คิดภาษี) ไม่มีสองบรรทัดนี้ · เปิดใบ IVT ยังมี

## 6 · เมนูรายรับ เปิดมาเลื่อนไปกลางหน้า

**ทำให้เกิดซ้ำได้แล้ว** (production build 1280×900): เปิด `/income?kind=QT` หน้าเลื่อนลง 859px · IVT 612 · RC 674 · ขายหน้าร้าน 612 — โฟกัสอยู่ที่ช่องยิงบาร์โค้ด
**ต้นเหตุ**: ช่องยิงบาร์โค้ด (`scan-box.tsx`) ดึงโฟกัสตอนเปิดหน้า เบราว์เซอร์เลื่อนหน้าไปหาช่องนั้น
**แก้**: ดึงโฟกัสแบบไม่เลื่อนหน้า (`focus({ preventScroll: true })`) — ยิงบาร์โค้ดได้ทันทีเหมือนเดิม แต่เปิดมาเห็นหัวเอกสาร
(หลังยิงแต่ละนัดยังโฟกัสกลับที่ช่องเหมือนเดิม — ตอนนั้นผู้ใช้อยู่ที่ช่องอยู่แล้ว)
**เทสต์**: e2e ทุกหน้าที่มีช่องยิง (ใบเสนอราคา ใบส่งมอบ 2 แบบ ใบเสร็จ ขายหน้าร้าน ใบซื้อ ใบเคลม ตรวจนับ ถ้ามี) เปิดมา `scrollY = 0` และหัวเอกสารอยู่ในจอ · ช่องยิงยังโฟกัส (`scan.spec` เดิมต้องผ่าน)

## 5 · ทะเบียนลูกค้า: คอลัมน์ทะเบียนไม่แสดงทะเบียน · ซ่อน/เปิดคอลัมน์ได้

**ตอนนี้**: หัวคอลัมน์ "ทะเบียนรถ / ภาษี" แต่ข้อมูลเป็น "รถ 1 คัน" + เลขภาษี
**แก้**
- แสดงทะเบียนจริง เช่น `1กก 1234 กทม.` — มีหลายคันแสดง 2 คันแรก + "อีก n คัน" · ไม่มีรถแสดง "-" · เลขภาษีบรรทัดเล็กด้านล่างเหมือนเดิม
- ปุ่ม **ตั้งค่าคอลัมน์** แบบเดียวกับทะเบียนสินค้า (ติ๊กเปิด/ปิด) — **ค่าเริ่มต้นปิด: ที่อยู่ · เครดิต · ยอดสะสม** · รหัส ชื่อ ปุ่ม ปิดไม่ได้
  จำค่าเป็นของอู่ (แบบเดียวกับตารางสินค้า) · ใช้ทั้งทะเบียนลูกค้าและทะเบียนผู้ขาย
- ตัวคอลัมน์เลือก (`ColPicker`) แยกออกมาเป็นของใช้ร่วม ไม่คัดลอก
- พื้นที่ที่ได้คืน → ชื่อ 16px · ข้อมูลอื่น 15px (จาก 14px) · ตรวจไม่ล้นที่ 1280/1920 · มือถือ/แท็บเล็ตเลื่อนในกรอบตารางเหมือนเดิม
**เทสต์**: e2e ลูกค้าที่มีรถเห็นทะเบียน · ค่าเริ่มต้นไม่มีคอลัมน์ที่อยู่/เครดิต/ยอดสะสม · ติ๊กเปิดแล้วขึ้นและจำค่า · ตัวอักษรชื่อ 16px ไม่ถูกตัด ·
DB: ค้นรายชื่อพร้อมทะเบียนไม่ช้าลงเป็นแถวละคำขอ (ดึงรวมคำสั่งเดียว)

## 1 · 2 · กดบันทึกแล้ว: popup การ์ดกลางจอ (ผู้ใช้เลือกแบบนามบัตร)

**ตอนนี้**
- เพิ่มผู้ติดต่อ → ไปหน้าผู้ติดต่อคนนั้น (`/customers/<id>?saved=1`) จอกระพริบเหมือนไม่ได้ทำอะไร
- เอกสารรายรับ/รายจ่าย/ใบวางบิล → กลับหน้าเดิม มีแถบ "บันทึกเรียบร้อย — เลขที่ · พิมพ์เอกสาร · เปิดเอกสาร" ด้านบน (เลื่อนลงอยู่จะไม่เห็น)
- ใบเคลม · สินค้า → ไปหน้าของใบนั้น · ชุดอะไหล่ → กลับรายการ

**แก้ — การ์ดเดียวใช้ทุกที่ (`SavedCard`)**
- การ์ดขนาดนามบัตร (ราว 360×220px) กลางจอ ฉากหลังจาง
- 2 วินาทีแรก: ✓ ใหญ่ + **"บันทึกเอกสารเรียบร้อย"** (ข้อมูลหลัก: **"บันทึกข้อมูลแล้ว"**) แล้วการ์ดเดิมแสดงต่อ:
  เลขที่ · ชื่อลูกค้า/ผู้ขาย · ยอดรวม → ปุ่ม **[🖨 พิมพ์เอกสาร]** (เปิดหน้า A4 พร้อมพิมพ์ แท็บใหม่) **[แก้ไขเอกสาร]** (เดิมชื่อ "เปิดเอกสาร") **[ปิด]**
- ปิด (ปุ่ม · Esc · กดฉากหลัง) แล้วลบค่าบันทึกออกจาก URL — กดรีเฟรชไม่เด้งซ้ำ
- ลดการเคลื่อนไหวตามการตั้งค่าเครื่อง (prefers-reduced-motion) · โฟกัสอยู่ในการ์ด ปุ่มพิมพ์ได้โฟกัสแรกหลัง 2 วินาที
- แทนแถบ `SavedBanner` ทุกจุด (ลบตัวเก่า)

**สร้างใหม่ → การ์ด + ฟอร์มด้านหลังเป็นใบเปล่า พร้อมออกใบถัดไป**
| ที่ | หลังบันทึก |
|---|---|
| ใบเสนอราคา · ใบส่งมอบ 2 แบบ · ใบเสร็จ (`/income?kind=` · `/income/new`) · ขายหน้าร้าน | ฟอร์มเปล่าชนิดเดิม + การ์ด (พิมพ์ · แก้ไข) |
| ใบซื้อ · ค่าใช้จ่าย | ฟอร์มเปล่าชนิดเดิม + การ์ด |
| ใบวางบิล IVT/IV | ฟอร์มเปล่า vat เดิม + การ์ด |
| ใบเคลม | ฟอร์มใบเคลมเปล่า + การ์ด (พิมพ์ใบเคลม) |
| เพิ่มผู้ติดต่อ (ลูกค้า/ผู้ขาย) | ฟอร์มเปล่าชนิดเดิม + การ์ด "บันทึกข้อมูลแล้ว" รหัส · ชื่อ · [แก้ไขข้อมูล] [ปิด] (ไม่มีปุ่มพิมพ์) |
| เพิ่มสินค้า | ฟอร์มเปล่า + การ์ด รหัส · ชื่อ · [แก้ไขข้อมูล] [พิมพ์บาร์โค้ด] [ปิด] |
| ชุดอะไหล่ | ฟอร์มเปล่า + การ์ด [แก้ไขข้อมูล] [ปิด] |

**แก้ไขของเดิม → การ์ดเดียวกัน แล้วกลับหน้าที่มา (ผู้ใช้เลือก)** — กลับหน้ารายการ/หน้าที่กดแก้ไขมา ไม่ใช่ฟอร์มเปล่า

- ปุ่ม "บันทึกแล้วพิมพ์" ในแผงยืนยันเดิม (ไปหน้าพิมพ์ทันที) คงไว้
- **ไม่อยู่ในรอบนี้**: ตรวจนับสต๊อก (บันทึกเป็นขั้น ๆ ไม่ใช่ฟอร์มออกใบ) · หน้าตั้งค่าร้าน/ผู้ใช้
- ข้อมูลในการ์ด (ชื่อ ยอด) หน้าปลายทางอ่านจากฐานด้วย id ที่ส่งมา — ตรวจสิทธิ์และอู่เดียวกันตามปกติ (id ปลอม/ของอู่อื่น = ไม่แสดงการ์ด)

**เทสต์**: e2e ทุกแถวในตาราง (3 ขนาดจอ) — บันทึกแล้วการ์ดอยู่กลางจอ ขนาดไม่เกินนามบัตรบนเดสก์ท็อป (มือถือกว้างเกือบเต็มจอ) ·
ข้อความ "บันทึกเอกสารเรียบร้อย" ขึ้นก่อนแล้วมีปุ่มหลัง ~2 วินาที · ฟอร์มด้านหลังเปล่าจริง (ไม่มีชื่อลูกค้า/รายการเดิม เลขที่เป็นเลขถัดไป) ·
พิมพ์เอกสารเปิดหน้า A4 ของใบนั้น · แก้ไขเอกสารไปหน้าแก้ไขใบนั้น · ปิดแล้วรีเฟรชไม่ขึ้นซ้ำ · แก้ไขของเดิมกลับหน้าที่มา ·
id ของอู่อื่นใน URL ไม่แสดงข้อมูล · ย้อนการแก้แต่ละจุดต้องแดง

---

## ตรวจรวม
typecheck · `npm test` (ล้างฐานในเครื่อง → reseed + ลิงก์ตั้งรหัสผ่าน) · e2e ครบบน production build · ย้อนการแก้ทีละจุด ·
ภาพข้อละ 1–2 รูป · `docs/ui-layout-spec.md` §13.4 บันทึกจุดที่เปลี่ยน

---

## ผล (16 ก.ย. 2569)

- typecheck ผ่าน · core 159 ข้อผ่าน (รวมเทียบของเดิมสุ่ม 4,000 ใบ + ไฟล์สำรอง)
- `npm test` (apps/web) 72 ไฟล์ 817 ข้อผ่าน (ล้างฐานในเครื่องแล้ว reseed)
- e2e ทั้งชุดบน production build 319 ผ่าน · 20 ข้ามตามออกแบบ · `e2e/popup-wht.spec.ts` 3 ขนาดจอ
- ย้อนการแก้ทีละจุด 21 จุด แดงตรงอาการทุกจุด (core 2 · vitest 1 · e2e 18)

**เจอระหว่างทำ**
- ข้อ 3: เทสต์เทียบโปรแกรมเดิมเทียบ `recTotals` / `arTotal` / `arDue` ทุกช่อง — ขั้นต่ำ 1,000 บาทต่างจากของเดิมโดยตั้งใจ
  จึงใส่กติกาทับผลของเดิมไว้ที่ `test/wht-minimum.ts` ที่เดียว (ตัวเลข 1,000 เขียนซ้ำ ไม่ import จาก src) · ช่องอื่นยังเท่าของเดิมทุกสตางค์ ·
  เคสตัวอย่างเดิม "ค่าแรง 500 หัก 15" ขัดกับกติกาใหม่ ยกเป็นค่าแรง 1,200 หัก 36
- ข้อ 5: รันทั้งชุดแล้วจับได้ — มือถือ/แท็บเล็ต ลูกค้ารถ 3 คัน แถวสูง 147px (เกณฑ์ 140) เพราะทะเบียนเรียงคันละบรรทัดในช่องที่ตัดบรรทัด
  → ทะเบียน 2 คันแรกบรรทัดเดียว + "อีก n คัน" ช่องไม่ตัดบรรทัดบนจอแคบ (เลื่อนในกรอบตารางแบบคอลัมน์อื่น)
- ข้อ 1–2: ตัวกรองหน้าที่จะกลับไป (`safeBack`) — ย้อนด่าน `//host` แล้วเทสต์ยังเขียว เพราะตัวกรองหมวดกันไว้ก่อน
  เพิ่มเทสต์หมวด "/" แล้วเจอว่าหมวด "/" ไม่ตรงอะไรเลย (ต่อ "/" ซ้ำเป็น "//") → แก้ให้หมวด "/" ใช้ได้ ด่าน // จึงเป็นตัวกันจริง · ย้อนแล้วแดง
- เทสต์ของตัวเอง: การ์ดตั้งค่าการแสดงผลสินค้าหาตารางด้วยหัว "รหัสสินค้า" แต่หัวจริงคือ "รหัส" — ข้อ "พื้นฐานไม่มีคอลัมน์หมวดหมู่" ผ่านเปล่า ๆ
  → หาด้วย "ชื่อสินค้า" และตรวจว่าเจอตารางก่อน · เพิ่มลูกค้าบนหลายขนาดจอพร้อมกันได้รหัสถัดไปเดียวกัน → เทสต์ใช้รหัสไม่ซ้ำ ·
  ข้อมูลตัวอย่างไม่มีใบวางบิล ใบเคลม ชุดอะไหล่ → เทสต์สร้างแถวเองแล้วลบ ไม่ปล่อยให้ข้าม
- ปรับ `e2e/more-ui.spec.ts` ตามพฤติกรรมใหม่ (เพิ่มสินค้าแล้วกลับฟอร์มเปล่า ไม่ใช่หน้าสินค้า) — เปลี่ยนตามความต้องการ ไม่ใช่ผ่อนเทสต์
