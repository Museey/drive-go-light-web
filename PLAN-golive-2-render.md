# งานขึ้นใช้งานจริง ช่วงที่ 2 — ขึ้น Render

## Context

เลือก Render เพราะไม่ต้องดูแลเซิร์ฟเวอร์เอง ค่าใช้จ่ายเดาได้ และเป็นเซิร์ฟเวอร์จริง
จึงไม่ติดขีดจำกัดอัปโหลด 4.5 MB แบบ Vercel — อู่กู้คืนข้อมูลเองได้ตามที่สัญญาไว้

งานช่วงที่ 1 ทำของที่ใช้ได้ทุกที่ไว้แล้ว — ตัวรันไมเกรชัน ตารางเก็บข้อผิดพลาด
หน้า error และ `/healthz` ช่วงนี้คือทำให้มันรันบน Render ได้จริง

**ข้อมูลจะอยู่ที่สิงคโปร์** ซึ่งกฎหมายไทยอนุญาต แต่ต้องบอกให้ชัดในหน้า `/privacy`
ที่ตอนนี้ยังไม่ได้พูดถึงเรื่องที่ตั้งข้อมูลเลยทั้งสองแบบ

---

## สิ่งที่ต้องตรวจก่อนเขียนโค้ด

**สองข้อนี้ผมยืนยันจากเอกสารไม่ได้ ต้องลองกับฐานข้อมูลจริง** และคำตอบเปลี่ยนรูปงานที่เหลือ
จึงเป็นขั้นตอนแรก ไม่ใช่ขั้นตอนสุดท้าย

### ก. role ที่ Render ให้มา ทำอะไรได้บ้าง

แอปบังคับตัวเองไว้ว่า **ห้ามต่อด้วย role ที่ข้าม RLS ได้** ([db.ts:45](apps/web/src/lib/db.ts:45))
ถ้า Render ให้ superuser มา แอปจะปฏิเสธไม่ยอมเริ่มทำงานเลย ซึ่งเป็นพฤติกรรมที่ถูกต้อง
แต่แปลว่าต้องมีทางออก

`tools/probe-db.mjs` (ไฟล์ใหม่) — ต่อฐานจริงแล้วรายงานสามข้อ

```
role ที่ได้มา         ชื่อ · superuser? · bypassrls? · createrole?
สร้าง role ใหม่ได้ไหม   ลองจริงแล้ว rollback
ติดตั้ง extension ได้ไหม  pgcrypto · citext (PG13+ ถือเป็น trusted ติดตั้งได้ถ้ามีสิทธิ์ CREATE)
ตั้งเขตเวลาได้ไหม      alter database ... set timezone
```

ผลที่เป็นไปได้และทางไปต่อ

| ถ้า role ที่ได้มา | ทำอย่างไร |
|---|---|
| ไม่ใช่ superuser และไม่มี bypassrls | **ใช้ตรง ๆ ได้เลย** — `force row level security` กันแม้เจ้าของตาราง |
| เป็น superuser แต่สร้าง role ได้ | สร้าง `dgl_app` แยกด้วย `app-role-managed.sql` แล้วให้แอปใช้ตัวนั้น |
| เป็น superuser และสร้าง role ไม่ได้ | ใช้ Render ไม่ได้ ต้องกลับไปทาง VPS |

### ข. เขตเวลา

ถ้า `alter database ... set timezone` ไม่ผ่าน แอปจะฟ้องทันทีว่าวันที่ไม่ตรงกัน
ซึ่งดี — จับได้ตั้งแต่วันแรกแทนที่จะเงียบ แต่ต้องมีทางแก้

ทางสำรองคือตั้งที่ระดับ role (`alter role ... set timezone`) หรือถ้าไม่ได้อีก
ให้ pool ส่ง `options=-c timezone=Asia/Bangkok` ตอนต่อ ซึ่งทำได้เสมอเพราะเป็นค่าฝั่ง client

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. ตั้งค่าด้วย `render.yaml` ในโปรเจกต์ ไม่ใช่กดในหน้าเว็บ

การตั้งค่าที่อยู่แต่ในหน้าเว็บของผู้ให้บริการคือการตั้งค่าที่ไม่มีใครรีวิวได้
ย้อนดูไม่ได้ว่าใครเปลี่ยนอะไรเมื่อไหร่ และสร้างใหม่ตอนย้ายไม่ได้

`render.yaml` อยู่ใน git ไปพร้อมโค้ด เปลี่ยนอะไรก็เห็นใน diff

### 2. เขียนตัวรันไมเกรชันใหม่เป็น Node แทน bash + psql

`tools/migrate.sh` ต้องมี `psql` บนเครื่อง ซึ่ง**ไม่มีในอิมเมจ Node ของ Render**
ถ้าไม่แก้ก็รันไมเกรชันบนนั้นไม่ได้เลย

เขียน `tools/migrate.mjs` ที่ใช้ `pg` ซึ่งเป็น dependency อยู่แล้ว —
ได้ตัวรันตัวเดียวที่ใช้ได้ทั้งบน Render บน VPS และบนเครื่องพัฒนา
ไม่ต้องมี Postgres client ติดตั้งไว้

ตัวแยกคำสั่งที่ต้องใช้ (เพราะ `alter type ... add value` อยู่ในทรานแซกชันเดียวกับที่ใช้ค่าใหม่ไม่ได้)
**เขียนไว้แล้วใน [migrations.test.ts](apps/web/test/migrations.test.ts)** ย้ายออกมาเป็นโมดูลใช้ร่วมกัน

เก็บ `migrate.sh` ไว้เป็นตัวห่อบาง ๆ ที่เรียก `.mjs` เพื่อไม่ให้คำสั่งใน `DEPLOY.md` เปลี่ยน

### 3. รันไมเกรชันด้วย `preDeployCommand` ไม่ใช่ตอนแอปเริ่ม

รันตอนแอปเริ่มแปลว่าทุก instance ที่เกิดใหม่จะแย่งกันรัน และถ้าไมเกรชันพัง
แอปจะขึ้น ๆ ลง ๆ วนไปโดยไม่มีใครบอกว่าทำไม

`preDeployCommand` รันครั้งเดียวก่อนเวอร์ชันใหม่ขึ้นจริง พังแล้วหยุด deploy
ของเดิมยังให้บริการอยู่ — เป็นพฤติกรรมที่ถูกต้องสำหรับฐานข้อมูล

### 4. พอร์ตต้องอ่านจาก `PORT` ไม่ใช่ผูกไว้ที่ 3100

ตอนนี้ `next start -p 3100` ผูกตายตัว ส่วน Render กำหนดพอร์ตให้ผ่านตัวแปร `PORT`
ถ้าไม่แก้ Render จะหาแอปไม่เจอแล้ว deploy ค้างที่ "waiting for port"
ซึ่งเป็นอาการที่หาสาเหตุยากถ้าไม่รู้มาก่อน

แก้เป็น `next start` เฉย ๆ (อ่าน `PORT` ให้เอง) แล้วตั้ง `PORT=3100` ไว้ใน `.env` ของเครื่องพัฒนา
เพื่อให้พฤติกรรมเดิมไม่เปลี่ยน

### 5. ใช้การสำรองข้อมูลของ Render เป็นหลัก แต่ยังซ้อมกู้เอง

`tools/backup-db.sh` ต้องมี `pg_dump` ซึ่งไม่มีในอิมเมจของ Render เหมือนกัน
Render สำรองให้อัตโนมัติอยู่แล้วในแพ็กเกจที่เสียเงิน จึงไม่ต้องทำซ้ำ

**แต่การสำรองที่ไม่เคยกู้คืนคือการสำรองที่ยังพิสูจน์ไม่ได้** — คงข้อ 6 ของ `DEPLOY.md` ไว้
เปลี่ยนเป็นดาวน์โหลดไฟล์สำรองจาก Render มากู้ที่เครื่องเราแล้วตรวจสามข้อเดิม
โดยเฉพาะข้อที่ว่า **ทุกตารางที่มี `tenant_id` ยังเปิด RLS แบบ force อยู่**

### 6. งานตามเวลาเริ่มด้วยงานเดียว

Cron Job บน Render สั่งล้าง `ops.errors` ที่เกิน 90 วัน (`select ops.prune_errors(90)`)
ให้ตรงกับที่หน้า PDPA บอกไว้ — เดือนละครั้งพอ

ยังไม่ต้องมี cron อื่น เพราะการสำรองเป็นของ Render แล้ว

---

## ไฟล์ที่จะเพิ่มหรือแก้

| ไฟล์ | ทำอะไร |
|---|---|
| `tools/probe-db.mjs` | **ทำก่อนอย่างอื่น** — รายงานว่า role ที่ได้มาทำอะไรได้บ้าง |
| `tools/migrate.mjs` | ตัวรันไมเกรชันที่ไม่ต้องมี psql |
| `tools/sql-statements.mjs` | ตัวแยกคำสั่ง SQL — ย้ายออกจากไฟล์เทสต์มาใช้ร่วมกัน |
| `tools/migrate.sh` | เหลือเป็นตัวห่อบาง ๆ ที่เรียก `.mjs` |
| `db/app-role-managed.sql` | สร้าง role แยกเมื่อทำได้ · ตรวจและบอกให้ชัดเมื่อทำไม่ได้ |
| `render.yaml` | เว็บ + Postgres + Cron ในไฟล์เดียว |
| `apps/web/package.json` | `start` อ่าน `PORT` |
| `apps/web/src/lib/ops.ts` | รายชื่อไมเกรชันไม่พึ่งการอ่านโฟลเดอร์ตอน runtime |
| `apps/web/src/lib/db.ts` | ส่งเขตเวลาไปกับ connection เป็นทางสำรอง |
| `apps/web/src/app/privacy/page.tsx` | **บอกที่ตั้งข้อมูลให้ชัด** — ตอนนี้ไม่ได้พูดถึงเลย |
| `DEPLOY.md` | เพิ่มทาง Render · คงทาง VPS ไว้เผื่ออนาคตย้ายมาไทย |

`ops.ts` ที่อ่าน `readdirSync('../../db')` ตอน runtime อาจใช้ได้บน Render
เพราะโค้ดทั้ง repo อยู่บนเครื่อง แต่พึ่งพา cwd ซึ่งเปลี่ยนได้ตามการตั้งค่า
เปลี่ยนเป็นรายชื่อที่ฝังตอน build ปลอดภัยกว่า และไม่เสียคุณสมบัติ
"เพิ่มไฟล์แล้วรู้เอง" ถ้าให้ตัว build อ่านโฟลเดอร์ให้

---

## ชุดทดสอบ

**`apps/web/test/migrate-runner.test.ts` (ไฟล์ใหม่)** — ย้ายของเดิมจาก `migrate.sh` มาเป็นของ `.mjs`
- รันสองรอบติดกัน รอบที่สองไม่ทำอะไร
- ไฟล์ที่รันแล้วถูกแก้เนื้อหา → ปฏิเสธพร้อมบอกชื่อไฟล์
- `--fresh` รันเฉพาะไฟล์ฐาน แล้วจดที่เหลือ
- ตัวแยกคำสั่งไม่ตัดผิดใน `$$ ... $$` · ในสตริง · ในคอมเมนต์

**`migrations.test.ts` เดิม** ต้องยังผ่าน — ข้อที่พิสูจน์ว่าทางติดตั้งใหม่กับทางอัปเกรด
ให้ฐานข้อมูลเหมือนกัน ยังเป็นข้อที่สำคัญที่สุด

**ทดสอบกับ Postgres ที่ไม่ใช่ superuser** — สร้าง role จำกัดสิทธิ์บน Postgres ในเครื่อง
ให้เหมือนที่ Render น่าจะให้มา แล้วรันไมเกรชันทั้งชุด
เป็นการซ้อมก่อนไปเจอของจริง ไม่ใช่ไปลองบนเครื่องที่มีข้อมูลลูกค้า

**เดิมต้องไม่พัง** — 448 ข้อผ่านครบ

---

## ขั้นตอนบน Render

1. สมัคร · ต่อ GitHub · เลือก repo
2. สร้าง **Postgres** ก่อน เลือกภูมิภาค **Singapore** และแพ็กเกจที่เสียเงิน
   (แพ็กเกจฟรีหมดอายุและไม่มีการสำรองข้อมูล — ห้ามใช้กับข้อมูลลูกค้า)
3. **รัน `tools/probe-db.mjs` ชี้ไปที่ฐานนั้น** แล้วอ่านผลก่อนไปต่อ
4. ถ้าผลผ่าน — `render.yaml` จะสร้างเว็บกับ cron ให้เอง (Blueprint)
5. ตั้ง `DATABASE_URL` ให้ชี้ไป role ที่ probe บอกว่าใช้ได้
6. deploy แล้วเช็ค `https://<โดเมน>/healthz` ต้องได้ `{"ok":true,"failed":[]}`
7. เปิดอู่แรกด้วย importer จาก**เครื่องเรา**ชี้ไปฐานจริง (ไม่ต้องรันบน Render)
8. ซ้อมกู้คืนหนึ่งรอบตาม `DEPLOY.md` ข้อ 6 · บันทึกผล

---

## ที่ไม่ทำในช่วงนี้

- **ย้ายมาไทย** — เก็บ `DEPLOY.md` ทาง VPS ไว้ครบ ย้ายทีหลังคือ dump แล้ว restore
  เพราะทั้งระบบเป็น Postgres กับ Node ธรรมดา ไม่ผูกกับ Render
- **โดเมนของตัวเอง** — ~~ใช้ `.onrender.com` ไปก่อนได้~~ ทำแล้ว 8 ก.ย. 2569
  ระบบอยู่ที่ `app.drivegolight.com` ดู `DEPLOY.md` ข้อ 7
- **แจ้งเตือนเมื่อพัง** — ต้องมีอีเมลก่อน เป็นงานถัดไป
- **หลายเครื่องพร้อมกัน** — 20–30 อู่ไม่ต้องใช้
