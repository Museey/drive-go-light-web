# ช่วงที่ 5 — ตรวจนับสต๊อกและบาร์โค้ด (05.5)

## Context

แท็บสุดท้ายที่ยังจางอยู่ในเมนู และเป็นกลุ่มข้อมูลรองสุดท้ายที่ตัวนำเข้ายังรับไม่ได้

ตรวจนับสต๊อกคือการ**เดินนับของจริงในชั้นวาง แล้วปรับยอดในระบบให้ตรง** —
ส่วนต่างที่พบคือของที่หายไปโดยไม่มีเอกสาร ซึ่งต้องลงเป็นค่าใช้จ่าย
ไม่งั้นกำไรจะสูงเกินจริงเท่ากับมูลค่าของที่หาย

ฐานพร้อมรับหมดแล้วตั้งแต่ช่วงที่ 1 — เหตุผล `count` อยู่ใน `stock_reason` แล้ว
และงบกำไรขาดทุนนับ `count` รวมอยู่ในบรรทัด "ปรับยอด/ตรวจนับ" เรียบร้อย
ช่วงนี้คือการทำ**เอกสาร**ที่ออกแถวเหล่านั้น แทนที่จะปรับทีละตัวอย่างที่ทำได้ตอนนี้

บาร์โค้ดมาด้วยกันเพราะเป็นเหตุผลเดียวที่ทำให้ตรวจนับไม่ทรมาน — อู่ที่มีอะไหล่
พันกว่ารายการ ถ้าต้องพิมพ์รหัสทีละตัวจะไม่มีใครนับ

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. "ระบบว่ามี" ไม่เก็บลงใบตอนร่าง — คิดสดทุกครั้งที่เปิด

6.4 เขียนคอมเมนต์ไว้ตรง ๆ ว่า *"จำนวนที่ระบบว่ามีต้องสดเสมอ เผื่อมีการขายหรือรับของ
ระหว่างที่นับค้างไว้"* แล้ว `renderCountForm` เขียนทับ `it.sys` จากตัวสินค้าทุกครั้งที่วาด

เราไม่เก็บมันลงแถวเลยตอนเป็นร่าง — อ่านสดจาก `product_stock` แทน
ค่าที่เก็บไว้แล้วต้องคอยเขียนทับ คือค่าที่วันหนึ่งจะลืมเขียนทับ

**ตรึงลง `system_qty` ตอนกดปรับยอดเท่านั้น** ซึ่งตรงกับ `it.sys = num(p.qty)`
ที่ 6.4 ทำใน `applyCount` — หลังจากนั้นใบกลายเป็นหลักฐานว่า ณ วันนั้นระบบว่ามีเท่าไร
นับได้เท่าไร และปรับไปเท่าไร

### 2. ปรับยอดแล้วย้อนไม่ได้ — ตามต้นฉบับ แต่ลบร่างได้

6.4 เตือนก่อนกดว่า *"ทุกรายการบันทึกลงประวัติความเคลื่อนไหว และย้อนกลับเองไม่ได้"*
แล้วใบที่ปรับยอดแล้วกลายเป็นอ่านอย่างเดียวทั้งใบ

ทำตาม เพราะการ "ยกเลิกการปรับยอด" ไม่มีความหมายทางบัญชี — ของที่หายไปจากชั้นวาง
ไม่ได้กลับมาเพราะเรากดยกเลิกเอกสาร ถ้านับผิดให้เปิดใบใหม่นับใหม่ ซึ่งเป็นความจริงกว่า

ต่างจาก 6.4 ตรงที่**ร่างที่ยังไม่ปรับยอดลบทิ้งได้** — ยังไม่มีอะไรเกิดขึ้นกับสต๊อก
ใบร่างที่เปิดผิดจึงไม่ควรค้างอยู่ในรายการตลอดไป

### 3. รายการที่นับได้ตรงกับระบบ ไม่ถูกแตะเลย

`ctOff()` กรองเฉพาะที่ต่างเกิน `0.0005` แล้ว `applyCount` ข้ามที่เหลือ
ไม่มีแถวใน `stock_moves` ไม่มีอะไรเปลี่ยน — สำคัญเพราะใบตรวจนับใบหนึ่งมีหลายร้อยรายการ
แต่ที่ต่างจริงมักมีไม่กี่สิบ ถ้าลงแถวทุกตัวบัญชีจะบวมโดยไม่ได้ข้อมูลเพิ่ม

### 4. ช่องที่ยังไม่ได้กรอก ≠ นับได้ศูนย์

`ctDiff` คืน `null` เมื่อช่องว่าง ต่างจากกรอก `0` ซึ่งแปลว่า**นับแล้วไม่เจอเลย**
เก็บความต่างนี้ไว้ทั้งในฐาน (`counted_qty` เป็น null ได้) และในหน้าจอ

พลาดข้อนี้แล้วใบที่นับไปได้ครึ่งเดียวจะตัดสต๊อกของอีกครึ่งเป็นศูนย์ทั้งหมด
ซึ่งเป็นความเสียหายที่ย้อนไม่ได้ตามข้อ 2

### 5. บาร์โค้ดเป็น Code 39 — พอร์ตตัวสร้างเข้า core แล้วเทียบผลกับ 6.4

`barcodeSVG()` เป็นฟังก์ชันบริสุทธิ์ ตาราง `C39` มี 44 ตัวอักษร
เอาเข้า `packages/core` ได้แล้ว**เทียบกับต้นฉบับด้วย differential test** เหมือนที่ทำกับสูตรภาษี
— ฉลากที่พิมพ์ออกมาต้องยิงได้ด้วยปืนตัวเดิม ตัวเลขผิดหนึ่งแถบคือทั้งม้วนใช้ไม่ได้

`genBarcode()` ใช้เวลาและเลขสุ่ม จึงเทียบตรง ๆ ไม่ได้ — ทดสอบด้วยรูปแบบและความไม่ซ้ำแทน

### 6. ยิงบาร์โค้ดซ้ำตัวเดิม = นับเพิ่มทีละ 1 และเด้งขึ้นแถวบนสุด

พฤติกรรมของ `countScan` ทั้งดุ้น รวมลำดับการค้นหา:
`barcode` → `code` → `oem` แบบตรงตัว แล้วค่อยค้นแบบมีบางส่วนตรงกัน
ถ้าเจอตัวเดียวก็ใช้เลย เจอหลายตัวหรือไม่เจอเลยจึงค่อยบอกผู้ใช้

คอมเมนต์ของ 6.4 อธิบายว่าทำไมต้องเด้งขึ้นบนสุด: *"จะได้เห็นทันทีไม่ต้องเลื่อนหา"* —
คนที่ถือปืนยิงอยู่หน้าชั้นวางไม่ได้จ้องจอ ต้องเหลือบมองแล้วรู้ทันทีว่ายิงติด

---

## แก้ฐานข้อมูล

`db/001_init.sql` (ติดตั้งใหม่) + `db/006_counts.sql` (ฐานที่มีอยู่)

```sql
-- บาร์โค้ดบนตัวสินค้า
alter table products add column barcode text;
create unique index products_barcode_uidx on products (tenant_id, barcode)
  where barcode is not null;          -- ว่างได้หลายตัว แต่ห้ามซ้ำกัน

create type count_status as enum ('draft', 'applied');

create table stock_counts (
  id, tenant_id, no,                  -- CT-YYYYMM-NNN
  count_date    date not null default current_date,
  note          text not null default '',
  status        count_status not null default 'draft',
  applied_at    timestamptz,
  applied_by    uuid references users(id) on delete set null,
  created_by, created_at, updated_at,
  unique (tenant_id, no),
  constraint count_applied_stamp check ((status = 'applied') = (applied_at is not null))
);

create table stock_count_items (
  id, tenant_id,
  count_id    uuid not null references stock_counts(id) on delete cascade,
  line_no     integer not null,
  product_id  uuid not null references products(id) on delete restrict,
  -- null = ยังไม่ได้กรอก · 0 = นับแล้วไม่เจอ — สองอย่างนี้ต่างกัน
  counted_qty numeric(12,3),
  -- ตรึงตอนกดปรับยอดเท่านั้น ตอนเป็นร่างอ่านสดจาก product_stock
  system_qty  numeric(12,3),
  unit_cost   numeric(14,2),          -- ต้นทุนที่ใช้ตอนปรับ ตรึงพร้อมกัน
  note        text not null default '',
  unique (count_id, product_id),      -- สินค้าตัวเดียวนับซ้ำในใบเดียวไม่ได้
  unique (count_id, line_no)
);

create table stock_count_sequences (tenant_id, period, last_no);
create function next_count_no(p_tenant, p_period) returns integer;
```

`unique (count_id, product_id)` สำคัญ — เป็นสิ่งที่ทำให้ "ยิงซ้ำ = นับเพิ่ม"
ไม่กลายเป็น "ยิงซ้ำ = เพิ่มแถวใหม่แล้วปรับยอดสองรอบ" โดยที่ไม่ต้องเชื่อโค้ดฝั่งหน้าจอ

`product_id` เป็น `on delete restrict` เหมือนใบเคลม — ลบสินค้าที่เคยถูกนับไม่ได้

**RLS** ทั้งสามตารางเปิดและ force · `tenant-isolation.test.ts` จะเห็นเองจาก pg catalog

---

## แก้ `packages/core` — `src/barcode.ts` (ไฟล์ใหม่)

```ts
export const C39: Record<string, string>          // ยกมาทั้งตาราง 44 ตัว
export function barcodeSVG(text: string, w: number, h: number): string
export function genBarcode(now?: number, rand?: () => number): string
```

`genBarcode` รับเวลาและตัวสุ่มเข้ามาได้ เพื่อให้เทสต์กำหนดผลลัพธ์ได้
ค่าปริยายคือ `Date.now()` และ `Math.random()` เหมือนเดิม

**Differential test** เพิ่ม `barcodeSVG` และ `C39` เข้า `FUNCTIONS`
ใน `packages/core/test/extract-legacy.mjs` แล้วเทียบ SVG ที่ได้กับต้นฉบับ
ทีละตัวอักษรที่รองรับทั้ง 44 ตัว บวกข้อความจริงอีก 200 ชุดแบบสุ่มด้วย seed คงที่

---

## แก้ `apps/web/src/lib` — `stock-counts.ts` (ไฟล์ใหม่ ไม่ใส่ `server-only`)

```ts
listCounts(c, { search, from, to })            // หน้ารายการ + ยอดสรุป
getCount(c, id)                                // ใบเดียว — ร่างอ่าน system_qty สด
createCount(c, { countDate, note }, userId)    // เปิดใบร่าง
addCountItems(c, id, productIds)               // ดึงสินค้ามาตรวจนับ (ข้ามตัวที่มีแล้ว)
scanIntoCount(c, id, term)                     // ยิงบาร์โค้ด — ตรรกะของ countScan
setCountedQty(c, itemId, qty | null)           // กรอกจำนวน
removeCountItem(c, itemId)
saveCountHead(c, id, { countDate, note })
deleteCount(c, id)                             // เฉพาะร่าง
applyCount(c, id, userId)                      // ปรับยอด — ทรานแซกชันเดียว
```

**`applyCount` คือหัวใจของช่วงนี้**

```
1. select ... for update — ใบต้องเป็น draft ไม่งั้นโยนทิ้ง (กันกดสองครั้ง)
2. อ่าน system_qty สดของทุกบรรทัดที่กรอกแล้ว
3. ข้ามบรรทัดที่ต่างน้อยกว่า 0.0005
4. ต่างเป็นบวก → receiveStock(reason 'count') ที่ต้นทุนล่าสุด
   ต่างเป็นลบ → consumeStock(reason 'count') ตัดตามล็อตจริง
5. ตรึง system_qty และ unit_cost ลงทุกบรรทัดที่กรอกแล้ว
6. status = 'applied'
```

ข้อ 4 ตรงกับ 6.4 ที่ใช้ `lotAdd(p, diff, num(p.cost), d)` กับ `lotConsume(p, -diff)` —
ของที่โผล่มาเข้าที่ต้นทุนล่าสุด (ไม่รู้ว่ามาจากล็อตไหน) ส่วนของที่หายตัดตามล็อตจริง
เพื่อให้รู้ว่าเสียเงินไปเท่าไรจริง ๆ

ทั้งหมดอยู่ในทรานแซกชันเดียว — ปรับยอด 300 รายการแล้วพังกลางทาง
ต้องไม่เหลือสภาพที่ปรับไปครึ่งใบโดยที่ไม่มีใครรู้ว่าถึงไหน

---

## แก้หน้าจอ

| ไฟล์ | สิ่งที่ทำ |
|---|---|
| `app/stock/count/page.tsx` | รายการใบตรวจนับ + สรุป (ร่าง / ปรับแล้ว / มูลค่าส่วนต่างสะสม) |
| `app/stock/count/[id]/page.tsx` | ใบตรวจนับ — ร่างแก้ได้ ปรับแล้วอ่านอย่างเดียว |
| `app/stock/count/count-editor.tsx` | ช่องยิงบาร์โค้ด · ตารางกรอก · แถบสรุปส่วนต่าง |
| `app/stock/count/apply-count.tsx` | ปุ่มปรับยอดพร้อมกล่องยืนยันที่บอกตัวเลขก่อน |
| `app/stock/count/[id]/print/page.tsx` | ใบตรวจนับที่พิมพ์ไปเดินนับ (ของใบนั้น) |
| `app/stock/sheet/page.tsx` | **ใบนับเปล่า** จากทะเบียนสินค้า — เลือกหมวด · เลือกว่าจะพิมพ์ยอดระบบไหม |
| `app/stock/sheet/csv/route.ts` | ใบนับเปล่าเป็น CSV เปิดด้วย Excel |
| `app/stock/[id]/barcode/page.tsx` | ฉลากบาร์โค้ด — เลือกจำนวนดวง พิมพ์ลงสติกเกอร์ A4 กริด 4 คอลัมน์ |
| `app/stock/product-form.tsx` | ช่องบาร์โค้ด + ปุ่มสร้างอัตโนมัติ |
| `components/menu-map.ts` | ปลด `todo` ออกจาก 05.5 — **เมนูไม่มีแท็บจางอีกแล้ว** |

**ใบนับเปล่ามีตัวเลือก "ไม่พิมพ์จำนวนที่ระบบมี"** พร้อมข้อความของ 6.4 คำต่อคำ:
*"เพื่อให้การนับสะท้อนของจริงโดยไม่ถูกตัวเลขในระบบชี้นำ"* — เป็นเหตุผลทางการควบคุมภายใน
ที่คนทำระบบมักตัดทิ้งเพราะไม่รู้ว่ามีไว้ทำไม

---

## นำเข้าและสำรองข้อมูล — ทำในช่วงนี้เหมือนสองช่วงก่อน

1. **`backup.ts`** ส่งออก `counts` รูปแบบเดียวกับ `DB.counts` ของ 6.4
   (`items[].sys/cnt/cost` · `applied`) และตัวนับ `seq.ct`
   พร้อม `barcode` บนสินค้าที่ส่งออกอยู่แล้ว
2. **`importer/import.ts`** อ่านกลับเข้าสามตาราง
   **ไม่สร้างแถวสต๊อก** ด้วยเหตุผลเดียวกับใบเคลม — ยอดที่นำเข้าเป็นยอดหลังปรับแล้ว
   ใบร่างเข้ามาเป็นร่าง · ใบที่ปรับแล้วเข้ามาเป็น `applied` พร้อม `system_qty` ที่ติดมา
   บรรทัดที่อ้างสินค้าที่ถูกลบไปแล้ว **ต้องตัดทิ้ง** (ต่างจากใบเคลม)
   เพราะ `product_id` เป็น `not null` — การนับของที่ไม่มีในทะเบียนไม่มีความหมาย · เตือนไว้
   `barcode` ที่ซ้ำกันในไฟล์เดิมให้ตัวหลังเป็นค่าว่าง แล้วเตือน
3. **`support.ts`** ถอด `counts` ออก — เหลือ `moves` กลุ่มเดียว
4. **`restore-core.ts`** `WIPE_ORDER` เพิ่ม `stock_count_items` `stock_counts`
   `stock_count_sequences` ไว้ **หลัง `stock_moves` และก่อน `products`**
   (ลำดับเดียวกับใบเคลม ซึ่งเป็นจุดที่ช่วงที่แล้วพลาดแล้วเทสต์จับได้)

---

## ชุดทดสอบ

**`packages/core/test/differential.test.ts`** — `barcodeSVG` ต้องได้ SVG ตรงกับ 6.4
ทุกตัวอักษรใน `C39` และข้อความสุ่ม 200 ชุด · ตัวอักษรนอกชุดต้องถูกตัดทิ้งเหมือนกัน

**`apps/web/test/stock-counts.test.ts` (ไฟล์ใหม่ · ฐานข้อมูลจริง)**

- นับได้น้อยกว่าระบบ → ตัดตามล็อตจริง ต้นทุนที่บันทึกต้องเป็นราคาล็อตแรกก่อน
- นับได้มากกว่าระบบ → รับเข้าที่ต้นทุนล่าสุด
- **บรรทัดที่นับได้ตรงกับระบบไม่สร้างแถวในบัญชีสต๊อกเลย**
- **ช่องว่างไม่ใช่ศูนย์** — ใบที่กรอกไปครึ่งเดียว ปรับยอดแล้วอีกครึ่งต้องไม่ถูกแตะ
- **ปรับยอดซ้ำใบเดิมไม่ได้** — ครั้งที่สองต้องโยนทิ้งและสต๊อกไม่ขยับ
- ยอดในระบบเปลี่ยนระหว่างที่นับค้างไว้ → ส่วนต่างคิดจากยอด ณ ตอนกดปรับ ไม่ใช่ตอนเปิดใบ
- ยิงบาร์โค้ดซ้ำตัวเดิม → จำนวนเพิ่มทีละ 1 ไม่ใช่เพิ่มแถว
- ยิงรหัสที่ไม่มี → บอกว่าไม่พบ ไม่ใช่สร้างของใหม่
- ลบใบร่างได้ · ลบใบที่ปรับยอดแล้วไม่ได้
- ลบสินค้าที่เคยถูกนับไม่ได้
- **ทั้งใบอยู่ในทรานแซกชันเดียว** — บรรทัดท้ายพังแล้วบรรทัดแรกต้องไม่ถูกบันทึก
  (วางกับดักที่สินค้าตัวที่สอง แบบเดียวกับที่ทำในช่วงที่ 3)

**`apps/web/test/pl.test.ts`** — ส่วนต่างจากการตรวจนับเข้าบรรทัด "ปรับยอด/ตรวจนับ"
ไม่เข้าต้นทุนขาย และกำไรขั้นต้นไม่ขยับ

**`packages/importer/test/hard-cases.test.ts`** — `countBackup()`
- **ไม่สร้างแถวสต๊อกจากใบตรวจนับที่นำเข้า** ยอดคงเหลือเท่าไฟล์เป๊ะ
- ใบร่างกับใบที่ปรับแล้วเข้ามาคนละสถานะ
- บรรทัดที่อ้างสินค้าที่หายไปถูกตัดทิ้งพร้อมคำเตือน
- บาร์โค้ดซ้ำ → ตัวหลังเป็นค่าว่าง ไม่ใช่ล้มทั้งไฟล์
- ตัวนับ `seq.ct` ตามมา

**`backup-roundtrip.test.ts` · `restore.test.ts`** — ใบตรวจนับตามมาครบ
และกู้คืนทับอู่ที่เคยตรวจนับไม่ล้มด้วย foreign key

**กลายพันธุ์แล้วต้องแดง**
1. ให้ช่องว่างนับเป็นศูนย์
2. ให้ `applyCount` ปรับซ้ำได้
3. ให้ตัวนำเข้าสร้างแถวตัดสต๊อกจากใบตรวจนับ
4. ให้ `applyCount` ใช้ `system_qty` ที่ตรึงไว้ตอนเปิดใบแทนยอดสด

**เดิมต้องไม่พัง** — 350 ข้อผ่านครบ

---

## วิธีตรวจว่าใช้ได้จริง

```bash
export PATH=~/.nvm/versions/node/v22.23.2/bin:$PATH
DGL_LEGACY_HTML=legacy/drivegolight-6.4-cloud.html npx vitest run --root packages/core
DATABASE_URL="postgresql://postgres:x@localhost:5433/dgl" npm test
npm run build -w @drivegolight/importer && npm run build -w @drivegolight/web
```

ตรวจด้วยมือบนหน้าเว็บ
1. ทะเบียนสินค้า → แก้สินค้าหนึ่งตัว → กดสร้างบาร์โค้ด → บันทึก → พิมพ์ฉลาก 12 ดวง
2. 05.1 → พิมพ์ใบนับเปล่าแบบไม่มียอดระบบ → ต้องเห็นช่องว่างให้เขียน
3. 05.5 → เปิดใบใหม่ → พิมพ์บาร์โค้ดที่สร้างไว้ในช่องยิงแล้วกด Enter → ขึ้นแถวบนสุด นับ 1
4. กด Enter ซ้ำ → เป็น 2 ไม่ใช่สองแถว
5. กรอกจำนวนที่ต่างจากระบบหนึ่งตัว เว้นอีกตัวไว้ → บันทึกร่าง → ปิดแล้วเปิดใหม่ต้องยังอยู่
6. กดปรับยอด → กล่องยืนยันบอกจำนวนรายการและมูลค่าส่วนต่างก่อน
7. ยืนยัน → ยอดคงเหลือของตัวที่กรอกเปลี่ยน · **ตัวที่เว้นว่างไม่เปลี่ยน**
8. หน้าสินค้า → ประวัติมีแถวเหตุผล "ตรวจนับ" อ้างเลขที่ใบ
9. งบกำไรขาดทุน → เข้าบรรทัดปรับยอด/ตรวจนับ · **กำไรขั้นต้นไม่ขยับ**
10. เปิดใบเดิม → อ่านอย่างเดียว ไม่มีปุ่มปรับยอดแล้ว

---

## ที่ไม่ทำในช่วงนี้

- **นำเข้าประวัติการเคลื่อนไหวสต๊อก (`moves`)** — กลุ่มสุดท้ายที่เหลือ
  ตัวนำเข้าลงยอดยกมาเป็นแถว `set` แถวเดียวต่อสินค้า ถ้าเอา `moves` เข้ามาด้วย
  ของจะถูกนับสองรอบ ต้องเปลี่ยนไปเล่นบัญชีจากศูนย์แทนการตั้งยอดยกมา
  ซึ่งเป็นงานคนละก้อนและเสี่ยงกับความถูกต้องของยอดที่ใช้งานอยู่ · ยังเตือนไว้ตามเดิม
- **สแกนด้วยกล้องมือถือ** — 6.4 ไม่มี ใช้ปืนยิงที่ต่อเป็นคีย์บอร์ด
  (ซึ่งทำงานกับช่องกรอกธรรมดาอยู่แล้ว ไม่ต้องเขียนอะไรเพิ่ม)
- **ซ่อนต้นทุนจากคนที่ไม่มีสิทธิ์** (`canCost()`) — ช่วงที่ 6 ทำทั้งระบบพร้อมกัน
  ช่วงนี้แสดงมูลค่าส่วนต่างให้ทุกคนไปก่อน เหมือนหน้าอื่นที่ยังไม่ได้ทำสิทธิ์ละเอียด
- **นับแยกตามชั้นวาง / โซน** — 6.4 ไม่มีแนวคิดตำแหน่งเก็บของ ใช้ช่องหมายเหตุแทน

---

# ผลที่ทำจริง

**ครบทุกข้อในแผน** · เทสต์ 394 ข้อผ่าน (เดิม 350) · build ผ่าน
**เมนูไม่มีแท็บจางเหลืออีกแล้ว** และตัวนำเข้าเหลือกลุ่มที่รับไม่ได้แค่ `moves` กลุ่มเดียว

## ใบตรวจนับ

- `db/001_init.sql` + `db/006_counts.sql` — `stock_counts` · `stock_count_items` ·
  `stock_count_sequences` · `next_count_no()` · `products.barcode`
- `unique (count_id, product_id)` ทำให้ "ยิงซ้ำ = นับเพิ่ม" ไม่กลายเป็น
  "ยิงซ้ำ = เพิ่มแถวแล้วปรับยอดสองรอบ" โดยไม่ต้องเชื่อโค้ดฝั่งหน้าจอ
- `lib/stock-counts.ts` — เปิดใบ · ดึงสินค้า · ยิงบาร์โค้ด · กรอก · ลบร่าง · ปรับยอด
- หน้ารายการ · ใบตรวจนับ · ใบพิมพ์ · ใบนับเปล่า (หน้าจอ + CSV) · ฉลากบาร์โค้ด

**สามกติกาที่ยกมาจาก 6.4 ทั้งดุ้น**
1. ร่างอ่าน "ระบบว่ามี" สดทุกครั้ง ตรึงลงตอนกดปรับยอดเท่านั้น
2. ช่องว่าง (`null`) ไม่ใช่ศูนย์ — ใบที่กรอกครึ่งเดียว อีกครึ่งไม่ถูกแตะเลย
3. บรรทัดที่นับได้ตรงกับระบบไม่สร้างแถวในบัญชีสต๊อก

## บาร์โค้ด

- `packages/core/src/barcode.ts` — `C39` · `barcodeSVG` · `genBarcode` พอร์ตจาก 6.4
- **พิสูจน์แล้วว่าได้ SVG ตรงกับต้นฉบับทุกแถบ** ทั้ง 40 ตัวอักษรและข้อความสุ่ม 200 ชุด
  กลายพันธุ์ความกว้างแถบหรือแก้ตาราง C39 ตัวเดียวแล้วเทสต์แดงทันที
- ตัวสกัดโค้ดเดิมรองรับ `const ชื่อ = { … }` เพิ่มขึ้นมา เพื่อดึงตาราง `C39` ออกมาเทียบได้

## นำเข้าและสำรองข้อมูล

ทำครบสามอย่างพร้อมกันเหมือนสองช่วงก่อน แล้วค่อยถอด `counts` ออกจากรายการที่ยังรับไม่ได้

- ใบตรวจนับที่นำเข้า **ไม่ปรับสต๊อกซ้ำ** — ยอดในไฟล์เป็นยอดหลังปรับไปแล้ว
- **ช่องว่างยังเป็นช่องว่าง** ตอนนำเข้า ถ้าตีเป็นศูนย์ ใบร่างที่ยกมาจะสั่งตัดสต๊อกเป็นศูนย์
  ตอนที่ผู้ใช้กดปรับยอด
- บรรทัดที่อ้างสินค้าที่ถูกลบไปแล้ว **ตัดทิ้ง** (ต่างจากใบเคลมที่ยังเก็บชื่อไว้ได้)
  เพราะ `product_id` เป็น `not null` — การนับของที่ไม่มีในทะเบียนไม่มีความหมาย
- บาร์โค้ดซ้ำในไฟล์เดิม → ตัวหลังเป็นค่าว่างแล้วเตือน ไม่ล้มทั้งไฟล์

## ที่แก้เพิ่มระหว่างทาง

- `friendlyDbError` เดิมเช็ค `c.includes('code')` ก่อน ซึ่งจับ `products_barcode_uidx`
  ไปด้วยเพราะชื่อมันมีคำว่า **code** อยู่ข้างใน — บาร์โค้ดซ้ำจะขึ้นข้อความว่า
  "รหัสสินค้าซ้ำ" ทั้งที่รหัสไม่ได้ซ้ำ ย้าย barcode ขึ้นมาเช็คก่อน
- บรรทัดที่ไม่เคยกรอกบนใบที่ปรับยอดแล้ว เคยแสดง "ระบบว่ามี 0"
  ทั้งที่ไม่เคยถูกแตะ — เปลี่ยนเป็น `null` แล้วแสดง "—" พร้อมเทสต์

## ชุดทดสอบ

- `stock-counts.test.ts` 24 ข้อ · `barcode.test.ts` 8 ข้อ (differential)
- `pl.test.ts` +2 · `hard-cases.test.ts` +10 · `backup-roundtrip.test.ts` +3 · `restore.test.ts` +1
- **กลายพันธุ์แล้วเทสต์ต้องแดง** ตรวจหกจุด: ช่องว่างเป็นศูนย์ (ทั้งฝั่งแอปและฝั่งนำเข้า) ·
  ปรับยอดซ้ำได้ · ใช้ยอดที่ตรึงตอนเปิดใบแทนยอดสด · ถอดใบตรวจนับออกจาก `WIPE_ORDER` ·
  ตัวนำเข้าปรับสต๊อกซ้ำ · แก้ตาราง C39 หนึ่งช่อง
