# DriveGoLight! — โครงสร้างระบบการแสดงผลและการนำทาง (UI/UX Layout & Navigation System)

ฉบับ 1.0 · 14 ก.ย. 2569 · ใช้กับเว็บแอป (Next.js) และไฟล์ต้นแบบ · ค่าทุกตัวในเอกสารนี้ตรงกับที่ใช้ในโค้ด (`globals.css` ตัวแปร `--h-*`, `--z-*`, จุดตัด 768/1280)

---

## 1. หลักการออกแบบ 5 ข้อ

| # | หลักการ | ความหมายในระบบนี้ |
|---|---|---|
| 1 | **ที่เดียว หน้าที่เดียว** | ปุ่ม/เมนูแต่ละหน้าที่มีตำแหน่งเดียวทั้งระบบ (ปุ่มย้อนกลับ = ปุ่มอำพันล่างขวาเท่านั้น, สร้างเอกสาร = การ์ดอำพันในแถบไทล์เท่านั้น) — ไม่มีของซ้ำสองที่ |
| 2 | **แสดงทีละเรื่อง** | หน้าเมนูย่อยแสดง "ฟอร์มสร้าง" หรือ "ประวัติ" อย่างใดอย่างหนึ่ง ไม่ซ้อนกัน (กดไทล์ประวัติ → ประวัติ, กดการ์ดสร้าง → ฟอร์ม) |
| 3 | **ของลอยมีชั้นตายตัวและเปิดได้ทีละชิ้น** | ลิ้นชัก / แผงยืนยัน / ปฏิทิน / ป๊อปอัปลิขสิทธิ์ ใช้ลำดับชั้น z ที่กำหนด เปิดชิ้นใหม่ = ปิดชิ้นเก่า และของที่ซ่อนอยู่ต้องรับคลิกไม่ได้ |
| 4 | **CTA ต้องกดได้เสมอ** | ปุ่มบันทึก/ยืนยัน/ย้อนกลับ ต้องไม่ถูกแถบเมนู ลิ้นชัก หรือคีย์บอร์ดบัง — มี safe zone ที่คำนวณจากความสูงแถบจริง |
| 5 | **จอโตขึ้น = ใช้พื้นที่โตขึ้น** | ไม่มีกรอบกว้างคงที่ (ไม่ยึด A4) ยกเว้นหน้าพิมพ์เอกสาร; ตาราง/ฟอร์มยืดเต็มความกว้างเสมอ |

---

## 2. จุดตัดขนาดจอ (Breakpoints)

ใช้ **mobile-first / min-width เท่านั้น** (ฐาน = จอแคบสุด) มีจุดตัดเพียง 2 จุด → 3 โหมด

| โหมด | ช่วงความกว้าง (CSS px) | Media query | อุปกรณ์ที่ตั้งใจให้ตรง |
|---|---|---|---|
| **Mobile** | 0 – 767 | (ฐาน ไม่มี query) | มือถือทุกขนาด แนวตั้ง/แนวนอน |
| **Tablet** | 768 – 1279 | `@media (min-width: 768px)` | iPad/แท็บเล็ต แนวตั้ง-แนวนอน, โน้ตบุ๊กจอเล็กที่ย่อหน้าต่าง |
| **Desktop** | ≥ 1280 | `@media (min-width: 1280px)` | โน้ตบุ๊ก/จอตั้งโต๊ะ |

กติกาเสริม
- **ห้ามใช้ max-width query** และห้ามเพิ่มจุดตัดอื่น (มีเทสต์ `css-layers.test.ts` บังคับ) — ทุกอย่างที่ต้อง "ต่างกันตามจอ" ให้เขียนเป็นกฎเพิ่มในสองบล็อกนี้
- การตัดสินใจโหมดใช้ **ความกว้าง viewport** ไม่ใช่ user-agent · หมุนจอแล้วโหมดเปลี่ยนได้ทันที (คอมโพเนนต์ที่ต้องรู้โหมดใน JS เช่น `SubnavPortal` ฟัง `matchMedia('(min-width: 768px)')`)
- ขนาดตัวอักษรฐาน 15px ทุกโหมด (ตาราง/ช่องกรอก 14px) · ปุ่มและไทล์สูงอย่างน้อย **44px** ทุกโหมด (ขนาดนิ้วโป้ง)

---

## 3. กายวิภาคของหน้า (Page Anatomy) และความสูงที่เป็นค่าคงที่

```
--h-topbar : 58px   แถบเมนูหลักบน (ทุกโหมด) / หัวหน้า (sticky ถัดจากเมนูหลัก)
--h-tabbar : 58px   แถบล่างบนมือถือ
```

| ชั้น (บน → ล่าง) | Mobile | Tablet | Desktop |
|---|---|---|---|
| A. แถบเมนูหลัก (`.rail`) | โลโก้ + ชื่อร้าน + ปุ่มลิ้นชัก ☰ · **sticky top:0** | เมนู 01–08 แนวนอน ไอคอน+เลข (ซ่อนข้อความ) + สถานะลิขสิทธิ์ · sticky | เมนู 01–08 ไอคอน+เลข+ชื่อเต็ม + ผู้ใช้ + ออกจากระบบ · sticky |
| B. หัวหน้า (`.topbar`) | ชื่อหน้า + คำอธิบาย + ปุ่มของหน้า · sticky ใต้ A | **ชื่อหน้า(ย่อ) + แถบเมนูย่อยบรรทัดเดียว + ปุ่มของหน้า** · sticky ใต้ A | เหมือน Tablet แต่มีที่ให้ชื่อไทล์เต็ม |
| C. เมนูย่อย (`.subnav`) | ตาราง 2 คอลัมน์ใต้ B (ไม่ sticky) | ย้ายขึ้นไปอยู่ใน B (portal) | ย้ายขึ้นไปอยู่ใน B |
| D. แถบไทล์ของหน้า (`.tiles`) | ไทล์เต็มแถว 2 ใบ/แถว | แถวเดียว ห่อได้ | แถวเดียว |
| E. เนื้อหา (`.wrap`) | padding 11px · ล่างเผื่อ tabbar+22px | padding 14px | padding 22–28px · **กว้างเต็มจอ ไม่จำกัด max-width** |
| F. แถบล่าง (`.tabbar`) | 5 ช่อง: หน้าแรก · รายรับ · สินค้า · ลูกค้า · เพิ่มเติม(☰) · **fixed bottom** | ไม่มี | ไม่มี |
| G. ปุ่มย้อนกลับลอย (`.backfab`) | fixed ขวาล่าง **เหนือ tabbar** (`bottom: 58+14px`) | fixed ขวาล่าง 18px | fixed ขวาล่าง 18px |

ระยะ "ปลอดภัย" ของเนื้อหาด้านล่าง (Mobile): `padding-bottom = var(--h-tabbar) + 22px` เพื่อไม่ให้ปุ่มบันทึกในฟอร์มถูกแถบล่างบัง; แถบฟอร์ม (`.formbar`) ไม่ sticky บนมือถือ เพื่อไม่ชนกับคีย์บอร์ดที่โผล่มา

---

## 4. ลำดับชั้นของสิ่งที่ลอย (Stacking / z-index) — ป้องกันการบังกัน

| ตัวแปร | ค่า | ใช้กับ | กติกา |
|---|---|---|---|
| `--z-content` | 0 | เนื้อหาปกติ | — |
| `--z-sticky` | 10 | หัวตาราง แถบเครื่องมือที่ติดขอบ | ต้องต่ำกว่าเมนูเสมอ (เลื่อนแล้วไม่ทับเมนู) |
| `--z-nav` | 20 | แถบเมนูหลัก · แถบล่างมือถือ | |
| `--z-header` | 30 | หัวหน้าที่ sticky | อยู่เหนือหัวตารางที่เลื่อนผ่าน |
| `--z-overlay` | 40 | ฉากหลังทึบ (scrim) | ครอบทั้งจอ กดแล้วปิดของที่เปิดอยู่ |
| `--z-drawer` | 50 | ลิ้นชัก · แผงยืนยัน · ปฏิทิน · ป๊อปอัปลิขสิทธิ์ · แผงเมนูหล่น | มีได้ **ชิ้นเดียว** ในเวลาหนึ่ง |
| `--z-toast` | 60 | ข้อความแจ้งผล | `pointer-events: none` ไม่บังการกด |

กฎที่บังคับใช้ในโค้ด
1. **ของลอยทุกชิ้นเป็น `position: fixed` ยึดกับจอ** (ไม่ยึดกับกรอบหน้า) — แผงยืนยันจึงเห็นครบไม่ว่าจะเลื่อนหน้าไปไกลแค่ไหน
2. **ของลอยที่ปิดอยู่ต้อง `display:none` หรือ `pointer-events:none`** (ลิ้นชักที่เลื่อนซ่อน ฉากหลัง ปฏิทิน ข้อความแจ้ง) — ห้ามมีชิ้นโปร่งใสรับคลิกค้าง
3. **เปิดชิ้นใหม่ = ปิดชิ้นเก่าเสมอ**: เปลี่ยนหน้า (`render`/route change) ต้องเรียก closeDrawer + closeConfirm + closeCalendar; กด Esc ปิดชิ้นบนสุด; กดฉากหลังปิด
4. **Focus**: เปิดแผงยืนยัน/ป๊อปอัป → โฟกัสไปที่ปุ่มหลักในแผง; ปิดแล้วโฟกัสกลับปุ่มที่กดเปิด; ปุ่ม Tab วนอยู่ในแผง (focus trap) ตอนเปิด
5. **Hydration**: ของที่ย้ายตำแหน่งตามจอด้วย JS (เมนูย่อย) วาดที่เดิมบนเซิร์ฟเวอร์ก่อน แล้วค่อยย้ายเมื่อรู้ขนาดจอ — ไม่ให้ layout กระโดดผิดตำแหน่ง

---

## 5. รูปแบบการนำทาง (Navigation Pattern) แยกตามอุปกรณ์

### 5.1 Mobile (0–767) — ใช้มือเดียว (Thumb zone)

```
┌──────────────────────────┐  A  แถบบน 58px: ชื่อร้าน · ☰ (ขวาบน สำหรับเมนูรอง)
│  หัวหน้า (sticky)         │  B
│  [เมนูย่อย 2 คอลัมน์]     │  C  ไทล์สูง 52px  กดได้ทั้งใบ
│  เนื้อหา / ฟอร์ม          │  E  ตาราง → การ์ด (สินค้า) หรือเลื่อนแนวนอนในกรอบเท่านั้น
│                          │
│                    (←)   │  G  ย้อนกลับ ลอยเหนือแถบล่าง
├──────────────────────────┤
│ หน้าแรก รายรับ สินค้า ลูกค้า ☰ │  F  แถบล่าง 58px  fixed  ทั้ง 5 อยู่ในระยะนิ้วโป้ง
└──────────────────────────┘
```

- **Bottom Navigation 5 ช่อง** = เมนูที่ใช้บ่อยที่สุดของหน้าเคาน์เตอร์ (หน้าแรก · รายรับ · สินค้า · ลูกค้า · เพิ่มเติม) — ช่อง "เพิ่มเติม" เปิด **ลิ้นชัก (Drawer)** ที่มีเมนูหลักครบ 01–08 + เมนูย่อยทั้งหมด + ออกจากระบบ
- **ลิ้นชัก** เลื่อนจากซ้าย กว้าง `min(88vw, 340px)` มีฉากหลังทึบ กดฉากหลัง/Esc/ปัดปิด · ตอนเปิด **เนื้อหาด้านหลังล็อกการเลื่อน** และแถบล่างยังอยู่ (ถูกฉากหลังคลุม กดไม่ได้)
- เมนูย่อยเป็น **ตาราง 2 คอลัมน์ใต้หัวหน้า** ไม่ใช่แถบเลื่อนแนวนอน (แถบเลื่อนบนมือถือมักมองไม่เห็นว่ามีต่อ)
- **Thumb zone**: ทุก CTA หลักของหน้าอยู่ล่าง (ปุ่มบันทึกในแถบฟอร์มท้ายหน้า, ปุ่มย้อนกลับลอยขวาล่าง) · ของที่ต้องกดบ่อยไม่วางมุมบนซ้าย
- **ไม่มีเมนูหล่น (dropdown) แบบเมาส์** — ใช้ลิ้นชัก/แผงเต็มความสูงแทน
- ตารางประวัติบนมือถือ: เลื่อนแนวนอนได้ **เฉพาะภายในกรอบตาราง** (`.tablewrap`) หน้าไม่ขยายตาม · ทะเบียนสินค้าใช้การ์ดต่อสินค้าแทนตาราง

### 5.2 Tablet (768–1279) — Hybrid

```
┌──────────────────────────────────────────────┐
│ โลโก้ · 01🏠 02👥 03🧾 04🛒 05📦 06📊 07⚙ 08🛡 · ลิขสิทธิ์ │ A  เมนูหลัก ไอคอน+เลข (ไม่มีชื่อ) sticky
│ รายรับ │ [ใบเสนอราคา][ใบส่งมอบ+VAT][ใบส่งมอบ][ใบเสร็จ][ใบวางบิล][ขายหน้าร้าน] │ พิมพ์ │ B+C  หัวหน้า + เมนูย่อยบรรทัดเดียว
│ [ประวัติทั้งหมด][ประวัติใบเสนอราคา][+ ใบเสนอราคา]  ค้นหา…  ☐ค้างส่งมอบ  │ D  ไทล์ของหน้า
│ เนื้อหาเต็มความกว้าง                                              (←) │ E+G
└──────────────────────────────────────────────┘
```

- เมนูหลักเป็น **Top Nav ย่อส่วน** (ไอคอน + เลข 01–08, ชื่อโผล่เป็น tooltip/`aria-label`) — ประหยัดที่แต่ยังกดตรงได้ ไม่ต้องเปิดอะไร
- เมนูย่อยเป็น **แถบไทล์บรรทัดเดียวในแถวหัวหน้า** ต่อจากชื่อหน้า (ไอคอน/คำอธิบายซ่อน) · ถ้าจอแคบจนไม่พอ **เลื่อนแนวนอนภายในแถบ** (`overflow-x:auto`, ซ่อนแถบเลื่อน) ห้ามตัดบรรทัดลงมา
- การ์ดสร้าง (อำพัน) และการ์ดเครื่องมือ (โทนกลาง: CSV, ตั้งค่าแสดงผล, ฉลากบาร์โค้ด) อยู่ในแถบเดียวกัน — **เครื่องมือชิดขวาสุด** (`margin-left:auto`)
- ไม่มีแถบล่าง ไม่มีลิ้นชัก (ลิ้นชักยังมีในโค้ดแต่ `visibility:hidden` + `pointer-events:none`)
- ตารางประวัติ: ซ่อนคอลัมน์รอง (ก่อนภาษี/ภาษี/ยอดสะสม `.opt`) เพื่อไม่ต้องเลื่อน

### 5.3 Desktop (≥1280) — Header Navigation เต็มรูปแบบ

- เมนูหลักเป็น **Top Nav เต็ม**: ไอคอน + เลข + ชื่อเมนู + ผู้ใช้/สถานะลิขสิทธิ์ + ออกจากระบบ (แถบเดียว 58px ไม่ต้องมี sidebar → เนื้อหากว้างเต็ม)
- เมนูย่อย/ไทล์/เนื้อหาเหมือน Tablet แต่โชว์คอลัมน์ครบ (`.opt` กลับมา) และตารางประวัติใช้คอลัมน์ความกว้างคงที่ (px) + คอลัมน์ชื่อ = ที่เหลือ จึง**ไม่มีเลื่อนซ้ายขวา**
- **ไม่จำกัดความกว้าง** (`.wrap` ไม่มี max-width) · หน้าพิมพ์เท่านั้นที่เป็น A4 (`.paper` 210×297 mm)
- คีย์บอร์ด: Enter = ช่องถัดไป (ทั้งระบบ), Esc = ปิดของลอย, Tab วนในแผง

### 5.4 ตารางสรุปพฤติกรรมเมนู

| องค์ประกอบ | Mobile | Tablet | Desktop |
|---|---|---|---|
| เมนูหลัก 01–08 | ในลิ้นชัก (จากช่อง ☰ แถบล่าง) | แถบบน ไอคอน+เลข | แถบบน ไอคอน+เลข+ชื่อ |
| เมนูย่อย | ตาราง 2 คอลัมน์ใต้หัวหน้า | บรรทัดเดียวในแถวหัวหน้า (เลื่อนในแถบได้) | บรรทัดเดียวในแถวหัวหน้า |
| การ์ดสร้าง (อำพัน) | ในแถบไทล์ของหน้า / ในเมนูย่อย (ลูกค้า) | ในแถบไทล์ของหน้า / เมนูย่อย | เหมือน Tablet |
| การ์ดเครื่องมือ | ท้ายตารางเมนูย่อย | ชิดขวาแถวหัวหน้า | ชิดขวาแถวหัวหน้า |
| ย้อนกลับ | ปุ่มลอยขวาล่าง เหนือแถบล่าง | ปุ่มลอยขวาล่าง | ปุ่มลอยขวาล่าง |
| ลิ้นชัก | ใช้ (จากซ้าย) | ไม่ใช้ | ไม่ใช้ |
| แผงยืนยันบันทึก | เต็มความสูง จากขวา กว้าง 92vw | จากขวา 380px | จากขวา 380px |
| ปฏิทินเลือกวัน | ป๊อปอัปใต้ปุ่ม (fixed, กันตกขอบจอ) | เหมือนกัน | เหมือนกัน |

---

## 6. กติกา "ไม่บังกัน" ทีละสถานการณ์

| สถานการณ์ | สิ่งที่ต้องเป็น |
|---|---|
| เลื่อนหน้ายาว ๆ | เมนูหลัก + หัวหน้า sticky ซ้อนกัน (58+58px) · หัวตาราง sticky อยู่ **ใต้** สองแถบนี้ (offset = 116px) ไม่ทับกัน |
| เปิดลิ้นชักบนมือถือ | ฉากหลังคลุมทั้งจอรวมแถบล่าง · เนื้อหาล็อกเลื่อน · ลิ้นชักกว้างไม่เกิน 88vw เหลือขอบขวาไว้กดปิด |
| กดบันทึก → แผงยืนยัน | แผงยึดจอ เลื่อนหน้าไม่หนี · ปุ่ม "บันทึก/แก้ไข" อยู่ท้ายแผงในระยะนิ้วโป้ง · เซิร์ฟเวอร์ตอบผิดพลาด → แผงปิดเองให้เห็นข้อความ |
| เปิดปฏิทินใกล้ขอบจอ | คำนวณตำแหน่งจากปุ่ม ถ้าเลยขอบล่าง/ขวาให้เลื่อนเข้ามา (`min(anchor, viewport-size)`) |
| คีย์บอร์ดมือถือโผล่ | แถบฟอร์มไม่ sticky · ช่องที่โฟกัสถูกเลื่อนขึ้นพ้นคีย์บอร์ดโดยเบราว์เซอร์ · ปุ่มย้อนกลับลอยไม่ทับช่องกรอก (อยู่ขวาล่างเล็ก) |
| ข้อความแจ้งผล (toast) | ลอยกลางล่าง `pointer-events:none` หายเอง 2.6 วิ ไม่บังปุ่ม |
| ป๊อปอัปลิขสิทธิ์หมดอายุ | ใช้แผงเดียวกับแผงยืนยัน (z 50) ขึ้นครั้งแรกของ session · ปิดได้ · ไม่ล็อกการทำงาน |
| เมนูย่อยหลายไทล์บนแท็บเล็ตแนวตั้ง | ไม่ตัดบรรทัด → เลื่อนแนวนอนในแถบ (แถบสูงคงที่) หัวหน้าไม่สูงขึ้นจนกินที่เนื้อหา |
| การ์ดสร้างกับปุ่มพิมพ์ | อยู่คนละแถว: การ์ดสร้างในแถบไทล์ (D) · ปุ่มพิมพ์รายงานท้ายแถบเครื่องมือของหน้า — ไม่มีสองปุ่มหน้าที่เดียวกันในหน้าเดียว |

---

## 7. ระบบสี/รูปทรงของสิ่งที่กดได้ (ให้ผู้ใช้เดาได้โดยไม่ต้องอ่าน)

| รูปแบบ | สี | ความหมาย |
|---|---|---|
| ไทล์เขียวเข้ม (พื้นทึบ) | `--steel-dk` | เมนู/ตัวกรอง **ที่เลือกอยู่** |
| ไทล์เทาอ่อน ขอบบาง | `#F4F6F5` | เมนู/ตัวกรองที่เลือกได้ |
| การ์ดอำพัน | `#FFF3CC` ขอบ `#F0C24A` | **สร้างของใหม่** (+ เอกสาร, + ผู้ติดต่อ, + สินค้า, + พนักงาน) และปุ่ม "พิมพ์เอกสาร" ในฟอร์ม |
| การ์ดขอบประโทนกลาง | `#EEF2F0` dashed | เครื่องมือ/ตั้งค่า (CSV, ตั้งค่าการแสดงผล, ฉลาก) |
| ช่องกรอกอำพัน | `.in.amber` | ช่อง "เงื่อนไข" ที่ต้องตัดสินใจ (ระดับราคา VAT เงื่อนไขชำระ เครดิต วันครบกำหนด รับโอนเข้าบัญชี) |
| ปุ่มในแถวประวัติ 2 บรรทัด | เขียว/แดงอ่อน/เทา/แดงเข้ม | รับชำระ · แก้ไข / พิมพ์ · ลบ (มี 3 = บน 2 ล่าง 1) |
| ปุ่มย้อนกลับลอย | อำพันโปร่ง | ที่เดียวทั้งระบบ |
| ป้ายสถานะ | เขียว/อำพัน/แดง(กะพริบเมื่อหมดอายุ) | ครบ / ค้าง-ใกล้หมด / เกินกำหนด-หมดอายุ |

---

## 8. โฟลว์หน้าเอกสาร (ใช้เหมือนกันทุกเมนู)

1. กดเมนูย่อย → **ฟอร์มสร้างใหม่ทันที** (เลขที่เอกสารถัดไปโชว์เลย, 5 บรรทัดว่างพร้อมยิงบาร์โค้ด)
2. แถบไทล์ด้านบน: `ประวัติทั้งหมด · ประวัติ<ชนิด> · [+ สร้าง (อำพัน)]` — กดประวัติ = **หน้าเปลี่ยนเป็นประวัติอย่างเดียว** (มีตัวกรองเวลา วันนี้/เดือนนี้/เดือนที่แล้ว/ปีนี้/ปีที่แล้ว/ปฏิทิน) กดการ์ดสร้าง = กลับเป็นฟอร์มอย่างเดียว
3. ปุ่มท้ายฟอร์ม: `บันทึก (เขียว) · ยกเลิก · 🖨 พิมพ์เอกสาร (อำพัน)` → แผงยืนยัน "ตรวจสอบถูกต้องแล้ว:" → บันทึกแล้ว **กลับหน้าเดิม** + แถบ "บันทึกเรียบร้อย [พิมพ์เอกสาร] [เปิดเอกสาร]" และฟอร์มใหม่พร้อมทำใบถัดไป (กดพิมพ์เอกสาร = บันทึกแล้วไปหน้าพิมพ์เลย)
4. Enter = ไปช่องถัดไปทุกช่อง · ในตารางรายการ: ช่องรหัสมีผลค้นหา → Enter เลือกตัวแรก, บรรทัดสุดท้ายว่าง → Enter เพิ่มบรรทัด

---

## 9. ตารางข้อมูล (Data tables) — กติกาความกว้าง

- ตารางประวัติ/ทะเบียน: `table-layout: fixed; width: 100%` · คอลัมน์ตัวเลข/สถานะ/ปุ่นกำหนดเป็น **px คงที่** · คอลัมน์ชื่อ (ลูกค้า/สินค้า) = ที่เหลือ และตัดบรรทัดได้ → รวมไม่เกินหน้าเสมอ
- ขนาดตัวอักษร: ข้อความ 14px · ตัวเลข mono 13px · **เลขที่เอกสารและชื่อลูกค้า 16px ตัวหนา** (สิ่งที่คนมองหาก่อน)
- Tablet ซ่อนคอลัมน์รอง (`.opt`) · Mobile ใช้การ์ด (สินค้า) หรือเลื่อนในกรอบ (ประวัติ)
- แถวรวมท้ายตาราง (`tfoot`) ตรึงคอลัมน์ตรงกับหัวตาราง
- แบ่งหน้า: ทะเบียนสินค้า 10/20 ต่อหน้า ตัวเลื่อน `‹ 1 2 [3] 4 … ›`

---

## 10. การเข้าถึงและคีย์บอร์ด

- ทุกไทล์/การ์ดเป็น `<a>` หรือ `<button>` จริง (ไม่ใช่ div) · `aria-current="true"` สำหรับตัวที่เลือก
- ลิ้นชัก/แผงยืนยัน/ป๊อปอัป: `role="dialog"` `aria-modal="true"` ปิดด้วย Esc · ฉากหลังเป็นปุ่มมี `aria-label="ปิด"`
- ปุ่มย้อนกลับลอย `aria-label="ย้อนกลับ"` · ปฏิทิน `role="dialog"` ปุ่มวันเป็น `<button>`
- คอนทราสต์ตัวหนังสือบนพื้นเขียวเข้ม/อำพัน ≥ 4.5:1 · เป้าหมายกด ≥ 44×44px

---

## 11. รายการตรวจสอบก่อนปล่อย (ต่อขนาดจอ)

- [ ] Mobile 360×740: แถบล่างไม่ทับปุ่มบันทึก · ปุ่มย้อนกลับลอยอยู่เหนือแถบล่าง · เปิดลิ้นชักแล้วกดฉากหลังปิดได้ · ตารางเลื่อนได้เฉพาะในกรอบ
- [ ] Tablet 768×1024 (แนวตั้ง): เมนูหลักไอคอน+เลขครบ 8 · เมนูย่อยรายรับ 6 ไทล์อยู่บรรทัดเดียว (เลื่อนในแถบได้) · ตารางประวัติไม่ต้องเลื่อน (ซ่อน `.opt`)
- [ ] Desktop 1366×768 และ 1920×1080: เนื้อหาเต็มกว้าง · ตารางประวัติครบทุกคอลัมน์ไม่เลื่อน · เมนูย่อย + ปุ่มของหน้าอยู่แถวเดียวกับชื่อหน้า
- [ ] ทุกจอ: ของลอยเปิดได้ทีละชิ้น · เปลี่ยนหน้าแล้วไม่มีปฏิทิน/แผงค้าง · Esc ปิดได้ · ไม่มีปุ่มย้อนกลับซ้ำ · ไม่มีปุ่มสร้างซ้ำ
- [ ] e2e (`nav.spec.ts`): ตำแหน่งปุ่มลอย, ไม่มี "← กลับ" ที่หัวหน้า, จุดตัดจอมีแค่ 768/1280

---

## 12. ไฟล์ที่เกี่ยวข้องในโค้ด

| หน้าที่ | ไฟล์ |
|---|---|
| ตัวแปรความสูง/ชั้น z, จุดตัด, กฎเมนูย่อย/ตาราง | `apps/web/src/app/globals.css` |
| แถบเมนูหลัก, แถบล่าง, ลิ้นชัก, หัวหน้า, ช่องรับเมนูย่อย | `components/shell.tsx`, `components/menu-bar.tsx`, `components/nav-drawer.tsx`, `components/nav-data.ts` |
| เมนูย่อย + ย้ายเข้าแถวหัวหน้าตามจอ | `components/sub-nav.tsx`, `components/subnav-portal.tsx` |
| ผังเมนูและการ์ดสร้าง/เครื่องมือ | `components/menu-map.ts`, `components/action-tiles.tsx` |
| แผงยืนยัน, ปฏิทิน, ป๊อปอัปลิขสิทธิ์, ปุ่มย้อนกลับ, Enter=ถัดไป | `components/confirm-save.tsx`, `thai-calendar.tsx`, `license-nag.tsx`, `back-fab.tsx`, `enter-to-next.tsx` |
| ตารางประวัติ/ทะเบียน (โครง `hist fit`) | `app/income/page.tsx`, `app/customers/page.tsx`, `app/expense/page.tsx`, `app/stock/page.tsx` |
| เทสต์ยาม | `test/css-layers.test.ts`, `test/menu-portal.test.ts`, `e2e/nav.spec.ts` |

---

## 13. สิ่งที่ปรับตอนนำเข้าระบบจริง (14 ก.ย. 2569)

สเปกข้างบนคงไว้ตามต้นฉบับ ส่วนนี้บันทึกจุดที่โค้ดจริงต่างออกไป พร้อมเหตุผลที่วัดหรือทดสอบได้

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล |
|---|---|---|---|
| ชื่อเมนูบนแถบบนเดสก์ท็อป (§3 A, §5.3) | ชื่อเต็ม | ย่อสี่ชื่อ: ลูกค้า/ผู้ขาย · การเงิน · ลิขสิทธิ์ · ฟอร์มเปล่า · ชื่อเต็มอยู่ใน title, aria-label, ลิ้นชัก และหัวหน้า | วัดที่ 1280px: ชื่อเต็มครบเก้าเมนู + ชื่อร้าน + ผู้ใช้ ต้องใช้ ~1,800px ปุ่ม 08 กับ 09 หลุดขอบขวา และห้ามเพิ่มจุดตัด (§2) |
| ชื่อร้านบนแถบบนเดสก์ท็อป | — | ซ่อน | §3 A ของเดสก์ท็อปไม่มีชื่อร้าน และที่ 1280px ไม่พอ |
| แถบล่างมือถือ (§5.1) | หน้าแรก · รายรับ · สินค้า · ลูกค้า · เพิ่มเติม | ตามสเปก (โค้ดที่ส่งมายังเป็น รายจ่าย แทน ลูกค้า) | — |
| ปุ่มย้อนกลับลอย (§3 G) | fixed ทุกจอ | position: fixed ย้ายเข้ากฎฐาน | ในโค้ดที่ส่งมา fixed อยู่ในบล็อก ≥768 เท่านั้น มือถือจึงได้ปุ่มที่ไหลไปต่อท้ายเนื้อหา |
| ช่องกรอกในตารางรายการ (§2, §10) | ≥ 44px | 44px (เดิม 36px ปุ่มลบ 32px) | e2e nav.spec ของชุดแก้เองจับได้ 37–38 จุด |
| Enter = ช่องถัดไป (§5.3, §8) | ทุกช่อง | ยกเว้นช่องยิงบาร์โค้ด (`data-enter="own"`) | Enter ของปืนย้ายโฟกัสออกจากช่องยิง นัดถัดไปจึงไปลงช่องอื่น ยิงสามนัดได้ของชิ้นเดียว |
| กู้คืนจากถังขยะ (SUMMARY ข้อ 7) | กู้คืนได้ทุกใบ ใบเสร็จตัดสต๊อกใหม่ | **ตามสเปก** (ชุดแก้ 22:37 เจ้าของกิจการเลือกกลับมาตามต้นฉบับ แทนกติกา "กู้ได้เฉพาะ QT/BN/PO/EX" ที่ใช้ช่วงสั้น ๆ) · ตัดสต๊อกกลับ **เท่าที่การยกเลิกครั้งล่าสุดคืนไป** ไม่ใช่อ่านจากบรรทัด | ต้นฉบับอ่านบรรทัดสินค้า — ไม่ตัดชิ้นส่วนชุดอะไหล่กลับ (สต๊อกเกินจริง) และจะตัดของใบที่นำเข้าจากรุ่นเดิมซึ่งไม่เคยตัดสต๊อก · เทสต์ trash-db |
| กู้ใบวางบิลจากถังขยะ | ล้างวันที่ยกเลิก | คืน `status` + ผูกใบในนั้นกลับ · ปฏิเสธถ้าใบในนั้นไปอยู่ใบวางบิลอื่นแล้ว | ตารางบังคับ status คู่กับ voided_at — ล้างอย่างเดียวกู้ไม่ได้เลยสักใบ |
| ลบถาวร (DELIVERY §2) | เจ้าของ + รหัสผ่าน | ตามสเปก · ตรวจรหัสในทรานแซกชันเดียวกับที่ลบ · พนักงานเห็นปุ่มจาง | — |
| สิทธิ์ชุดอะไหล่ (DELIVERY §4 ข้อ 6) | `stock.product` | `stock.kits` ตามผังเมนู | ไม่มีเมนูย่อย `product` — พนักงานที่ตั้งสิทธิ์รายเมนูย่อยแล้วจะบันทึกชุดไม่ได้เลย |
| หน้า 05.7 ชุดอะไหล่ | ผังเมนูชี้ `/stock/kits` | สร้างหน้าให้ (ชุดแก้มีแต่ `lib/kits.ts`) · รหัสถัดไปจากเลขสูงสุด + 1 | ต้นฉบับนับจำนวนแถว — ตั้งรหัสเองข้ามลำดับแล้วรหัสที่เสนอชน |
| `029_kits.sql` | — | เพิ่ม grant ให้ `dgl_app` แบบมีเงื่อนไข | ทุกตารางข้อมูลอู่มี (004 005 010 025) — ไม่มีแล้วฐานที่อัปเกรดอ่านชุดไม่ได้ |
| ไฟล์สำรอง | — | พา `_kits` และ `_kitId` ไปด้วย | กู้ข้อมูลแล้วชุดหายและบรรทัดเอกสารหลุดจากชุด |
| หน้าพิมพ์เอกสารขาย / ใบซื้อ-ค่าใช้จ่าย (15 ก.ย. 2569) | — | **ตามต้นแบบ 22:37 ทุกอย่าง** (pagePrint) — `components/doc-paper.tsx` ใช้ร่วมกันทั้งสองหน้า · หัว: ร้าน \| ชื่อเอกสาร+เลขที่ · ลูกค้า(ผู้ขาย) \| ข้อมูลเอกสาร · รถ 3 คอลัมน์ · หมายเหตุ(+การรับประกันเฉพาะใบเสร็จ) \| ยอดรวมไม่มีเส้น · ลายเซ็น 3 ช่อง · บัญชีรับโอน | เจ้าของกิจการเลือกให้เหมือนต้นแบบ รู้แล้วว่า **ไม่มี** กรอบวิธีชำระเงิน หัก ณ ที่จ่าย ยอดสุทธิที่รับชำระ จำนวนเงินตัวอักษร (ที่รุ่น 6.4 ซึ่งตรวจมาตรา 35 แล้วมี) · ต่างจากต้นแบบ: บันทึกค่าใช้จ่ายใช้หัว "ผู้ขาย" ไม่มีกรอบรถเหมือนใบซื้อ (ต้นแบบกันเฉพาะ PO จึงได้ "ลูกค้า" + กรอบรถว่าง) · ชื่อผู้รับเงิน/ผู้เสนอราคาใช้ชื่อที่กรอกในเอกสารก่อน ไม่มีจึงใช้ชื่อเจ้าของ |
| ฟอร์มเปล่า (09) | 6 แบบ (ต้นแบบ) | ตามต้นแบบ — เอาใบบันทึกค่าใช้จ่ายแยก ใบสั่งงานซ่อมแบบเดิม และตัวเลือกพิมพ์กี่ชุดออก · ใบรับรถ: ช่องติ๊กอยู่ในแถวของตัวเอง (ระบบแอร์ลงทีละข้อ) · "ลูกค้าลงชื่อรับทราบ" ชิดขวา (คลาส end ไม่มีกฎรองรับ) · บรรทัดวิธีชำระในฟอร์มใบเสร็จตัดบรรทัดได้ (ต้นแบบ nowrap แล้วเส้นประทะลุขอบกล่อง) · ใบรับรถระยะแถว/กล่องแน่นกว่าต้นแบบเล็กน้อย | วัดแล้วต้นแบบใบรับรถสูง 1,235px ที่ตัวอักษร 9.2px — เกิน A4 (1,123px) พิมพ์แล้วท้ายถูกตัด · ตอนพิมพ์: .wrap ไม่เว้นขอบบน และขึ้นหน้าใหม่เฉพาะระหว่างแผ่น (เดิมได้หน้าว่างต่อท้าย) → ทุกฟอร์มพิมพ์ A4 หน้าเดียว |
| หน้าพิมพ์บนมือถือ (.paper.doc2) | — | ต้นแบบเองล้นจอมือถือ (หัวเอกสาร min-width 228px · ผังรถ 360px · วันเดือนปีแบบกล่อง · สองคอลัมน์งาน · ลายเซ็นสามช่อง) → < 768 ลงบรรทัด/คอลัมน์เดียว · ≥ 768 และตอนพิมพ์ = ค่าต้นแบบ | วัดที่ 411px: ทั้งหน้าไม่เลื่อนซ้ายขวาทุกหน้า |
| ตารางประวัติ "พอดีหน้า" (§ ตาราง hist.fit — รายรับ รายจ่าย ใบวางบิล ลูกค้า ถังขยะ ชุดอะไหล่) | พอดีหน้าไม่ต้องเลื่อน · แท็บเล็ตซ่อนก่อนภาษี/ภาษี | ≥ 1280 ตามสเปก (fixed) · **< 1280 เลื่อนซ้ายขวาในกรอบตาราง** ช่องทั่วไปบรรทัดเดียว ช่องชื่อกว้างขั้นต่ำ 160px | วัดของที่ขึ้นเครื่องจริงแล้ว: ความกว้างที่ตั้งไว้รวม 822px เกินกรอบแท็บเล็ต 723px และมือถือ 372px — table-layout: fixed บีบช่องลูกค้าเหลือ **0px** แถวสูง 531px อ่านไม่ได้ทั้งแท็บเล็ตและมือถือ · หลังแก้ แถวสูง 77px (มือถือ/แท็บเล็ต/เดสก์ท็อปเท่ากัน) หน้าไม่เลื่อนซ้ายขวา · ถ้าต้องพอดีแท็บเล็ตจริงต้องซ่อนคอลัมน์เพิ่ม — รอเจ้าของกิจการเลือก |

### 13.1 ตรวจการแสดงผลทุกขนาดจอ (15 ก.ย. 2569)

สำรวจ 75 หน้า × 11 ความกว้าง (360–1920) — แผนและผลใน `PLAN-responsive-audit.md` · กันถาวรด้วย `e2e/responsive.spec.ts`

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| กล่องขวาของแถบบนเดสก์ท็อป (§3 A) | ชื่อผู้ใช้ · บทบาท + นับถอยหลังลิขสิทธิ์ | **ลิขสิทธิ์แบบสั้นอย่างเดียว** ("เหลือ 14 วัน" · "หมดอายุ 12 วัน" ยังแดงกะพริบ) · ชื่อ · บทบาท และข้อความเต็มอยู่ใน title/aria-label · ลิ้นชักมือถือยังแสดงครบ · ระยะปุ่มเมนู 8→7px ช่องไฟ 7→5px ขอบแถบ 18→14px | ที่ 1280px เมนูชื่อเต็มกว้าง 991px ในกล่อง 823px ทับกล่องขวา (กว้าง 188px) 158px — "09 ฟอร์มเปล่า" ซ้อนข้อความลิขสิทธิ์ · หลังแก้ ปุ่มเมนูสุดท้ายห่างกล่องขวา 42px ที่ 1280 ไม่ล้นที่ 1366/1440/1920 · แท็บเล็ตระยะปุ่ม 10→8px (เดิมล้น 5px ที่ 768) · ห้ามเพิ่มจุดตัด (§2) |
| ปุ่มในแถวตารางประวัติ/ลูกค้า (`.row-acts.grid2`) | 2×2 | ตามสเปก — คอลัมน์ปุ่มกว้างตามปุ่มที่ยาวที่สุด ไม่ตัดบรรทัด · สูง 44px เมื่อจอ < 1280 (32–34px ที่ ≥ 1280) | กฎ `table.hist .row-acts` (inline-flex) ความจำเพาะสูงกว่า ปุ่มสี่ปุ่มจึงเรียงแถวเดียว — ที่ 1280 "ใบเสนอราคา" กว้าง 41px สามบรรทัด ปุ่มล้นขอบการ์ด · หลังแก้ 81×32 / 43×32 อยู่ในการ์ดครบ |
| ปุ่มในแถวแบบเรียงเดียว (ประวัติรายจ่าย · ใบวางบิล · ชุดอะไหล่ · ถังขยะ) | — | ≥ 1280 ปุ่มขึ้นบรรทัดสองได้ทั้งปุ่ม · < 1280 แถวเดียวเหมือนเดิม | ≥ 1280 ตาราง fixed คอลัมน์ปุ่มรายจ่ายได้ 112px (127px ที่ 1440) ปุ่ม เปิด/พิมพ์/ยกเลิก กว้าง 150px — "ยกเลิก" ถูกช่อง overflow: hidden ตัดหาย 10 ใน 30 ปุ่ม · หลังแก้แถวสูง 81px ไม่มีปุ่มล้น · ลองให้ตัดบรรทัดทุกขนาดจอแล้วแถวมือถือสูง 153px (auto layout ให้คอลัมน์เท่า min-content) จึงจำกัดที่ ≥ 1280 |
| คอลัมน์เสริม `.opt` ของตาราง hist.fit (ลูกค้า · ประวัติรายรับ · ชุดอะไหล่) | ≥ 1280 แสดง | ตามสเปก — กฎ ≥ 1280 แยก `td/th.opt` (table-cell) กับ `col.opt` (table-column) | กฎเดิม `.opt { display: table-cell }` โดน `<col class="opt">` ด้วย col จึงไม่นับเป็นคอลัมน์ ความกว้างใน colgroup เลื่อนไปหนึ่งช่อง — ลูกค้า 1280: คงค้างได้ 176px ของปุ่ม ปุ่มกับที่อยู่เหลือ 143px แถวสูง 147px (ผู้ขายก็เป็น) · หลังแก้ทุกคอลัมน์ตรงค่าที่ตั้ง ที่อยู่ 194px แถวสูงสุด 103px |
| กรอบหน้า `.wrap` | — | selector เป็น `.main > .wrap` | `.main .wrap` โดนช่องตาราง `td.wrap` ทั้ง 40 จุด ช่องชื่อ padding ล่าง 80px (มือถือ 130px) แถวทะเบียนสินค้าสูง 111px → 39px |
| ชิปตัวกรองบนมือถือ | ไทล์ | ชิปกลุ่มเดียวกันอยู่ใน `.tag-row` (ลูกค้า · ช่วงวันที่หน้าแรก · ช่วงวันที่ของทุกหน้าประวัติ) | ลูกตรงของ `.toolbar` บนมือถือยืดเต็มจอ — ชิปห้าปุ่มกว้าง 319px เรียงลงห้าแถว สูงรวม ~280px |
| เมนูย่อยมือถือ (§5.1 ไทล์สองคอลัมน์) | ไอคอน + ชื่อ + เลขเมนู | ตามสเปก จัดใหม่: ไอคอน 24px · เลขเมนูลงบรรทัดใต้ชื่อ · ชื่อ 13.5px · ≥ 768 ค่าเดิมทุกค่า | ไทล์กว้าง 165px ที่จอ 360 — ไอคอน 30 + เลขเมนูข้างชื่อ เหลือที่ให้ชื่อราว 57px "ใบส่งมอบงาน/ใบแจ้งหนี้/ใบกำกับภาษี" 5 บรรทัด · หลังแก้ทุกชื่อไม่เกินสองบรรทัดที่ 360–430 |
| หน้าพิมพ์รายงานบนจอแคบ | — | แถบพิมพ์ตัดแถวได้ ปุ่มไม่ตัดบรรทัด · กระดาษเลื่อนซ้ายขวาในกรอบ `.printview` (`@media screen` เท่านั้น) | รายงานการเงินกว้าง 873px ที่มือถือ ทั้งหน้าเลื่อน · ปุ่ม "← กลับทะเบียนผู้ติดต่อ" 3 บรรทัด · จำนวนหน้า PDF A4 ก่อน/หลังเท่ากันทุกหน้า |
| ปุ่มคำสั้น (ตัวแบ่งหน้า · หัวหน้า · แถบเครื่องมือ) | — | ไม่ตัดบรรทัด · หัวหน้ามือถือให้ปุ่มลงแถวใหม่ทั้งก้อน | "ถัดไป" ถูกบีบ 55px สองบรรทัด · "🖨 พิมพ์รายงาน" · "+ เปิดใบเคลม" สองบรรทัดที่ 390px |
| หัวหน้ามือถือ (ตัวดันขวา `.spacer`) | — | < 768 ซ่อนตัวดัน ปุ่มหัวหน้าดันขวาด้วย `margin-left: auto` (≥ 768 ซ่อนอยู่แล้ว) | ผลข้างเคียงจากให้หัวหน้าตัดแถวได้ — ชื่อหน้าเต็มแถว ตัวดัน (flex: 1) ขึ้นแถวว่างของตัวเอง + ช่องไฟ 12px หัวหน้าขายหน้าร้านสูง 104px ชื่อค้างบน (ว่างบน 0 ล่าง 12) · หลังแก้สูง 92px ว่างบน/ล่างเท่ากัน · สำรวจซ้ำบน production จับได้ |
| ไทล์ครึ่งแถวบนมือถือ (`.tiles .tile`) | ไทล์ | < 768 เป็นบล็อก ตัวหนังสือกลาง (`align-content: center`) · ≥ 768 inline-flex เดิม | ไทล์ inline-flex ทำให้เลขเมนู/ช่องติ๊ก/ข้อความเป็นคนละก้อน ข้อความเหลือ 51–75px ขึ้นสองบรรทัด แล้ว `.k` ลอยเป็นแถวที่สาม ("05.11 + เพิ่มรายการสินค้าใหม่" 147–182px · "รหัสสินค้า เสมอ" 130–156px) — ลดตัวอักษร/ระยะอย่างเดียววัดแล้วยัง 3 บรรทัด · หลังแก้ทุกไทล์ ≤ 2 บรรทัด ตัวหนังสือกลาง (ไทล์บรรทัดเดียวที่ถูกยืด 74px ก็กลาง) · `align-content` บนบล็อกต้อง Chrome/Edge 123+ Safari 17.4+ — รุ่นเก่าตัวหนังสือชิดบนแต่ยังไม่เกินสองบรรทัด |
| ปุ่ม × ลบบรรทัดใบเคลม · ไทล์วิธีคิดต้นทุน | เป้ากด ≥ 44px (§2) | × 44px เมื่อจอ < 1280 · ไทล์วิธีคิดต้นทุนเต็มแถวบนมือถือ | × สูง 21px · "FEFO — หมดอายุก่อนออกก่อน (ค่าเริ่มต้น)" 4 บรรทัดที่ 360px |

### 13.2 ปรับแก้เล็ก 10 ข้อ (15 ก.ย. 2569)

แผน `PLAN-small-ui-2569-09-15.md` · เทสต์ถาวร `e2e/small-ui.spec.ts` · `test/form-hygiene.test.ts` · `test/contact-vehicles-db.test.ts` · `test/billnotes.test.ts`

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| ตารางรายการสินค้า (ใบขาย · ใบซื้อ · ใบเคลม · ชุดอะไหล่) | — | ช่องกรอกเต็มคอลัมน์ · ความกว้างที่ CSS `table.lines .c-*` (width + min-width): รหัส 150 · จำนวน 84 · หน่วย 88 · ราคา/หน่วย 120 · ส่วนลด 66 · ชื่อสินค้าได้ที่เหลือ (ขั้นต่ำ 220) · หัว "ส่วนลด %" ตัดสองบรรทัดได้ | ผู้ใช้กำหนด จำนวน 6 หลัก · ชื่อหน่วย 8 ตัวอักษร · ส่วนลด 4 หลัก · เดิม `style={{ width }}` ถูกเมิน `<input>` กว้างเอง 172px ทุกคอลัมน์ 184px ตาราง 1,419px ในกรอบ 1,234px · หลังแก้ 1280 ตารางพอดีกรอบ ชื่อสินค้า 335–366px · จอแคบเลื่อนในกรอบตาราง |
| ช่วงเวลาหน้าแรก | ชิปตัวกรอง | แถบปุ่มต่อกัน `.seg` (ทั้งหมด · วันนี้ · เดือนนี้ · เดือนที่แล้ว · ปีนี้ · ปีที่แล้ว) · มือถือ 3+3 · ป้าย "ตั้งแต่/ถึง" ติดกับช่องของตัวเอง | ผู้ใช้เลือกแบบ — เดิมชิป 52px ห้าปุ่มไม่เป็นชุด · มือถือ "ถึง" ตกไปท้ายบรรทัดห่างจากช่อง |
| ช่องค้นหา `.in.search` ทั้งระบบ | ไอคอนแว่น | ไอคอน + กรอบเขียวเข้ม `--steel-dk` | ผู้ใช้กำหนด — ไอคอนเดิม #7C8B96 จางจนมองไม่ออกว่าเป็นช่องค้นหา |
| ช่องวันที่ | ปฏิทินไทย พ.ศ. | ใช้ `ThaiDateInput` / `ThaiDateField` ทุกจุด (เพิ่ม id · required · disabled · full · name · allowEmpty · onCommit) | ยังมี `<input type="date">` 12 จุด (รับ/จ่ายชำระ ใบวางบิล สต๊อก ตรวจนับ ใบเคลม ผู้ดูแลระบบ) ปฏิทินของเบราว์เซอร์แสดง ค.ศ. และภาษาตามเครื่อง · form-hygiene บังคับห้ามมี |
| autocomplete | — | `autoComplete="off"` ทุก `<form>` (69) + ช่องที่อยู่นอกฟอร์ม · ช่องล็อกอินคง `username` / `current-password` ที่ตัวช่อง | ผู้ใช้กำหนด ไม่ให้เบราว์เซอร์เด้งค่าที่เคยกรอก · Chrome/Edge บางรุ่นไม่ฟัง `off` กับช่องที่เดาว่าเป็นชื่อ/ที่อยู่ — ตรวจด้วยเบราว์เซอร์จริง |
| ผู้ติดต่อชนิดผู้ขาย | — | ไม่มีส่วน "รถที่ดูแล" · เซิร์ฟเวอร์ไม่แตะตารางรถของผู้ขาย (`syncVehiclesWith`) | ผู้ใช้กำหนด · ถ้าซ่อนอย่างเดียว ผู้ขายที่เคยมีรถกดบันทึกแล้วรถถูกลบเงียบ ๆ |
| ฟอร์มสินค้าใหม่: วันหมดอายุ | — | ถามทีละขั้น: ไม่มี / มีวันหมดอายุ → อายุการเก็บ (เดือน) · วันหมดอายุของยอดยกมา (ปฏิทินไทย) · ยอดยกมาเป็นช่องของตัวเอง · "ไม่มี" ส่งค่าว่าง | ผู้ใช้เลือกแบบ — ช่องเดิมชื่อ "วันหมดอายุ / อายุการเก็บ (เดือน)" รับจำนวนเดือน วันที่อยู่อีกช่องไม่มีป้าย |
| เมนู 03 รายการใบในหน้าใบวางบิล | — | เรียงวันที่ล่าสุดบนสุด (`openInvoices`) · ใบวางบิลที่พิมพ์คงลำดับเดิม | ผู้ใช้กำหนด ทุกหน้าในเมนู 03 ล่าสุดบนสุด — รายการอื่นในเมนู 03 เรียงแบบนี้อยู่แล้ว |

### 13.3 ปรับแก้อีก 8 ข้อ (15 ก.ย. 2569)

แผน `PLAN-more-ui-2569-09-15.md` · เทสต์ถาวร `e2e/more-ui.spec.ts` · `test/nav-current.test.ts` · `test/sales-bars.test.ts` · `test/doc-sequences-db.test.ts`

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| สลับชนิดเอกสารในหน้าเดียวกัน (QT → IVT → RC · PO → EX · ใบวางบิล IVT ↔ IV) | — | ตัวแก้เอกสาร (`DocEditor` · `BuyEditor` · `BillForm`) มี `key` ตามชนิด/ต้นทาง/ใบที่แก้ | กดแท็บเปลี่ยนแค่ query ของหน้าเดิม React ใช้ตัวแก้เดิม state ที่ตั้งจากค่าเริ่มต้นครั้งแรกค้าง — หัวเปลี่ยนแต่ฟอร์มยังเป็นใบเสนอราคา เลขที่ยัง QT |
| ช่องติ๊ก "เปิดใช้งาน" ฟอร์มสินค้า | — | กดติดเฉพาะช่องสี่เหลี่ยม ข้อความข้าง ๆ ไม่ใช่ `<label>` (มี aria-label แทน) | ผู้ใช้กำหนด — คลิกข้อความแล้วเผลอปิดสินค้า · ตรวจแล้ว ไม่ติ๊ก = active false และไม่ขึ้นในช่องค้นหาของทุกเอกสาร |
| รูปสินค้าในฟอร์มเพิ่มสินค้าใหม่ | — | เลือกรูปได้เลย ย่อที่เบราว์เซอร์ด้วยตัวเดียวกับกล่องรูปหน้าสินค้า (`pic-shrink.ts`) · บันทึกในทรานแซกชันเดียวกับสินค้า รูปไม่ผ่าน = ไม่สร้างสินค้า · สินค้าที่มีอยู่แล้วแก้รูปที่กล่องรูปหน้าสินค้าเหมือนเดิม | ผู้ใช้กำหนด — เดิมต้องบันทึกสินค้าก่อนแล้วเข้าไปใส่รูปอีกรอบ |
| คอลัมน์ชื่อ ทะเบียนลูกค้า/ผู้ขาย | ชื่อได้ที่เหลือ | ชื่อ 240px คงที่ ที่อยู่ได้ที่เหลือ | ผู้ใช้กำหนด พอดีข้อมูล — ชื่อยาวสุดในข้อมูล 220px แต่ `24%` ให้ 296px ที่ 1280 และ 450px ที่ 1920 |
| ขีดเหลืองใต้เมนูหลัก | เมนูที่อยู่ | ติดทุกหน้าในหมวด (`/finance/sales` ติดเมนู 06) — เทียบส่วนแรกของ path | เดิมเทียบ path เต็มของเมนู หน้าย่อยการเงินไม่ติดเลย |
| กราฟยอดขายหกเดือนหน้าแรก | — | เขียวไล่อ่อน → เข้ม เดือนปัจจุบันเข้มสุด (`barShade`) | ผู้ใช้กำหนดสีต่างกันทุกเดือน |
| เลขที่เอกสาร | — | ไม่เปลี่ยน — รันอัตโนมัติ นับใหม่ 0001 ทุกเดือน แยกชนิดเอกสาร แยกอู่ (ใบวางบิล · ใบเคลมแยกฝั่ง · ใบตรวจนับ ก็เหมือนกัน) · คง 4 หลัก | ผู้ใช้ให้ตรวจ · เพิ่มเทสต์ฐานข้อมูลจริงเรื่องเปลี่ยนเดือน/ข้ามปี/ออกย้อนเดือน |
| ค้นหาในหน้ารายการเมนู 03 (ทุกใบ · ใบวางบิล · ขายหน้าร้าน) | — | ไม่มีปุ่ม "ค้นหา" พิมพ์แล้ว Enter · ไม่มี dropdown เดือน/ปี เหลือปฏิทิน ตั้งแต่–ถึง + ดู (`DocDateFilter monthPicker={false}`) · ฟอร์มค้นหามี `data-enter="own"` · เมนูอื่นยังมี dropdown เดือน · ช่องค้นหาในฟอร์มออกเอกสารไม่แตะ | ผู้ใช้กำหนด ซ้ำซ้อน · เอาปุ่มออกแล้ว `EnterToNext` ดัก Enter ไปโฟกัสช่องติ๊ก "รวมใบที่ยกเลิก" ค้นไม่ได้เลย — เทสต์จับได้ |

### 13.4 popup บันทึก · หัก ณ ที่จ่าย 1,000 บาท · หน้าเอกสารไม่มี VAT · ตารางลูกค้า · หน้ารายรับเปิดมาเห็นหัว (16 ก.ย. 2569)

แผน `PLAN-save-popup-wht-2569-09-16.md` · เทสต์ถาวร `e2e/popup-wht.spec.ts` · `test/saved-target.test.ts` · `packages/core/test/totals.test.ts` (+ `wht-minimum.ts` ในเทสต์เทียบของเดิม)

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| หลังกดบันทึก (ทุกเอกสาร + ผู้ติดต่อ สินค้า ชุดอะไหล่) | แถบบันทึกเรียบร้อยด้านบน | **การ์ดขนาดนามบัตรกลางจอ** (`SavedCard` กว้างไม่เกิน 380px · มือถือเว้นขอบ 16px) — 2 วินาทีแรก ✓ + "บันทึกเอกสารเรียบร้อย" / "บันทึกข้อมูลแล้ว" แล้วแสดง เลขที่ · ชื่อ · ยอดรวม · [พิมพ์เอกสาร] (แท็บใหม่ หน้า A4) [แก้ไขเอกสาร] [ปิด] · ปิด (ปุ่ม/Esc/ฉากหลัง) ลบค่าออกจาก URL · สร้างใหม่ → ฟอร์มเปล่าชนิดเดิมด้านหลัง · แก้ไข → หน้าที่กดแก้ไขมา | ผู้ใช้กำหนด — เพิ่มผู้ติดต่อแล้วไปหน้าผู้ติดต่อ จอกระพริบเหมือนไม่ได้บันทึก · แถบบนหายไปเมื่อเลื่อนลงอยู่ · ข้อมูลในการ์ดอ่านจากฐานด้วย id (ไม่เชื่อ URL) · returnTo กรองด้วย `safeBack` กันพาไปเว็บอื่น · ใบเคลมแก้ไขไม่ได้ ปุ่มที่สองเป็น "เปิดดูใบเคลม" |
| หัก ณ ที่จ่ายเอกสารรายรับ | — | ค่าแรง/ค่าบริการ (ก่อน VAT หลังเฉลี่ยส่วนลด) **ต่ำกว่า 1,000 บาทไม่หัก** (`WHT_MIN_BASE`) · ข้อความแดง "(ค่าบริการ 1,000 บาทขึ้นไป)" ใต้ช่องอัตรา · ไม่ถึงแล้วสรุปยอดบอกเหตุผล · ค่าใช้จ่ายคงเดิม | ผู้ใช้กำหนด (กติกาสรรพากร จ่ายครั้งหนึ่ง 1,000 บาทขึ้นไป) — เดิมค่าแรง 500 หัก 15 บาท · ต่างจากโปรแกรมเดิมโดยตั้งใจ เทสต์เทียบของเดิมใส่กติกานี้ทับไว้ที่เดียว |
| หน้าเปิดดูเอกสารขายไม่คิดภาษี | — | ไม่แสดงมูลค่าก่อนภาษี / ภาษีมูลค่าเพิ่ม | ผู้ใช้แจ้ง — ฟอร์มกับหน้าพิมพ์ซ่อนถูกแล้ว เหลือหน้า `/income/[id]` |
| ช่องยิงบาร์โค้ดโฟกัสตอนเปิดหน้า | — | `focus({ preventScroll: true })` | เปิดหน้ารายรับเลื่อนลง 612–859px ไปหาช่องยิง ไม่เห็นหัวเอกสาร · หลังแก้ scrollY 0 ช่องยิงยังโฟกัส |
| ทะเบียนลูกค้า/ผู้ขาย | ทะเบียนรถ / ภาษี | แสดงทะเบียนจริง 2 คันแรก + "อีก n คัน" · การ์ดตั้งค่าการแสดงผล (ใช้ร่วมกับทะเบียนสินค้า `components/col-picker.tsx`) ค่าเริ่มต้นซ่อน ที่อยู่ · เครดิต · ยอดสะสม · ข้อมูล 15px ชื่อ 16px | ผู้ใช้แจ้ง — หัวคอลัมน์บอกทะเบียนแต่ข้อมูลเป็น "รถ 1 คัน" · ทะเบียนดึงในคำสั่งเดียวกับรายชื่อ ไม่ยิงถามทีละแถว |

### 13.5 ลิงก์หน้าแรกพาไปประวัติ (16 ก.ย. 2569)

แผน `PLAN-home-links-2569-09-16.md` · เทสต์ถาวร `e2e/home-links.spec.ts`

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| ลิงก์จากหน้าแรกไปเมนู 03 / 04 | — | ส่ง `hist=1` ทุกลิงก์: การ์ดสรุปยอดขาย "ดูใบเสร็จทั้งหมด" · การ์ดงานค้างส่งมอบ "ดูรายการ" · ตาราง "เอกสารในระบบ" ทุกแถว (รายรับและรายจ่าย) | เมนู 03/04 เปิดมาเป็นฟอร์มสร้างใหม่เสมอ (`histFirst = hist=1 หรือไม่ได้เลือกชนิด`) ลิงก์ที่ส่งแค่ `?kind=` จึงได้ฟอร์มเปล่า — ผู้ใช้แจ้งว่ากด "ดูใบเสร็จทั้งหมด" แล้วได้หน้าสร้างใบเสร็จใหม่ · ตัวกรองเดิม (`open=1`) ยังติดไปด้วย · ไม่แตะกติกาเปิดเมนูย่อยมาเป็นฟอร์ม |

### 13.6 คอนโซลผู้ให้บริการ: ผูกกับอู่ที่เลือก · อีเมลเจ้าของ (16 ก.ย. 2569)

แผน `PLAN-ops-owner-scope-2569-09-16.md` · ไมเกรชัน `db/031_ops_owner_scope.sql` (อยู่ใน `FRESH_FILES` ด้วย — ฐานติดตั้งใหม่รันไฟล์นี้ต่อจาก 011 เพราะไฟล์ที่ปล่อยไปแล้วห้ามแก้) · เทสต์ `test/ops-console.test.ts`

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| ออกลิงก์ตั้งรหัสผ่านให้เจ้าของอู่ | ลิงก์ของอู่ที่เลือก | กรอง `u.tenant_id = p_tenant` ในคำสั่งอ่านโดยตรง | ผู้ใช้แจ้ง: กดที่อู่ ข ได้อีเมลอู่ ก · ฟังก์ชันเป็น SECURITY DEFINER และ `users` ปิด force RLS ไว้ (db/012) นโยบายจึงไม่มีผล — โทเคนที่ออกเปิดบัญชีของอู่อื่นได้จริง ไม่ใช่แค่ข้อความผิด · เทสต์ตรวจถึงระดับโทเคนว่าผูกกับบัญชีของอู่ที่กด |
| จำนวนผู้ใช้ในรายชื่ออู่ | ต่ออู่ | กรอง `u.tenant_id` เช่นกัน | สาเหตุเดียวกัน — เดิมได้ยอดรวมทุกอู่เท่ากันหมด (อู่ที่มีเจ้าของคนเดียวขึ้น 3) |
| หน้ารายละเอียดอู่ | — | แสดง "อีเมลเจ้าของ" ในการ์ดผู้ใช้งาน (`owner_email` จาก `ops.list_shops`) | ผู้ใช้ขอ — เห็นก่อนกดออกลิงก์ว่าลิงก์จะเข้าบัญชีไหน · คืนเฉพาะอีเมลเจ้าของ ไม่ได้เปิดตาราง `users` ให้คอนโซลอ่าน (เทสต์ขอบเขตข้อมูลยังคุมรายการคอลัมน์อยู่) |

### 13.7 ตั้งรหัสผ่านตามลิงก์ · ออกจากระบบ (16 ก.ย. 2569)

แผน `PLAN-setup-session-logout-2569-09-16.md` · เทสต์ `e2e/setup-logout.spec.ts` · `test/see-other.test.ts` · `test/setup-clears-session.test.ts`

| เรื่อง | สเปกเขียน | โค้ดจริง | เหตุผล (วัดได้) |
|---|---|---|---|
| ตั้งรหัสผ่านตามลิงก์ (อู่ · คอนโซล) | ตั้งเสร็จไปหน้าล็อกอิน | ออกจากบัญชีที่ค้างในเบราว์เซอร์ก่อน (`signOut()` / `opsSignOut()`) แล้วค่อยไปหน้าล็อกอิน | ผู้ใช้แจ้ง: เปิดอู่ใหม่ ตั้งรหัสตามลิงก์ แล้วเข้าไปอยู่อู่อื่น · หน้าล็อกอินเด้งคนที่มีเซสชันไปหน้าแรกทันที คนที่ยังล็อกอินอู่เดิมจึงไม่มีโอกาสกรอกอีเมลใหม่ (รหัสของอู่ใหม่ถูกตั้งถูกต้องแล้ว ไม่ได้ตั้งข้ามอู่) |
| รีไดเรกต์หลังออกจากระบบ | — | ตอบ 303 พร้อม `Location` เป็น path ล้วน (`lib/http.ts` → `seeOther`) | ผู้ใช้แจ้ง: กดออกจากระบบแล้วไป `https://localhost:10000/login` · เดิมประกอบ URL จาก `request.url` ซึ่งบน Render เป็นที่อยู่ภายในหลัง proxy · ไม่เดาโฮสต์จาก header ที่ปลอมได้ — ไม่ใส่โฮสต์เลย เบราว์เซอร์ต่อกับโฮสต์ที่เปิดอยู่เอง |
