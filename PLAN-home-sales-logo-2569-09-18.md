# แผน — ยอดขายหน้าแรกให้เท่า 6.4 · นำเข้าโลโก้จากไฟล์สำรอง (18 ก.ย. 2569)

## ที่มา
ผู้ใช้กู้คืนไฟล์สำรองแล้วเห็นตัวเลขไม่เท่าโปรแกรมเดิม ตรวจแล้วข้อมูลเข้าครบถูกต้องทุกยอด
(หน้า 06.1 · ลูกหนี้ · เจ้าหนี้ · สินค้า · คงเหลือ ตรงกับ 6.4 เป๊ะ) ต่างที่ "การแสดงผล" สองจุด
และไฟล์ร้านจริงเสียโลโก้ไประหว่างนำเข้า

ผู้ใช้ตัดสินใจ 18 ก.ย. 2569:
1. ตัวเลขใหญ่ใต้ "สรุปยอดขาย" = **ยอดสุทธิรับแบบ 6.4** (รวม VAT แล้วหักภาษี ณ ที่จ่าย)
2. ช่วงเวลาตั้งต้นหน้าแรก = **"ทั้งหมด" คงเดิม** (6.4 เป็น "เดือนนี้" — ต่างโดยตั้งใจ)
3. โลโก้ในไฟล์สำรอง = **นำเข้าให้อัตโนมัติ**

## งาน A — ยอดขายหน้าแรกเป็นยอดสุทธิรับ
- แยก `homeSummaryWith(c, from, to)` ออกจาก `getHomeSummary()` ใน `lib/queries.ts`
  (แบบเดียวกับ `ar-ap.ts` / `home-report.ts`) เพื่อให้ชุดทดสอบเรียกได้ตรง ๆ ไม่ต้องผ่าน session
- ยอดที่การ์ดใช้เปลี่ยนจาก `sum(net_amount)` เป็น `sum(payable)` — เงื่อนไขเอกสารขายชุดเดิม
  (`SALES_DOCS`: ใบส่งมอบทุกใบ + ใบเสร็จที่ไม่ได้ออกต่อจากใบส่งมอบ) ไม่แตะ
- `เฉลี่ยต่อใบ` คิดจากยอดเดียวกัน ป้ายใต้ตัวเลขเปลี่ยน `บาท ก่อนภาษี` → `บาท สุทธิรับ`
- การ์ดเล็กของจอแคบ (`mstat` ยอดขาย) ใช้ยอดเดียวกัน — สองแบบต้องไม่พูดคนละเรื่อง
- ไม่แตะ: กราฟ 6 เดือน (คิดจาก payable อยู่แล้ว) · รายจ่าย · ลูกหนี้ · เจ้าหนี้ · หน้า 06.1

## งาน B — นำเข้าโลโก้จากไฟล์สำรอง
- `packages/core`: ค่าคงที่ `LOGO_MAX_BYTES = 200 KB` + `isLogoDataUri()` ที่เดียว
- `packages/importer/src/import.ts`: `shop.logo` ที่เป็น data URI และไม่เกินขนาด → เขียนลง `shops.logo_url`
  (คอลัมน์นี้เก็บ data URI อยู่แล้วตามที่หน้า 07.1 ใช้ — คำเตือนเดิมที่บอกให้ไปอัปโหลด object storage ตกยุคแล้ว)
  ถ้าเกินขนาดหรือไม่ใช่รูปที่รองรับ → ไม่เขียน และเตือนด้วยข้อความที่บอกเหตุผลจริง
- `apps/web/src/app/settings/actions.ts` ใช้ตัวตรวจชุดเดียวกับ importer แทน regex ที่เขียนซ้ำ
- ผลพลอยได้: สำรอง → กู้คืน โลโก้ไม่หายอีก (`backup.ts` ส่งออก `logo` อยู่แล้ว)

## เทสต์ (เขียนให้แดงก่อนแก้ทุกตัว แล้วทุบเช็คทีละจุด)
1. `test/home-summary-db.test.ts` — ยอดขายหน้าแรก = Σ payable ของเอกสารขาย ไม่ใช่ Σ net
   · ชุดข้อมูลมีทั้ง VAT และหัก ณ ที่จ่าย เพื่อให้ net / grand / payable ต่างกันจริง
   · ใบยกเลิกไม่ถูกนับ · ใบเสร็จที่ออกต่อจากใบส่งมอบไม่นับซ้ำ · ช่วงวันที่กรองถูก
   · เฉลี่ยต่อใบ = ยอด ÷ จำนวนใบ
2. e2e หน้าแรก — ตัวเลขบนการ์ด "สรุปยอดขาย" เท่ากับยอด "สุทธิรับ" รวมของหน้า 06.1 ช่วงเดียวกัน
   (ผูกสองหน้าเข้าหากัน ถ้าใครแก้ข้างเดียวจะแดง) · ตรวจการ์ดเล็กจอแคบว่าเลขตรงกับการ์ดใหญ่
3. `packages/importer` — ไฟล์ที่มีโลโก้ PNG data URI → `logo_url` ถูกตั้ง และไม่มีคำเตือนเรื่องโลโก้
   · โลโก้เกิน 200 KB และโลโก้ที่ไม่ใช่รูป → ไม่เขียนลงฐาน + มีคำเตือน
4. `test/restore.test.ts` — กู้คืนไฟล์ที่มีโลโก้แล้วอ่าน `getShopSettings()` ได้โลโก้กลับมา
5. `backup-roundtrip` — สำรองแล้วกู้คืน โลโก้ยังอยู่

## ตรวจซ้ำก่อนส่ง
- `npm test -w @drivegolight/web` ทั้งชุด + เทสต์ของ core/importer
- e2e ครบ 3 โปรเจกต์ (มือถือ · แท็บเล็ต · เดสก์ท็อป) บนบิลด์โปรดักชัน
- เทียบยอดหน้าแรกกับสูตร 6.4 จากไฟล์สำรองจริงอีกรอบ ต้องได้ 657,081.70 เท่ากัน
- ส่งภาพหน้าแรก 1–2 รูป

## ไม่ทำในรอบนี้
- ช่วงเวลาตั้งต้นหน้าแรก (คง "ทั้งหมด" ตามที่ผู้ใช้เลือก)
- รหัสสินค้าซ้ำยังเปลี่ยนชื่ออัตโนมัติเหมือนเดิม (A11-055 → A11-055-2)
- บรรทัดที่อ้างสินค้าที่ถูกลบ ยังเก็บเป็นรายการไม่ผูกทะเบียนเหมือนเดิม
