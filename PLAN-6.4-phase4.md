# ช่วงที่ 4 — ใบเคลมสินค้า ลูกค้าและผู้ขาย (05.3 / 05.4)

## Context

สองแท็บสุดท้ายที่ยังจางอยู่ในหมวดสินค้า นอกจาก 05.5 ที่เป็นช่วงถัดไป

รุ่น 6.4 ใช้ตารางเดียว `DB.claims` เก็บทั้งสองทิศทาง แยกด้วยฟิลด์ `side`
(`customer` / `vendor`) — เลขที่ขึ้นต้น `CL-` กับ `VC-` และมีชุดประเภทคนละชุด
แต่ฟอร์ม การตัดสต๊อก การพิมพ์ และการยกเลิกใช้โค้ดชุดเดียวกันทั้งหมด

**ทั้งสองทิศทางตัดสต๊อกจริง** — ฝั่งลูกค้าคือจ่ายอะไหล่ออกไปโดยไม่เก็บเงิน
ฝั่งผู้ขายคือส่งของชำรุดออกจากร้านไปคืนโรงงาน ของออกจากคลังเหมือนกันทั้งคู่

ช่องในงบกำไรขาดทุนทำรอไว้แล้วตั้งแต่ช่วงที่ 1 — `writeOff.claim` ยังเป็นศูนย์
พร้อมคอมเมนต์ว่า "รอโมดูลเคลมในช่วงที่ 4" ช่วงนี้คือการเติมค่านั้น

---

## สิ่งที่ตัดสินใจไว้ก่อน พร้อมเหตุผล

### 1. ตารางแยก `claims` + `claim_items` ไม่ยัดเข้า `documents`

ใบเคลมไม่มียอดเงิน ไม่มีภาษี ไม่ตั้งลูกหนี้หรือเจ้าหนี้ ไม่มีเลขผู้เสียภาษีบนหัวเอกสาร
สิ่งเดียวที่มีคือ**ต้นทุนของที่หายไปจากคลัง** ถ้ายัดเข้า `documents` จะต้องใส่ศูนย์
ในคอลัมน์เงินสิบกว่าช่อง แล้วทุกคิวรีที่นับยอดขายต้องจำไว้ว่าต้องกรองใบเคลมออก
— เหตุผลเดียวกับที่แยกใบวางบิลออกมาในช่วงที่แล้ว

### 2. บันทึกแล้วแก้ไม่ได้ ต้องยกเลิกแล้วเปิดใบใหม่ — ตามต้นฉบับ

6.4 ปิดปุ่มบันทึกทันทีที่ใบอยู่ในฐาน (`inDb` → `disabled`) และเขียนไว้บนฟอร์มว่า
"เอกสารนี้บันทึกแล้ว — แก้ไขไม่ได้ ถ้าผิดให้ยกเลิกแล้วเปิดใบใหม่"

ทำตาม เพราะทำให้บัญชีสต๊อกเรียบร้อยมาก — ใบหนึ่งมีการตัดครั้งเดียวและการคืนอย่างมากครั้งเดียว
ไม่ต้องมีตรรกะเทียบส่วนต่างอย่างที่ `syncSalesStock` ต้องทำ

(6.4 มีโค้ดคืนของเดิมก่อนตัดใหม่อยู่ใน `clSave` แต่เข้าไม่ถึงเพราะปุ่มถูกปิด — เราไม่พอร์ตส่วนที่ตายแล้ว)

### 3. ไม่มีคอลัมน์ `deducted` — บัญชีสต๊อกคือความจริง

6.4 ต้องมีธง `deducted` เพราะล็อตเป็นอาเรย์ที่เขียนทับตัวเอง ไม่มีทางรู้ย้อนหลังว่าตัดไปหรือยัง

เรามี `stock_moves` ที่เพิ่มอย่างเดียว "ตัดไปแล้วหรือยัง" ตอบได้ด้วยการดูว่ามีแถวอยู่ไหม
ธงที่ซ้ำกับความจริงคือธงที่วันหนึ่งจะไม่ตรงกับความจริง

สถานะจึงเหลือ `issued` / `void` ชุดเดียวกับเอกสารอื่นทั้งระบบ

### 4. ฝั่งผู้ขายก็นับเป็นของหายจากคลัง — ตามต้นฉบับ พร้อมคำเตือนบนใบพิมพ์

`claimWriteOff` ของ 6.4 รวมทั้งสองทิศทาง และใบพิมพ์ฝั่งผู้ขายเขียนกำกับไว้ว่า
**"เมื่อผู้ขายส่งของทดแทนมาให้ ให้เปิดใบซื้อรับเข้าสต๊อกตามปกติ"**

เป็นวิธีที่ถูกก็ต่อเมื่ออู่เปิดใบซื้อของทดแทนที่**ต้นทุนศูนย์** ถ้าเปิดที่ราคาเต็ม
ต้นทุนจะถูกนับสองรอบ — ครั้งหนึ่งตอนเคลม อีกครั้งตอนของใหม่เข้า

ทำตาม 6.4 แต่ย้ำข้อความนั้นให้ชัดกว่าเดิมทั้งบนหน้าจอและบนใบพิมพ์
ไม่เปลี่ยนวิธีคิด เพราะอู่ที่ใช้ของเดิมอยู่คุ้นกับตัวเลขชุดนี้แล้ว

### 5. บรรทัดที่ไม่ผูกทะเบียนสินค้าไม่ตัดสต๊อก — ตามต้นฉบับ

6.4 ให้เพิ่ม "รายการอื่น ๆ" ที่พิมพ์ชื่อเองได้ ใช้กับของที่ไม่ได้ลงทะเบียน
บรรทัดพวกนี้ขึ้นบนใบพิมพ์แต่ไม่แตะสต๊อก และมีดอกจันแดงเตือนไว้ที่ชื่อ

เก็บไว้ทั้งพฤติกรรมและคำเตือน แล้วบอกจำนวนซ้ำในข้อความหลังบันทึกเหมือนต้นฉบับ

---

## แก้ฐานข้อมูล

`db/001_init.sql` (ติดตั้งใหม่) + `db/005_claims.sql` (ฐานที่มีอยู่แล้ว)

```sql
create type claim_side as enum ('customer', 'vendor');

create table claims (
  id, tenant_id, no,                    -- CL-YYYYMM-NNN / VC-YYYYMM-NNN
  side          claim_side  not null,
  kind          text        not null,   -- warranty/supplier/damage/other · defect/wrong/damaged/return/other
  claim_date    date        not null,
  party_id      uuid references contacts(id) on delete set null,
  party_name, party_tel,                -- snapshot เหมือนเอกสารอื่น
  ref_no        text,                   -- ใบเสร็จ/ใบซื้อ/เลขเคลมของผู้ขาย
  vehicle_id, vehicle, vehicle_plate,   -- ฝั่งผู้ขายว่างไว้
  reason        text not null,          -- 6.4 บังคับกรอกก่อนบันทึก
  by_whom, note,
  status        doc_status not null default 'issued',
  voided_at, voided_reason, created_by, created_at,
  unique (tenant_id, no),
  constraint claim_void_stamp check ((status = 'void') = (voided_at is not null)),
  -- ชนิดต้องอยู่ในชุดของทิศทางนั้น ไม่ให้ใบฝั่งลูกค้ามี kind ของฝั่งผู้ขาย
  constraint claim_kind_side check (
    (side = 'customer' and kind in ('warranty','supplier','damage','other')) or
    (side = 'vendor'   and kind in ('defect','wrong','damaged','return','other'))),
  -- ฝั่งผู้ขายไม่มีรถ เหมือน doc_buy_no_vehicle ของเอกสารซื้อ
  constraint claim_vendor_no_vehicle check (
    side = 'customer' or (vehicle_id is null and vehicle is null))
);

create table claim_items (
  id, tenant_id, claim_id references claims(id) on delete cascade,
  line_no, product_id references products(id) on delete set null,
  code, oem, name, unit,
  qty         numeric(12,3) not null check (qty > 0),
  unit_cost   numeric(14,2) not null default 0,
  -- ต้นทุนที่คิดได้จริงตอนตัดสต๊อก ตรึงไว้เหมือน stock_moves.cost_amount
  -- ว่างได้เฉพาะบรรทัดที่ไม่ผูกสินค้า และใบที่นำเข้ามาจากไฟล์สำรอง
  cost_amount numeric(14,2)
);

create table claim_sequences (tenant_id, side, period, last_no);
create function next_claim_no(p_tenant, p_side, p_period) returns integer;
```

**`stock_moves` ต้องรู้จักใบเคลม**

```sql
alter type stock_reason add value 'claim';

alter table stock_moves
  add column claim_id      uuid references claims(id)      on delete restrict,
  add column claim_item_id uuid references claim_items(id) on delete set null;

-- ตัดจากเอกสารต้องอ้างเอกสาร · ตัดจากใบเคลมต้องอ้างใบเคลม
alter table stock_moves drop constraint stock_move_doc_ref;
alter table stock_moves add constraint stock_move_doc_ref check (
  reason in ('opening','adjust','use','count','set')
  or doc_id is not null or claim_id is not null);
```

`on delete restrict` ที่ `claim_id` ตั้งใจ — ลบใบเคลมที่ตัดสต๊อกไปแล้วไม่ได้ ต้องยกเลิกเท่านั้น
กฎเดียวกับที่เอกสารขายใช้อยู่

**RLS** ทั้งสามตารางเปิดและ force เหมือนตารางอื่น และ `tenant-isolation.test.ts`
จะเห็นเองเพราะมันไล่ตารางจาก pg catalog ไม่ได้เขียนชื่อไว้ตายตัว

---

## แก้โค้ดฝั่งเซิร์ฟเวอร์

**`apps/web/src/lib/claims.ts` (ไฟล์ใหม่ · ไม่ใส่ `server-only` เพราะรับ client เข้ามา)**

```ts
export const CLAIM_KINDS: Record<ClaimSide, { key; label; hint }[]>  // ยกจาก 6.4 ทั้งชุด

listClaims(c, { side, search, from, to, page })     // หน้ารายการ
getClaim(c, id)                                     // ใบเดียวพร้อมรายการ
saveClaim(c, input, userId)                         // สร้างใหม่เท่านั้น + ตัดสต๊อก
voidClaim(c, id, reason, userId)                    // คืนสต๊อกกลับล็อต
claimWriteOff(c, range)                             // ยอดของหายจากคลัง สำหรับงบ
```

`saveClaim` เรียก `consumeStock()` ต่อบรรทัดที่มี `product_id`
เก็บค่าที่คืนมาไว้ที่ `claim_items.cost_amount` — ต้นทุนถูกตรึง ณ ตอนตัด ไม่คำนวณใหม่ตอนอ่าน

**`stock-cost.ts` ต้องรับใบเคลมได้**
- `ConsumeInput.reason` เพิ่ม `'claim'` และรับ `claimId` / `claimItemId`
- `returnDocStock(c, docId, …)` แคบไปแล้ว — แยกไส้ในออกมาเป็น
  `returnStockOf(c, { docId } | { claimId }, …)` แล้วให้ตัวเดิมเรียกต่อ
  ตัวเรียกเดิมทั้งหมดไม่ต้องแก้

**`reports.ts` — เติมค่าที่รอไว้**

```ts
writeOff.claim = 0   // ← ลบคอมเมนต์ "รอโมดูลเคลมในช่วงที่ 4" ออก
```

อ่านจาก `stock_moves` ที่ `reason = 'claim'` ด้วยคิวรีเดิมที่มีอยู่แล้ว
(แค่เติม `'claim'` ใน `where m.reason in (…)`) แล้วแยกออกจากก้อน `adjust`

**ใบเคลมที่นำเข้ามาจากไฟล์ไม่มีแถวใน `stock_moves`** — เหมือนเอกสารขายที่นำเข้ามา
ซึ่งช่วงที่ 1 แก้ด้วย `doc_est` (จำนวน × ต้นทุนล่าสุด) ใบเคลมใช้วิธีเดียวกัน
แต่แม่นกว่า เพราะไฟล์ 6.4 มี `it.cogs` ติดมาด้วยอยู่แล้ว เก็บลง `claim_items.cost_amount`
ตอนนำเข้า แล้วงบบวกใบที่ไม่มี move จากคอลัมน์นั้น

---

## แก้โค้ดฝั่งหน้าจอ

| ไฟล์ | สิ่งที่ทำ |
|---|---|
| `app/stock/claim/page.tsx` | รายการใบเคลมฝั่งลูกค้า (05.3) |
| `app/stock/vclaim/page.tsx` | รายการฝั่งผู้ขาย (05.4) — หน้าเดียวกัน ต่างที่ `side` |
| `app/stock/claim/[id]/page.tsx` | เปิดดูใบที่บันทึกแล้ว (อ่านอย่างเดียว) |
| `app/stock/claim/[id]/print/page.tsx` | ใบพิมพ์ ยกโครงจาก `printClaimHtml` |
| `app/stock/claim/claim-editor.tsx` | ฟอร์มออกใบใหม่ · ใช้ `searchProductsAction` และ `searchCustomersAction` / `searchVendorsAction` ที่มีอยู่แล้ว |
| `app/stock/claim/void-claim.tsx` | ปุ่มยกเลิก แบบเดียวกับ `void-billnote.tsx` |
| `components/menu-map.ts` | ปลด `todo` ออกจาก 05.3 และ 05.4 |
| `app/finance/pl/page.tsx` | บรรทัด "เคลม" มีค่าจริงแล้ว ไม่ต้องแก้โครง |

ฟอร์มเก็บผังของ 6.4 ไว้ทั้งสี่บล็อก — ประเภทการเคลม · คู่ค้าและรถ ·
รายการสินค้า · เหตุผลและผู้อนุมัติ พร้อมคำอธิบายใต้ช่องประเภทที่เปลี่ยนตามที่เลือก

---

## นำเข้าและสำรองข้อมูล — ทำในช่วงนี้ ไม่ผัดไปทีหลัง

บทเรียนจากช่วงที่แล้ว: ใบวางบิลเคยถูกถอดออกจากรายการ "ยังรองรับไม่ได้"
ทั้งที่ตัวนำเข้ายังอ่านไม่ได้จริง อู่ที่ย้ายเข้ามาจึงจะเสียข้อมูลไปเงียบ ๆ
ช่วงนี้จึงทำครบสามอย่างพร้อมกัน:

1. **`backup.ts`** ส่งออก `claims` ในรูปแบบเดียวกับ `DB.claims` ของ 6.4
   (`side` `kind` `items[].cogs` `veh` `deducted` `void`) และส่งตัวนับ `seq.cl` / `seq.vc`
2. **`importer/import.ts`** อ่านกลับเข้าสามตาราง
   - `void: true` → `status = 'void'` · ไม่สร้างแถวสต๊อก
   - ใบปกติ → `status = 'issued'` · **ไม่สร้างแถวสต๊อกเช่นกัน** เพราะยอดคงเหลือ
     ที่นำเข้ามาเป็นยอดหลังหักเคลมไปแล้ว สร้างซ้ำจะตัดสองรอบ
   - `items[].cogs` → `claim_items.cost_amount` งบย้อนหลังจึงยังตรง
   - เลขที่ซ้ำ · อ้างสินค้าที่ถูกลบไปแล้ว · `kind` ที่ไม่อยู่ในชุดของทิศทางนั้น
     — ทุกกรณีเก็บของไว้ให้มากที่สุดแล้วเตือน ไม่ทิ้งใบไหน
3. **`support.ts`** ถอด `claims` ออกจาก `UNSUPPORTED` (เหลือ `counts` กับ `moves`)
4. **`restore-core.ts`** `WIPE_ORDER` เพิ่ม `claim_items` `claims` `claim_sequences`
   ไว้**ก่อน** `products` และ `stock_moves` เพราะ `claim_id` เป็น `on delete restrict`
   — จุดเดียวกับที่ใบวางบิลเคยพังในช่วงที่แล้ว

---

## ชุดทดสอบ

**`apps/web/test/claims.test.ts` (ไฟล์ใหม่ · ฐานข้อมูลจริง)**

- รับเข้าสองล็อตคนละราคา → เคลม 15 ชิ้น → `cost_amount` ต้องเป็นราคาล็อตแรกก่อน
  (พิสูจน์ว่าใบเคลมใช้ FIFO ชุดเดียวกับการขาย ไม่ใช่ต้นทุนล่าสุด)
- **ยกเลิกใบเคลมแล้วของกลับเข้าล็อตเดิมด้วยต้นทุนเดิม** — ขายต่อได้ต้นทุนเท่าเดิม
- ยกเลิกแล้วยกเลิกซ้ำไม่ได้ ไม่งั้นของงอกขึ้นมาจากอากาศ
- บรรทัดที่ไม่ผูกสินค้าไม่สร้างแถวใน `stock_moves` แต่ยังอยู่บนใบ
- `kind` ของฝั่งลูกค้าใส่ในใบฝั่งผู้ขายไม่ได้ — ฐานข้อมูลปฏิเสธ
- **ใบเคลมไม่โผล่ในยอดขาย ภาษี หรือลูกหนี้** — ข้อเดียวกับที่ใบวางบิลต้องผ่าน
- ลบสินค้าที่เคยถูกเคลมไม่ได้ (`on delete restrict` ทำงาน)

**`apps/web/test/pl.test.ts` (เพิ่มในไฟล์เดิม)**

- เคลมออกไป → **ไม่เข้าต้นทุนขาย** แต่เข้าบรรทัด "เคลม" ในของหายจากคลัง
  และกำไรขั้นต้นต้องไม่ขยับ — ถ้าเข้าต้นทุนขาย กำไรขั้นต้นจะบอกความจริงผิด
- ใบเคลมที่ยกเลิกแล้วไม่นับในงบ
- ใบเคลมที่นำเข้ามา (ไม่มีแถวสต๊อก) ยังขึ้นในงบตาม `cost_amount`

**`packages/importer/test/hard-cases.test.ts` (เพิ่ม `claimBackup()`)**

- ใบเคลมทั้งสองทิศทางตามมาครบ ไม่ถูกทิ้งเงียบ
- **ไม่สร้างแถวสต๊อกจากใบเคลมที่นำเข้า** — ยอดคงเหลือหลังนำเข้าต้องเท่าที่อยู่ในไฟล์เป๊ะ
  (ข้อนี้สำคัญที่สุดของกลุ่มนี้ พลาดแล้วสต๊อกทุกตัวที่เคยเคลมจะติดลบ)
- `kind` ที่ไม่รู้จักถูกแปลงเป็น `other` แล้วเตือน
- เลขที่ซ้ำถูกเติมเลขต่อท้าย
- ตัวนับ `seq.cl` / `seq.vc` ตามมา ออกใบถัดไปแล้วไม่ทับของเก่า

**`backup-roundtrip.test.ts` · `restore.test.ts`** — เพิ่มใบเคลมเข้าไปในอู่ต้นทาง
แล้วยืนยันว่าตามมาครบ และกู้คืนทับอู่ที่เคยมีใบเคลมไม่ล้มด้วย foreign key

**กลายพันธุ์แล้วต้องแดง** ตรวจสามจุด
1. ให้ `saveClaim` ใช้ `last_cost` แทน FIFO
2. ให้ `voidClaim` คืนของโดยไม่ใช้ต้นทุนเดิม
3. ถอด `claim_items` `claims` ออกจาก `WIPE_ORDER`

**เดิมต้องไม่พัง** — 312 ข้อผ่านครบ

---

## วิธีตรวจว่าใช้ได้จริง

```bash
export PATH=~/.nvm/versions/node/v22.23.2/bin:$PATH
DATABASE_URL="postgresql://postgres:x@localhost:5433/dgl" npm test
npm run build -w @drivegolight/importer && npm run build -w @drivegolight/web
```

ตรวจด้วยมือบนหน้าเว็บ
1. สินค้าตัวหนึ่ง จดยอดคงเหลือกับมูลค่าล็อตไว้
2. 05.3 → เปิดใบเคลม → ดึงลูกค้า ดึงอะไหล่ 3 ชิ้น → บันทึก
   → ยอดคงเหลือลด 3 · หน้าสินค้าแสดงแถวใหม่เหตุผล "เคลม" พร้อมต้นทุน
3. งบกำไรขาดทุน → บรรทัด **เคลม** มีค่าเท่าต้นทุนที่ตัด · **กำไรขั้นต้นไม่ขยับ**
4. ยกเลิกใบเคลมนั้น → ยอดคงเหลือกลับมาเท่าเดิม · มูลค่าล็อตกลับมาเท่าเดิม · งบกลับเป็นศูนย์
5. 05.4 → ทำแบบเดียวกันฝั่งผู้ขาย → เลขที่ขึ้นต้น `VC-` และไม่มีช่องรถ
6. พิมพ์ทั้งสองใบ → หัวเอกสาร ช่องลงชื่อ และข้อความท้ายใบต่างกันตามทิศทาง

---

## ที่ไม่ทำในช่วงนี้

- **ติดตามผลการเคลม** (ผู้ขายรับเรื่องแล้ว / ส่งของทดแทนมาแล้ว) — 6.4 ไม่มี
  ใช้ช่องหมายเหตุแทน · ของทดแทนเข้าด้วยใบซื้อตามปกติ
- **ผูกใบเคลมกลับไปที่ใบเสร็จที่รับประกัน** — 6.4 มีแค่ช่องเลขที่อ้างอิงเป็นข้อความ ทำตาม
- **ซ่อนต้นทุนจากพนักงานที่ไม่มีสิทธิ์** (`canCost()` ของ 6.4) — อยู่ในช่วงที่ 6
  ซึ่งทำสิทธิ์ละเอียดทั้งระบบพร้อมกัน ไม่ทำครึ่งเดียวเฉพาะหน้านี้
- **ใบตรวจนับ (05.5)** — ช่วงถัดไป

---

# ผลที่ทำจริง

**ครบทุกข้อในแผน** · เทสต์ 350 ข้อผ่าน (เดิม 312) · build ผ่าน

## ใบเคลม

- `db/001_init.sql` + `db/005_claims.sql` — `claims` · `claim_items` · `claim_sequences` ·
  `next_claim_no()` · เหตุผลใหม่ `claim` · `stock_moves.claim_id` / `claim_item_id`
  กฎสองข้อบังคับที่ฐาน: ชนิดต้องตรงกับทิศทาง (`claim_kind_side`)
  และฝั่งผู้ขายไม่มีรถ (`claim_vendor_no_vehicle`)
- `lib/claims.ts` — `listClaims` · `getClaim` · `saveClaim` · `voidClaim`
  พร้อมชุด `CLAIM_KINDS` ที่ยกจาก 6.4 คำต่อคำ
- หน้ารายการใช้ตัวเดียวกันทั้งสองทิศทาง (`list-view.tsx`) ตามที่ 6.4 ทำ
  ฟอร์มก็ตัวเดียวกัน ต่างที่ `side` — ฝั่งผู้ขายไม่มีช่องรถ
- แท็บ 05.3 และ 05.4 ไม่จางแล้ว เหลือ 05.5 อย่างเดียว

## งบกำไรขาดทุน

- `writeOff.claim` มีค่าจริงแล้ว — บรรทัด "รอโมดูลเคลมในช่วงที่ 4" ถูกลบทิ้ง
- ยกเลิกใบเคลมแล้วยอดหักกลบเอง เพราะแถวคืนที่ชี้กลับไปหาใบเคลมถูกนับเป็น `claim` ติดลบ
  (นับเฉพาะแถวคืนที่มี `claim_id` — การคืนของเอกสารขายเป็นเรื่องของต้นทุนขาย ไม่ใช่ตรงนี้)
- แยก `profitAndLossWith(c, …)` ออกเป็น `lib/reports-pl.ts` ที่ไม่กัน `server-only`
  ชุดทดสอบจึงเทียบตัวเลขจากผลลัพธ์จริง ไม่ใช่จาก SQL ที่เขียนซ้ำในเทสต์แล้วเผลอถูกทั้งคู่

## นำเข้าและสำรองข้อมูล

ทำครบสามอย่างพร้อมกันตามที่วางไว้ — export · import · `WIPE_ORDER` ·
แล้วค่อยถอด `claims` ออกจากรายการ "ยังรองรับไม่ได้" (เหลือ `counts` กับ `moves`)

ตัวนำเข้ารับมือไฟล์จริงที่รุ่นเดิมไม่เคยบังคับความสะอาด: เลขที่ซ้ำ ·
ชนิดที่ไม่อยู่ในชุดของทิศทางนั้น (ตั้งเป็น `other`) · เหตุผลที่เว้นว่าง (ฐานใหม่บังคับ) ·
อ้างสินค้าที่ถูกลบไปแล้ว — ทุกกรณีเก็บของไว้แล้วเตือน

## สองบั๊กที่เจอระหว่างทาง — ทั้งคู่มีอยู่ก่อนช่วงนี้

**1. แก้เอกสารเดิมซ้ำรอบที่สอง สต๊อกงอก**

`returnDocStock` ไล่คืนทีละแถวที่ `reason in ('sale','use')` โดยไม่หักกับแถวที่คืนไปแล้ว
ใบที่แก้สองรอบจะมีแถว −10 · +10 · −6 แล้วการคืนรอบที่สองคืน 16 ทั้งที่เอาออกไปแค่ 6

แก้เป็นรวมยอดต่อสินค้าแล้วคืนเฉพาะที่ยังค้างอยู่จริง พิสูจน์แล้วด้วยเทสต์ที่แก้เอกสารสองรอบ —
ตรรกะเดิมได้ 26 ตรรกะใหม่ได้ 16 (ของงอก 10 ชิ้นต่อการแก้หนึ่งครั้ง)

**2. ลำดับการล้างก่อนกู้คืนผิด**

`stock_moves` ต้องถูกล้างก่อน `claims` เพราะ `stock_moves.claim_id` เป็น `on delete restrict`
เทสต์จับได้ทันทีที่เขียน — ถ้าไม่มีเทสต์นี้จะไปพังตอนอู่กดปุ่มกู้คืนจริง

## ชุดทดสอบ

- `claims.test.ts` 15 ข้อ · `pl.test.ts` +5 · `fifo-db.test.ts` +1
- `hard-cases.test.ts` +12 — ไฟล์ 6.4 ที่ไม่สะอาด
- `backup-roundtrip.test.ts` +3 · `restore.test.ts` +1 · `tenant-isolation.test.ts` ครอบสามตารางใหม่
- **กลายพันธุ์แล้วเทสต์ต้องแดง** ตรวจสี่จุดตามแผน: ใช้ต้นทุนล่าสุดแทน FIFO ·
  คืนของโดยไม่ใช้ต้นทุนเดิม · ถอดใบเคลมออกจาก `WIPE_ORDER` ·
  **ให้ตัวนำเข้าสร้างแถวตัดสต๊อกจากใบเคลม** (จุดนี้สำคัญที่สุด — จับได้ว่ายอดคงเหลือเพี้ยนทันที)
