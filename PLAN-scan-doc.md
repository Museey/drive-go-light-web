# ยิงบาร์โค้ดเข้าเอกสาร

**ทำได้ และของที่ต้องใช้มีอยู่แล้วเกือบครบ** — ไล่ดูของเดิมก่อนเขียนแผน

| ของที่มีอยู่แล้ว | อยู่ที่ไหน |
|---|---|
| ช่อง `barcode` ในทะเบียนสินค้า พร้อมดัชนีห้ามซ้ำในอู่เดียวกัน | `products.barcode` |
| พิมพ์ฉลากบาร์โค้ด Code 39 | 05.1 → ปุ่มพิมพ์ฉลากบาร์โค้ด |
| **ตรรกะการยิงที่ใช้งานจริงอยู่แล้ว** | `scanIntoCount()` ของใบตรวจนับ 05.5 |
| แบบแผนหน้าจอของช่องยิง (โฟกัสค้าง · ยิงซ้ำ = เพิ่มทีละ 1 · ตัวที่เพิ่งยิงเด้งขึ้นบน) | `count-editor.tsx` |

**ที่ขาดคือสองอย่างเท่านั้น** — ช่องค้นอะไหล่ของหน้าออกเอกสารไม่ได้ค้น `barcode`
(ค้นแค่ code · name · oem) และยังไม่มีช่องยิงในหน้าออกเอกสาร

---

## เรื่องที่ต้องรู้ก่อน — ปืนยิงบาร์โค้ดคือคีย์บอร์ด

ปืนยิงแบบ USB และบลูทูธทำงานเหมือนคนพิมพ์เร็วมากแล้วกด Enter ปิดท้าย
**ไม่ต้องลงไดรเวอร์ ไม่ต้องขอสิทธิ์อะไร ไม่ต้องต่อ API** เว็บรับได้ทันที
ขอแค่มีช่องที่โฟกัสค้างอยู่ให้มันพิมพ์ลงไป

นี่คือเหตุผลที่ใบตรวจนับใช้ได้อยู่แล้ววันนี้ และเป็นเหตุผลที่งานนี้เล็กกว่าที่คิด

---

## สิ่งที่จะทำ

### ก. ให้ช่องค้นอะไหล่ค้นบาร์โค้ดด้วย

`searchProducts()` เพิ่ม `barcode` เข้าไปในเงื่อนไข และ **เรียงผลให้ตัวที่บาร์โค้ดตรงเป๊ะมาก่อน**
ลำดับเดียวกับใบตรวจนับ — บาร์โค้ด → รหัส → OEM → ชื่อ

ข้อนี้อย่างเดียวก็ทำให้ยิงในช่องค้นหาเดิมแล้วเจอของแล้ว แต่ยังต้องกดปุ่ม "เพิ่ม" อยู่

### ข. ช่องยิงในหน้าออกเอกสาร — ยิงแล้วเข้าเป็นบรรทัดเลย

เพิ่มช่องยิงไว้เหนือรายการ แยกจากช่องค้นหา ทำงานแบบเดียวกับใบตรวจนับ

- **โฟกัสค้างไว้เสมอ** คนถือปืนยิงไม่ได้จ้องจอ ต้องยิงติด ๆ กันได้โดยไม่ต้องเอามือมาคลิก
- **ยิงแล้วเข้าเป็นบรรทัดทันที** ไม่ต้องกดอะไรต่อ ราคาดึงตามระดับราคาของใบนั้น (A/B/C)
- **ยิงซ้ำตัวเดิม = เพิ่มจำนวนทีละ 1** ไม่ใช่เพิ่มบรรทัดใหม่ (ตรงกับที่ `addProduct()` ทำอยู่แล้ว)
- **ตัวที่เพิ่งยิงเน้นไว้ให้เห็น** ว่าเข้าไปแล้วจริง

ใช้กับทุกเอกสารที่มีรายการอะไหล่ — ใบเสนอราคา · ใบส่งมอบ · ใบเสร็จ · **และใบซื้อ**
(หน้าใบซื้อใช้ช่องค้นตัวเดียวกันอยู่แล้ว ได้ไปด้วยฟรี)

### ค. เมื่อยิงแล้วไม่เจอ หรือเจอหลายตัว

| กรณี | ทำอะไร |
|---|---|
| เจอตัวเดียว | เข้าเป็นบรรทัดทันที |
| **ไม่เจอ** | บอกว่าไม่พบรหัสนี้ พร้อมปุ่ม **"เพิ่มเป็นสินค้าใหม่"** ที่พาไปทะเบียนสินค้าโดยกรอกบาร์โค้ดให้แล้ว |
| เจอหลายตัว | แสดงรายการให้เลือก ไม่เดาให้ |
| สินค้าปิดใช้งาน | ไม่เข้า และบอกว่าสินค้าปิดใช้งานอยู่ ไม่ใช่บอกว่าไม่พบ |

**ข้อ "ไม่เจอ" สำคัญกว่าที่คิด** — อู่ยิงของที่เพิ่งรับเข้ามาแล้วไม่เจอเป็นเรื่องปกติ
ถ้าระบบบอกแค่ "ไม่พบ" คนจะต้องออกจากใบที่ทำค้างอยู่ไปสร้างสินค้าใหม่แล้วกลับมาเริ่มใหม่

### ง. เสียงและการเตือน

ยิงติดให้เสียงสั้นหนึ่งครั้ง ยิงไม่เจอให้เสียงต่ำสองครั้ง — คนถือปืนอยู่หน้าชั้นวาง
ไม่ได้มองจอ **ปิดได้ที่หน้าตั้งค่า** เพราะอู่ที่ทำงานในห้องเดียวกันหลายคนอาจรำคาญ

---

## ที่ต้องตัดสินใจ

### 1. กล้องมือถือยิงบาร์โค้ด — เอาด้วยไหม

ต่างจากปืนยิงคนละเรื่อง ต้องใช้ไลบรารีอ่านภาพ ขอสิทธิ์กล้อง และทำงานได้ดีเฉพาะ
ตอนแสงพอ **ผมเสนอให้ยังไม่ทำในรอบนี้** ทำปืนยิงให้จบก่อนเพราะได้ผลเต็มที่ด้วยงานน้อยกว่ามาก
แล้วค่อยดูว่าอู่อยากยิงด้วยมือถือจริงไหม

### 2. ยิงบาร์โค้ดของผู้ผลิตที่ติดมากับกล่อง (EAN/UPC) ได้ไหม

ได้ ถ้าอู่คีย์เลขนั้นลงช่องบาร์โค้ดของสินค้า — ระบบไม่สนใจว่าเลขมาจากไหน
แต่ต้องรู้ว่า**อะไหล่ในไทยจำนวนมากไม่มีบาร์โค้ดจากโรงงาน** อู่จึงต้องพิมพ์ฉลากเองอยู่ดี
ซึ่งทำได้แล้วที่ 05.1

### 3. ยิงแล้วให้ใส่จำนวนเลยไหม

รุ่น 6.4 และใบตรวจนับของเราคือ **ยิงหนึ่งครั้ง = หนึ่งชิ้น** ยิงสิบครั้งได้สิบชิ้น
ซึ่งเร็วกว่าสำหรับของไม่กี่ชิ้น แต่ช้ามากถ้าขายสี่สิบตัว
ทางเลือกคือ **พิมพ์จำนวนก่อนแล้วยิง** (เช่นพิมพ์ `40` แล้วยิง = 40 ชิ้น) ซึ่งเป็นแบบที่
เครื่องขายหน้าร้านส่วนใหญ่ทำ **ผมเสนอให้ทำทั้งสองแบบ** — พิมพ์ตัวเลขไว้ก่อนได้ ไม่พิมพ์ก็เป็น 1

---

## เทสต์ที่ต้องมี

| เรื่อง | พิสูจน์อะไร |
|---|---|
| ลำดับการค้นหา | บาร์โค้ดตรงเป๊ะชนะรหัสที่ตรงบางส่วน · รหัสชนะ OEM |
| ยิงซ้ำ | เพิ่มจำนวนบรรทัดเดิม ไม่สร้างบรรทัดใหม่ |
| จำนวนนำหน้า | พิมพ์ 40 แล้วยิง ได้ 40 ไม่ใช่ 1 หรือ 401 |
| ไม่เจอ | ไม่เพิ่มบรรทัดว่าง และบอกให้ไปสร้างสินค้าใหม่พร้อมบาร์โค้ดที่ยิง |
| สินค้าปิดใช้งาน | ไม่เข้า และข้อความต่างจาก "ไม่พบ" |
| เจอหลายตัว | ให้เลือก ไม่เดา |
| ราคา | ใช้ระดับราคาของใบนั้น ไม่ใช่ราคา A เสมอ |
| ข้ามอู่ | ยิงบาร์โค้ดที่ตรงกับสินค้าของอู่อื่น ต้องไม่เจอ |
| เบราว์เซอร์จริง | ยิง (พิมพ์เร็ว + Enter) แล้วบรรทัดเข้าจริง และโฟกัสยังอยู่ที่ช่องยิง |

## ที่ไม่ทำในก้อนนี้

- **กล้องมือถือ** — ดูข้อ 1
- **อ่านบาร์โค้ดจากรูปที่ถ่ายไว้** — คนละเรื่องกับการยิงหน้างาน
- **บาร์โค้ดรายล็อต/รายชิ้น** — ตอนนี้บาร์โค้ดผูกกับ*สินค้า* ไม่ใช่ชิ้นของ
  ถ้าจะแยกรายชิ้นต้องรื้อโครงสร้างสต๊อก ซึ่งเป็นงานคนละขนาด

## เสร็จเมื่อ

- [ ] ช่องค้นอะไหล่ค้นบาร์โค้ดได้ และเรียงตัวที่ตรงเป๊ะมาก่อน
- [ ] ยิงในหน้าออกเอกสารแล้วเข้าเป็นบรรทัดทันที ทั้งใบขายและใบซื้อ
- [ ] ยิงซ้ำเพิ่มจำนวน · พิมพ์จำนวนนำหน้าได้
- [ ] ยิงไม่เจอแล้วไปสร้างสินค้าใหม่ต่อได้โดยไม่เสียใบที่ทำค้างไว้
- [ ] `npm test` เขียว · `npm run e2e` เขียว · ยิงจริงด้วยการพิมพ์เร็วบนเบราว์เซอร์
