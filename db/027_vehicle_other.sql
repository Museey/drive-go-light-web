-- =====================================================================
-- DriveGoLight! Web — migration 027 · รถในทะเบียนลูกค้า: ช่อง "อื่นๆ" (ชื่อผู้ขับ + เบอร์) — ผู้ใช้กำหนด
-- =====================================================================
alter table vehicles add column other text not null default '';
