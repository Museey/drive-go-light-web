-- =====================================================================
-- DriveGoLight! Web — migration 021 · หมายเหตุมาตรฐานของร้าน
--
-- ตั้งครั้งเดียวที่ 07 ตั้งค่าร้าน แล้วขึ้นในช่อง "หมายเหตุ" ของทุกเอกสารตอนสร้างใหม่
-- (ใบขายทุกชนิด ใบซื้อ ค่าใช้จ่าย) ผู้ใช้แก้/พิมพ์เพิ่มรายใบได้ — แบบเดียวกับ warranty_text
-- =====================================================================
alter table tenants add column note_default text not null default '';
