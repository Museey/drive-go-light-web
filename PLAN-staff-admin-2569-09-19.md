# แผน — ตั้งค่าพนักงาน 4 เรื่อง (19 ก.ย. 2569)

ผู้ใช้แจ้ง: ปุ่ม "ตั้งเป็นเจ้าของ" ขึ้นทุกแถว เผลอกดแล้วกลายเป็นเจ้าของกันหมด ·
ขอปุ่มลบพนักงาน · ขอช่องตำแหน่งงาน · ขอให้เจ้าของตั้งรหัสผ่านให้ได้เองโดยไม่ต้องส่งลิงก์

ผู้ใช้เลือก (19 ก.ย. 2569): โอนสิทธิ์แบบมีเจ้าของคนเดียว · ลบทิ้งจริงโดยประวัติยังเก็บชื่อ ·
ตำแหน่งพิมพ์เองและขึ้นบนเอกสารที่พิมพ์ · ตั้งรหัสแล้วใช้ได้เลย และคนที่ตั้ง/แก้รหัสได้คือเจ้าของเท่านั้น

---

## 1. โอนสิทธิ์เจ้าของกิจการ — มีเจ้าของได้คนเดียว
- `transferOwnership(userId)` แทน `promoteToOwner` — ในทรานแซกชันเดียว: ตั้งคนใหม่เป็นเจ้าของ
  และลดเจ้าของคนเดิมทุกคนเป็นพนักงาน (สิทธิ์เมนูที่เคยมีคงไว้ ไม่ได้ถูกถอดตาม
  — เสียแค่ตำแหน่ง "เจ้าของกิจการ" ไม่ใช่เสียการเข้าถึงกลางอากาศ)
- **ปุ่มเดี่ยว ๆ กดแล้วเปลี่ยนทันทีคือต้นเหตุของเรื่องนี้** — เปลี่ยนเป็นกดแล้วขึ้น popup
  บอกชัดว่า "X จะเป็นเจ้าของกิจการ · คุณจะกลายเป็นพนักงาน" แล้วต้องยืนยันอีกครั้ง
- ปุ่มนี้เห็นเฉพาะเจ้าของกิจการ (พนักงานที่มีสิทธิ์ตั้งค่าร้านโอนสิทธิ์ไม่ได้)
- ปุ่มไม่ขึ้นบนแถวของเจ้าของเอง และไม่ขึ้นบนพนักงานที่ปิดใช้งาน
**เทสต์** db: โอนแล้วเหลือเจ้าของคนเดียวเสมอ · คนเก่าเป็น staff และสิทธิ์เมนูไม่หาย ·
โอนให้คนที่ปิดใช้งานไม่ได้ · e2e: ต้องยืนยันก่อนถึงเปลี่ยน · ตารางขึ้น "เจ้าของกิจการ" แถวเดียว

## 2. ลบพนักงาน
- `deleteStaff(userId)` — ลบแถวจริง ก่อนลบตัดของที่ผูกไว้: session และลิงก์ตั้งรหัสที่ยังไม่ใช้
- ประวัติไม่หาย: `doc_edits` เก็บ **ชื่อ** ไว้เป็นข้อความอยู่แล้ว ส่วน `documents.created_by`
  เป็น `on delete set null` ตามสคีมาเดิม — เอกสารเก่ายังอยู่ครบ
- กันไว้: ลบตัวเองไม่ได้ · ลบเจ้าของกิจการไม่ได้ (ต้องโอนสิทธิ์ก่อน) · ต้องยืนยันใน popup
**เทสต์** db: ลบแล้วแถวหาย · ชื่อในประวัติการแก้ไขยังอยู่ · เอกสารที่เคยสร้างยังอยู่ ·
ลบตัวเอง/ลบเจ้าของถูกปฏิเสธ · e2e: กดลบ → ยืนยัน → หายจากตาราง

## 3. ตำแหน่งงาน
- ไมเกรชัน `033_staff_job_title.sql` เพิ่มคอลัมน์ `users.job_title text not null default ''`
  (อัปเดต `db/001_init.sql` ที่เป็นไฟล์ภาพรวมด้วย ตามกติกาของโปรเจกต์)
- ฟอร์มพนักงานมีช่อง "ตำแหน่งงาน" พิมพ์เองอิสระ · ตารางพนักงานขึ้นใต้ชื่อ
- **บนเอกสารที่พิมพ์**: ใต้ชื่อผู้ลงนาม (ผู้เสนอราคา / ผู้รับเงิน / ผู้ส่งมอบงาน)
  แสดงตำแหน่งของคนที่สร้างเอกสาร **เฉพาะเมื่อชื่อที่พิมพ์บนใบเป็นชื่อคนเดียวกัน**
  — ไม่งั้นจะกลายเป็นเอาตำแหน่งของคนหนึ่งไปแปะใต้ชื่ออีกคน
**เทสต์** db: บันทึก/อ่านค่าได้ · e2e: กรอกแล้วขึ้นในตาราง · หน้าพิมพ์ขึ้นตำแหน่งใต้ชื่อ
และไม่ขึ้นเมื่อชื่อบนใบเป็นคนอื่น

## 4. เจ้าของตั้งรหัสผ่านให้พนักงานได้เลย
- ฟังก์ชัน `auth.set_password(p_user, p_hash)` (ลงใน `002_auth.sql` + ไมเกรชัน 033)
  — ตั้งรหัส ล้างตัวนับล็อก ไล่ session เดิมของคนนั้นออก และลบลิงก์ตั้งรหัสที่ยังไม่ได้ใช้ทิ้ง
  (ลิงก์เก่าที่ค้างอยู่ต้องใช้ไม่ได้อีก ไม่งั้นมีสองทางเข้าพร้อมกัน)
- `setStaffPassword(userId, plain)` — **เฉพาะเจ้าของกิจการเท่านั้น** (ตรวจ role ไม่ใช่แค่สิทธิ์เมนู)
  ใช้เกณฑ์รหัสผ่านตัวเดียวกับหน้าตั้งรหัสผ่าน (`checkPasswordStrength` ยาว ≥ 10)
- หน้าจอ: ปุ่ม "ตั้งรหัสผ่านให้" ในแถว (เห็นเฉพาะเจ้าของ) → แผงกรอกรหัสสองช่อง + ยืนยัน
  บอกให้ชัดว่าพนักงานใช้รหัสนี้เข้าระบบได้ทันที และอุปกรณ์ที่เคยเข้าไว้จะถูกไล่ออก
- ปุ่มออกลิงก์ยังอยู่เหมือนเดิมสำหรับคนที่อยากให้พนักงานตั้งเอง
**เทสต์** db: ตั้งแล้ว `verifyPassword` ผ่านด้วยรหัสใหม่ · session เดิมหาย · ลิงก์ที่ค้างใช้ไม่ได้ ·
รหัสสั้นถูกปฏิเสธ · พนักงานที่มีสิทธิ์ตั้งค่าร้าน (ไม่ใช่เจ้าของ) เรียกแล้วถูกปฏิเสธ
· e2e: เจ้าของตั้งรหัสให้พนักงาน แล้วล็อกอินด้วยรหัสนั้นได้จริง

---

## เรื่องที่ตรวจแล้วไม่ใช่บั๊ก
"แก้ไขแล้วไม่แสดงผล" — ลองบนบิลด์ล่าสุดในเครื่อง: แก้ชื่อแล้วบันทึก ตารางเปลี่ยนทันที
เดาว่าเครื่องจริงยังเป็นรุ่นก่อน push (main นำ origin 12 คอมมิต) ถ้าหลัง deploy แล้วยังเจอ
รบกวนบอกว่าแก้ช่องไหนแล้วไม่ขึ้น จะได้ตามต่อตรงจุด

## ผลข้างเคียงที่ยอมรับ
ให้เจ้าของตั้งรหัสให้พนักงานได้ = เจ้าของรู้รหัสของพนักงาน ซึ่งอ่อนกว่าการส่งลิงก์ให้ตั้งเอง
(ผู้ใช้เลือกแล้ว 19 ก.ย. 2569) — ประวัติการแก้เอกสารยังผูกกับบัญชีคนทำเหมือนเดิม
