# แผน — คอนโซลผู้ให้บริการ: ลิงก์ตั้งรหัสผ่านออกผิดอู่ · อีเมลเจ้าของในหน้ารายละเอียด

branch `fix/ops-owner-scope-2569-09-16` (แตกจาก `main` = `3ec6f25`) · **อนุมัติแล้ว 16 ก.ย. 2569** · ไม่ merge จนกว่าอนุมัติอีกรอบ

กติกาเดิม: แก้ที่ต้นเหตุ · เทสต์ใหม่ต้องแดงก่อนแก้ ย้อนการแก้แล้วแดง · ห้ามผ่อนเทสต์ · ทดสอบกับฐานในเครื่องเท่านั้น

## อาการ (ผู้ใช้แจ้ง)

กด "ออกลิงก์ตั้งรหัสผ่านใหม่" ให้อู่ B แต่หน้าจอขึ้นว่าเป็นลิงก์ของอีเมลอู่ A

**ต้นเหตุ**: `ops.issue_owner_reset` (db/011_ops_console.sql) ตั้ง `app.tenant_id` แล้วค้นเจ้าของด้วย
`select u.id, u.email from users u where u.role = 'owner' and u.active limit 1` — พึ่ง RLS ให้กรองอู่
แต่ `users` ถูกปิด force RLS ไว้โดยตั้งใจ (db/012_auth_rls.sql) ฟังก์ชันเป็น SECURITY DEFINER
ทำงานในนามเจ้าของตาราง จึงเห็นผู้ใช้ **ทุกอู่** แล้วหยิบรายแรกที่เจอ

**ผลกระทบจริงหนักกว่าที่เห็นบนจอ** — โทเคนที่ออกผูกกับบัญชีเจ้าของของอู่อื่น ใครถือลิงก์ตั้งรหัสผ่านของบัญชีนั้นได้

**จุดเดียวกันอีกแห่ง**: `ops.list_shops` นับ "บัญชีที่เปิดอยู่" ด้วยวิธีเดียวกัน (`count(*) from users where active`
หลังตั้ง app.tenant_id) — น่าจะได้ยอดรวมทุกอู่ ต้องพิสูจน์ด้วยเทสต์ก่อนแก้

## แก้

ไมเกรชันใหม่ `db/031_ops_owner_scope.sql`
- `ops.issue_owner_reset` — กรอง `u.tenant_id = p_tenant` ตรง ๆ · `order by u.created_at` ให้ผลแน่นอน
- `ops.list_shops` — นับผู้ใช้ด้วย `where u.tenant_id = <อู่นั้น> and u.active` ตรง ๆ
- เพิ่ม `owner_email` ใน `ops.list_shops` (คืนเฉพาะอีเมลเจ้าของ ไม่เปิดตาราง users เพิ่ม)
  เปลี่ยนรูปผลลัพธ์ต้อง drop แล้วสร้างใหม่ → ไฟล์นี้ให้สิทธิ์ใหม่ด้วย (`ops.grant_app`) ไม่งั้นแอปเรียกไม่ได้หลังอัปเกรด
- ไม่พึ่ง RLS ในฟังก์ชัน SECURITY DEFINER ที่แตะ `users` อีก

หน้า `/ops/shop/[id]` แสดงอีเมลเจ้าของในการ์ด "ผู้ใช้งาน"

## เทสต์ (`test/ops-console.test.ts` — ฐานข้อมูลจริง)

- สองอู่: ออกลิงก์ให้อู่ B ต้องได้อีเมลของ B **และโทเคนผูกกับบัญชีเจ้าของของ B** (ตรวจที่ตารางโทเคน ไม่ใช่แค่ข้อความบนจอ)
- จำนวนผู้ใช้ของแต่ละอู่ถูกต้อง ไม่ใช่ยอดรวมทุกอู่
- `owner_email` ตรงกับเจ้าของของแต่ละอู่
- ย้อนการแก้ทีละจุดแล้วต้องแดง

## ตรวจรวม
typecheck · `npm test` · e2e ครบบน production build · ภาพหน้ารายละเอียดอู่ 1 รูป · §13.6 ในสเปก
ทดสอบกับฐานในเครื่องเท่านั้น · ขึ้นเครื่องจริงต้องรันไมเกรชันตอน deploy
